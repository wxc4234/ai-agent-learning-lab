# AI Agent 学习详细大纲

方向：前端 / 全栈产品工程
更新时间：2026-09-10（Asia/Shanghai）

[LEARNING_PLAN.md](LEARNING_PLAN.md) 决定“12 周走什么路线”，这份文档决定“每一天具体做什么、做到什么程度算完成”。两份文档分工固定：

| 文档 | 只负责 | 不负责 |
|---|---|---|
| `LEARNING_PLAN.md` | 目标岗位、能力配比、技术选型、12 周周目标 | 逐日任务、验收清单、资料、坑 |
| `LEARNING_CURRICULUM.md`（本文） | 逐日任务、可验证验收、官方资料、常见坑、面试题 | 修改路线和周目标 |
| `LEARNING_HANDOFF.md` | 今天学到哪、下一课是什么 | 完整路线和完整大纲 |

第 1 周记录见 [week-learning/week-01/README.md](week-learning/week-01/README.md)，第 2 周复盘见 [week-learning/week-02/REVIEW.md](week-learning/week-02/REVIEW.md)，第 2 周原始逐日计划保留在 `LEARNING_PLAN.md` 第 8 节；本文从第 3 周开始展开。

本文第 3 ～ 12 周内容已按 2026-08-31 的岗位调研调整过，调研依据与差距分析见 [SKILL_GAP_ANALYSIS.md](SKILL_GAP_ANALYSIS.md) 第 10 节。

> 当前执行口径（2026-09-10）：日历处于第 3 周，第 1 ～ 3 周已经完成，正式周进度为 3 / 12（25%）。原第 7 周的 Tool Registry、参数校验和基础 Agent Loop 已归入第 3 周的“最小 Runtime”，消除课程依赖倒置；第 7 周只保留高级 Runtime。后续从第 4 周 Day 1 顺序推进，未通过整周验收前不勾选整周完成。

## 1. 学习方法

### 1.1 每个新知识按四步推进

1. 是什么：用一句话说清它解决的问题。
2. 为什么需要：不用它时会出现什么具体故障。
3. 怎么用：写出最小可运行示例。
4. 放进项目：接进 `apps/api` 或 `apps/web` 跑通。

只做第 3 步会变成抄代码，面试时讲不出取舍。

### 1.2 每天的固定顺序

按每天 4 ～ 6 小时安排：

```text
40 分钟：只读官方文档中今天要用的那一节
150 分钟：自己写，允许查文档，不允许直接抄完整实现
40 分钟：跑通并按验收清单逐条勾选
20 分钟：跑一遍评测脚本，确认没有回归
20 分钟：记录今天的三个问题和答案，追加到 week-learning/week-XX/REVIEW.md
10 分钟：git commit，更新 LEARNING_HANDOFF.md
```

投入时间不足时按比例缩放，但顺序不要变，尤其不要砍掉最后三步。从第 3 周起「跑评测」是每天的固定动作，这是「你怎么知道改动之后变好了」的日常来源。

### 1.3 卡住时的处理顺序

1. 看终端里的真实报错，不看浏览器上的提示。
2. 写最小复现脚本，剥掉框架，确认是自己的代码还是依赖问题。
3. 查官方文档和 GitHub Issue。
4. 超过 45 分钟未解决，让 AI 解释原因，但要求先讲原理再给代码。
5. 解决后把这个坑写进 `ENVIRONMENT.md` 常见问题或本文对应周的坑清单。

### 1.4 什么算“学完了”

课程结束以核心实现和工程验收通过、重要面试题归档、交接更新为准，不要求学习者当堂回答问题。

教练每课筛选重要知识点，将题目、参考答案、失败场景、定位方法和替代方案取舍放入 `interview-questions/` 对应分类；优先更新已有题目，同步索引，标记复习优先级与“参考答案已整理、尚未模拟”。本大纲第 4 节保留问题及链接，不重复存放全文答案。

独立实现、解释失败路径和讲清取舍仍是能力目标；口头表达与追问检验留到用户要求模拟面试时进行，不作为每课结束门槛。

## 2. 全局验收标准

以下要求适用于每一周，不在逐日清单中重复。

- 每天至少一次可运行的提交，提交信息写清做了什么。
- 新增依赖必须锁定版本，并同步写进 `requirements.txt` 或 `package.json` 与 `ENVIRONMENT.md`。
- 真实密钥只出现在 `.env`；`.env.example` 只放占位符。
- 任何跨周持续演进的代码进 `apps/`、`packages/`、`infra/`，不进 `week-XX/`（约束见 [AGENTS.md](AGENTS.md)）。
- 每周结束前，`ENVIRONMENT.md` 中的启动命令必须能在干净环境下按字面执行成功。
- 遇到的坑写进文档，而不是只留在记忆里。

## 3. 逐周详细大纲

### 第 3 周（09-07 ～ 09-13）流式 Agent UI + 最小 Runtime

本周目标：完成第一个真正的纵向 Agent 闭环。Next.js 能流式消费 FastAPI 的输出，后端具备安全的最小 Agent Loop，界面能展示模型文本、工具过程与唯一终态，并用 `run_id` 还原整次运行。

评测和 Trace 从本周就做，不放到第 9 周。原因：招聘方把 evals 和 guardrails 称为承重技能，面试核心问题是「你怎么知道改动之后变好了」。如果第 3 ～ 8 周的代码没有轨迹和评测，第 9 周补出来的只是事后附加物，讲不出「评测帮我发现了一个回归」这种故事。本周只做最小版本，之后每周往里加用例，不额外增加总工作量。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 初始化 Next.js + TypeScript + Tailwind；定义 Agent 事件与 UI 状态 | `pnpm dev` 可运行；能解释领域事件与 UI 状态为什么不能混为一谈 |
| 2 | FastAPI 流式端点 + Next.js BFF 原样透传；处理任意网络分块 | 浏览器逐块渲染；API Key 不出现在客户端；半条/多条事件都有测试 |
| 3 | 六态状态机、可读错误、重试和唯一终态 | `idle/thinking/streaming/done/aborted/error` 均可验证；终态后不再消费事件 |
| 4 | `AbortController`、超时原因与 Redis 跨实例取消 | 用户停止与超时语义一致；后端停止模型/工具并回滚未完成轮次 |
| 5 | **最小 Runtime**：Tool Registry、Pydantic 参数校验、ToolAction/FinalAnswer、顺序循环、超时与步数上限 | 未知工具和非法参数不进入执行器；成功/失败 Observation 都能回灌模型 |
| 6 | Runtime 接入正式聊天；工具/文本/终态事件落库；工具卡片与运行指标 | 真实 DeepSeek 完成多步骤调用；给定 `run_id` 可还原步骤、耗时、Token 和费用 |
| 7 | **冒烟评测 + 复盘**：固定用例断言状态、工具错误、取消与持久化；画事件流图 | 一条命令跑完用例并输出通过/失败；`week-learning/week-03/REVIEW.md` 完成 |

关键概念（必须能讲清）：

- Server Component 与 Client Component 的边界在哪，为什么流式渲染的消费方必须是 Client Component。
- 为什么选 SSE 而不是 WebSocket：单向、走 HTTP、天然可穿透代理、断线重连语义简单；需要双向或二进制时才换 WebSocket。
- 为什么需要 BFF：密钥收敛、跨域收敛、把后端事件裁剪成前端类型。
- `AbortController` 中止的是浏览器请求，后端是否真正停止取决于后端是否检测到断开。
- 事件日志是事实来源，界面、记忆和评测都是它的投影。
- 模型输出的工具名和参数都不可信；白名单解析与参数校验是 Runtime 的安全边界。
- 基础 Agent Loop 必须先于工具过程 UI，否则前端只能展示伪造事件，这正是它从原第 7 周前移的原因。

常见坑：

- Nginx / 代理层缓冲导致流式变成一次性返回，需要关闭缓冲。
- 生产环境的响应压缩会让流被攒在缓冲区里。
- 用 `useEffect` 直接发起流式请求，在 React 严格模式下触发两次，产生重复消息。
- Route Handler 里 `await response.json()` 会把流读完，必须转发 `response.body`。
- 事件表里记录了完整用户输入和密钥，后期变成日志泄露；一开始就要脱敏。
- 同步工具阻塞事件循环，或把普通工具异常直接抛出导致整次运行无法自我修正。

资料：Next.js App Router 官方文档的 Routing 与 Data Fetching 两章、MDN 的 `Server-sent events` 与 `AbortController`、Vercel AI SDK 的 streaming 协议说明、AG-UI 协议文档的事件类型清单。



### 第 4 周（09-14 ～ 09-20）本地工作台：Workspace、Task 与恢复

2026-09-15 收尾：详情 HTTP 已验收，下一课为详情 BFF；本轮目录按领域整理完成，不计为新增周进度。学习进度以本节及 LEARNING_HANDOFF.md 为准，新路径见 [目录导航](docs/project-structure.md)。

本周目标：在用户 PC 本地运行 Web UI 与 Agent Runtime，无需注册登录。用户配置自己的模型服务，创建 Workspace，后续绑定本地项目目录并恢复任务。2026-09-15 用户确认转向本地优先；此前认证课程保留为已完成的账号模式扩展，不再作为主流程门槛，不继续实现匿名访客系统。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 本地单用户身份、启动配置与 BFF/API 内部凭证 | 无登录 Cookie 可聊天、取消和创建 Workspace；稳定身份、并发首次访问不重复创建；伪造内部凭证被拒绝 |
| 2 | PC 工作台与 Workspace 创建、列表 | 1366×768 和 1920×1080 可用；成功、明确拒绝、结果未确认分别展示，不自动重试创建 |
| 3 | 系统目录选择与 Workspace 自动绑定 | 点击选择文件夹后直接校验并保存，取消不写入；规范化路径并验证目录存在；目录范围和符号链接边界清晰；不能接管原账号或其他 Workspace 资源 |
| 4 | 对话式 Task：笔形入口、首发创建、首轮自动标题；列表、分页、切换、删除及运行历史 | 刷新后回到原 Task，消息、状态和时间线一致 |
| 5 | 幂等、并发预算、短期状态与取消通知 | 重复提交不产生重复 Task；缓存丢失不损坏事实数据 |
| 6 | 可观测收尾：成功/失败摘要、Token/费用/模型与工具耗时 | 正常与已产生指标的错误均有准确摘要，不伪造网络失败成本 |
| 7 | 本地产品验收：启动、恢复、跨站请求、目录越界和错误恢复 | 固定 PC 场景可重复；内部凭证不进入浏览器；本地端口不对外开放 |

截至 2026-09-15，本阶段学习内容与实现范围如下：

| 内容 | 学习与工程结果 | 当前边界 |
|---|---|---|
| Workspace 与目录 | 项目创建/列表、系统选择目录并绑定；理解本地身份、归属检查及目录边界 | 绑定目录不代表获得任意文件读写或命令执行权限 |
| Task 创建 | 学习者完成创建事务、HTTP/BFF 及原创建表单；理解 Task/Conversation 同事务与提交结果未确认 | 原手填标题表单已被对话式入口替换；服务端创建幂等仍未实现 |
| 对话式工作台 | 教练按明确授权完成项目/任务侧栏、笔形新建入口、首发创建、固定会话续聊、历史读取和首轮标题总结 | 工程已验收，不记作学习者独立实现；刷新目前需手动选回任务 |
| 异步一致性 | 首轮标题失败保留消息摘录，条件更新防止迟到总结覆盖后来标题；旧历史请求不污染新任务 | 辅助标题费用未计入 AgentRun 指标 |
| 任务详情读取 | 学习者完成 TaskDetailResponse、详情服务与 HTTP；理解直接定位不依赖列表分页、项目/会话双重归属、只读事务和关闭 Session 后序列化 | 本课新增 18 条，相关 62 条通过；详情 BFF 与 URL 自动恢复待接入 |
| PC 体验 | 教练按授权优化空白页/聊天布局、项目设置、18px 字号、键盘发送与中文输入法保护 | 保持 PC 三栏方向，复用共享组件；样式不单列面试考点 |
| 工程组织 | 教练按授权迁移 97 个源码/测试文件，按层级和领域归类，同步导入、脚本与文档 | 未改变接口或数据库结构；目录整理不增加正式周完成数 |

目录整理后后端全量 899 条、前端 Workspace 相关 325 条及浏览器主流程定向场景通过。具体命令和各轮范围归入 ENVIRONMENT.md，重要设计取舍与参考答案归入现有 interview-questions/backend/web/task-model.md，不把整理答案视为已通过模拟面试。

下一课先完成 **Task 详情 BFF**，随后推进 URL 选中状态与刷新自动恢复；任务删除、历史运行状态/时间线恢复、幂等和并发预算仍待完成。正式进度保持第 1～3 周完成、第 4 周进行中。

关键概念：本地运行不等于离线模型；免产品登录不等于移除访问控制；Workspace 仍是文件、执行与检索边界。账号模式的 Cookie、密码哈希、跨用户隔离与事务测试保留，用于扩展学习。当前保持 PostgreSQL 与 Redis，不在同一步改写存储或制作桌面安装包。

---

### 第 5 周（09-21 ～ 09-27）Coding Tools + 安全执行

本周目标：让 Agent 不只“建议代码”，而是能在隔离的样例 Workspace 中安全地读取、搜索、修改并验证代码。模型永远不能直接执行任意 Python 或越过 Workspace 边界；所有工具调用都经过注册、校验、策略、执行、审计和结果回灌。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 文件与搜索工具：目录树、读文件、`rg`、符号定位；规范化路径并限制大小 | 能找到跨文件定义；`../`、绝对路径、软链接逃逸、二进制和超大文件被拦截 |
| 2 | Shell Worker / Sandbox：固定 Workspace、清理环境变量、时间/资源/输出限制、取消传播 | 命令无法访问 Workspace 外部或密钥；超时、取消、非零退出返回结构化结果 |
| 3 | Apply Patch：结构化 Diff、上下文匹配、原子写入、冲突与失败回滚 | 成功补丁只改目标文件；失败补丁不留下半写状态 |
| 4 | Git 与验证工具：status、diff、受控测试/lint/typecheck；结果摘要回灌模型 | 一次任务能展示 Diff 并运行最相关检查，原始输出可追踪且有截断上限 |
| 5 | 工具风险与审批策略：只读/写入/执行/外部副作用分级，服务端决定是否暂停 | 高风险调用未批准前不会进入执行器；重放同一批准不会执行两次 |
| 6 | 前端编码任务体验：计划、工具、终端、Diff、审批与结果卡片 | 浏览器能看见正在做什么、改了什么、验证是否通过，并可停止任务 |
| 7 | 固定编码冒烟集：查找、单文件修复、跨文件修复、失败测试、危险命令 | 一条命令运行任务集；每项输出终态、步骤、工具、耗时、成本与 Diff |

关键概念：

- Sandbox 是执行边界，不是提示词；路径检查、进程权限、资源限制和环境变量隔离缺一不可。
- Apply Patch 比“让模型重写整个文件”更可审查，也更容易检测并发冲突和失败回滚。
- Shell 输出和退出码都是 Observation；非零退出不一定是系统异常，可能是 Agent 需要修正的反馈。
- 工具能力与当前允许使用的工具子集要分离，避免模型在不需要时拥有高风险能力。

常见坑：

- 只检查字符串是否包含 `..`，却没有处理符号链接和规范化后的真实路径。
- 把宿主机真实环境变量传进 Shell，导致 API Key 出现在命令或日志中。
- Patch 失败仍写入一部分文件；测试失败后又覆盖原始错误输出。
- 审批只存在前端，攻击者绕过 UI 直接请求执行接口。

### 第 6 周（09-28 ～ 10-04）代码上下文工程 + RAG

本周目标：让 Agent 能为真实跨文件任务找到“足够但不过量”的仓库上下文。检索不是单独做一个文档问答 Demo，而是直接服务于计划、工具选择、代码修改与引用说明。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | Workspace 导入：Git URL 或 ZIP、本地样例仓库；校验大小、类型、路径和所有权 | 非法压缩包、路径穿越、超限仓库被拒绝；导入状态可恢复 |
| 2 | 仓库扫描：遵守 `.gitignore`，识别语言/文件/符号，跳过二进制、生成物和疑似密钥 | 生成可查询的文件与符号清单，且不索引被忽略或敏感内容 |
| 3 | 关键词与符号检索：`rg`、文件路径、定义/引用线索 | 精确标识符与配置项能稳定命中，结果带文件、行号和匹配原因 |
| 4 | 代码 Chunk、Embedding 与 pgvector：保留文件、符号和行号元数据 | 向量写入成功；维度/索引匹配；语义问题能召回相关实现 |
| 5 | 混合检索、Rerank 与 Context Builder：融合排序、去重、Token 预算、工具结果裁剪 | 上下文包可解释来源、分数、Token 占比和被裁剪原因，不超过预算 |
| 6 | 引用与检索调试 UI：展示查询、候选、分数、采纳片段和代码定位 | 点击引用能定位代码行；用户能看出为什么某段被选中 |
| 7 | 代码检索评测：固定跨文件任务集，比较关键词、向量与混合策略 | 输出 Recall@K、MRR、引用正确率、延迟与成本对比表 |

关键概念：

- 代码搜索优先依赖路径、符号和精确文本；Embedding 补充语义，不应替代 `rg` 和语言结构。
- Chunk 边界应尽量遵循函数、类和模块，而不是只按固定字符数切割。
- 权限过滤必须发生在召回阶段；后置过滤会让无权限片段占据 Top-K。
- Context Engineering 还包括仓库指令、历史、计划和工具结果的选择，不只是 RAG。
- 固定评测集是判断检索改动是否有效的事实来源，不能只凭一次聊天感觉。

常见坑：

- 把 `node_modules`、构建产物、`.git` 或密钥文件送去 Embedding，成本和安全同时失控。
- 只做向量检索，导致函数名、错误码和配置键等精确问题召回很差。
- 引用只有文件名没有行号，用户无法验证 Agent 依据。
- 同时修改 Chunk、召回数量和 Rerank 后再评测，无法归因改进来自哪里。

### 第 7 周（10-05 ～ 10-11）高级 Agent Runtime：可恢复长任务

本周目标：在第 3 周最小循环和第 5 ～ 6 周工具/上下文能力之上，构建可计划、可压缩、可暂停、可恢复、可修正并可度量的长任务 Runtime。Tool Registry、参数校验和基础循环不再重复教学，只在 Day 1 做掌握复核。

| Day | 任务 | 对应能力 | 验收 |
|---|---|---|---|
| 1 | Runtime 基础复核 + 显式 Plan/Step 状态：计划创建、更新、完成与失败 | Planning | 不看资料讲清现有循环；UI 与数据库能看到计划变化，但不记录隐藏思维链 |
| 2 | Context Engineering 与 Compaction：上下文清单、Token 占比、阶段性摘要与关键 ID 保留 | Context | 长轨迹压缩后仍能继续任务；可解释保留、丢弃和压缩了什么 |
| 3 | 记忆分层：对话短期记忆、Task/Run 状态、长期用户/Workspace 事实 | Memory | 三类记忆存储、作用域、淘汰和隐私策略不同，不把运行状态塞进聊天摘要 |
| 4 | LangGraph State + Checkpoint：把循环迁移为持久状态图 | Durable Runtime | 在模型前、工具后或审批中断后均可从 Checkpoint 恢复 |
| 5 | 暂停、恢复、Steering、取消、重试与幂等 | Control | 新指令只在安全边界生效；恢复不会重复已提交副作用；终态唯一 |
| 6 | Reflection 与验证闭环：测试失败 → 分析 Observation → 调整计划 → 修复 | Reflection | Agent 首次补丁失败后能根据真实测试输出完成一次受限自我修正 |
| 7 | 轨迹评测：任务成功、工具选择、补丁正确、步骤、恢复、成本与 LLM-as-Judge | Evaluation | 固定任务集输出指标；至少展示一个改动前后对比和一次评测发现的回归 |

关键概念：

- Plan 是可观察、可更新的任务状态，不是暴露模型隐藏推理。
- Checkpoint 保存可恢复状态；聊天摘要负责压缩语义，两者不能互相替代。
- Steering 只能在定义好的安全边界改变后续计划，不能让已执行副作用“假装没发生”。
- Reflection 必须读取真实验证结果并受步数/时间/预算限制，否则会变成无限重试。
- 检索指标回答“找没找对上下文”，轨迹指标回答“有没有用正确步骤完成任务”。

常见坑：

- 只序列化消息列表，没有保存计划、工具调用、审批和已提交副作用，导致无法可靠恢复。
- 压缩时丢掉文件路径、补丁基线、测试命令或工具 call ID。
- 用户取消与服务异常竞争，产生两个终态或重复清理。
- Reflection 没有新证据却重复相同调用，持续消耗 Token。
- LLM-as-Judge 未经人工小样本校准，评分看似精确但不可相信。

资料：LangGraph 官方文档的 State / Checkpointer，两篇 ReAct / Reflexion 论文的方法部分，以及官方 OpenAI Conversation State、Compaction、Streaming 与 Steering 文档。



### 第 8 周（10-12 ～ 10-18）MCP、Skills、审批与有限多 Agent；开始第一批投递

本周目标：让 Coding Agent 可以通过标准协议和可复用指令扩展能力，让危险操作必须经过人工确认，并只在任务确实独立时进行有限并行委派；**同时开始投递**。

投递不等作品集全部完成。第 8 周时你已经有流式 UI、安全 Coding Tools、代码上下文、ReAct + LangGraph、审批、评测和 Trace，足以支撑面试对话。面试反馈能反向告诉你第 9 ～ 12 周该补什么，这比闭门造车更高效。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | MCP 基础 + 自建 Server：Tool、Resource、传输、错误和信任边界 | 本地 Client 能发现并调用自建代码分析工具；能解释 MCP 与 A2A 的区别 |
| 2 | FastAPI 作为 MCP Client：动态发现、名称冲突、超时、结果清洗和权限映射 | MCP 工具进入统一 Tool Gateway，外部错误和返回不会绕过本地策略 |
| 3 | Skills 与仓库指令：可发现的 `SKILL.md`、版本、作用域、优先级和渐进加载 | 至少一个测试/修复 Skill 被真实任务使用；仓库指令能约束工具行为 |
| 4 | 持久审批：批准、修改参数后批准、拒绝、过期；服务端 Guardrails 强制 | 三种操作改变后续轨迹；重启后待审批状态不丢；原始/最终参数均审计 |
| 5 | Prompt Injection 与外部工具防护：不可信内容标记、最小工具集、敏感数据隔离 | 恶意仓库文本或 MCP 返回不能提升权限、读取密钥或静默执行危险工具 |
| 6 | 有限子 Agent 委派：只拆独立读任务/测试任务，限制并发、预算和写权限，主 Agent 合并 | 两个子任务可并行且结果可追踪；冲突写入被禁止或显式合并 |
| 7 | **简历 v1 + 讲解稿 v1 + 首批 5 ～ 8 家投递**；安全与能力矩阵成文 | 简历每条有可量化结果；投递记录成文；能演示一次 MCP/Skill/审批/委派 |

关键概念：

- MCP 的价值是把「每接一个工具写一次胶水代码」变成「实现一次协议」。
- Skill 封装的是可复用工作流与资源，不是放宽工具权限；仓库指令也不能绕过系统安全策略。
- 审批必须在服务端强制，前端只是展示层；否则绕过前端就能执行危险操作。
- 修改参数后批准，意味着执行的参数与模型提议的参数不同，两者都要记进事件日志。
- 外部 MCP Server 的返回是不可信输入，要防 Prompt Injection。
- 多 Agent 只解决可独立并行的工作，不应拿来包装普通函数调用；写入责任和最终合并必须唯一。

常见坑：

- 审批状态只存在内存，服务重启后待审批任务丢失。
- 审批超时没有默认策略，任务永久挂起。
- 把 MCP Server 的错误信息直接展示给用户，泄露内部路径或配置。
- 子 Agent 共用可变 Workspace 并同时写文件，产生难以复现的竞态和覆盖。
- 简历只写「使用了 LangGraph、MCP、RAG」这类关键词堆叠。招聘方明确把 framework name-dropping 列为被高估的信号，每条必须带结果或数字。



### 第 9 周（10-19 ～ 10-25）生产化：Worker、可观测、评测与部署

本周目标：让项目具备可部署、可观测、可回归的工程属性。**本周不可压缩**——它直接决定作品集能否被评估。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 后台 Worker 与任务队列：并发上限、租约、心跳、重试、取消和僵尸任务回收 | API 不被长任务阻塞；Worker 崩溃后任务可重领且不重复提交副作用 |
| 2 | Trace 完善：贯通模型、检索、工具、审批、补丁和测试 | 能按 `run_id` 还原完整轨迹、因果顺序、耗时、Token 和错误阶段 |
| 3 | 成本与性能：按用户/Task/Run/工具统计，模型路由、缓存、预算与 P95 延迟 | 超预算被拦截；能解释一次真实任务的成本和慢点 |
| 4 | 评测入 CI：代码检索 + 补丁任务 + 轨迹评测作为回归门禁 | PR 自动跑确定性子集；指标低于阈值时失败并保留报告 |
| 5 | 安全红队：Prompt Injection、越权、路径穿越、命令注入、SSRF、密钥与日志脱敏 | 攻击用例有自动测试和处理记录；高风险能力默认最小化 |
| 6 | **本地交付与受限云部署**：本地启动/升级，独立样例服务部署与发布回滚 | 干净 PC 按文档启动并完成样例任务；本地服务仅监听回环地址；用户自行配置模型 |
| 7 | CI/CD 与恢复演练：lint、类型、测试、构建、迁移、备份、回滚、干净环境 | 全部检查通过；模拟服务重启后任务可解释地恢复 |

保留一次受限云部署实践：部署独立样例服务，覆盖 HTTPS、反向代理、环境配置、日志、健康检查和发布回滚；不得直接公开本地免登录执行接口。主产品本地运行；线上域名用于介绍、视频、文档和下载。最终分发目标：普通体验者无需手动安装或维护 Docker、PostgreSQL、Redis；由产品内嵌存储、替代实现或受管理的运行依赖提供能力。当前源码版尚未达到此目标。存储/取消机制调整必须验证迁移、事务、备份恢复和跨进程行为，不能把数据库文件替换等同于完成适配。公开执行演示仅为可选扩展，需要独立隔离和预算，不能将本地免登录服务直接绑定公网。桌面打包与存储轻量化另行评估，不提前阻塞核心 Runtime。

关键概念：

- Trace 的意义在于 Agent 是非确定性的，没有轨迹就无法定位问题。
- 评测进 CI 才能防止「修好一个问题，弄坏三个」。这是「你怎么知道改动之后变好了」的可核查答案。
- Prompt Injection 的主要防线是权限最小化和工具审批，而不是靠提示词请求模型不要听坏话。
- 队列“至少一次投递”要求副作用幂等；不能因为任务重领就重复应用补丁或重复调用外部系统。
- 部署文档的正确性只能通过在干净环境实际执行来验证。

常见坑：

- Trace 里记录了完整的用户输入和密钥，造成日志泄露。
- 评测在 CI 里真实调用模型，费用和不稳定同时失控；应固定样本或使用缓存/录制。
- Compose 里数据库没做健康检查，应用先起来直接连接失败。
- 部署时把 `.env` 打进镜像。

### 第 10 ～ 11 周（10-26 ～ 11-08）Codex-like 主项目收尾与质量证明

**这两周不是开始做项目，而是把第 3 ～ 9 周已有的代码收尾成作品集。** 项目定义见 `LEARNING_PLAN.md` 第 9 节。

第 10 周（产品化与失败路径）：

| Day | 任务 |
|---|---|
| 1 | Workspace / Task / Conversation 列表、空状态、分页和恢复体验 |
| 2 | Plan / Tool / Terminal / Approval 时间线与实时状态 |
| 3 | 文件树、代码引用、Git Diff Review、测试结果与接受/拒绝改动体验 |
| 4 | 响应式、键盘操作、基础无障碍、长输出与大 Diff 性能 |
| 5 | 构造工具超时、测试失败、审批拒绝、Worker 重启中的至少两个恢复场景 |
| 6 | 架构图、信任边界图、事件流图和数据模型图 |
| 7 | 端到端自测、干净环境验证、修复暴露的问题 |

第 11 周（表达与可选补齐）：

| Day | 任务 |
|---|---|
| 1 ～ 2 | 完成代码检索、补丁正确率、轨迹、成本和延迟评测报告；记录一次真实回归 |
| 3 | 安全红队与失败恢复报告：攻击输入、预期策略、真实结果和残余风险 |
| 4 ～ 5 | README、截图/GIF、3 ～ 5 分钟演示视频；包含测试失败后修正和审批场景 |
| 6 | 第二框架只做概念对比；若主项目九项最终验收未全过，优先补主项目 |
| 7 | 干净环境复现、项目讲解演练、复盘与第二批投递准备 |

刻意演示的失败场景至少两个，例如测试失败后根据 Observation 修正、审批被拒绝后调整方案、Worker 重启后从 Checkpoint 恢复。**主动展示失败处理比只演示顺利路径更有说服力**，招聘方看的是 build → measure → refine 的痕迹。



### 第 12 周（11-09 ～ 11-15）面试强化与第二批投递

第一批投递已在第 8 周完成，本周重点是把面试反馈补齐并扩大投递面。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 根据第 8 ～ 11 周的面试反馈修订简历 | 每条经历有可量化结果 |
| 2 | 项目讲解稿定版：2 分钟版和 8 分钟版 | 不看稿能讲完 |
| 3 | 技术问答：过完第 4 节题库 | 每题都能给出取舍理由 |
| 4 | 现场编码：ReAct 循环、流式状态机、路径安全或工具调度 | 40 分钟内写出一个可运行、带失败测试的最小实现 |
| 5 | 第二批定向投递（15 ～ 20 家） | 投递完成并记录 |
| 6 | 模拟面试与复盘 | 记录被问住的问题并补齐 |
| 7 | 整理投递期继续学习清单 | 明确下一阶段补什么 |



## 4. 面试题库

按周积累，每题的目标是能讲出取舍，不是背定义。标记 **[必问]** 的来自公开面试题库统计，出现频率最高。

### 评测、可观测与可靠性（招聘方称为承重技能，优先准备）

1. **[必问]** 你怎么知道改动之后变好了？
2. **[必问]** 你怎么知道你的评测本身可信？
3. 检索评测和 Agent 轨迹评测有什么区别，各自度量什么？
4. LLM-as-Judge 的偏差从哪来，怎么校准？
5. 举一个「评测帮你发现了回归」的具体例子。
6. 单次运行的成本和延迟是多少？超预算时怎么拦截？
7. Agent 是非确定性的，你怎么写测试？
8. 线上出现 Agent 行为异常，你的定位路径是什么？

前两题是招聘方明确点出的第一区分点，必须用具体数字和真实案例回答，不能只讲方法论。

### Agent 原理与架构

1. **[必问]** 手写一个 ReAct 循环。
2. Planning、Memory、Tool Use、Reflection 在你的代码里分别落在哪？
3. **[区分度最高]** 短期上下文、运行状态、长期记忆分别存在哪里，为什么要分开？记忆如何淘汰？
4. CoT、ReAct、Reflexion、Self-Refine 的差别和适用场景。
5. Agent Loop 的终止条件有哪些？只靠最大步数会出什么问题？
6. 单步 95% 成功率，20 步后整体成功率是多少？这个数字说明什么？
7. Context Engineering：一次推理的上下文由哪几部分拼成？超长时压缩掉什么、怎么避免丢关键信息？
8. 工具执行失败，是抛异常还是把错误返回给模型？依据是什么？
9. LangGraph 的 Checkpoint 让哪些能力成为可能？
10. 什么场景**不该**用多 Agent？

### 工具与协议

1. **[必问]** MCP 解决了什么问题？没有它之前是怎么做的？
2. MCP 的 Tool、Resource、Prompt 分别用在什么场景？
3. MCP 和 A2A 的分工差异是什么？
4. Function Calling 和 Computer Use / GUI Agent 的取舍。
5. Tool Registry 怎么设计才能新增工具不改调度代码？
6. 文件、Shell 和 Apply Patch 工具的输入、输出与失败契约怎么设计？
7. Skill、仓库级指令和普通系统提示有什么区别，优先级怎么处理？
8. 什么任务值得委派给子 Agent，如何避免并发写冲突？

### 安全与人工介入

1. **[必问]** Prompt Injection 的现实防线有哪些？
2. **[必问]** Human-in-the-loop 怎么设计？审批状态存在哪？
3. Guardrails 和审批的分工是什么？
4. 外部 MCP Server 的返回为什么是不可信输入？
5. 这个项目有哪些安全风险，分别怎么处理？
6. 如何同时防止路径穿越、软链接逃逸、命令注入和宿主环境变量泄漏？
7. 为什么 Docker 容器不自动等于完整 Sandbox？公网版本还需要限制什么？

### 代码上下文与 RAG

1. **[必问]** 完整讲一遍从仓库扫描到 Context Builder 的代码检索链路。
2. 跨文件任务召回质量差，你的排查顺序是什么？
3. 符号、关键词和向量检索各自的优势与失效场景是什么？
4. Rerank 带来的收益和成本怎么量化？
5. Workspace 权限如何在检索阶段生效，而不是回答后过滤？
6. 为什么选 pgvector 而不是 Pinecone / Weaviate / Milvus？
7. 如何给代码片段做 Chunk，并保留文件、符号和行号引用？

### Agent 前端

1. Agent UI 和普通聊天 UI 的本质区别是什么？
2. [模型 token、tool call、approval、error 分别对应前端哪些状态？状态机怎么画？](interview-questions/frontend/agent-ui/chat-state-machine.md)
3. 为什么选 SSE 而不是 WebSocket？什么时候必须换成 WebSocket？
4. 你的事件协议和 AG-UI 有什么异同？为什么这样选？
5. 用户点击「停止生成」，从浏览器到后端到模型分别发生了什么？
6. 流式响应过程中网络断开，如何恢复而不产生重复内容？
7. 一次运行有 30 个工具调用，界面怎么设计才不淹没用户？
8. 前端如何保证与后端的类型契约不漂移？
9. Plan、Terminal、Diff、Approval 和最终回答在 UI 中如何分层？

### 后端与工程

1. [一次 Agent 运行需要记录什么，如何按 `run_id` 还原时间线？](interview-questions/backend/web/agent-run-observability.md)
2. 用户重复点击发送，如何保证不产生两条消息？
3. Redis 在这个项目里存什么，丢了会怎样？
4. 数据库迁移在多人多环境下如何避免冲突？
5. 同一套项目怎么在 macOS、Windows 和服务器上运行？
6. 你的部署方式是什么？为什么不用 K8s？
7. Task、Run、Conversation、Checkpoint 与 Event 为什么要分开？
8. Worker 崩溃后如何重领任务，又如何避免补丁或外部副作用重复执行？

### 系统设计（面试重头戏，各准备一张架构图）

1. 设计一个支持多用户、隔离 Workspace 和任务恢复的 Coding Agent。
2. 设计一个安全的文件/Shell/Apply Patch 工具执行平台。
3. 设计一个代码仓库 Context Builder 与检索评测系统。
4. 设计一套 Agent 可观测与轨迹评测系统。
5. 设计一个有限多 Agent 编码任务调度器，并说明什么时候不该并行。

### 反问面试官

1. 团队的 Agent 产品里，前端负责到哪一层？
2. 工具和 MCP 的接入规范由谁定义？
3. 效果回归靠人工还是自动评测？评测集谁维护？
4. 线上出现 Agent 行为异常，目前的定位手段是什么？
5. 成本和延迟目前是谁在负责优化？

## 5. 每周复盘模板

每周第 7 天把下面模板填入 `week-learning/week-XX/REVIEW.md`。

```markdown
# 第 X 周完成记录

周期：YYYY-MM-DD ～ YYYY-MM-DD

## 本周目标
（一句话）

## 已完成
- [x] ...

## 未完成及原因
- ...（明确挪到哪一周）

## 关键决策
| 决策 | 备选方案 | 选择理由 |
|---|---|---|

## 踩过的坑
| 现象 | 原因 | 解决方式 |
|---|---|---|

## 能讲清的问题
1. ...

## 还讲不清的问题
1. ...（下周补）
```

“还讲不清的问题”是这份模板里最重要的一栏，不要留空。

## 6. 进度自检表

每周末对照勾选，用于判断是否可以进入下一周。连续两周有未完成项时，优先补齐而不是继续推进。

- [x] 第 1 周：Python、FastAPI、DeepSeek、SQLite、持久化对话和最小时间工具闭环
- [x] 第 2 周：分层、配置、PostgreSQL、Alembic、pytest、Docker Compose 全部落地
- [x] 第 3 周：流式 UI + 取消 + 六态状态机 + **最小 Runtime + run_id 事件落库 + 冒烟评测**
- [ ] 第 4 周：本地单用户、Workspace/项目目录、Task、会话恢复、幂等/并发预算、**完整运行摘要**（可观测核心已预完成）
- [ ] 第 5 周：文件/搜索/Shell/Apply Patch/Git/测试工具在 Sandbox 内完成一次可审查代码修改
- [ ] 第 6 周：代码关键词/符号/向量混合检索可比较，引用与 Context Token 预算可解释
- [ ] 第 7 周：Plan + Compaction + 记忆分层 + LangGraph Checkpoint + 暂停恢复 + 轨迹评测
- [ ] 第 8 周：自建 MCP + Skill + 审批三态 + Prompt Injection 防护 + 有限子 Agent + **首批投递**
- [ ] 第 9 周：后台 Worker、Trace、评测入 CI、安全红队、Sandbox、本地交付与**展示/分发入口**
- [ ] 第 10 周：Task/Plan/Terminal/Diff/Approval 产品体验完成，两个恢复场景可演示
- [ ] 第 11 周：代码任务评测报告 + 安全报告 + 演示视频 + README 可复现
- [ ] 第 12 周：讲解稿终版、题库过完、第二批投递完成

## 7. 风险与应对

| 风险 | 信号 | 应对 |
|---|---|---|
| 广度失控 | 同时深入 3 个框架 | 冻结技术栈；第二框架只做概念级（`LEARNING_PLAN.md` 第 4 节「概念级了解」） |
| 前端优势被稀释 | 连续两周只写 Python | 强制补一天 Agent UI |
| 只有 Demo 没有工程 | 没有评测、没有 Trace | 第 3 周和第 9 周的可观测任务不可跳过 |
| 关键词对不上 JD | 简历里没有 Planning / Memory / Reflection / Context Engineering / Evaluation | 第 7 周按术语表对齐后重写简历 |
| 看似实现但没有掌握 | 代码能跑却讲不清边界、失败和替代方案 | 每课都按“是什么 → 为什么 → 怎么用 → 示例”讲解，并由学习者完成核心步骤 |
| Coding Agent 权限过大 | 能访问宿主密钥、其他目录或任意公网 | 第 5、8、9 周的 Sandbox、审批和红队验收不可跳过 |
| 进度落后 | 累计落后超过 5 天 | 按下面的取舍顺序砍 |
| 时间不足 | 每天不足 4 小时 | 按 1.2 节比例缩放，但不砍验收和复盘 |

进度落后时的取舍顺序（从先砍到后砍）：

1. 第二框架、A2A 和 Computer Use 的可选演示（保留概念对比）。
2. 可选第二项目（本来就是有余力才做）。
3. 第 6 周的 Rerank 深度优化（保留混合检索和评测即可）。
4. 第 8 周多 Agent 的复杂写入，只保留有限、只读或隔离子任务委派。
5. 第 10 周的 UI 打磨深度。

**不参与压缩的部分**：第 5 周安全 Coding Tools、第 6 周代码上下文、第 7 周持久 Runtime 与轨迹评测、第 9 周生产化，以及第 8 周首批投递。这些共同决定项目是可验证的 Coding Agent，还是只有聊天界面的 Demo。

## 8. 文档关系

- [项目入口](README.md)
- [12 周主计划](LEARNING_PLAN.md)
- [仓库与目录规则](AGENTS.md)
- [环境安装与启动](ENVIRONMENT.md)
- [岗位与技能校准](SKILL_GAP_ANALYSIS.md)
- [当前进度与交接](LEARNING_HANDOFF.md)
- [Codex-like 能力边界与最终验收](docs/codex-like-agent-scope.md)
