# AI Agent 学习详细大纲

方向：前端 / 全栈产品工程
更新时间：2026-08-31（Asia/Shanghai）

[LEARNING_PLAN.md](LEARNING_PLAN.md) 决定“12 周走什么路线”，这份文档决定“每一天具体做什么、做到什么程度算完成”。两份文档分工固定：

| 文档 | 只负责 | 不负责 |
|---|---|---|
| `LEARNING_PLAN.md` | 目标岗位、能力配比、技术选型、12 周周目标 | 逐日任务、验收清单、资料、坑 |
| `LEARNING_CURRICULUM.md`（本文） | 逐日任务、可验证验收、官方资料、常见坑、面试题 | 修改路线和周目标 |
| `LEARNING_HANDOFF.md` | 今天学到哪、下一课是什么 | 完整路线和完整大纲 |

第 1 周记录见 [week-learning/week-01/README.md](week-learning/week-01/README.md)，第 2 周逐日计划在 `LEARNING_PLAN.md` 第 8 节。本文从第 3 周开始展开。

本文第 3 ～ 12 周内容已按 2026-08-31 的岗位调研调整过，调研依据与差距分析见 [SKILL_GAP_ANALYSIS.md](SKILL_GAP_ANALYSIS.md) 第 10 节。

## 1. 学习方法

### 1.1 每个新知识按四步推进

1. 是什么：用一句话说清它解决的问题。
2. 为什么需要：不用它时会出现什么具体故障。
3. 怎么用：写出最小可运行示例。
4. 放进项目：接进 `apps/api` 或 `apps/web` 跑通。

只做第 3 步会变成抄代码，面试时讲不出取舍。

### 1.2 每天的固定顺序

按每天 4 ～ 6 小时安排：

```text
40 分钟：只读官方文档中今天要用的那一节
150 分钟：自己写，允许查文档，不允许直接抄完整实现
40 分钟：跑通并按验收清单逐条勾选
20 分钟：跑一遍评测脚本，确认没有回归
20 分钟：记录今天的三个问题和答案，追加到 week-XX/README.md
10 分钟：git commit，更新 LEARNING_HANDOFF.md
```

投入时间不足时按比例缩放，但顺序不要变，尤其不要砍掉最后三步。从第 3 周起「跑评测」是每天的固定动作，这是「你怎么知道改动之后变好了」的日常来源。

### 1.3 卡住时的处理顺序

1. 看终端里的真实报错，不看浏览器上的提示。
2. 写最小复现脚本，剥掉框架，确认是自己的代码还是依赖问题。
3. 查官方文档和 GitHub Issue。
4. 超过 45 分钟未解决，让 AI 解释原因，但要求先讲原理再给代码。
5. 解决后把这个坑写进 `ENVIRONMENT.md` 常见问题或本文对应周的坑清单。

### 1.4 什么算“学完了”

一个知识点算掌握，需要同时满足三条：

- 能不看资料写出最小可运行版本。
- 能说出它的一个失败场景，以及失败时怎么定位。
- 能说出为什么不用替代方案（例如为什么用 SSE 不用 WebSocket）。

第三条是面试的主要区分点，也是最容易被跳过的一条。

## 2. 全局验收标准

以下要求适用于每一周，不在逐日清单中重复。

- 每天至少一次可运行的提交，提交信息写清做了什么。
- 新增依赖必须锁定版本，并同步写进 `requirements.txt` 或 `package.json` 与 `ENVIRONMENT.md`。
- 真实密钥只出现在 `.env`；`.env.example` 只放占位符。
- 任何跨周持续演进的代码进 `apps/`、`packages/`、`infra/`，不进 `week-XX/`（约束见 [AGENTS.md](AGENTS.md)）。
- 每周结束前，`ENVIRONMENT.md` 中的启动命令必须能在干净环境下按字面执行成功。
- 遇到的坑写进文档，而不是只留在记忆里。

## 3. 逐周详细大纲

### 第 3 周（09-07 ～ 09-13）Next.js Agent UI + 可观测底座

本周目标：Next.js 能流式消费 FastAPI 的输出，并把「正在生成 / 已完成 / 出错 / 被中止」清楚呈现；**同时把 run_id 事件落库和冒烟评测建起来**。

评测和 Trace 从本周就做，不放到第 9 周。原因：招聘方把 evals 和 guardrails 称为承重技能，面试核心问题是「你怎么知道改动之后变好了」。如果第 3 ～ 8 周的代码没有轨迹和评测，第 9 周补出来的只是事后附加物，讲不出「评测帮我发现了一个回归」这种故事。本周只做最小版本，之后每周往里加用例，不额外增加总工作量。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | `apps/web` 初始化 Next.js + TypeScript + Tailwind；读一遍 AG-UI 事件定义 | `pnpm dev` 可运行；能说出自己的事件设计与 AG-UI 的异同 |
| 2 | FastAPI 增加 `POST /chat/stream`，用 `StreamingResponse` 逐块输出 | `curl -N` 能看到分块到达，而不是一次性返回 |
| 3 | 前端用 Route Handler 作为 BFF 代理后端流；理解 Server/Client Component 边界 | 浏览器逐字渲染；API Key 不出现在客户端网络请求中 |
| 4 | 实现停止生成：前端 `AbortController` + 后端感知断开 | 点击停止后前端立即停；后端日志显示已中断，不再继续消耗 token |
| 5 | 定义前端状态机：`idle / thinking / streaming / done / aborted / error`；超时、断网、500、限流分别给出可读提示与重试 | 六种状态都能手动触发；断网后可重试且不产生重复消息 |
| 6 | **可观测底座**：每次运行生成 `run_id`，把请求、分块、完成、错误、中止写入 `run_events` | 给定 run_id 能从数据库还原整次运行的时间线与耗时 |
| 7 | **冒烟评测 + 复盘**：5 条固定用例的脚本，断言状态流转与错误处理；画事件流图 | 一条命令跑完 5 条用例并输出通过/失败；`week-03/README.md` 完成 |

关键概念（必须能讲清）：

- Server Component 与 Client Component 的边界在哪，为什么流式渲染的消费方必须是 Client Component。
- 为什么选 SSE 而不是 WebSocket：单向、走 HTTP、天然可穿透代理、断线重连语义简单；需要双向或二进制时才换 WebSocket。
- 为什么需要 BFF：密钥收敛、跨域收敛、把后端事件裁剪成前端类型。
- `AbortController` 中止的是浏览器请求，后端是否真正停止取决于后端是否检测到断开。
- 事件日志是事实来源，界面、记忆和评测都是它的投影。

常见坑：

- Nginx / 代理层缓冲导致流式变成一次性返回，需要关闭缓冲。
- 生产环境的响应压缩会让流被攒在缓冲区里。
- 用 `useEffect` 直接发起流式请求，在 React 严格模式下触发两次，产生重复消息。
- Route Handler 里 `await response.json()` 会把流读完，必须转发 `response.body`。
- 事件表里记录了完整用户输入和密钥，后期变成日志泄露；一开始就要脱敏。

资料：Next.js App Router 官方文档的 Routing 与 Data Fetching 两章、MDN 的 `Server-sent events` 与 `AbortController`、Vercel AI SDK 的 streaming 协议说明、AG-UI 协议文档的事件类型清单。



### 第 4 周（09-14 ～ 09-20）Agent 产品全栈：登录、会话、工具过程

本周目标：从“一个流式对话框”变成“有身份、有历史、能看见工具执行过程的产品”。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 登录与会话：注册/登录、密码哈希、JWT 或 Session Cookie | 未登录访问受保护接口返回 401，不返回数据 |
| 2 | 前端登录态：受保护路由、登录后跳转、登出、Token 过期处理 | Token 过期时自动跳登录页，不出现白屏 |
| 3 | Redis 接入：会话缓存、限流、幂等键 | 重复提交同一请求只产生一条消息 |
| 4 | 会话列表与历史恢复：分页加载、切换会话、删除会话 | 刷新页面后能回到原会话并看到完整历史 |
| 5 | 文件上传：大小/类型校验、对象存储或本地存储、上传进度 | 超限文件被拒绝并有明确提示 |
| 6 | 工具执行卡片 + **成本与延迟看板**：工具名、参数、耗时、结果、失败原因；单次运行的 token 与费用 | 工具调用可展开查看参数与结果；界面显示本次运行成本 |
| 7 | 复盘与权限自查：越权访问他人会话是否被拦截；评测用例加到 10 条 | 用 A 用户 Token 访问 B 用户会话返回 403/404；评测脚本仍全绿 |

成本与延迟被招聘方列为承重技能之一（routing、预算上限、spend cap），不只是界面上显示一个数字。本周至少实现单次运行的 token 上限保护。

关键概念：

- JWT 与 Session 的取舍：JWT 无状态但难以即时吊销，Session 可吊销但需要共享存储。作品集里说明选择理由即可，不必两套都实现。
- Token 放在哪：`httpOnly` Cookie 防 XSS 读取，但要处理 CSRF；放 `localStorage` 则相反。
- 幂等：网络重试和用户重复点击是两个不同来源，都要处理。
- 权限校验必须在后端做，前端隐藏按钮不是权限。

常见坑：

- 只在前端判断登录态，后端接口裸奔。
- 会话查询忘记加 `user_id` 过滤，任何人换 ID 就能看别人的对话。
- 上传接口没有大小限制，被大文件打满磁盘或内存。
- Redis 当作数据库使用，重启后数据丢失。

### 第 5 周（09-21 ～ 09-27）RAG 基础：从文档到带引用的回答

本周目标：上传文档后能问答，并且答案能指回原文片段。这一周只做“能跑通”，效果优化放第 6 周。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 概念与链路：为什么需要 RAG，向量检索解决什么，不能解决什么 | 能画出 ingest 与 query 两条链路图 |
| 2 | 文档解析：PDF/Markdown/TXT 提取文本，保留页码或标题位置 | 解析结果保留可回溯的位置信息 |
| 3 | 分块策略：固定长度、重叠、按标题切分的差异 | 同一文档三种切分方式的块数与内容对比记录在案 |
| 4 | Embedding 与 pgvector：建表、写入向量、建索引 | 向量写入成功，`EXPLAIN` 能看到索引被使用 |
| 5 | 相似度检索：Top-K、距离度量、阈值过滤 | 给定问题能返回相关片段及其相似度分数 |
| 6 | 引用渲染：答案中标注来源，点击定位到原文片段 | 前端能展开查看被引用的原始片段 |
| 7 | 复盘：整理一份 15 ～ 20 条问题的评测集 | 评测集入库，含问题、期望来源、期望要点 |

关键概念：

- Embedding 是把语义映射到向量空间，相似度高不等于答案正确。
- 距离度量选择要和索引类型匹配（余弦、内积、L2），写入时是否归一化会影响结果。
- HNSW 与 IVFFlat 的差异：HNSW 建索引慢、查询快、内存高；IVFFlat 需要先有数据才能训练聚类。
- 分块太大导致噪声多，太小导致上下文断裂，这是 RAG 效果的主要变量之一。

常见坑：

- 向量维度和模型不匹配，写入时才报错。
- 忘记为向量列建索引，数据量上来后全表扫描。
- Embedding 调用没有批量和重试，大文档处理中途失败且无法续传。
- 解析时丢掉了页码，导致引用无法定位。

### 第 6 周（09-28 ～ 10-04）RAG 产品化：混合检索、Rerank、评测

本周目标：能用数据回答“为什么这次召回不好”，并在界面上把检索过程暴露出来。这是知识型 Agent 岗位最看重的部分。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 关键词检索：PostgreSQL 全文检索或 BM25 | 专有名词、编号类问题的召回明显优于纯向量 |
| 2 | 混合检索与融合排序（如 RRF） | 同一评测集上，混合检索指标不低于单一策略 |
| 3 | Rerank：交叉编码器或模型重排 Top-N | 重排前后顺序变化和耗时都被记录 |
| 4 | 权限过滤：检索阶段就按用户权限过滤，而不是回答后再删 | 无权限文档不出现在检索结果与引用中 |
| 5 | 评测脚本：命中率、MRR、引用正确率 | 一条命令跑完评测并输出对比表格 |
| 6 | 检索调试 UI：展示查询改写、候选片段、分数、被采纳片段 | 界面上能看到为什么某片段被选中 |
| 7 | 复盘：写一份检索策略对比结论 | 三种策略的指标表和结论落库 |

关键概念：

- 向量检索擅长语义相近，关键词检索擅长精确匹配，业务问题往往同时需要。
- Rerank 是精度换延迟，要给出候选数量和耗时的取舍数据。
- 权限必须前置到检索，后置过滤会导致 Top-K 被无权限文档挤占。
- 评测集固定下来后才能判断“改动是否有效”，否则只能靠感觉。

常见坑：

- 评测集里的问题都是自己照着文档写的，过于友好，测不出问题。
- 改了多个变量后一起评测，无法归因。
- Rerank 模型延迟没有上限保护，拖垮整个回答链路。

### 第 7 周（10-05 ～ 10-11）Agent Runtime：Planning / Memory / Tool Use / Reflection

本周目标：把第 1 周的单次工具调用，重构成可恢复、可观察、**可度量**的多步执行。第 1 周被推迟的「带参数工具、通用工具调度、多工具循环」在这一周补齐。

**术语对齐说明**：腾讯等 JD 的原文是「深入理解 Agent 原理，包括 Planning、Memory、Tool Use、Reflection」，面试题库把 CoT / ReAct / Reflexion 列为第一个考察维度，并把「手写一个 ReAct loop」列为必备项、把 **memory 设计称为区分度最高的领域**。本周做的事情和原计划一致，但必须用这套词汇描述，否则简历和面试对不上关键词。

| Day | 任务 | 对应 JD 术语 | 验收 |
|---|---|---|---|
| 1 | Tool Registry：统一注册 Schema、执行函数、权限、超时 | Tool Use | 新增一个工具只需注册，不改调度代码 |
| 2 | 带参数工具与参数校验：用 Pydantic 校验模型给的参数 | Tool Use | 模型给出非法参数时返回结构化错误，不抛未捕获异常 |
| 3 | 手写 ReAct 循环：Thought → Action → Observation；多重终止条件 | Planning / Reflection | 不看资料能写出 ReAct 骨架；循环有硬上限 |
| 4 | Context Engineering：显式管理进入上下文的每一部分；超长压缩策略 | Context 层 | 能列出一次推理上下文的组成清单及各部分 token 占比 |
| 5 | 记忆分层：短期上下文、运行状态、长期用户记忆分开存 | Memory | 三类记忆分别有明确存储位置和淘汰策略 |
| 6 | 迁移到 LangGraph：节点、边、State、Checkpoint | Planning | 中断后能从 Checkpoint 恢复继续执行 |
| 7 | **轨迹评测 + LLM-as-Judge**：度量工具选择正确率、步数、该拒答时是否拒答 | Evaluation | 20 条用例输出工具选择正确率；能给出一个改动前后的对比 |

Day 7 是本周新增的关键内容。第 6 周的命中率、MRR、引用正确率都是**检索指标**，回答不了 Agent 层面的问题：工具选择是否正确、多步轨迹是否合理、失败后是否恢复。面试题库明确点了 LLM-as-Judge、AgentBench、WebArena、SWE-bench，其中 LLM-as-Judge 是唯一在你项目规模下值得自己实现的。

关键概念：

- Agent Loop 的本质：模型输出 → 判断是否需要工具 → 执行 → 结果回灌 → 再次推理，直到满足终止条件。ReAct 是它最常被考的具体形态。
- 终止条件必须多重：模型给出最终答案、达到最大步数、超时、成本上限、用户中止。
- Reflection 与 Reflexion 的区别：前者是单步自检，后者带跨轮次的经验记忆。
- 失败复合效应：单步 95% 成功率，20 步后约 36%（`0.95²⁰`）。这解释了为什么约束、验证和恢复比换更强的模型更重要。
- Context Engineering 已取代 prompt engineering 成为主流表述，指控制每次推理进入上下文的全部内容，而非只写好提示词。
- Checkpoint 让「可恢复」成为可能，这是长任务 Agent 与玩具 Demo 的分界线。

常见坑：

- 工具执行异常直接抛出，整次运行失败，模型没有机会自我修正。
- 把工具结果原样塞进上下文，几轮后就超出窗口。
- 上下文压缩把关键信息（如工具返回的 ID）压掉了。
- 并发执行多个工具时共享可变状态，出现竞态。
- 轨迹评测用 LLM-as-Judge 时，judge 的提示词本身有偏好，需要用人工标注的小样本校准 judge。

资料：LangGraph 官方文档的 State / Checkpointer 两章、ReAct 与 Reflexion 原始论文的方法部分、Anthropic 关于 context engineering 的工程文章。



### 第 8 周（10-12 ～ 10-18）MCP、审批、Guardrails；开始第一批投递

本周目标：用标准协议接入外部工具，让危险操作必须经过人工确认，**同时开始投递**。

投递不等作品集全部完成。第 8 周时你已经有流式 UI、工具过程、RAG 带引用、ReAct + LangGraph、评测和 Trace，足以支撑面试对话。面试反馈能反向告诉你第 9 ～ 12 周该补什么，这比闭门造车更高效。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | MCP 协议基础：Tool、Resource、Prompt、传输方式；A2A 与 Agent Card 概念对比 | 能说清 MCP 与 A2A 的分工差异 |
| 2 | 用 MCP Python SDK 写一个自己的 Server | 本地 Client 能发现并调用该工具 |
| 3 | 后端作为 MCP Client 接入 2 ～ 3 个 Server | 工具列表由 Server 动态提供，不硬编码 |
| 4 | 审批机制 + Guardrails：工具标注风险级别，高风险中断等待确认；越权与危险参数拦截 | 高风险工具在未确认前不会执行；拦截规则有测试覆盖 |
| 5 | 审批界面：批准、修改参数后批准、拒绝并说明理由 | 三种操作都能改变后续执行路径 |
| 6 | 失败恢复：工具失败后重试、降级、跳过或让模型换方案 | 单个工具失败不导致整次运行不可用 |
| 7 | **简历 v1 + 讲解稿 v1 + 首批 5 ～ 8 家投递**；工具风险分级表成文 | 简历每条经历有可量化结果；投递记录成文 |

关键概念：

- MCP 的价值是把「每接一个工具写一次胶水代码」变成「实现一次协议」。
- 审批必须在服务端强制，前端只是展示层；否则绕过前端就能执行危险操作。
- 修改参数后批准，意味着执行的参数与模型提议的参数不同，两者都要记进事件日志。
- 外部 MCP Server 的返回是不可信输入，要防 Prompt Injection。
- Guardrails 与审批的分工：Guardrails 是自动规则拦截，审批是人工介入；两者都是承重能力，面试必问。

常见坑：

- 审批状态只存在内存，服务重启后待审批任务丢失。
- 审批超时没有默认策略，任务永久挂起。
- 把 MCP Server 的错误信息直接展示给用户，泄露内部路径或配置。
- 简历只写「使用了 LangGraph、MCP、RAG」这类关键词堆叠。招聘方明确把 framework name-dropping 列为被高估的信号，每条必须带结果或数字。



### 第 9 周（10-19 ～ 10-25）生产化：后台任务、云部署、评测入 CI

本周目标：让项目具备可部署、可观测、可回归的工程属性。**本周不可压缩**——它直接决定作品集能否被评估。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 后台任务：文档解析、长任务异步化与进度上报 | 上传大文档不阻塞接口，前端能看到进度 |
| 2 | Trace 完善（OpenTelemetry 或 Langfuse），对接第 3 周的 run_events | 能按 run_id 还原全部步骤、耗时和 token |
| 3 | 成本控制：按会话/运行/工具统计；预算上限与模型路由 | 超出预算上限时运行被拦截，而不是静默烧钱 |
| 4 | 评测入 CI：检索评测 + 轨迹评测作为回归门禁 | PR 上自动跑评测；指标下降时 CI 失败 |
| 5 | 安全清单：Prompt Injection、越权、SSRF、密钥管理、输出转义 | 逐条自查并记录处理方式 |
| 6 | **云部署**：选定一个平台，让项目有公网可访问地址 | 陌生人点开链接就能用；README 写明部署方式 |
| 7 | CI/CD 与文档：lint、类型检查、测试、构建；干净环境验证 | 全部检查通过；按 README 在干净环境跑通 |

云部署是新增的必做项。JD 普遍要求 AWS/Azure/GCP 之一，而「有公网地址的作品」和「只能本地跑的作品」在投递时的说服力差距很大。国内可选阿里云/腾讯云轻量服务器，海外可选 Railway、Fly.io、Render，前端可直接放 Vercel。选一个能在一天内跑通的，不要在这一步陷入 K8s。

关键概念：

- Trace 的意义在于 Agent 是非确定性的，没有轨迹就无法定位问题。
- 评测进 CI 才能防止「修好一个问题，弄坏三个」。这是「你怎么知道改动之后变好了」的可核查答案。
- Prompt Injection 的主要防线是权限最小化和工具审批，而不是靠提示词请求模型不要听坏话。
- 部署文档的正确性只能通过在干净环境实际执行来验证。

常见坑：

- Trace 里记录了完整的用户输入和密钥，造成日志泄露。
- 评测在 CI 里真实调用模型，费用和不稳定同时失控；应固定样本或使用缓存/录制。
- Compose 里数据库没做健康检查，应用先起来直接连接失败。
- 部署时把 `.env` 打进镜像。

### 第 10 ～ 11 周（10-26 ～ 11-08）主项目收尾

**这两周不是开始做项目，而是把第 3 ～ 9 周已有的代码收尾成作品集。** 项目定义见 `LEARNING_PLAN.md` 第 9 节。

第 10 周（产品化与失败路径）：

| Day | 任务 |
|---|---|
| 1 ～ 2 | UI 打磨：空状态、加载骨架、移动端可用、无障碍基本项 |
| 3 | 刻意构造两个失败恢复场景并确保可复现 |
| 4 | 架构图与数据流图 |
| 5 | README：定位、截图/GIF、启动命令、已知限制 |
| 6 ～ 7 | 端到端自测；干净环境验证；修复暴露的问题 |

第 11 周（表达与可选补齐）：

| Day | 任务 |
|---|---|
| 1 | 评测报告：策略对比表 + 一个「评测发现回归」的具体案例 |
| 2 ～ 3 | 演示视频：3 ～ 5 分钟，必须包含失败恢复路径 |
| 4 | 第二框架（CrewAI 或 OpenAI Agents SDK）：读文档 + 20 行 demo，能讲清定位差异 |
| 5 | 多 Agent 协作：画架构图，说清什么场景不该用 |
| 6 | 第二项目可行性评估：主项目达标才启动，否则跳过 |
| 7 | 复盘 + 第二批投递准备 |

刻意演示的失败场景至少两个，例如工具超时后重试成功、检索不到内容时明确说明无法回答而不是编造。**主动展示失败处理比只演示顺利路径更有说服力**，招聘方看的是 build → measure → refine 的痕迹。



### 第 12 周（11-09 ～ 11-15）面试强化与第二批投递

第一批投递已在第 8 周完成，本周重点是把面试反馈补齐并扩大投递面。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 根据第 8 ～ 11 周的面试反馈修订简历 | 每条经历有可量化结果 |
| 2 | 项目讲解稿定版：2 分钟版和 8 分钟版 | 不看稿能讲完 |
| 3 | 技术问答：过完第 4 节题库 | 每题都能给出取舍理由 |
| 4 | 现场编码：手写 ReAct 循环、流式渲染、状态机 | 40 分钟内写出可运行的 ReAct 最小实现 |
| 5 | 第二批定向投递（15 ～ 20 家） | 投递完成并记录 |
| 6 | 模拟面试与复盘 | 记录被问住的问题并补齐 |
| 7 | 整理投递期继续学习清单 | 明确下一阶段补什么 |



## 4. 面试题库

按周积累，每题的目标是能讲出取舍，不是背定义。标记 **[必问]** 的来自公开面试题库统计，出现频率最高。

### 评测、可观测与可靠性（招聘方称为承重技能，优先准备）

1. **[必问]** 你怎么知道改动之后变好了？
2. **[必问]** 你怎么知道你的评测本身可信？
3. 检索评测和 Agent 轨迹评测有什么区别，各自度量什么？
4. LLM-as-Judge 的偏差从哪来，怎么校准？
5. 举一个「评测帮你发现了回归」的具体例子。
6. 单次运行的成本和延迟是多少？超预算时怎么拦截？
7. Agent 是非确定性的，你怎么写测试？
8. 线上出现 Agent 行为异常，你的定位路径是什么？

前两题是招聘方明确点出的第一区分点，必须用具体数字和真实案例回答，不能只讲方法论。

### Agent 原理与架构

1. **[必问]** 手写一个 ReAct 循环。
2. Planning、Memory、Tool Use、Reflection 在你的代码里分别落在哪？
3. **[区分度最高]** 短期上下文、运行状态、长期记忆分别存在哪里，为什么要分开？记忆如何淘汰？
4. CoT、ReAct、Reflexion、Self-Refine 的差别和适用场景。
5. Agent Loop 的终止条件有哪些？只靠最大步数会出什么问题？
6. 单步 95% 成功率，20 步后整体成功率是多少？这个数字说明什么？
7. Context Engineering：一次推理的上下文由哪几部分拼成？超长时压缩掉什么、怎么避免丢关键信息？
8. 工具执行失败，是抛异常还是把错误返回给模型？依据是什么？
9. LangGraph 的 Checkpoint 让哪些能力成为可能？
10. 什么场景**不该**用多 Agent？

### 工具与协议

1. **[必问]** MCP 解决了什么问题？没有它之前是怎么做的？
2. MCP 的 Tool、Resource、Prompt 分别用在什么场景？
3. MCP 和 A2A 的分工差异是什么？
4. Function Calling 和 Computer Use / GUI Agent 的取舍。
5. Tool Registry 怎么设计才能新增工具不改调度代码？

### 安全与人工介入

1. **[必问]** Prompt Injection 的现实防线有哪些？
2. **[必问]** Human-in-the-loop 怎么设计？审批状态存在哪？
3. Guardrails 和审批的分工是什么？
4. 外部 MCP Server 的返回为什么是不可信输入？
5. 这个项目有哪些安全风险，分别怎么处理？

### RAG

1. **[必问]** 完整讲一遍你的 RAG pipeline。
2. RAG 召回质量差，你的排查顺序是什么？
3. 向量检索和关键词检索各自的失效场景是什么？
4. Rerank 带来的收益和成本怎么量化？
5. 文档权限如何在检索阶段生效，而不是回答后过滤？
6. 为什么选 pgvector 而不是 Pinecone / Weaviate / Milvus？

### Agent 前端

1. Agent UI 和普通聊天 UI 的本质区别是什么？
2. [模型 token、tool call、approval、error 分别对应前端哪些状态？状态机怎么画？](interview-questions/frontend/agent-ui/chat-state-machine.md)
3. 为什么选 SSE 而不是 WebSocket？什么时候必须换成 WebSocket？
4. 你的事件协议和 AG-UI 有什么异同？为什么这样选？
5. 用户点击「停止生成」，从浏览器到后端到模型分别发生了什么？
6. 流式响应过程中网络断开，如何恢复而不产生重复内容？
7. 一次运行有 30 个工具调用，界面怎么设计才不淹没用户？
8. 前端如何保证与后端的类型契约不漂移？

### 后端与工程

1. [一次 Agent 运行需要记录什么，如何按 `run_id` 还原时间线？](interview-questions/backend/web/agent-run-observability.md)
2. 用户重复点击发送，如何保证不产生两条消息？
3. Redis 在这个项目里存什么，丢了会怎样？
4. 数据库迁移在多人多环境下如何避免冲突？
5. 同一套项目怎么在 macOS、Windows 和服务器上运行？
6. 你的部署方式是什么？为什么不用 K8s？

### 系统设计（面试重头戏，各准备一张架构图）

1. 设计一个支持多用户的 RAG 系统，数据量到千万级文档。
2. 设计一个通用的 Tool Calling 框架。
3. 设计一套 Agent 可观测系统。
4. 设计一个多 Agent 客服系统。
5. 设计一个 Computer Use Agent 的安全边界。

### 反问面试官

1. 团队的 Agent 产品里，前端负责到哪一层？
2. 工具和 MCP 的接入规范由谁定义？
3. 效果回归靠人工还是自动评测？评测集谁维护？
4. 线上出现 Agent 行为异常，目前的定位手段是什么？
5. 成本和延迟目前是谁在负责优化？

## 5. 每周复盘模板

每周第 7 天把下面模板填入 `week-XX/README.md`。

```markdown
# 第 X 周完成记录

周期：YYYY-MM-DD ～ YYYY-MM-DD

## 本周目标
（一句话）

## 已完成
- [x] ...

## 未完成及原因
- ...（明确挪到哪一周）

## 关键决策
| 决策 | 备选方案 | 选择理由 |
|---|---|---|

## 踩过的坑
| 现象 | 原因 | 解决方式 |
|---|---|---|

## 能讲清的问题
1. ...

## 还讲不清的问题
1. ...（下周补）
```

“还讲不清的问题”是这份模板里最重要的一栏，不要留空。

## 6. 进度自检表

每周末对照勾选，用于判断是否可以进入下一周。连续两周有未完成项时，优先补齐而不是继续推进。

- [x] 第 2 周：分层、配置、PostgreSQL、Alembic、pytest、Docker Compose 全部落地
- [x] 第 3 周：流式渲染 + 停止 + 六态状态机 + **run_id 事件落库 + 5 条冒烟评测**
- [ ] 第 4 周：登录、会话恢复、Redis、权限、上传、工具卡片、**成本看板**
- [ ] 第 5 周：文档问答可用且带引用
- [ ] 第 6 周：三种检索策略可对比，评测脚本可运行
- [ ] 第 7 周：**手写 ReAct** + 记忆分层 + LangGraph 恢复 + **轨迹评测有正确率数字**
- [ ] 第 8 周：MCP 工具可用 + 审批三种操作可用 + **首批投递完成**
- [ ] 第 9 周：Trace、评测入 CI、安全清单、**公网可访问地址**
- [ ] 第 10 周：主项目产品化完成，两个失败恢复场景可演示
- [ ] 第 11 周：评测报告 + 演示视频 + 第二框架能讲清
- [ ] 第 12 周：讲解稿终版、题库过完、第二批投递完成

## 7. 风险与应对

| 风险 | 信号 | 应对 |
|---|---|---|
| 广度失控 | 同时深入 3 个框架 | 冻结技术栈；第二框架只做概念级（`LEARNING_PLAN.md` 第 4 节「概念级了解」） |
| 前端优势被稀释 | 连续两周只写 Python | 强制补一天 Agent UI |
| 只有 Demo 没有工程 | 没有评测、没有 Trace | 第 3 周和第 9 周的可观测任务不可跳过 |
| 关键词对不上 JD | 简历里没有 Planning / Memory / Reflection / Context Engineering / Evaluation | 第 7 周按术语表对齐后重写简历 |
| 进度落后 | 累计落后超过 5 天 | 按下面的取舍顺序砍 |
| 时间不足 | 每天不足 4 小时 | 按 1.2 节比例缩放，但不砍验收和复盘 |

进度落后时的取舍顺序（从先砍到后砍）：

1. 第 11 周的第二框架和多 Agent（纯概念级，可挪到投递期补）。
2. 可选第二项目（本来就是有余力才做）。
3. 第 6 周的 Rerank 深度优化（保留混合检索和评测即可）。
4. 第 4 周的文件上传（RAG 阶段会重做一次）。
5. 第 10 周的 UI 打磨深度。

**不参与压缩的部分**：第 3 周的可观测底座、第 7 周的 ReAct 与轨迹评测、第 9 周整周、第 8 周的首批投递。前三项是面试第一区分点的来源，最后一项是时间不可逆的机会成本。

## 8. 文档关系

- [项目入口](README.md)
- [12 周主计划](LEARNING_PLAN.md)
- [仓库与目录规则](AGENTS.md)
- [环境安装与启动](ENVIRONMENT.md)
- [岗位与技能校准](SKILL_GAP_ANALYSIS.md)
- [当前进度与交接](LEARNING_HANDOFF.md)


