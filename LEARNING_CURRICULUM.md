# AI Agent 学习详细大纲

方向：前端 / 全栈产品工程
更新时间：2026-09-22（Asia/Shanghai）

[LEARNING_PLAN.md](LEARNING_PLAN.md) 决定“12 周走什么路线”，这份文档决定“每一天具体做什么、做到什么程度算完成”。两份文档分工固定：

| 文档 | 只负责 | 不负责 |
|---|---|---|
| `LEARNING_PLAN.md` | 目标岗位、能力配比、技术选型、12 周周目标 | 逐日任务、验收清单、资料、坑 |
| `LEARNING_CURRICULUM.md`（本文） | 逐日任务、可验证验收、官方资料、常见坑、面试题 | 修改路线和周目标 |
| `LEARNING_HANDOFF.md` | 今天学到哪、下一课是什么 | 完整路线和完整大纲 |

第 1 周记录见 [week-learning/week-01/README.md](week-learning/week-01/README.md)，第 2 周复盘见 [week-learning/week-02/REVIEW.md](week-learning/week-02/REVIEW.md)，第 2 周原始逐日计划保留在 `LEARNING_PLAN.md` 第 8 节；本文从第 3 周开始展开。

本文第 3 ～ 12 周内容已按 2026-08-31 的岗位调研调整过，调研依据与差距分析见 [SKILL_GAP_ANALYSIS.md](SKILL_GAP_ANALYSIS.md) 第 10 节。

> 当前执行口径（2026-09-21）：第1～4周已完成，正式进度4/12（33.3%）；第5周路径边界与受限文本读取已完成，可信执行上下文构造已完成，执行器上下文透传与缺失拒绝已完成，只读文件工具适配与能力过滤已完成，流式聊天上下文装配已完成，受限目录枚举服务已完成，目录工具适配与注册已完成，受限单文件文本搜索服务已完成，单文件搜索工具适配与注册已完成，只读工具链PC闭环已验收，受限命令请求与结果契约已完成，命令输出有界缓冲已完成，单路异步排空已完成，双路并发排空与失败收尾已完成，命令环境白名单已完成，Docker Sandbox最小隔离已验收，Docker Sandbox创建参数已完成，创建响应解析与身份确认已完成，有界Docker客户端调用已完成；Sandbox创建与身份确认装配已完成；创建结果未确认后的只读核对已完成；已确认未启动容器的显式清理已完成；启动前执行配置复核已完成；Sandbox隔离与资源配置复核已完成；容器停止与停止状态确认已完成；容器启动与启动结果确认已完成；命令退出结果与退出码确认已完成；Docker attach输出帧解析已完成；attach异步读取与收尾已完成；attach HTTP响应读取与校验已完成；attach请求构造已完成；固定socket的attach连接生命周期已完成；启动前订阅与执行收尾协调已完成；已退出容器的显式清理已完成；执行结果到CommandResult的适配已完成；创建、执行与清理的统一编排已完成；失败执行的只读状态核对已完成；受限命令工具适配与安全错误映射已完成；工具定义异步执行支持已完成；Runtime同步/异步工具分派已完成；命令工具内部恢复证据接收已完成；异步工具超时与取消分类已完成；命令恢复记录作用域装配已完成；请求级命令工具注册与能力开放已完成；受限命令工具PC闭环验收已完成；命令结果结构化展示已完成；受限跨目录文件查找已完成；文件查找工具适配与注册已完成；文件查找PC闭环验收已完成；单文件文本替换预览已完成；授权文件预览与基线摘要已完成；文件修改预览工具适配与注册已完成；文件修改预览PC闭环验收已完成；文件修改预览结构化展示已完成；文件修改提案持久化已完成；文件修改提案授权查询已完成；文件修改提案工具适配与注册已完成；文件修改提案PC闭环验收已完成；文件修改提案回执结构化展示已完成；文件修改提案详情HTTP接口已完成；文件修改提案详情BFF代理已完成；文件修改提案详情按需展示已完成；下一课文件修改提案审批状态与决策服务。第4周恢复限定原生本机进程退出后恢复数据库状态，不是执行checkpoint续跑。高级Runtime仍留在第7周。

## 1. 学习方法

### 1.1 每个新知识按四步推进

1. 是什么：用一句话说清它解决的问题。
2. 为什么需要：不用它时会出现什么具体故障。
3. 怎么用：写出最小可运行示例。
4. 放进项目：接进 `apps/api` 或 `apps/web` 跑通。

只做第 3 步会变成抄代码，面试时讲不出取舍。

### 1.2 每天的固定顺序

按每天 4 ～ 6 小时安排：

```text
40 分钟：只读官方文档中今天要用的那一节
150 分钟：自己写，允许查文档，不允许直接抄完整实现
40 分钟：跑通并按验收清单逐条勾选
20 分钟：跑一遍评测脚本，确认没有回归
20 分钟：记录今天的三个问题和答案，追加到 week-learning/week-XX/REVIEW.md
10 分钟：git commit，更新 LEARNING_HANDOFF.md
```

投入时间不足时按比例缩放，但顺序不要变，尤其不要砍掉最后三步。从第 3 周起「跑评测」是每天的固定动作，这是「你怎么知道改动之后变好了」的日常来源。

### 1.3 卡住时的处理顺序

1. 看终端里的真实报错，不看浏览器上的提示。
2. 写最小复现脚本，剥掉框架，确认是自己的代码还是依赖问题。
3. 查官方文档和 GitHub Issue。
4. 超过 45 分钟未解决，让 AI 解释原因，但要求先讲原理再给代码。
5. 解决后把这个坑写进 `ENVIRONMENT.md` 常见问题或本文对应周的坑清单。

### 1.4 什么算“学完了”

课程结束以核心实现和工程验收通过、重要面试题归档、交接更新为准，不要求学习者当堂回答问题。

教练每课筛选重要知识点，将题目、参考答案、失败场景、定位方法和替代方案取舍放入 `interview-questions/` 对应分类；优先更新已有题目，同步索引，标记复习优先级与“参考答案已整理、尚未模拟”。本大纲第 4 节保留问题及链接，不重复存放全文答案。

独立实现、解释失败路径和讲清取舍仍是能力目标；口头表达与追问检验留到用户要求模拟面试时进行，不作为每课结束门槛。

## 2. 全局验收标准

以下要求适用于每一周，不在逐日清单中重复。

- 每天至少一次可运行的提交，提交信息写清做了什么。
- 新增依赖必须锁定版本，并同步写进 `requirements.txt` 或 `package.json` 与 `ENVIRONMENT.md`。
- 真实密钥只出现在 `.env`；`.env.example` 只放占位符。
- 任何跨周持续演进的代码进 `apps/`、`packages/`、`infra/`，不进 `week-XX/`（约束见 [AGENTS.md](AGENTS.md)）。
- 每周结束前，`ENVIRONMENT.md` 中的启动命令必须能在干净环境下按字面执行成功。
- 遇到的坑写进文档，而不是只留在记忆里。

## 3. 逐周详细大纲

### 第 3 周（09-07 ～ 09-13）流式 Agent UI + 最小 Runtime

本周目标：完成第一个真正的纵向 Agent 闭环。Next.js 能流式消费 FastAPI 的输出，后端具备安全的最小 Agent Loop，界面能展示模型文本、工具过程与唯一终态，并用 `run_id` 还原整次运行。

评测和 Trace 从本周就做，不放到第 9 周。原因：招聘方把 evals 和 guardrails 称为承重技能，面试核心问题是「你怎么知道改动之后变好了」。如果第 3 ～ 8 周的代码没有轨迹和评测，第 9 周补出来的只是事后附加物，讲不出「评测帮我发现了一个回归」这种故事。本周只做最小版本，之后每周往里加用例，不额外增加总工作量。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 初始化 Next.js + TypeScript + Tailwind；定义 Agent 事件与 UI 状态 | `pnpm dev` 可运行；能解释领域事件与 UI 状态为什么不能混为一谈 |
| 2 | FastAPI 流式端点 + Next.js BFF 原样透传；处理任意网络分块 | 浏览器逐块渲染；API Key 不出现在客户端；半条/多条事件都有测试 |
| 3 | 六态状态机、可读错误、重试和唯一终态 | `idle/thinking/streaming/done/aborted/error` 均可验证；终态后不再消费事件 |
| 4 | `AbortController`、超时原因与 Redis 跨实例取消 | 用户停止与超时语义一致；后端停止模型/工具并回滚未完成轮次 |
| 5 | **最小 Runtime**：Tool Registry、Pydantic 参数校验、ToolAction/FinalAnswer、顺序循环、超时与步数上限 | 未知工具和非法参数不进入执行器；成功/失败 Observation 都能回灌模型 |
| 6 | Runtime 接入正式聊天；工具/文本/终态事件落库；工具卡片与运行指标 | 真实 DeepSeek 完成多步骤调用；给定 `run_id` 可还原步骤、耗时、Token 和费用 |
| 7 | **冒烟评测 + 复盘**：固定用例断言状态、工具错误、取消与持久化；画事件流图 | 一条命令跑完用例并输出通过/失败；`week-learning/week-03/REVIEW.md` 完成 |

关键概念（必须能讲清）：

- Server Component 与 Client Component 的边界在哪，为什么流式渲染的消费方必须是 Client Component。
- 为什么选 SSE 而不是 WebSocket：单向、走 HTTP、天然可穿透代理、断线重连语义简单；需要双向或二进制时才换 WebSocket。
- 为什么需要 BFF：密钥收敛、跨域收敛、把后端事件裁剪成前端类型。
- `AbortController` 中止的是浏览器请求，后端是否真正停止取决于后端是否检测到断开。
- 事件日志是事实来源，界面、记忆和评测都是它的投影。
- 模型输出的工具名和参数都不可信；白名单解析与参数校验是 Runtime 的安全边界。
- 基础 Agent Loop 必须先于工具过程 UI，否则前端只能展示伪造事件，这正是它从原第 7 周前移的原因。

常见坑：

- Nginx / 代理层缓冲导致流式变成一次性返回，需要关闭缓冲。
- 生产环境的响应压缩会让流被攒在缓冲区里。
- 用 `useEffect` 直接发起流式请求，在 React 严格模式下触发两次，产生重复消息。
- Route Handler 里 `await response.json()` 会把流读完，必须转发 `response.body`。
- 事件表里记录了完整用户输入和密钥，后期变成日志泄露；一开始就要脱敏。
- 同步工具阻塞事件循环，或把普通工具异常直接抛出导致整次运行无法自我修正。

资料：Next.js App Router 官方文档的 Routing 与 Data Fetching 两章、MDN 的 `Server-sent events` 与 `AbortController`、Vercel AI SDK 的 streaming 协议说明、AG-UI 协议文档的事件类型清单。



### 第 4 周（09-14 ～ 09-20）本地工作台：Workspace、Task 与恢复

2026-09-20 收尾完成：本地工作台、创建幂等、URL/消息/运行历史及摘要恢复、执行互斥与进程内预算、有历史任务的安全删除、本机异常进程退出恢复均已接通。旧数据无身份、非本机或停止证据不足时明确拒绝，不提供强制清理。恢复不自动重放工具；源码交付、跨进程总预算与checkpoint分别按后续周次推进。正式4/12，下一课进入第5周。

本周目标：在用户 PC 本地运行 Web UI 与 Agent Runtime，无需注册登录。用户配置自己的模型服务，创建 Workspace，后续绑定本地项目目录并恢复任务。2026-09-15 用户确认转向本地优先；此前认证课程保留为已完成的账号模式扩展，不再作为主流程门槛，不继续实现匿名访客系统。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 本地单用户身份、启动配置与 BFF/API 内部凭证 | 无登录 Cookie 可聊天、取消和创建 Workspace；稳定身份、并发首次访问不重复创建；伪造内部凭证被拒绝 |
| 2 | PC 工作台与 Workspace 创建、列表 | 1366×768 和 1920×1080 可用；成功、明确拒绝、结果未确认分别展示，不自动重试创建 |
| 3 | 系统目录选择与 Workspace 自动绑定 | 点击选择文件夹后直接校验并保存，取消不写入；规范化路径并验证目录存在；目录范围和符号链接边界清晰；不能接管原账号或其他 Workspace 资源 |
| 4 | 对话式 Task：笔形入口、首发创建、首轮自动标题；列表、分页、切换、删除及运行历史 | 刷新后回到原 Task，消息、状态和时间线一致 |
| 5 | 幂等、并发预算、短期状态与取消通知 | 重复提交不产生重复 Task；缓存丢失不损坏事实数据 |
| 6 | 可观测收尾：成功/失败摘要、Token/费用/模型与工具耗时 | 正常与已产生指标的错误均有准确摘要，不伪造网络失败成本 |
| 7 | 本地产品验收：启动、恢复、跨站请求、目录越界和错误恢复 | 固定 PC 场景可重复；内部凭证不进入浏览器；本地端口不对外开放 |

截至 2026-09-20，本阶段学习内容与实现范围如下（表内测试数量保留各课当时的范围）：

| 内容 | 学习与工程结果 | 当前边界 |
|---|---|---|
| Workspace 与目录 | 项目创建/列表、系统选择目录并绑定；理解本地身份、归属检查及目录边界 | 绑定目录不代表获得任意文件读写或命令执行权限 |
| Task 创建 | 学习者完成创建事务、HTTP/BFF 及原创建表单；理解 Task/Conversation 同事务与提交结果未确认 | 原手填标题表单已被对话式入口替换；创建幂等已端到端接入；刷新恢复重试信息仍不在范围 |
| 对话式工作台 | 教练按明确授权完成项目/任务侧栏、笔形新建入口、首发创建、固定会话续聊、历史读取和首轮标题总结 | 原工作台由教练按授权实现；本次 URL 刷新恢复由学习者实现 |
| 异步一致性 | 首轮标题失败保留消息摘录，条件更新防止迟到总结覆盖后来标题；旧历史请求不污染新任务 | 辅助标题费用未计入 AgentRun 指标 |
| 任务详情读取 | 学习者完成 TaskDetailResponse、详情服务与 HTTP；理解直接定位不依赖列表分页、项目/会话双重归属、只读事务和关闭 Session 后序列化 | HTTP 新增 18 条，相关 62 条通过；BFF 与 URL 自动恢复已接入 |
| 任务详情 BFF | 学习者完成详情路由、公开字段解析与项目/任务匹配；复用本地凭证、no-store、取消超时及错误脱敏 | BFF 新增 58 条；本次真实浏览器恢复链路已覆盖 |
| URL 选中与刷新恢复 | 学习者完成 URL 定位、独立详情恢复、错误态禁止发送、取消隔离及创建后保留组件 key；教练修正 effect 挂载时机并补测试 | URL 单元新增 13 条，Workspace 396 条、聊天 84 条、真实浏览器 8 组通过；未发送草稿与历史运行状态不在本课范围 |
| 空任务删除事务 | 学习者实现授权加锁、空历史检查、会话/任务同事务删除及回滚；理解取消终态不能证明执行停止 | 新增 27 条，Task 领域 116 条通过；仅支持无消息且无任何 Run 的任务，当时仅完成服务，现已接通 HTTP/BFF |
| 空任务删除 HTTP/BFF | 学习者完成 DELETE HTTP；教练按明确授权实现同源 BFF，204 空正文、错误白名单与删除结果未确认语义 | HTTP 42 条；后端相关回归 422 条；BFF 43 条、Workspace 439 条通过 |
| 空任务删除 UI | 学习者完成 Provider 删除状态、同步防重、迟到结果隔离、详情确认及 URL 清理；教练按截图授权调整悬停图标、确认框与项目菜单 | 仅支持空任务；未知结果不重试 DELETE，详情 200 仍保留未确认态；刷新页面不保留操作状态 |
| 本地任务会话边界 | 学习者实现本地模式只读取已有 Task 会话、双重归属校验、Run 创建事务锁与缓存前授权；账号模式保留兼容 | 新增 40 条，相关后端 189 条、工作台 1 组及本地模式 3 组浏览器通过；普通非流式在途模型调用不自动取消 |
| 任务运行历史查询 | 学习者完成概要响应与只读服务；先授权再查询，ID 倒序游标、多取一条判断下一页、关闭 Session 前构造普通数据 | 新增 44 条专项通过；已接 HTTP/BFF/UI，不读取事件正文，不将未知状态归类为终态 |
| 运行历史 HTTP | 学习者接入 GET runs、参数校验、404/422/500 安全映射及 no-store | 新增 42 条 HTTP 专项通过；已接 BFF/UI，复用本地身份和访问边界 |
| 运行历史 BFF | 教练按明确授权完成同源路由、凭证隔离、分页输入与概要响应校验、取消超时及错误脱敏 | 新增 79 条、Workspace 518 条通过；TypeScript/ESLint 通过，UI 已接入 |
| 运行历史 UI | 学习者实现右侧概要列表、分页刷新、保留旧结果重试及任务切换隔离；教练恢复原运行摘要 | 浏览器 5 组、Workspace 518 条、聊天 84 条通过；PC 截图视查，手动刷新快照 |
| 运行详情 BFF | 教练按明确授权接入同源 GET，验证 Run 与事件结构，复用流解析器并兼容取消事件；未知 payload 不透传 | 新增 61 条、Workspace 579 条和聊天 84 条通过；详情 UI 已接入 |
| 历史运行详情 UI | 教练按明确授权接入选择、只读事件时间线、返回保留列表、错误重试和旧请求隔离；与实时聊天状态分离 | 浏览器 8 组含原列表回归通过；完整历史费用摘要/执行恢复尚未实现 |
| 数据库启动职责 | 教练按明确授权移除启动自动建表，读取源码/数据库 migration heads，不一致拒绝启动；浏览器夹具使用真实迁移 | 新增 7 条专项、后端全量 1101 条通过；开发库只读就绪检查通过 |
| PC 右侧栏体验修正 | 按截图改为行式摘要、可拖动/键盘调宽、宽度记忆；保留 18px 基线和聊天挂载状态 | 浏览器 9 组及 1366px 窄/宽截图通过，320px 长指标无拆行 |
| Markdown 回复展示 | 按用户问题直接接入统一助手 Markdown 组件，覆盖历史和流式内容；GFM 表格/列表、代码内部滚动、安全 HTML/链接边界 | 浏览器3组、聊天84/Workspace579及类型/lint通过；不计新增周进度 |
| 创建幂等模型与迁移 | 学习者完成请求键作用域、指纹、唯一约束和 Task 删除 SET NULL；教练修正表名与主键参数拼写 | 新增 17 条专项、迁移目录 43 条通过；开发库升级并检查无结构差异；事务服务现已验收 |
| 创建幂等事务服务 | 学习者完成授权加锁、规范化指纹、作用域查询、重放/冲突/删除拒绝及三记录同事务；教练仅做等价 lint 修正和补换行 | 新增 37 条专项，Task/Workspace/local 545 条通过；覆盖真实锁等待、提交确认丢失、归属重查和 Session 缓存；HTTP/BFF/UI 现已接入 |
| 创建幂等 HTTP | 学习者完成可选请求键校验与转交、安全 409/422 映射，保留本地访问边界及未确认语义；核心无需修正 | 新增 37 条真实 HTTP 专项通过；覆盖重放、删除、SQL 回滚及提交后响应失败的同键找回；BFF/UI 现已接入 |
| 创建幂等 BFF | 教练按本课明确授权实现可选键严格校验/原样转发、409/422 错误白名单，保留凭证隔离和未确认语义；不生成键或自动重试 | 创建路由 92 条（新增 33），Workspace 全量 612 条、TypeScript/ESLint 通过；fetch 模拟，UI 现已发送键 |
| 创建幂等 UI | 教练按本课明确授权实现创建意图内存保存、同键重试、切换恢复草稿、明确换键和历史读取后手动发送；用户要求完成后解释实现 | 新增浏览器4组及既有流程4组通过，关键恢复复跑1组通过；Workspace612/聊天84及类型/lint通过；刷新/关闭不保存未发送内容与键，创建幂等不等于消息幂等 |
| 会话执行占用模型与迁移 | 学习者实现单会话主键占用、持有者 token、数据库时间与非级联外键；区分 Run 终态与执行结束 | 新增13条专项、迁移目录56条通过；占用存在时真实删除回滚；运行链路尚未使用占用 |
| 会话执行占用事务服务 | 学习者完成先授权加锁、原子获取、持有者精确释放及独立事务边界；核心无需修正 | 新增 42 条专项，runtime/tasks/local 共 508 条通过；覆盖真实竞争、回滚、提交确认丢失与迟到释放；尚未接入聊天运行 |
| 执行后台线程跟踪 | 学习者完成 ExecutionThreads：强引用跟踪、取消隔离、关闭后拒绝新工作、等待全部结束后传播取消；核心无需修正 | 新增 12 条真实线程专项，随 Agent Loop 共 28 条通过；未接入工具、聊天入口或占用释放 |
| 会话执行作用域 | 学习者组合短事务获取、后台线程收尾和精确释放，兼容 asyncio 与 AnyIO 取消收尾；修正参考返回标注为 AsyncGenerator | 新增 17 条隔离 PostgreSQL 专项，Runtime 共 165 条通过；尚未接入真实聊天入口 |
| Agent Loop 工具线程接入 | 学习者让两种 Runtime 入口传递同一 ExecutionThreads，登记真实工具执行，保留原超时/错误协议与兼容调用；核心无需修正 | 新增 12 条，Runtime 共 177 条通过；真实 PostgreSQL 验证超时/取消后占用保留至工具结束；聊天入口尚未接入 |
| 聊天入口执行占用 | 教练按明确授权接入 request 级依赖、两个入口、全链路线程跟踪、监听器/生成器收尾、Run 兜底终态和缓存失效；API/BFF/UI 安全 409 | 新增 ASGI/PostgreSQL 12 条、后端全量 1300 条通过；BFF 37/聊天状态85、类型及 lint 通过；PC 并发/取消恢复两组通过，截图已视查；详情见 ENVIRONMENT.md |
| 会话占用只读查询 | 学习者完成先授权、只选公开时间字段及不可变快照；教练仅补末尾换行 | 新增 12 条隔离 PostgreSQL 专项，Runtime 189 条通过；验证无写入/提交、无 token 暴露、Run 状态独立及异常传播；HTTP/UI 尚未接入 |
| 会话占用查询 HTTP | 学习者完成公开响应模型及 GET 接口；复用身份和错误边界，线程执行同步查询 | 新增 13 条真实 HTTP/PostgreSQL 专项，相关回归 90 条通过；占用仍返回 200，安全错误与成功均 no-store，查询不改业务记录；BFF/UI 待接入 |
| 会话占用查询 BFF | 学习者完成解析器、代理及 GET 路由；可信本地凭证、公开契约重建和错误脱敏 | 新增 60 条，Workspace 672 条、类型/ESLint 通过；模拟上游覆盖取消/超时、不重试与 no-store；UI 待接入 |
| 会话占用查询 UI | 学习者接入右侧只读查询/手动刷新；会话 key、请求取消及回调身份防迟到覆盖 | 新增组件异步测试8条，Workspace680/聊天85、类型/ESLint通过；PC浏览器5组通过，含真实占用与释放查询、失败重试及切换/关闭隔离；1366/1920截图已视查 |
| 进程内执行并发预算组件 | 学习者完成正整数容量、循环绑定、同步准入与作用域归还；核心无需修正 | 新增23条，随线程跟踪共35条通过；验证真实协程竞争及异常/取消/跨循环边界，尚未装配到聊天入口 |
| 聊天入口并发预算 | 学习者完成配置/依赖/错误映射；教练补漏掉的 lifespan 共享实例装配与测试配套 | 新增后端23条、全量1371条通过；认证/BFF207、聊天86、类型及lint通过；预算保留至响应与后台收尾，单进程两个聊天入口共享 |
| 历史运行摘要恢复 | 学习者复用唯一终态事件与既有摘要卡片恢复步骤/用量/耗时/成本，不补零或拼接不同事件 | 新增23条，Workspace703/聊天86、类型/ESLint通过；PC浏览器4组及1366/1920截图验收通过；非法详情契约仍由既有解析器拒绝 |
| PC 体验 | 教练按授权优化空白页/聊天布局、项目菜单、悬停删除图标、18px 字号、键盘发送与中文输入法保护；三栏保持挂载，后台刷新保留列表，历史加载延迟显示骨架 | 保持 PC 三栏方向，复用共享组件；样式不单列面试考点 |
| 工程组织 | 教练按授权迁移 97 个源码/测试文件，按层级和领域归类，同步导入、脚本与文档 | 未改变接口或数据库结构；目录整理不增加正式周完成数 |

目录整理后后端全量 899 条、前端 Workspace 相关 325 条及浏览器主流程定向场景通过。具体命令和各轮范围归入 ENVIRONMENT.md，重要设计取舍与参考答案归入现有 interview-questions/backend/web/task-model.md，不把整理答案视为已通过模拟面试。

2026-09-20 整周验收完成：有历史任务按外键顺序同事务删除，创建幂等回执保留；占用及Run记录进程身份，确认本机原进程退出后手动中止遗留运行并恢复。后端全量1411条、Workspace718/聊天86及类型/静态检查通过；真实kill、数据库回滚、迁移与浏览器闭环证据见ENVIRONMENT.md。平台实机验收为macOS；无身份旧数据、外机和外部子进程不在自动恢复承诺内。

关键概念：本地运行不等于离线模型；免产品登录不等于移除访问控制；Workspace 仍是文件、执行与检索边界。账号模式的 Cookie、密码哈希、跨用户隔离与事务测试保留，用于扩展学习。当前保持 PostgreSQL 与 Redis，不在同一步改写存储或制作桌面安装包。

---

### 第 5 周（09-21 ～ 09-27）Coding Tools + 安全执行

2026-09-22 受限扩展属性复制原语课完成（用户授权直接实现，临时样例）：新增copy_file_xattrs，校验源快照及源/目标对象身份，拒绝同inode、目标多硬链接/非当前属主/非零flags/特殊权限位，目标额外属性在写前拒绝。复制仅使用当次有界读取值，相同属性跳过设置，不删除属性/绕过权限；最终回读完整可见快照并复核源与文件信息。失败可留下部分修改，目标清理由可信调用方负责，不关闭调用方描述符、不执行replace。新增26条分批通过（21条0.14s、补充5条0.08s），Ruff/diff check通过；真实临时文件验证普通属性复制及重复调用零设置、正文/偏移不变，受控测试覆盖返回成功但不生效、源变化、目标额外属性、部分失败和中断。不保证临时性/独占性/原子快照，provenance原本相同不证明不同值复制能力。未接现有替换入口，无数据库/前端/浏览器/全量回归，未提交推送。下一课元数据保护装配扩展属性复制（临时样例）。

2026-09-22 扩展属性复制能力探测课完成（临时样例）：学习者核心与参考一致，仅合并临时文件上下文并整理缩进。探测函数只创建自有临时文件，分别记录matched_before/set_accepted/readback、目标额外可见属性与最终快照一致性，不输出属性值/摘要，不删除系统属性、不修改替换策略。新增19条0.08s通过，Ruff/diff check通过；真实macOS三个普通样例从目标缺失到回读相同，provenance源/目标原本相同且调用成功/回读相同，不能据此证明不同值复制能力。受控测试覆盖调用失败但原本相同、成功却缺失/不同、额外属性、源变化、读取失败、创建/清理/中断路径及资源关闭。无数据库/前端/浏览器/既有整套/全量回归，未提交推送。下一课受限扩展属性复制原语（临时样例）。

2026-09-22 有界扩展属性读取课完成：学习者核心与参考一致，无需修正。新增macOS描述符级只读xattr模块，固定两轮快照及前中后fstat、名称集合复核，预算128项/16KiB名称/64KiB单值/256KiB每轮逻辑总量；空值合法，异常不降级为空，名称和值不进入repr。新增39条0.13s通过，Ruff/diff check通过；真实临时文件验证二进制/空值/中文名称及内容、元信息、描述符偏移不变，受控ABI验证预算分配前拒绝、数量/总量边界、非法名称、变化/权限/平台失败及固定轮数。没有修改现有替换策略、无数据库/前端/浏览器/既有整套/全量回归，未提交推送。读取只覆盖当前进程可见属性，不是原子快照/CAS或复制许可。下一课扩展属性复制能力探测（临时样例）。

2026-09-22 提案应用状态按需展示课完成（用户授权直接实现）：新增独立查询组件接入提案详情，五状态、加载/错误、重新查询和取消；按三资源标识key隔离实例，卸载abort并通过controller身份阻止旧响应。审批标签改为已批准，不再从审批状态推断尚未应用。新增8条与受影响详情8条共16条通过（1.41s），TypeScript/定向ESLint/启动器Ruff/脚本语法通过。PC真实BFF/API/隔离PostgreSQL验证idle，另外四状态和异常响应受控；收起卸载后的延迟响应隔离、显式取消、跨任务导航、键盘查询、1366/1920无横向溢出及截图视查通过，原文件不变，提案写请求为0。无执行入口/全量回归/真实模型调用，未提交推送。下一课有界扩展属性读取。

2026-09-22 提案应用状态查询BFF代理课完成（用户授权直接实现）：新增四字段状态解析器、服务端代理与同源GET application-status路由；local访问边界、内部凭证隔离、严格三标识与额外查询拒绝、五状态及资源匹配、公开字段投影、no-store和20秒取消/超时预算覆盖正文读取。未知协议安全502，不默认idle、不自动重试或触发执行。55条新增测试通过（0.59s），TypeScript/定向ESLint/diff check通过。上游fetch受控模拟，无真实HTTP/数据库/浏览器/后端/既有整套/全量回归，无UI或执行入口，未提交推送。下一课提案应用状态按需展示。

2026-09-22 提案应用状态查询HTTP课及workspace路由整理完成：学习者接口核心与参考一致。新增GET application-status返回四字段严格Schema，复用local身份边界、统一404与no-store，非法标识/额外查询422，未知状态及响应异常安全500。用户要求整理workspace.py，已合并排序导入，集中请求/标识类型，按安全边界、工作空间/目录、任务/运行历史、提案审阅/审批/状态分区并统一格式；全部函数/类含装饰器AST与整理前一致，无业务逻辑调整。新增34条和直接受影响既有18条共52条分批通过（首批33通过，1条OpenAPI测试缺本地凭证；补凭证后修正1条+既有18条共19条2.95s通过），Ruff/diff check通过。隔离PostgreSQL与真实HTTP验证；无文件执行/前端/浏览器/模型/全量回归，未提交推送。下一课提案应用状态查询BFF代理。

2026-09-22 提案应用状态只读查询课完成：学习者核心与参考一致，教练仅补仓库函数间空行和末尾换行。单次联合查询重新授权Workspace/Task/Conversation/Proposal，仅投影三资源标识与application_status；五种状态严格校验，不读取文件、不领取/登记/重试，未知状态拒绝。新增29条分批通过（首轮24通过/5条因测试令牌与公开项目编号碰撞误报；修正独立令牌后5条1.22s通过），Ruff/diff check通过。隔离PostgreSQL验证归属、仅SELECT/无锁/无flush/无commit、私有字段不加载、会话关闭、绑定变化和文件删除后历史查询及无缓存读取。无既有全量/文件替换/前端/浏览器/迁移，未提交推送。下一课提案应用状态查询HTTP接口。

2026-09-22 临时样例中的提案应用执行装配课完成：学习者实现与参考一致，核心无需修正。执行器按单次领取、重新授权/占用令牌与绑定核对、只读preflight、再次快照核对、受限替换、单次终态登记运行，文件I/O前释放数据库事务。回执分开表达文件证据与已确认数据库状态；登记回执丢失返回unknown，不回滚文件或重试，BaseException传播并保留占用。新增37条分批通过（35条4.70s、补充2条1.00s），Ruff/diff check通过；真实隔离PostgreSQL验证提交前/后故障与并发仅一个写入者。成功文件路径沿用仅过滤provenance的测试模拟，额外验证无模拟真实xattr拒绝和核对后外部编辑；不宣称当前宿主完整无模拟写入或CAS。仅新增测试，无既有整套/数据库迁移/前端/浏览器/模型调用，未提交推送。下一课提案应用状态的只读查询。

2026-09-22 文件替换元数据保护课完成（临时样例范围）：学习者已完成元数据模块及大部分接入，用户授权教练补齐替换后核对与错误分类。macOS描述符ACL读取/复制/回读，保留mode/uid/gid，检测可见扩展属性或非零文件标志即拒绝；替换前拒绝与替换后uncertain沿用原提交边界。新增28条与直接受影响既有25条共53条分批通过，Ruff/diff check通过。当前宿主新建文件自动带com.apple.provenance且删除后仍存在；真实扩展属性拒绝已验证，成功路径仅过滤该系统属性模拟支持环境，ACL与文件操作仍为真实调用，不宣称当前宿主无模拟成功写入或完整元数据保留。仍非CAS，未接真实项目/数据库/HTTP/工具，无全量回归、未提交推送。下一课为临时样例中的提案应用执行装配。

2026-09-22 受限文件替换原语课完成（临时样例范围）：学习者核心与参考一致，教练仅补参考遗漏的BLE001说明注释。实现描述符逐级无跟随打开、双次字节基线核对、同目录独占临时文件、短写入循环、基本权限/属主属组保留、同步与replace后核对；结果区分not_replaced/replaced/uncertain，清理失败独立表达。新增67条分批通过（首批60通过/1测试环境特殊权限位失败；核对发现setuid被清除后补平台分支元信息注入，修正1条及新增6条共7条0.80s通过），Ruff/diff check通过。测试直接证明最后检查到replace之间仍可覆盖外部编辑，不是CAS；ACL/扩展属性等元数据尚未保护，仅临时样例可用，不接真实项目执行入口。无数据库/前端/浏览器/模型/全量回归，未提交推送。下一课文件替换的元数据保护。

2026-09-22 应用占用期间的资源删除与目录变更保护完成（用户授权直接实现）：持有已授权Workspace锁时检查running/uncertain，Task删除仅检查目标任务，绑定目标变更检查整个项目；同一规范路径重复绑定保持可用。现有产品无Workspace删除/解绑/覆盖重绑入口，未新增。HTTP与三处BFF安全映射409 proposal_application_busy，删除UI归类明确拒绝并保留任务和草稿。后端20新增+6受影响共26条通过（3.46s），BFF新增3条分批通过；类型/定向ESLint/Ruff/diff check通过。真实数据库4组锁等待验证两种顺序，PC单场景双宽度通过。无新迁移、真实模型或全量回归，未提交推送。下一课受限文件替换原语。

2026-09-22 提案应用状态与执行占用完成（用户授权直接实现）：审批status保持原契约，新增application_status=idle/running/applied/not_applied/uncertain及内部随机令牌；固定归属锁序重新授权，只允许idle领取一次、匹配令牌的running登记终态，所有已领取状态均不可重领，无超时释放或自动重试。新增48条及直接受影响15条共63条分批通过（61条7.09s，提交后响应失败2条1.00s），Ruff/diff check通过。开发库升级60d32ae695cd，旧列业务数据逐行不变，新字段idle/null，Alembic结构与启动就绪通过。无实际文件应用、HTTP/UI/模型/全量回归，未提交推送。下一课应用占用期间的资源删除与目录变更保护。

2026-09-22 已批准提案的应用前授权与基线核对完成（用户授权教练实现）：新增内部授权投影与只读preflight服务，要求approved、绑定相同、Diff完整及完整新内容摘要有效；受限读取前再次比较预期绑定，按原始UTF-8字节核对基线，读取后新Session重新授权并比对快照。返回不可变公开核对结果，不带正文/宿主路径、不写文件或状态，不是写入授权。新增42条、直接受影响既有10条共52条分批通过；首批48通过，3条测试准备被数据库约束拒绝后改为内部边界注入，补跑这3条及1条新增只读仓库测试通过。Ruff/diff check通过，隔离库清理。无迁移/HTTP/UI/浏览器/模型/全量回归，未提交推送。下一课提案应用状态与执行占用；当前快照不防ABA或后续外部修改。

2026-09-22 历史运行提案审阅与审批入口完成（用户明确授权教练实现）：TaskRunPanel仅将成功提案结果接入既有ToolResult，使用当前Workspace/Task范围，未知协议安全回退，历史事件不自动查询或提交。新增8条与直接受影响详情8条共16条通过（1.30s），TypeScript/定向ESLint/脚本语法通过。真实PC四场景覆盖刷新后批准、截断拒绝、未知结果刷新防重、切换离开后返回防重；1366/1920无横向溢出，截图视查通过。隔离PostgreSQL恰好4条逐ID匹配approved/rejected/pending/pending，所有源文件字节不变，临时资源清理。上一课历史入口缺口已补齐；无全量回归或真实模型，未提交推送。下一课已批准提案的应用前授权与基线核对，本次代写授权不自动延续。

2026-09-22 文件修改提案审批交互课完成（当前运行入口）：学习者核心无需修正；新增Actions组件接入详情，pending可决策、截断仅可拒绝，提交前保存sessionStorage防重标记，未知结果及卸载保留标记，已核对回执更新状态。新增11条及直接受影响详情8条，共19条通过；TypeScript、定向ESLint、浏览器脚本语法与启动器Ruff通过。PC六场景分两批通过，原文件字节不变，后续三场景额外完成隔离PostgreSQL精确状态/数量核对。刷新仅验证标记保留，历史运行尚无结构化审阅入口，这是参考方案遗漏，下一课补齐；不宣称刷新后审批界面已恢复。未运行全量回归，未提交推送。

2026-09-22 文件修改提案审批BFF代理课完成：学习者三个新文件与参考一致，核心无需修改。新增同源POST、严格决策解析与公开回执核对；使用服务端凭证和已校验Origin，拒绝额外正文/查询，状态和错误码双重白名单。转发前取消表示尚未转发，转发后取消/超时/异常为结果未确认，不自动重试；20秒预算覆盖上游请求及响应正文。新增93条专项通过（0.66s），TypeScript/新增文件及测试ESLint/diff check通过。只运行新增测试，上游fetch受控，无后端/数据库/浏览器/既有整套/全量回归，未提交推送。下一课文件修改提案审批交互。

2026-09-22 文件修改提案审批HTTP接口课完成：学习者Schema、路由及安全错误分支与参考一致，核心无需修正，仅补新增函数间空行。POST决策入口使用可信身份、严格正文与三标识，复用local/Origin/JSON边界；固定映射404/409/422，未知异常及响应失败统一500结果未确认，成功/失败no-store。新增60条、按共享错误分支选取既有11条，共71个不同用例通过（58条首批、14条补充批次，其中1条调整后重跑）；Ruff/diff check通过。真实隔离PostgreSQL与HTTP测试覆盖提交后详情核对和文件不变；无前端/浏览器/事务服务整套/迁移整套/全量回归，无开发库变更，未提交推送。下一课文件修改提案审批BFF代理。

2026-09-22 文件修改提案审批状态与决策服务课完成：学习者核心与参考一致，无需业务修正，仅补文件末尾换行与函数间空行。pending只能转换一次为approved/rejected，固定Workspace→Task→Conversation→Proposal锁序重新授权；批准要求目录绑定一致且Diff完整，拒绝允许绑定失效或截断。审批不读写文件，也不检查实时文件基线。新增后端53条、前端9条；后端直接相关208条分批通过，前端172条通过，Ruff/TypeScript/定向ESLint/diff check通过。开发库升级5fc219d584bc，既有业务数据逐行一致，Alembic结构与启动就绪检查通过。未运行浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案审批HTTP接口。

2026-09-21 文件修改提案详情按需展示课完成：学习者核心已实现；教练修正参考遗漏的selection.task可空判断为selection?.task，并仅整理该段缩进。新增5条初始展示测试，相关163条通过（1.26s），tsc/定向ESLint通过。更新真实组件夹具与隔离启动器详情路由；PC5场景通过，真实BFF/API详情与DOM Diff一致，键盘读取/聚焦、256px限高/长Diff滚动、双宽度、刷新不自动读详情或重复创建、文件不变及数据库3条pending核对通过。额外受控404→真实重试、挂起旧响应→取消→新请求验证通过；任务切换清理由key/卸载代码核对，本轮未单独做跨任务挂起响应注入。截图视查、临时资源清理，无批准/写入/真实模型/全量回归，未提交推送。下一课文件修改提案审批状态与决策服务。

2026-09-21 文件修改提案详情BFF代理课完成：学习者三文件核心与参考一致，无需修改。新增同源GET路由、local凭证代理及公开详情解析，三标识匹配、拒绝额外查询参数、只转发公开字段、no-store和20秒取消/超时覆盖正文读取。新增56条，连同相邻运行代理/回执解析共169条通过（1.02s），tsc/定向ESLint/diff check通过。覆盖凭证隔离、本地边界、错误脱敏、错误正文取消、无重试、三个阶段取消/超时、Unicode截断Diff与非法响应。上游fetch受控，无真实HTTP/数据库/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情按需展示。

2026-09-21 文件修改提案详情HTTP接口课完成：学习者Schema和路由已实现；教练修正参考遗漏的str到Literal[pending]类型衔接，使用model_validate显式公开字段并保留运行时状态校验，不强转或硬编码pending。新增24条HTTP专项，直接相关81条分批通过（首批80通过/1条测试断言误判，修正后24条3.67s通过），Ruff/diff check通过。覆盖真实local身份/授权查询、越权与不存在统一404、非法标识422、本地边界403、未知状态/异常安全500、no-store、公开字段、历史文件删除及截断。身份依赖含幂等INSERT，提案业务查询仅SELECT；隔离库清理，无开发库变更/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情BFF代理。

2026-09-21 文件修改提案回执结构化展示课完成（用户授权教练实现）：新增公开协议解析与提案卡片，显示创建时待审批/尚未写入、路径、编号、带时区时间及折叠双摘要；截断独立提示，未知协议通用回退。新增34条，连同预览/命令共102条通过（1.26s），tsc/定向ESLint通过。真实PC5场景通过，1366/1920无横向溢出，键盘摘要展开/收起、回执精确字段、刷新不重复、原文件不变及数据库3条pending核对通过；成功/截断截图视查，临时资源清理。无批准/应用按钮，无真实模型/全量回归，未提交推送。下一课文件修改提案详情HTTP接口；本课无新增重要考点独立入库。

2026-09-21 文件修改提案PC闭环验收完成（用户授权教练完成）：新增proposal-tools.mjs/proposal_model.py，沿用隔离启动器，仅模型决策受控，浏览器/BFF/API/工具/文件/数据库真实。成功、删除至空、纯预览、歧义拒绝、截断5场景全部通过；1366/1920无横向溢出，键盘发送、回执字段/双摘要、消息事件持久化和刷新不重发通过。每场景逐文件核对原始字节不变；数据库最终恰好3条pending，完整新内容和授权查询一致，预览/失败无提案。成功1366和截断1920截图已视查；临时目录/服务/测试库清理。Ruff/脚本语法/diff check通过，无真实模型/Docker/全量回归，未提交推送。下一课文件修改提案回执结构化展示。

2026-09-21 文件修改提案工具适配与注册课完成：学习者三文件核心与参考一致，无需修改；新增独立create_file_edit_proposal工具，复用严格替换参数，身份来自可信context，返回pending创建回执，数据库异常映射proposal_save_unconfirmed且不重试。新增79条专项，直接相关230条分批通过（工具194条1.41s，2条Docker未选；隔离PostgreSQL闭环与服务36条3.79s）。旧能力集合断言同步，Ruff/diff check通过；真实工具保存→授权查询、越权拒绝、预览不保存及原文件字节不变通过。数据库/schema自动清理，无浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案PC闭环验收。

2026-09-21 文件修改提案授权查询课完成（用户授权教练实现）：同一SELECT关联提案/Task/Workspace/Conversation并校验两处用户归属，仅投影公开审阅字段；只读服务返回不可变快照，不加载bound_root或proposed_content、不读文件、不加锁或提交。新增15条，连同保存服务18条共33条通过（3.72s），Ruff/diff check通过。覆盖跨用户/项目/任务及缺失资源统一拒绝、会话/项目归属变化、任务删除、Session清理、无自动flush、截断Diff精确保留，以及文件删除/解绑后历史快照仍可查询。隔离PostgreSQL自动清理，无迁移/浏览器/模型/全量回归，未提交推送。下一课文件修改提案工具适配与注册。

2026-09-21 文件修改提案持久化课完成：学习者模型、迁移、仓库与服务核心通过验收，仅补文件末尾换行。提案保存原文摘要、完整新内容及其摘要、有界Diff、目录绑定与pending状态；先结束查询事务再读取文件，保存事务按Workspace→Task→Conversation加锁重新授权。新增37条专项，连同直接相关共109条分批通过（107条9.06s、锁竞争2条1.20s）。真实迁移往返、约束、提交回滚、归属/绑定变化、删除级联与双向锁竞争通过，原文件不变。开发库升级至4eb108c473ab，原业务数据逐行一致，启动就绪和Alembic结构检查通过。无HTTP/工具接入、批准或文件写入，无浏览器/模型/全量回归，未提交推送。下一课文件修改提案授权查询。

2026-09-21 文件修改预览结构化展示课：学习者四文件核心与参考一致。独立协议校验、仅预览状态、零字节显示、折叠SHA-256与纯文本Diff着色/截断提示完成。新增38条与相关82条通过，tsc/ESLint通过；真实PC4场景核对键盘操作、Diff文本及滚动、双宽度、刷新无重放和文件不变，临时资源清理。无需为样式新增独立面试题。下一课文件修改提案持久化，不自动延续核心代写授权。

2026-09-21 文件修改预览PC闭环课：教练配套验收完成。真实文件成功/删除预览/歧义/截断4场景，独立摘要核对、所有夹具字节不变、双PC宽度、刷新无重放及持久化事件通过；模型受控。新增浏览器脚本与决策夹具，生产代码未修改。临时资源清理，下一课结构化展示有界Diff和仅预览状态，不添加写入或批准能力。

2026-09-21 文件修改预览工具课：适配器与注册由学习者完成，教练补遗漏的8项固定错误文案及旧能力断言。公开结果显式选择字段，不含完整新文件，status=preview_only；不写文件或批准修改。新增69条与相关172条通过（1.36s），模型/服务受控，无数据库/浏览器。下一课PC预览闭环验收。

2026-09-21 授权文件预览与基线摘要课：学习者组合授权读取、纯内存预览及SHA-256，核心无需修改。原文摘要与预览共用单次读取，不重读、不写入。新增28条与相关共128条通过（1.89s），含真实临时文件、BOM/换行、读取后外部修改、隔离PostgreSQL授权/事务关闭及失败传播。下一课文件修改预览工具适配与注册，仍不提供写入。

2026-09-21 单文件文本替换预览课：学习者完成纯函数与不可变结果，精确唯一替换、删除、重叠歧义拒绝、UTF-8大小/行数/展示限额、原始换行及完整新内容保留。新增65条纯内存通过，相关测试表达调整后25条复验通过，核心无需修改。Diff是JSON行审阅表示，不是可应用补丁；无文件读写或工具注册。下一课授权文件预览与基线摘要。

2026-09-21 文件查找PC闭环课（用户授权教练验收）：新增受控决策夹具与浏览器脚本，真实文件/授权/BFF/API/数据库通过成功查找读取、空结果、截断、越界、未绑定5场景；双PC宽度、刷新无重放、历史答案与工具事件通过，截图视查。原文件和外部哨兵不变，临时资源清理。未改生产代码，无真实模型/全量回归。下一课单文件文本替换预览，先做纯内存契约/唯一匹配/Diff，不写文件。

2026-09-21 文件查找工具课：学习者find_files适配器、错误映射及注册与参考一致；教练新增54条，直接相关148条分批通过，更新旧能力集合断言以包含新工具。请求级模型/Runtime能力一致，查找→读取和错误Observation链路受控验证；无真实模型、数据库或浏览器。下一课PC闭环验收。

2026-09-21 受限跨目录文件查找课：学习者实现workspace_find.py，核心与参考一致。固定预算、描述符递归、目录身份/版本核对和失败清理；区分文件名匹配与路径匹配、范围不完整与确定有更多匹配。新增55条专项，直接相关分批144条通过，含隔离PostgreSQL授权/只读事务；无浏览器或模型。下一课文件查找工具适配与注册。

2026-09-21 命令结果结构化展示课（用户授权教练实现）：公开结果校验与卡片分层，明确区分工具调用完成和命令成功，展示退出码、错误事实、命令耗时及独立截断提示。stdout/stderr纯文本转义并限高滚动，旧结果未知事实不补False，协议异常与其他工具保留通用文本。新增30条专项，连同摘要卡片/耗时共48条通过；PC验收证据见ENVIRONMENT.md。此课无新增重要面试题，不为样式展示单独入库。下一课受限跨目录文件查找。

2026-09-21 命令工具PC闭环课（用户授权教练验收）：成功、非零退出、截断、参数拒绝与取消5场景经过真实浏览器/BFF/API/Docker，模型受控；双PC视口、刷新不重执行及持久化事件通过。取消恢复证据保留，临时容器及数据库清理。新增配套验收代码并修正取消后工具状态文案；下一课改进原始JSON命令结果的结构化展示。

2026-09-21 请求级命令注册课：学习者完成请求绑定能力集合、模型/Runtime共用快照、local流式入口开放及默认兼容，核心无需修改。新增14条常规、2条真实Docker；相关常规分批94条通过，Docker退出码0/7并确认无残留。尚未浏览器或真实模型验收；下一课受限命令工具PC闭环验收。

2026-09-21 恢复作用域装配课：学习者完成应用有界存储与ChatExecution注入，32运行×16命令上限、归属隔离、请求结束后的异常证据保留和正常记录释放；仅补参考遗漏的Ruff说明。新增29条，聊天生命周期/预算18条，共47条分批通过。下一课请求级命令工具注册与能力开放，当前命令尚未开放给模型。

2026-09-21 超时与取消分类课（用户授权教练实现）：统一等待器按预算来源产生ToolWaitTimeout，内部TimeoutError按执行失败，外部取消优先，重复取消等待拥有的子任务收尾；恢复证据不依赖异常转换。新增14条，相关68条通过。下一课命令恢复记录作用域装配，命令仍未注册，恢复学习者核心实现。

2026-09-21 命令恢复证据接收课：学习者实现有界作用域记录及适配器保存，原请求快照、容量前置、在途收尾与取消证据保留完成。新增34条，相关86条通过，核心无需修改。发现本机wait_for对取消子类不转换的版本边界，下一课异步工具超时与取消分类；命令未注册。

2026-09-21 Runtime分派课：学习者完成is_async分支，异步直接交给统一等待边界，同步保留线程跟踪路径；原错误/成功Observation及耗时不变。新增28条，相关82条通过，核心无需修改。下一课命令工具内部恢复证据接收，尚未注册命令工具。

2026-09-21 工具定义异步支持课：学习者完成互斥执行器定义、共用参数/上下文边界、直接await及结果字符串契约；同步注册保持兼容。新增39条，直接相关分批104条通过，含两条PostgreSQL线程占用回归。核心无需修改。原调度与注册拆课，下一课仅接Runtime同步/异步分派，命令工具尚未注册。

2026-09-21 受限命令适配课（用户授权教练实现）：新增异步适配器和安全错误映射，复用唯一参数契约，正常仅输出命令事实，失败内部保留恢复证据，取消原样传播。新增24条，直接相关57条通过；未注册到现有同步调度器。下一课异步工具调度与受限命令注册，恢复学习者实现核心。

2026-09-21 失败执行只读核对课（用户授权教练实现）：复用创建核对与身份/状态校验，区分当前状态、成功查询缺失与未确认，保留原历史证据与取消语义，不执行恢复写操作。新增36条常规，分批定向验收通过；真实验证及命令见ENVIRONMENT.md。下一课受限命令工具适配与安全错误映射，恢复学习者核心实现。

2026-09-21 统一编排课：学习者完成sandbox_command.py，独立token及请求复制、正常执行后清理、阶段恢复证据与取消语义已接通；命令结果和清理凭据分离，失败不自动删除或重试。新增33条常规，直接相关192条通过；2条真实Docker验证退出码0/7并确认清理，无需修改核心。下一课失败执行的只读状态核对，证据见ENVIRONMENT.md。

2026-09-21 结果适配课：学习者完成CommandResult的OOM/daemon三态事实和保守成功判定，并新增纯函数适配器。已确认退出结果保留输出、双路截断与耗时，内部身份不进入公开协议；异常与清理未确认保持独立。核心无需修改，新增67条，直接相关共228条通过；下一课创建、执行与清理的统一编排，证据见ENVIRONMENT.md。

2026-09-21 已退出容器显式清理课（用户本课授权教练实现）：新增独立exited入口，重新核对身份/停止状态、非强制删除、确认缺失及取消恢复信息；原created-only入口保持不变。50条新增，直接相关共124条通过；4条真实Docker验证删除成功后的确认丢失与取消边界，资源清理完成，证据见ENVIRONMENT.md。下一课执行结果到CommandResult的适配，恢复学习者实现核心。

2026-09-21 执行协调课（用户本课授权教练实现）：已有容器的先订阅后启动、并发排空/退出确认、总预算与失败停止收尾完成；44条新增常规，直接相关377条通过，5条真实Docker验证通过且临时容器清理。保留未确认结果与停止证据，不自动删除或开放模型Shell。下一课已退出容器显式清理，恢复学习者实现核心；详情见ENVIRONMENT.md。

2026-09-21 attach连接课（用户本课授权教练实现）：固定socket、握手总预算、reader借用与关闭所有权、失败/取消收尾已实现；27条新增常规及2条真实Unix socket/Docker验证通过，详细证据见ENVIRONMENT.md。未启动测试容器，已清理；尚未实现启动前订阅协调及整体命令执行。下一课恢复学习者亲手实现核心，正式进度仍4/12。

2026-09-21 attach请求构造课完成：固定API/HTTP升级请求与输出订阅策略，完整容器ID严格校验；核心由学习者实现，教练仅补空行。新增34条，同文件HTTP专项共104条通过，详情见ENVIRONMENT.md。下一课固定socket的attach连接生命周期；尚未验证本机API兼容或建立连接。

2026-09-21 attach HTTP响应课完成：有界头部读取、严格升级契约、首帧保留、总超时与取消传播。学习者核心无需修改；70条新增及直接相关41条共111条通过。尚未发送请求或连接真实daemon；下一课请求构造与连接生命周期，证据见ENVIRONMENT.md。

2026-09-21 attach异步读取与收尾课完成：固定块排空、EOF完成检查、异常/取消传播与主动调度；核心由学习者实现，教练无需修正。新增41条专项，连同解析器共132条通过；详细证据见ENVIRONMENT.md。下一课有界HTTP attach握手，正式进度仍4/12。

工程整理（2026-09-20）：按用户授权，将 Runtime 服务与对应测试按 agent、execution、command、sandbox、docker 职责分组，同步导入、阅读导航和验证命令；核心逻辑不变，不增加课程完成数。目录与依赖规则见 `docs/project-structure.md`，本轮定向验证证据见 `ENVIRONMENT.md`。

attach帧解析课：非TTY原始帧有界增量解析、双路捕获与永久协议失败状态完成，91条纯字节专项通过（0.09s），核心无需修改。不连接Docker，无全量回归；下一课异步读取与收尾。

退出结果课（用户授权教练实现）：严格exited身份/停止证据及ExitCode/OOMKilled/Error解析，不可变结果和成功判定完成，不暴露daemon错误正文、不伪造输出。新增75条纯解析（0.09s）及3条真实退出码0/7/137专项通过，临时容器已清理。下一课attach输出帧解析，尚未接完整命令执行器。

容器启动课（用户授权教练实现）：冻结请求、启动前身份/执行/隔离复核、完整ID启动与running/快速exited查询完成；尝试启动后异常或取消先停止核对并保留结果，重复取消不打断收尾。新增30条常规，直接相关63条通过（0.91s）；4条真实Docker通过（1.23s）并清理资源。命令退出码/输出及运行中整体生命周期仍待接；下一课退出结果确认。

容器停止课（用户授权教练实现）：严格归属解析与阶段状态判断分离，原created契约保留；按完整ID核对→停止→重新查询确认，不自动删除。新增53条，直接相关共181条通过（0.97s）；1条真实Docker验证忽略SIGTERM的父子进程停止、exited/Pid=0/exec拒绝（2.98s），资源已清理。下一课启动与启动结果确认，代写授权不延续。

隔离与资源复核课：网络/命名空间、权限/只读根/禁止提权、资源整数限额、禁额外设备与挂载、固定tmpfs、重启和日志策略纯校验完成；218条专项通过（0.16s），1条真实created容器验收通过（0.22s），已清理资源。核心无需修改，不接启动；下一课停止与停止状态确认。

执行配置复核课：用户/工作目录/入口/完整Cmd、入口环境基线、严格交互布尔及无健康检查纯校验完成；新增71条专项通过（0.08s），1条真实未启动容器验收通过（0.22s），临时资源已清理。只修正参考字符串拼接括号；HostConfig/挂载/资源复核待下一课，不接启动。

显式清理课（用户授权教练实现）：原上下文/完整ID复核created身份，按ID非强制删除、核对回执并查询缺失；错误统一清理未确认，取消传播，不自动补偿。新增41条常规，直接相关共74条通过（0.90s），2条真实Docker专项通过（0.48s）并清理资源。启动前安全配置复核待下一课，直接实现授权不延续。

只读核对课：未知ID候选发现后复用严格身份核对，已知ID保持原约束，查询失败统一未确认且取消传播；新增50条常规专项，与身份解析共145条通过（0.13s）。1条真实Docker专项验证两种ID路径、错ID拒绝无副作用、删除后仍未确认（0.27s），临时容器已清理。核心无需修改，仅补空行/换行；显式安全清理入口待下一课。

Sandbox创建装配课：创建前校验、固定规格create调用、完整ID解析及inspect身份确认已接通，普通异常报告未确认且保留执行标识，取消传播；不自动重试或删除。新增33条常规专项，直接相关共66条通过（0.89s）；2条真实Docker验证正常创建及创建成功后模拟响应丢失，临时容器均核对后清理（0.27s）。核心无需修改，仅补客户端空行/换行；只读核对恢复服务待下一课。

有界Docker客户端课：固定宿主程序/socket/环境、argv调用、有界双路捕获、超时/取消与重复取消收尾已完成；新增33条，直接相关回归共45条通过（0.87s），含5条真实Python子进程测试；Docker只读版本及不存在容器inspect通过。仅补两处解释性Ruff豁免，未改核心行为；未创建容器，创建与身份装配待下一课。

创建响应/身份确认课：鉴于Docker调用生命周期基础尚缺，先完成sandbox_identity.py纯解析；完整ID、严格JSON、名称/标签/批准镜像引用及created状态一致性已验收，95条专项通过（0.07s），核心无需修改。未调用Docker，实际创建与响应丢失恢复待接；身份确认失败不代表容器不存在。

Sandbox参数构造课：固定镜像批准列表、执行token/名称/标签、Docker权限与资源选项、env分层、请求重新校验和不可变参数快照已完成；44条纯参数专项通过（0.05s）。仅修正参考代码两处字符串拼接括号，未调用Docker；实际创建/身份确认待下一课。

Sandbox最小验证课：独立Docker Desktop Linux/ARM64容器3组真实专项通过，验证非root/权限、只读根与tmpfs、网络、cgroup限制、主进程白名单环境及带子孙进程停止和重启临时数据清空。核心Compose无需修改；通用执行器、项目挂载和故障恢复尚未接入，正式进度仍4/12。

命令环境课：从空映射构造POSIX目标环境，固定PATH/语言环境、执行器目录及XDG派生路径，拒绝任意env覆盖；62条纯函数专项通过（0.05s），核心无需修改。没有继承宿主环境或访问文件系统，尚未接执行器；固定PATH不等于程序允许列表，环境清理不等于Sandbox。

双路输出课：TaskGroup并发读取、同流拒绝、两路成功返回、故障/取消等待兄弟任务收尾及异常组保留已完成；12条专项通过（0.05s），核心无需修改。仅内存流及受控读取器，不代表进程树停止或Sandbox完成。

单路异步输出课：固定4096字节读取、超限继续排空、EOF快照、异常与取消传播及主动让出执行权已完成；31条专项通过（0.06s），核心无需修改。仅内存流/受控读取器，无命令进程或真实管道；双路生命周期仍待下一课。

输出缓冲课：有界字节前缀、UTF-8统一替换解码、独立字节/字符截断、结束快照及两路隔离已完成；41条纯内存专项通过（0.05s），仅修正参考代码的__slots__排序。尚未接入真实管道或进程，不代表完整进程内存上限或Sandbox。

命令契约课：严格argv/相对工作目录、服务端策略常量、四类执行结果及跨字段一致性已完成；120条纯校验专项通过（0.10s），只清理末尾空白。未启动进程，语法校验不等于执行授权，字节/字符上限尚需执行器实施。

只读工具链PC验收：4组专项通过，真实BFF/API/隔离PostgreSQL/临时文件验证三工具串联、刷新无重放、越界和未绑定安全错误；1366/1920截图已视查。模型决策受控，无真实模型效果验证，业务代码无需修改。仅本课专项，无全量回归；完整Sandbox、递归搜索和外部命令执行尚未完成。

搜索工具适配课：严格参数、可信上下文、固定安全错误、定位与截断结果已接通；恢复误覆盖的时间工具注册。新增45条，直接相关回归共122条通过（0.91s）；模型及底层服务模拟，后续PC闭环已验收。

单文件搜索服务课：复用受限读取，字面量/大小写敏感、字符行列号、每行首次命中、50行与200字符片段限制及独立截断标记已完成；43条专项通过（1.07s）。后续搜索工具已注册，不提供仓库递归搜索。

目录工具课：严格默认相对目录参数、身份独立注入、截断JSON与安全错误、按上下文注册已完成；新增31条，直接相关回归共77条通过（0.71s）。受控模型验证先列目录再依据结果读文件，底层服务模拟，未宣称真实模型或浏览器验收。

受限目录枚举课：单层200项上限、第201项检测截断、不跟随子项链接、描述符清理与变化检测已完成；26条专项通过（1.11s），后续目录工具适配与注册已完成。

聊天上下文装配课：local流式入口在模型前加载并传递同一上下文，账号模式保留无文件能力；新增7条、流式9条、生命周期4条分组通过。隔离数据库与真实临时文件链路验证，模型模拟，未做浏览器验收。

文件工具适配与能力过滤课：严格relative_path参数、固定安全错误、模型按上下文选择工具已完成；新增37条，直接相关回归共83条通过（0.93s）。后续local流式聊天入口已完成装配，可调用文件工具；普通/chat保持直接文本请求；该入口已完成装配，受限目录枚举服务已完成，目录工具适配与注册已完成，受限单文件文本搜索服务已完成，单文件搜索工具适配与注册已完成，只读工具链PC闭环已验收，受限命令请求与结果契约已完成，命令输出有界缓冲已完成，单路异步排空已完成，双路并发排空与失败收尾已完成，命令环境白名单已完成，Docker Sandbox最小隔离已验收，Docker Sandbox创建参数已完成，创建响应解析与身份确认已完成，有界Docker客户端调用已完成；Sandbox创建与身份确认装配已完成；创建结果未确认后的只读核对已完成；已确认未启动容器的显式清理已完成；启动前执行配置复核已完成；Sandbox隔离与资源配置复核已完成；容器停止与停止状态确认已完成；容器启动与启动结果确认已完成；命令退出结果与退出码确认已完成；Docker attach输出帧解析已完成；下一课attach异步读取与收尾。

2026-09-20：路径边界服务已完成，由已授权任务取得绑定根目录，统一相对路径协议并拒绝上级/符号链接逃逸。后续受限文本读取课也已完成：逐级无跟随打开、普通文件及身份检查、256KiB限制、UTF-8/NUL策略、变化检测与句柄清理。新增35条，连同路径边界共98条通过；尚未注册工具，不提供完整Sandbox或原子快照，可信上下文构造也已完成（24条专项，1.20s）：由会话查询用户/Task/Workspace归属并返回不可变对象，不含根目录。执行器上下文透传与缺失拒绝也已完成（20条新增，定向回归共43条，1.17s）；只读文件工具适配与能力过滤已完成，流式聊天上下文装配已完成，受限目录枚举服务已完成，目录工具适配与注册已完成，受限单文件文本搜索服务已完成，单文件搜索工具适配与注册已完成，只读工具链PC闭环已验收，受限命令请求与结果契约已完成，命令输出有界缓冲已完成，单路异步排空已完成，双路并发排空与失败收尾已完成，命令环境白名单已完成，Docker Sandbox最小隔离已验收，Docker Sandbox创建参数已完成，创建响应解析与身份确认已完成，有界Docker客户端调用已完成；Sandbox创建与身份确认装配已完成；创建结果未确认后的只读核对已完成；已确认未启动容器的显式清理已完成；启动前执行配置复核已完成；Sandbox隔离与资源配置复核已完成；容器停止与停止状态确认已完成；容器启动与启动结果确认已完成；命令退出结果与退出码确认已完成；Docker attach输出帧解析已完成；下一课attach异步读取与收尾。正式进度仍为4/12，第5周进行中。

本周目标：让 Agent 不只“建议代码”，而是能在隔离的样例 Workspace 中安全地读取、搜索、修改并验证代码。模型永远不能直接执行任意 Python 或越过 Workspace 边界；所有工具调用都经过注册、校验、策略、执行、审计和结果回灌。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 文件与搜索工具：目录树、读文件、`rg`、符号定位；规范化路径并限制大小 | 能找到跨文件定义；`../`、绝对路径、软链接逃逸、二进制和超大文件被拦截 |
| 2 | Shell Worker / Sandbox：固定 Workspace、清理环境变量、时间/资源/输出限制、取消传播 | 命令无法访问 Workspace 外部或密钥；超时、取消、非零退出返回结构化结果 |
| 3 | Apply Patch：结构化 Diff、上下文匹配、原子写入、冲突与失败回滚 | 成功补丁只改目标文件；失败补丁不留下半写状态 |
| 4 | Git 与验证工具：status、diff、受控测试/lint/typecheck；结果摘要回灌模型 | 一次任务能展示 Diff 并运行最相关检查，原始输出可追踪且有截断上限 |
| 5 | 工具风险与审批策略：只读/写入/执行/外部副作用分级，服务端决定是否暂停 | 高风险调用未批准前不会进入执行器；重放同一批准不会执行两次 |
| 6 | 前端编码任务体验：计划、工具、终端、Diff、审批与结果卡片 | 浏览器能看见正在做什么、改了什么、验证是否通过，并可停止任务 |
| 7 | 固定编码冒烟集：查找、单文件修复、跨文件修复、失败测试、危险命令 | 一条命令运行任务集；每项输出终态、步骤、工具、耗时、成本与 Diff |

关键概念：

- Sandbox 是执行边界，不是提示词；路径检查、进程权限、资源限制和环境变量隔离缺一不可。
- Apply Patch 比“让模型重写整个文件”更可审查，也更容易检测并发冲突和失败回滚。
- Shell 输出和退出码都是 Observation；非零退出不一定是系统异常，可能是 Agent 需要修正的反馈。
- 工具能力与当前允许使用的工具子集要分离，避免模型在不需要时拥有高风险能力。

常见坑：

- 只检查字符串是否包含 `..`，却没有处理符号链接和规范化后的真实路径。
- 把宿主机真实环境变量传进 Shell，导致 API Key 出现在命令或日志中。
- Patch 失败仍写入一部分文件；测试失败后又覆盖原始错误输出。
- 审批只存在前端，攻击者绕过 UI 直接请求执行接口。

### 第 6 周（09-28 ～ 10-04）代码上下文工程 + RAG

本周目标：让 Agent 能为真实跨文件任务找到“足够但不过量”的仓库上下文。检索不是单独做一个文档问答 Demo，而是直接服务于计划、工具选择、代码修改与引用说明。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | Workspace 导入：Git URL 或 ZIP、本地样例仓库；校验大小、类型、路径和所有权 | 非法压缩包、路径穿越、超限仓库被拒绝；导入状态可恢复 |
| 2 | 仓库扫描：遵守 `.gitignore`，识别语言/文件/符号，跳过二进制、生成物和疑似密钥 | 生成可查询的文件与符号清单，且不索引被忽略或敏感内容 |
| 3 | 关键词与符号检索：`rg`、文件路径、定义/引用线索 | 精确标识符与配置项能稳定命中，结果带文件、行号和匹配原因 |
| 4 | 代码 Chunk、Embedding 与 pgvector：保留文件、符号和行号元数据 | 向量写入成功；维度/索引匹配；语义问题能召回相关实现 |
| 5 | 混合检索、Rerank 与 Context Builder：融合排序、去重、Token 预算、工具结果裁剪 | 上下文包可解释来源、分数、Token 占比和被裁剪原因，不超过预算 |
| 6 | 引用与检索调试 UI：展示查询、候选、分数、采纳片段和代码定位 | 点击引用能定位代码行；用户能看出为什么某段被选中 |
| 7 | 代码检索评测：固定跨文件任务集，比较关键词、向量与混合策略 | 输出 Recall@K、MRR、引用正确率、延迟与成本对比表 |

关键概念：

- 代码搜索优先依赖路径、符号和精确文本；Embedding 补充语义，不应替代 `rg` 和语言结构。
- Chunk 边界应尽量遵循函数、类和模块，而不是只按固定字符数切割。
- 权限过滤必须发生在召回阶段；后置过滤会让无权限片段占据 Top-K。
- Context Engineering 还包括仓库指令、历史、计划和工具结果的选择，不只是 RAG。
- 固定评测集是判断检索改动是否有效的事实来源，不能只凭一次聊天感觉。

常见坑：

- 把 `node_modules`、构建产物、`.git` 或密钥文件送去 Embedding，成本和安全同时失控。
- 只做向量检索，导致函数名、错误码和配置键等精确问题召回很差。
- 引用只有文件名没有行号，用户无法验证 Agent 依据。
- 同时修改 Chunk、召回数量和 Rerank 后再评测，无法归因改进来自哪里。

#### Obsidian Vault 接入实践（已纳入，尚未开始）

目标：让自研 Agent 读取、检索并逐步整理用户授权的 Obsidian 笔记库，复用本地 Workspace、文件工具和检索能力。安排在当前第 5 周 Sandbox 执行链路完成之后，不改变当前下一课；融合到现有课程中，每次仍只推进一个可验证的小任务。

| 阶段 | 接入范围与前置条件 | 验收 |
|---|---|---|
| 第 6 周：只读闭环 | 用户显式选择并授权本地 Vault；复用 Workspace 边界，扫描 Markdown、跨文件关键词搜索、受限读取和来源引用；不要求 Vault 是 Git 仓库，不以向量数据库或 Obsidian 插件为前置 | 从 PC 工作台查询笔记，回答能定位到实际笔记路径及行号/片段；无匹配时明确说明；越界路径与符号链接逃逸被拒绝；文件数、读取量及结果有界并提示截断 |
| 第 8 周：受控写入 | 只读闭环及 Diff、服务端审批、并发冲突检测具备后，支持新建或修改 Markdown；用户先预览并确认变更，写入前重新校验权限与文件基线 | 拒绝审批不会写入；Obsidian 同时修改文件时拒绝覆盖并要求重新生成预览；过期或重复批准不会静默覆盖或重复写入 |
| 后续按需增强 | 解析双向链接、标签并在授权边界内追踪关联；语义检索复用第 6 周能力并以评测决定收益 | 链接不会扩大文件访问范围；索引随笔记修改/删除更新；比较关键词与语义检索的召回和引用正确率 |

第一阶段按本地文件夹接入，不读取或执行 `.obsidian` 插件与配置，附件、外部链接及非 Markdown 文件不在初版范围。笔记正文作为不可信资料，不能提升工具权限或成为执行指令；本地存储不代表模型推理离线，接入时明确告知检索片段会提供给用户配置的模型。若需要在 Obsidian 内直接聊天或读取当前打开笔记，再评估专用插件或 MCP 接口，不作为首版依赖。

固定示例任务：“查找笔记库中 Docker attach、异步取消和资源清理的相关笔记，解释它们之间的关系，每个结论附来源，找不到的内容明确说明。”验收使用专门的样例 Vault，覆盖成功、无结果、越界、超限和笔记变更场景；测试配套仍在学习者明确说本课“完成了”后创建。

### 第 7 周（10-05 ～ 10-11）高级 Agent Runtime：可恢复长任务

本周目标：在第 3 周最小循环和第 5 ～ 6 周工具/上下文能力之上，构建可计划、可压缩、可暂停、可恢复、可修正并可度量的长任务 Runtime。Tool Registry、参数校验和基础循环不再重复教学，只在 Day 1 做掌握复核。

| Day | 任务 | 对应能力 | 验收 |
|---|---|---|---|
| 1 | Runtime 基础复核 + 显式 Plan/Step 状态：计划创建、更新、完成与失败 | Planning | 不看资料讲清现有循环；UI 与数据库能看到计划变化，但不记录隐藏思维链 |
| 2 | Context Engineering 与 Compaction：上下文清单、Token 占比、阶段性摘要与关键 ID 保留 | Context | 长轨迹压缩后仍能继续任务；可解释保留、丢弃和压缩了什么 |
| 3 | 记忆分层：对话短期记忆、Task/Run 状态、长期用户/Workspace 事实 | Memory | 三类记忆存储、作用域、淘汰和隐私策略不同，不把运行状态塞进聊天摘要 |
| 4 | LangGraph State + Checkpoint：把循环迁移为持久状态图 | Durable Runtime | 在模型前、工具后或审批中断后均可从 Checkpoint 恢复 |
| 5 | 暂停、恢复、Steering、取消、重试与幂等 | Control | 新指令只在安全边界生效；恢复不会重复已提交副作用；终态唯一 |
| 6 | Reflection 与验证闭环：测试失败 → 分析 Observation → 调整计划 → 修复 | Reflection | Agent 首次补丁失败后能根据真实测试输出完成一次受限自我修正 |
| 7 | 轨迹评测：任务成功、工具选择、补丁正确、步骤、恢复、成本与 LLM-as-Judge | Evaluation | 固定任务集输出指标；至少展示一个改动前后对比和一次评测发现的回归 |

关键概念：

- Plan 是可观察、可更新的任务状态，不是暴露模型隐藏推理。
- Checkpoint 保存可恢复状态；聊天摘要负责压缩语义，两者不能互相替代。
- Steering 只能在定义好的安全边界改变后续计划，不能让已执行副作用“假装没发生”。
- Reflection 必须读取真实验证结果并受步数/时间/预算限制，否则会变成无限重试。
- 检索指标回答“找没找对上下文”，轨迹指标回答“有没有用正确步骤完成任务”。

常见坑：

- 只序列化消息列表，没有保存计划、工具调用、审批和已提交副作用，导致无法可靠恢复。
- 压缩时丢掉文件路径、补丁基线、测试命令或工具 call ID。
- 用户取消与服务异常竞争，产生两个终态或重复清理。
- Reflection 没有新证据却重复相同调用，持续消耗 Token。
- LLM-as-Judge 未经人工小样本校准，评分看似精确但不可相信。

资料：LangGraph 官方文档的 State / Checkpointer，两篇 ReAct / Reflexion 论文的方法部分，以及官方 OpenAI Conversation State、Compaction、Streaming 与 Steering 文档。



### 第 8 周（10-12 ～ 10-18）MCP、Skills、审批与有限多 Agent；开始第一批投递

本周目标：让 Coding Agent 可以通过标准协议和可复用指令扩展能力，让危险操作必须经过人工确认，并只在任务确实独立时进行有限并行委派；**同时开始投递**。

本周审批实践同时承接 Obsidian 笔记受控写入，复用已有 Diff 与文件编辑能力；前置条件及验收见第 6 周下的“Obsidian Vault 接入实践”。

投递不等作品集全部完成。第 8 周时你已经有流式 UI、安全 Coding Tools、代码上下文、ReAct + LangGraph、审批、评测和 Trace，足以支撑面试对话。面试反馈能反向告诉你第 9 ～ 12 周该补什么，这比闭门造车更高效。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | MCP 基础 + 自建 Server：Tool、Resource、传输、错误和信任边界 | 本地 Client 能发现并调用自建代码分析工具；能解释 MCP 与 A2A 的区别 |
| 2 | FastAPI 作为 MCP Client：动态发现、名称冲突、超时、结果清洗和权限映射 | MCP 工具进入统一 Tool Gateway，外部错误和返回不会绕过本地策略 |
| 3 | Skills 与仓库指令：可发现的 `SKILL.md`、版本、作用域、优先级和渐进加载 | 至少一个测试/修复 Skill 被真实任务使用；仓库指令能约束工具行为 |
| 4 | 持久审批：批准、修改参数后批准、拒绝、过期；服务端 Guardrails 强制 | 三种操作改变后续轨迹；重启后待审批状态不丢；原始/最终参数均审计 |
| 5 | Prompt Injection 与外部工具防护：不可信内容标记、最小工具集、敏感数据隔离 | 恶意仓库文本或 MCP 返回不能提升权限、读取密钥或静默执行危险工具 |
| 6 | 有限子 Agent 委派：只拆独立读任务/测试任务，限制并发、预算和写权限，主 Agent 合并 | 两个子任务可并行且结果可追踪；冲突写入被禁止或显式合并 |
| 7 | **简历 v1 + 讲解稿 v1 + 首批 5 ～ 8 家投递**；安全与能力矩阵成文 | 简历每条有可量化结果；投递记录成文；能演示一次 MCP/Skill/审批/委派 |

关键概念：

- MCP 的价值是把「每接一个工具写一次胶水代码」变成「实现一次协议」。
- Skill 封装的是可复用工作流与资源，不是放宽工具权限；仓库指令也不能绕过系统安全策略。
- 审批必须在服务端强制，前端只是展示层；否则绕过前端就能执行危险操作。
- 修改参数后批准，意味着执行的参数与模型提议的参数不同，两者都要记进事件日志。
- 外部 MCP Server 的返回是不可信输入，要防 Prompt Injection。
- 多 Agent 只解决可独立并行的工作，不应拿来包装普通函数调用；写入责任和最终合并必须唯一。

常见坑：

- 审批状态只存在内存，服务重启后待审批任务丢失。
- 审批超时没有默认策略，任务永久挂起。
- 把 MCP Server 的错误信息直接展示给用户，泄露内部路径或配置。
- 子 Agent 共用可变 Workspace 并同时写文件，产生难以复现的竞态和覆盖。
- 简历只写「使用了 LangGraph、MCP、RAG」这类关键词堆叠。招聘方明确把 framework name-dropping 列为被高估的信号，每条必须带结果或数字。



### 第 9 周（10-19 ～ 10-25）生产化：Worker、可观测、评测与部署

本周目标：让项目具备可部署、可观测、可回归的工程属性。**本周不可压缩**——它直接决定作品集能否被评估。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 后台 Worker 与任务队列：并发上限、租约、心跳、重试、取消和僵尸任务回收 | API 不被长任务阻塞；Worker 崩溃后任务可重领且不重复提交副作用 |
| 2 | Trace 完善：贯通模型、检索、工具、审批、补丁和测试 | 能按 `run_id` 还原完整轨迹、因果顺序、耗时、Token 和错误阶段 |
| 3 | 成本与性能：按用户/Task/Run/工具统计，模型路由、缓存、预算与 P95 延迟 | 超预算被拦截；能解释一次真实任务的成本和慢点 |
| 4 | 评测入 CI：代码检索 + 补丁任务 + 轨迹评测作为回归门禁 | PR 自动跑确定性子集；指标低于阈值时失败并保留报告 |
| 5 | 安全红队：Prompt Injection、越权、路径穿越、命令注入、SSRF、密钥与日志脱敏 | 攻击用例有自动测试和处理记录；高风险能力默认最小化 |
| 6 | **本地交付与受限云部署**：本地启动/升级，独立样例服务部署与发布回滚 | 干净 PC 按文档启动并完成样例任务；本地服务仅监听回环地址；用户自行配置模型 |
| 7 | CI/CD 与恢复演练：lint、类型、测试、构建、迁移、备份、回滚、干净环境 | 全部检查通过；模拟服务重启后任务可解释地恢复 |

保留一次受限云部署实践：部署独立样例服务，覆盖 HTTPS、反向代理、环境配置、日志、健康检查和发布回滚；不得直接公开本地免登录执行接口。主产品本地运行；线上域名用于介绍、视频、文档和下载。最终分发目标：普通体验者无需手动安装或维护 Docker、PostgreSQL、Redis；由产品内嵌存储、替代实现或受管理的运行依赖提供能力。当前源码版尚未达到此目标。存储/取消机制调整必须验证迁移、事务、备份恢复和跨进程行为，不能把数据库文件替换等同于完成适配。公开执行演示仅为可选扩展，需要独立隔离和预算，不能将本地免登录服务直接绑定公网。桌面打包与存储轻量化另行评估，不提前阻塞核心 Runtime。

关键概念：

- Trace 的意义在于 Agent 是非确定性的，没有轨迹就无法定位问题。
- 评测进 CI 才能防止「修好一个问题，弄坏三个」。这是「你怎么知道改动之后变好了」的可核查答案。
- Prompt Injection 的主要防线是权限最小化和工具审批，而不是靠提示词请求模型不要听坏话。
- 队列“至少一次投递”要求副作用幂等；不能因为任务重领就重复应用补丁或重复调用外部系统。
- 部署文档的正确性只能通过在干净环境实际执行来验证。

常见坑：

- Trace 里记录了完整的用户输入和密钥，造成日志泄露。
- 评测在 CI 里真实调用模型，费用和不稳定同时失控；应固定样本或使用缓存/录制。
- Compose 里数据库没做健康检查，应用先起来直接连接失败。
- 部署时把 `.env` 打进镜像。

### 第 10 ～ 11 周（10-26 ～ 11-08）Codex-like 主项目收尾与质量证明

**这两周不是开始做项目，而是把第 3 ～ 9 周已有的代码收尾成作品集。** 项目定义见 `LEARNING_PLAN.md` 第 9 节。

第 10 周（产品化与失败路径）：

| Day | 任务 |
|---|---|
| 1 | Workspace / Task / Conversation 列表、空状态、分页和恢复体验 |
| 2 | Plan / Tool / Terminal / Approval 时间线与实时状态 |
| 3 | 文件树、代码引用、Git Diff Review、测试结果与接受/拒绝改动体验 |
| 4 | 响应式、键盘操作、基础无障碍、长输出与大 Diff 性能 |
| 5 | 构造工具超时、测试失败、审批拒绝、Worker 重启中的至少两个恢复场景 |
| 6 | 架构图、信任边界图、事件流图和数据模型图 |
| 7 | 端到端自测、干净环境验证、修复暴露的问题 |

第 11 周（表达与可选补齐）：

| Day | 任务 |
|---|---|
| 1 ～ 2 | 完成代码检索、补丁正确率、轨迹、成本和延迟评测报告；记录一次真实回归 |
| 3 | 安全红队与失败恢复报告：攻击输入、预期策略、真实结果和残余风险 |
| 4 ～ 5 | README、截图/GIF、3 ～ 5 分钟演示视频；包含测试失败后修正和审批场景 |
| 6 | 第二框架只做概念对比；若主项目九项最终验收未全过，优先补主项目 |
| 7 | 干净环境复现、项目讲解演练、复盘与第二批投递准备 |

刻意演示的失败场景至少两个，例如测试失败后根据 Observation 修正、审批被拒绝后调整方案、Worker 重启后从 Checkpoint 恢复。**主动展示失败处理比只演示顺利路径更有说服力**，招聘方看的是 build → measure → refine 的痕迹。



### 第 12 周（11-09 ～ 11-15）面试强化与第二批投递

第一批投递已在第 8 周完成，本周重点是把面试反馈补齐并扩大投递面。

| Day | 任务 | 验收 |
|---|---|---|
| 1 | 根据第 8 ～ 11 周的面试反馈修订简历 | 每条经历有可量化结果 |
| 2 | 项目讲解稿定版：2 分钟版和 8 分钟版 | 不看稿能讲完 |
| 3 | 技术问答：过完第 4 节题库 | 每题都能给出取舍理由 |
| 4 | 现场编码：ReAct 循环、流式状态机、路径安全或工具调度 | 40 分钟内写出一个可运行、带失败测试的最小实现 |
| 5 | 第二批定向投递（15 ～ 20 家） | 投递完成并记录 |
| 6 | 模拟面试与复盘 | 记录被问住的问题并补齐 |
| 7 | 整理投递期继续学习清单 | 明确下一阶段补什么 |



## 4. 面试题库

按周积累，每题的目标是能讲出取舍，不是背定义。标记 **[必问]** 的来自公开面试题库统计，出现频率最高。

### 评测、可观测与可靠性（招聘方称为承重技能，优先准备）

1. **[必问]** 你怎么知道改动之后变好了？
2. **[必问]** 你怎么知道你的评测本身可信？
3. 检索评测和 Agent 轨迹评测有什么区别，各自度量什么？
4. LLM-as-Judge 的偏差从哪来，怎么校准？
5. 举一个「评测帮你发现了回归」的具体例子。
6. 单次运行的成本和延迟是多少？超预算时怎么拦截？
7. Agent 是非确定性的，你怎么写测试？
8. 线上出现 Agent 行为异常，你的定位路径是什么？

前两题是招聘方明确点出的第一区分点，必须用具体数字和真实案例回答，不能只讲方法论。

### Agent 原理与架构

1. **[必问]** 手写一个 ReAct 循环。
2. Planning、Memory、Tool Use、Reflection 在你的代码里分别落在哪？
3. **[区分度最高]** 短期上下文、运行状态、长期记忆分别存在哪里，为什么要分开？记忆如何淘汰？
4. CoT、ReAct、Reflexion、Self-Refine 的差别和适用场景。
5. Agent Loop 的终止条件有哪些？只靠最大步数会出什么问题？
6. 单步 95% 成功率，20 步后整体成功率是多少？这个数字说明什么？
7. Context Engineering：一次推理的上下文由哪几部分拼成？超长时压缩掉什么、怎么避免丢关键信息？
8. 工具执行失败，是抛异常还是把错误返回给模型？依据是什么？
9. LangGraph 的 Checkpoint 让哪些能力成为可能？
10. 什么场景**不该**用多 Agent？

### 工具与协议

1. **[必问]** MCP 解决了什么问题？没有它之前是怎么做的？
2. MCP 的 Tool、Resource、Prompt 分别用在什么场景？
3. MCP 和 A2A 的分工差异是什么？
4. Function Calling 和 Computer Use / GUI Agent 的取舍。
5. Tool Registry 怎么设计才能新增工具不改调度代码？
6. 文件、Shell 和 Apply Patch 工具的输入、输出与失败契约怎么设计？
7. Skill、仓库级指令和普通系统提示有什么区别，优先级怎么处理？
8. 什么任务值得委派给子 Agent，如何避免并发写冲突？

### 安全与人工介入

1. **[必问]** Prompt Injection 的现实防线有哪些？
2. **[必问]** Human-in-the-loop 怎么设计？审批状态存在哪？
3. Guardrails 和审批的分工是什么？
4. 外部 MCP Server 的返回为什么是不可信输入？
5. 这个项目有哪些安全风险，分别怎么处理？
6. 如何同时防止路径穿越、软链接逃逸、命令注入和宿主环境变量泄漏？
7. 为什么 Docker 容器不自动等于完整 Sandbox？公网版本还需要限制什么？

### 代码上下文与 RAG

1. **[必问]** 完整讲一遍从仓库扫描到 Context Builder 的代码检索链路。
2. 跨文件任务召回质量差，你的排查顺序是什么？
3. 符号、关键词和向量检索各自的优势与失效场景是什么？
4. Rerank 带来的收益和成本怎么量化？
5. Workspace 权限如何在检索阶段生效，而不是回答后过滤？
6. 为什么选 pgvector 而不是 Pinecone / Weaviate / Milvus？
7. 如何给代码片段做 Chunk，并保留文件、符号和行号引用？

### Agent 前端

1. Agent UI 和普通聊天 UI 的本质区别是什么？
2. [模型 token、tool call、approval、error 分别对应前端哪些状态？状态机怎么画？](interview-questions/frontend/agent-ui/chat-state-machine.md)
3. 为什么选 SSE 而不是 WebSocket？什么时候必须换成 WebSocket？
4. 你的事件协议和 AG-UI 有什么异同？为什么这样选？
5. 用户点击「停止生成」，从浏览器到后端到模型分别发生了什么？
6. 流式响应过程中网络断开，如何恢复而不产生重复内容？
7. 一次运行有 30 个工具调用，界面怎么设计才不淹没用户？
8. 前端如何保证与后端的类型契约不漂移？
9. Plan、Terminal、Diff、Approval 和最终回答在 UI 中如何分层？

### 后端与工程

1. [一次 Agent 运行需要记录什么，如何按 `run_id` 还原时间线？](interview-questions/backend/web/agent-run-observability.md)
2. 用户重复点击发送，如何保证不产生两条消息？
3. Redis 在这个项目里存什么，丢了会怎样？
4. 数据库迁移在多人多环境下如何避免冲突？
5. 同一套项目怎么在 macOS、Windows 和服务器上运行？
6. 你的部署方式是什么？为什么不用 K8s？
7. Task、Run、Conversation、Checkpoint 与 Event 为什么要分开？
8. Worker 崩溃后如何重领任务，又如何避免补丁或外部副作用重复执行？

### 系统设计（面试重头戏，各准备一张架构图）

1. 设计一个支持多用户、隔离 Workspace 和任务恢复的 Coding Agent。
2. 设计一个安全的文件/Shell/Apply Patch 工具执行平台。
3. 设计一个代码仓库 Context Builder 与检索评测系统。
4. 设计一套 Agent 可观测与轨迹评测系统。
5. 设计一个有限多 Agent 编码任务调度器，并说明什么时候不该并行。

### 反问面试官

1. 团队的 Agent 产品里，前端负责到哪一层？
2. 工具和 MCP 的接入规范由谁定义？
3. 效果回归靠人工还是自动评测？评测集谁维护？
4. 线上出现 Agent 行为异常，目前的定位手段是什么？
5. 成本和延迟目前是谁在负责优化？

## 5. 每周复盘模板

每周第 7 天把下面模板填入 `week-learning/week-XX/REVIEW.md`。

```markdown
# 第 X 周完成记录

周期：YYYY-MM-DD ～ YYYY-MM-DD

## 本周目标
（一句话）

## 已完成
- [x] ...

## 未完成及原因
- ...（明确挪到哪一周）

## 关键决策
| 决策 | 备选方案 | 选择理由 |
|---|---|---|

## 踩过的坑
| 现象 | 原因 | 解决方式 |
|---|---|---|

## 能讲清的问题
1. ...

## 还讲不清的问题
1. ...（下周补）
```

“还讲不清的问题”是这份模板里最重要的一栏，不要留空。

## 6. 进度自检表

每周末对照勾选，用于判断是否可以进入下一周。连续两周有未完成项时，优先补齐而不是继续推进。

- [x] 第 1 周：Python、FastAPI、DeepSeek、SQLite、持久化对话和最小时间工具闭环
- [x] 第 2 周：分层、配置、PostgreSQL、Alembic、pytest、Docker Compose 全部落地
- [x] 第 3 周：流式 UI + 取消 + 六态状态机 + **最小 Runtime + run_id 事件落库 + 冒烟评测**
- [x] 第 4 周：本地单用户、Workspace/项目目录、Task、会话恢复、幂等/并发预算、**完整运行摘要**、历史删除与本机进程退出恢复（未知停止证据保守拒绝）
- [ ] 第 5 周：文件/搜索/Shell/Apply Patch/Git/测试工具在 Sandbox 内完成一次可审查代码修改
- [ ] 第 6 周：代码关键词/符号/向量混合检索可比较，引用与 Context Token 预算可解释
- [ ] 第 7 周：Plan + Compaction + 记忆分层 + LangGraph Checkpoint + 暂停恢复 + 轨迹评测
- [ ] 第 8 周：自建 MCP + Skill + 审批三态 + Prompt Injection 防护 + 有限子 Agent + **首批投递**
- [ ] 第 9 周：后台 Worker、Trace、评测入 CI、安全红队、Sandbox、本地交付与**展示/分发入口**
- [ ] 第 10 周：Task/Plan/Terminal/Diff/Approval 产品体验完成，两个恢复场景可演示
- [ ] 第 11 周：代码任务评测报告 + 安全报告 + 演示视频 + README 可复现
- [ ] 第 12 周：讲解稿终版、题库过完、第二批投递完成

## 7. 风险与应对

| 风险 | 信号 | 应对 |
|---|---|---|
| 广度失控 | 同时深入 3 个框架 | 冻结技术栈；第二框架只做概念级（`LEARNING_PLAN.md` 第 4 节「概念级了解」） |
| 前端优势被稀释 | 连续两周只写 Python | 强制补一天 Agent UI |
| 只有 Demo 没有工程 | 没有评测、没有 Trace | 第 3 周和第 9 周的可观测任务不可跳过 |
| 关键词对不上 JD | 简历里没有 Planning / Memory / Reflection / Context Engineering / Evaluation | 第 7 周按术语表对齐后重写简历 |
| 看似实现但没有掌握 | 代码能跑却讲不清边界、失败和替代方案 | 每课都按“是什么 → 为什么 → 怎么用 → 示例”讲解，并由学习者完成核心步骤 |
| Coding Agent 权限过大 | 能访问宿主密钥、其他目录或任意公网 | 第 5、8、9 周的 Sandbox、审批和红队验收不可跳过 |
| 进度落后 | 累计落后超过 5 天 | 按下面的取舍顺序砍 |
| 时间不足 | 每天不足 4 小时 | 按 1.2 节比例缩放，但不砍验收和复盘 |

进度落后时的取舍顺序（从先砍到后砍）：

1. 第二框架、A2A 和 Computer Use 的可选演示（保留概念对比）。
2. 可选第二项目（本来就是有余力才做）。
3. 第 6 周的 Rerank 深度优化（保留混合检索和评测即可）。
4. 第 8 周多 Agent 的复杂写入，只保留有限、只读或隔离子任务委派。
5. 第 10 周的 UI 打磨深度。

**不参与压缩的部分**：第 5 周安全 Coding Tools、第 6 周代码上下文、第 7 周持久 Runtime 与轨迹评测、第 9 周生产化，以及第 8 周首批投递。这些共同决定项目是可验证的 Coding Agent，还是只有聊天界面的 Demo。

## 8. 文档关系

- [项目入口](README.md)
- [12 周主计划](LEARNING_PLAN.md)
- [仓库与目录规则](AGENTS.md)
- [环境安装与启动](ENVIRONMENT.md)
- [岗位与技能校准](SKILL_GAP_ANALYSIS.md)
- [当前进度与交接](LEARNING_HANDOFF.md)
- [Codex-like 能力边界与最终验收](docs/codex-like-agent-scope.md)


2026-09-20收尾：第5周累计只读工具链、命令边界、Sandbox生命周期基础及attach帧解析随源码/测试统一归档；正式4/12，第5周进行中。下一课attach异步读取与收尾，接续以LEARNING_HANDOFF.md为准。各课验收不合并为全量回归结论。

2026-09-21当日收尾：累计完成第5周attach异步连接、命令工具执行与恢复证据、文件查找、预览、提案持久化/授权查询及HTTP/BFF/PC审阅详情。正式4/12，第5周进行中；批准/拒绝决策与文件应用尚未实现。下一课为提案审批状态与决策服务，具体边界以LEARNING_HANDOFF.md唯一下一课为准。源码、测试和文档统一归档main；历史分课验收不合并为全量回归结论。

- 2026-09-22：元数据保护装配扩展属性复制课完成（临时样例）：FileMetadata纳入完整可见xattr，两轮ACL/xattr快照及前中后stat核对；属组→权限→xattr→ACL→完整回读。拒绝同inode、目标额外属性和源/目标变化，失败保留临时目标部分修改并交调用方清理。学习者核心与参考一致；教练修正contextmanager返回标注为Generator[int, None, None]及空行。删除测试代理的provenance过滤，真实临时样例ACL/xattr复制、替换及隔离PostgreSQL执行登记通过。16条新增与40条直接受影响测试共56条分批通过；Ruff/diff check通过。仍非CAS，不保证不可见属性、不同provenance值可复制或断电持久化，无HTTP/UI写入口、全量回归、提交推送。

- 2026-09-22：临时样例提案应用完整链路验收完成：新增7条服务串联测试，经真实创建、审批、执行和授权查询，不直接改审批状态。隔离PostgreSQL及真实临时文件验证有/无ACL、普通二进制/空xattr与完整可见元数据保留（不滤provenance）、正文精确替换、临时文件清理、文件I/O前关闭事务、一次领取及重复不写；验证批准后基线过期、pending/rejected拒绝、终态登记提交前/后故障下文件成功与记录确认分离。7 passed（1.83s），Ruff/diff check通过。仅新增测试与文档，无业务代码修改，无既有回归/前端/浏览器/模型调用/开发库操作，未提交推送。

- 2026-09-22：提案应用执行结果的公开响应契约课完成：学习者核心与参考一致，教练仅补类间空行。新增严格冻结Schema与显式投影，校验三个公开标识、回执提案匹配、文件/登记/清理状态及固定错误码组合；拒绝额外字段、隐式类型转换和未知协议。新增57条0.94s通过，包含240种组合穷举（17合法/223拒绝）、敏感字段隔离、缺字段/错误对象及无执行/数据库/文件I/O调用检查。Ruff/diff check通过。未接HTTP路由、未改执行器，无既有回归/数据库/前端/浏览器/全量回归，未提交推送。

- 2026-09-22：提案应用执行结果的前端解析契约课完成：学习者实现与参考一致，无核心修正。纯解析器校验三个资源标识、自有必填字段、严格类型及17种合法组合，显式投影公开字段，保留replaced/unknown证据，不查询/执行/重试。新增51条通过（0.78s），含调用真实后端Schema逐项对照240种组合（17接受/223拒绝）、缺字段与原型链、错误资源/状态/清理值、冻结输入不变及敏感字段隔离。TypeScript、定向ESLint、diff check通过。无HTTP/BFF/UI接入、数据库/浏览器/既有全量回归，未提交推送。

- 2026-09-22：提案应用执行结果的只读展示组件课完成（用户授权直接实现）：新增ProposalExecutionResultPanel，展示边界重新解析资源及协议，分别表达文件结果、登记确认、临时资源清理；区分领取未确认与登记未确认，非法回执不推断未写入，不展示令牌/路径/正文。独立无状态组件，无fetch/执行/重试按钮，尚未接工作台页面。新增25条静态渲染测试0.88s通过，覆盖17种合法回执、非法输入/错资源、切换隔离及敏感字段；TypeScript、定向ESLint、diff check通过。未做浏览器视觉验收、既有回归或全量回归，未提交推送。

- 2026-09-22：提案应用请求契约课完成：学习者实现与参考一致，仅补类间空行。新增严格冻结FileEditProposalExecutionRequest，只接受必填action=apply，禁止额外身份/路径/正文/令牌/权限字段。新增32条0.10s通过，覆盖字典与JSON合法/非法输入、精确动作、冻结及Schema必填说明，Ruff/diff check通过。执行范围仅完成设计约束：未来实验入口须由服务端创建并登记临时样例，APP_MODE=local或浏览器声明不构成范围授权；可信样例登记与门禁尚未实现。未接路由、无文件执行/数据库/前端/既有或全量回归，未提交推送。

- 2026-09-22：服务端临时样例目录生命周期课完成（用户授权直接实现）：新增temporary_proposal_sample上下文管理器，无外部路径参数，macOS服务端临时目录内独占创建0700目录和0600固定example.txt（old\n）；持有父/子目录描述符核对身份，支持短写，退出仅清理已知单硬链接普通样例文件（允许可信原子替换），拒绝未知内容/链接/身份或权限变化，不递归删除。异常和中断释放描述符；清理失败保留现场，以固定错误报告或给原异常追加note。21条最终0.10s通过，Ruff/diff check通过。无HTTP/数据库/注册门禁接入，无既有或全量回归，未提交推送。路径对象不是授权凭证，必须作用域内结束使用且无外部写者，身份核对不是CAS。

- 2026-09-22：临时样例服务端登记与失效边界课完成（用户授权直接实现）：新增TemporarySampleRegistry，只经内部自建流程登记，不提供任意路径/对象注册。随机内部句柄、最多32项、独占借用；close立即撤销新借用，活动借用退出后清理，正常归还允许后续借用。借用异常撤销并清理，清理失败保留失效登记/容量和现场，不自动重试；目录/父目录身份及权限变化拒绝。新实例不恢复旧登记，PID变化在碰锁前拒绝。18条分批通过（16条0.10s、补充2条0.07s），Ruff/diff check通过；包含真实线程关闭、创建失败、失效与异常保留。未改旧模块，无数据库/HTTP/用户资源授权接入、既有或全量回归，未提交推送。

- 2026-09-22：临时样例与用户任务授权绑定课完成（用户授权直接实现）：新增内部TaskSampleBindings，local模式下按用户/Workspace/Task建立私有登记，只接收资源标识不接受路径/句柄；创建前授权，创建后持项目→任务→会话锁重新授权，仅绑定未绑定Workspace，复用整个项目的running/uncertain占用保护。借用重新授权及核对目录绑定，文件使用期间无数据库事务；忙时拒绝关闭。正常关闭先确认解绑提交再清理。提交尝试前失败清理未发布样例；绑定/解绑commit结果未知及使用异常保留目录并封锁登记，不自动恢复或重试。23条分批通过（18条1.62s、补充5条0.67s），Ruff/diff check通过。无既有模块修改、HTTP/执行器接入、前端/浏览器/全量回归，未提交推送。

- 2026-09-22：样例授权门禁与提案执行器装配课完成（用户授权直接实现）：新增execute_sample_proposal，在可信TaskSampleBindings借用内重新授权提案及目录/固定文件范围后调用执行器，借用持续到执行结束。临时样例保存创建时目录dev/ino；执行器及预检增加可选期望目录/文件约束，替换原语在打开实际父目录后、访问文件前核对期望身份。回执经既有严格Schema验证，unknown/uncertain、非法回执和中断使登记封锁，保留目录/原文件证据，无自动重试。19条新增与24条直接受影响既有共43条不同用例通过，Ruff/diff check通过。真实隔离PostgreSQL及临时文件验证成功、错误范围、数据库改绑/目录替换、登记故障、重复执行及清理拒绝；无HTTP/前端/浏览器/全量回归，未提交推送。

- 2026-09-22：受限样例提案应用HTTP课完成（学习者核心实现，教练验收及授权重构）：严格apply请求、本机身份/来源/模式边界、进程样例门禁与安全公开回执接通；无登记拒绝，写入后响应失败仍报告未确认。新增23条及直接影响386条共409条通过；Workspace服务和测试按六类职责分组，路由按操作拆分且保留统一边界，OpenAPI/业务AST不变。当前无公开登记、BFF或UI执行入口；下一课为受限样例提案应用BFF代理。

- 2026-09-22：受限样例提案应用BFF代理课完成：学习者核心与参考一致，无需修正。新增同源apply路由及proposalExecutionProxy，复用本机访问边界和公开回执解析；严格动作/资源/查询校验，服务端凭证隔离，状态与错误码白名单，20秒上游预算覆盖响应读取。转发前取消标记尚未转发，转发后取消/超时/解析失败均保持结果未确认，不重试；合法unknown/uncertain回执原样保留。新增106条受控上游测试通过（0.70s），TypeScript、定向ESLint、diff check通过。未访问真实后端、数据库或文件，未做浏览器或全量回归，未提交推送。下一课为提案应用客户端请求状态管理。

- 2026-09-22：提案应用客户端请求状态管理课完成（用户授权直接实现）：新增独立createProposalExecutionRequest控制器，按资源快照管理idle/submitting/receipt/uncertain/unavailable；请求前同步保存本标签页防重复标记，重建实例重新读取，存储不可用拒绝提交。25秒浏览器等待预算，取消/切换/销毁不清除标记，代次隔离迟到响应；合法公开回执持久保存并保留unknown/uncertain，回执保存失败仍展示已核对证据。保守策略：所有非200响应均保留未确认且禁止重提，不提供解锁/自动重试。新增25条分批通过，TypeScript及定向ESLint通过；未改既有模块、未接页面、未访问真实后端/文件、无既有或全量回归，未提交推送。下一课为受限样例提案应用交互组件。

- 2026-09-22：受限样例提案应用交互组件课完成（用户授权直接实现）：新增独立ProposalExecutionActions，按资源key重建生命周期与确认状态，effect内装配真实sessionStorage、fetch和请求控制器；首次checking避免水合差异，勾选后才允许提交，停止等待不撤销写入。清理先取消订阅再dispose，未知结果保留防重复标记，合法回执复用既有只读展示。新增14条静态渲染/受控Hook调度测试通过，包含真实控制器的点击、取消、卸载及迟到结果；TypeScript、定向ESLint通过。尚未挂通用工作台，未做真实浏览器验收、真实后端或文件写入、既有或全量回归；未提交推送。下一课为受限样例应用组件的PC浏览器验收。

- 2026-09-22：受限样例应用组件PC浏览器验收完成（用户授权直接实现）：新增test/browser/execution-component隔离编译/页面/验证脚本，复用真实组件、按钮、全局样式及React StrictMode；仅监听127.0.0.1随机端口，API请求全部受控拦截，漏拦截时服务器503拒绝。真实Chrome在1366×900与1920×1080下7组专项通过，7次受控请求，pageerror为0；覆盖键盘勾选提交、StrictMode不重发、刷新回执、停止等待、卸载重挂、资源切换及迟到响应、unknown恢复、非法回执和存储不可用。三张截图已检查，内容区宽度/滚动宽度均720px无越界。TypeScript、定向ESLint及diff check通过，组件无须修改。无真实后端/数据库/文件写入，无既有或全量回归，未提交推送。下一课为受限样例应用真实端到端验收。

- 2026-09-22：受限样例应用真实端到端验收完成（用户授权直接实现）：新增execution-e2e隔离API启动夹具、Next/BFF启动器和真实Chrome脚本。使用独立PostgreSQL库/schema及真实Alembic迁移；API内部创建本机用户/任务、自建样例并生成批准提案，页面仅获公开标识。真实页面→BFF→API→门禁→文件替换→数据库登记通过：old\n变new\n，idle变applied，HTTP200公开回执replaced/applied/cleanup_complete=true；UI请求一次且刷新不重发，非法额外path返回422。直接重复HTTP返回200但code=proposal_application_claim_unconfirmed，文件内容/inode/mtime均不变并封锁登记，正常close拒绝。仅测试夹具在请求结束且核对applied后解绑并清理自有样例；生产保护未改。最终样例目录删除、Workspace解绑、进程及测试库/schema清理确认，pageerror=0；Ruff/定向ESLint/diff check通过，无全量回归，未提交推送。下一课为受限样例登记状态的只读查询。

- 2026-09-22：受限样例登记状态只读查询课完成（用户授权直接实现）：TaskSampleBindings新增read_status与冻结TaskSampleStatus，只返回missing/busy/sealed/ready。先检查local模式/进程身份，再按进程锁→数据库归属锁顺序重新授权Workspace/Task/Conversation，包含无登记情况；根目录绑定不匹配仅报告sealed，不修复或改变登记。查询不创建/借用/关闭样例、不访问文件、不提交数据库，退出短事务后返回快照。ready不证明文件仍有效、不预留执行权，不依据提案审批/应用状态恢复内存登记。新增18条及同类生命周期直接影响23条共41条通过（3.08s），Ruff/diff check通过；无HTTP/BFF/UI变更、无全量回归，未提交推送。下一课为受限样例登记状态HTTP接口。

- 2026-09-22：受限样例登记状态HTTP接口课完成（用户授权直接实现）：新增GET /workspaces/{workspace_id}/tasks/{task_id}/sample-status，独立samples路由复用WorkspaceRoute、CurrentUser及进程get_sample_bindings。严格冻结TaskSampleStatusResponse仅公开workspace_id/task_id/status；路径严格校验，拒绝任何查询参数或正文，响应统一no-store。未知/越权404，非法输入422；查询/内部状态/响应失败统一500 sample_status_read_failed，不降级missing/ready。同步服务在线程池执行，路由不持数据库事务或访问文件。新增20条与共享边界直接相关57条共77条通过（8.29s），Ruff/diff check通过；独立PostgreSQL测试库/schema自动清理，无BFF/UI接入或全量回归，未提交推送。下一课为受限样例登记状态BFF代理。

2026-09-22收尾：本轮课程完成范围已推进至受限样例登记状态HTTP接口，正式进度4/12、第5周进行中。提案审批/应用服务、BFF与独立交互组件、PC及真实样例文件链路验收、Workspace职责分组均已归档；普通项目写入与生产工作台应用入口仍未开放。后续从登记状态BFF代理继续，唯一接续与约束见LEARNING_HANDOFF.md。
