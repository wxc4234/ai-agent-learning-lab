# AI Agent Learning Lab 仓库规则

## 目录生命周期规则（长期有效）

- `week-XX/` 只保存该周结束后不再持续演进的学习记录、一次性练习和复盘材料。
- 预计跨越两周及以上持续开发的源码、配置、环境说明、测试和基础设施，不得放入任何 `week-XX/` 目录。
- 跨周应用放在 `apps/`：当前 FastAPI Agent 后端位于 `apps/api/`，后续 Next.js 前端放在 `apps/web/`。
- 跨应用共享代码放在 `packages/`，部署和基础设施放在 `infra/`；仓库级说明放在根目录。
- 新增文件前先判断它的生命周期。只要会被后续周次继续修改，就必须使用跨周目录，而不是当前周目录。

## 应用内部目录规则

- 后端保持 routers / services / repositories 分层，每层按 auth、chat、workspace、tasks、model、runtime 等现有业务领域归类，避免重新把新模块平铺在层级根目录。
- 后端测试按领域放到 `apps/api/tests/` 子目录，共用根 `conftest.py` 的 PostgreSQL 隔离夹具；跨目录导入使用明确的 `tests.<领域>.<模块>` 包路径。
- 不为凑层级创建空业务目录，不保留旧路径的转发壳；移动模块时同步导入、测试、脚本及当前文档。Alembic versions 继续遵循迁移修订顺序。
- 目录导航见 `docs/project-structure.md`。前端沿用 Next.js app 路由和 features 领域结构。

## 文件格式规则

- 新建文件默认每级缩进使用 4 个空格，不使用 Tab；对话中的参考代码同样使用 4 个空格缩进。文件格式自身强制要求的语法除外。
- 修改已有文件时，不为统一缩进而重排与本次任务无关的代码。
- 后续教学参考代码与教练编写的配套代码需要必要注释；借鉴学习者已有的注释位置，在字段、关系、约束和关键逻辑前解释用途及原因，尤其注明事务边界，不仅复述代码。

## 产品使用场景

- 主产品为本地优先 Coding Agent：浏览器 UI、API 与执行服务运行在用户电脑；无需产品注册登录，模型由用户配置。账号模式保留为扩展，不继续推进匿名访客主线；域名用于展示、文档和分发。
- 后续课程、功能设计与默认验收以 APP_MODE=local 的 PC 场景为主；不再主动推进账号功能，也不将账号模式专项回归作为每课完成条件。既有账号实现和测试保留为历史扩展；共享代码按本地功能实际影响范围验证，用户明确要求时再处理账号模式。
- 最终 PC 界面采用用户指定的 DeepSeek Harness / Codex Harness 风格三栏工作台；顶部“聊天首页 / 工作空间”为临时导航。布局规划为左侧项目/任务、中间对话与执行过程、右侧文件/Diff/运行详情，支持侧栏收起与调宽；具体视觉实现前核对用户所指参考版本，不把临时独立页面当最终布局。
- 当前产品面向 PC 端，默认按桌面场景设计并验收。浏览器测试默认使用 PC 视口，重点检查笔记本与桌面宽屏、键盘操作和主要流程；移动端适配不能替代 PC 验收。

## 学习推进规则

- 新知识按照“是什么 → 为什么需要 → 怎么使用 → 示例”讲解。
- 一次只推进一个可运行、可验证的小任务，完成并确认后再继续。
- “小任务”限制的是每课范围，不代表只给零散的最小代码。开始动手前，默认提供当前任务的完整可运行参考实现：明确文件路径、给出全部核心改动与验收方案；测试文件等配套等学习者明确说本课“完成了”后由教练创建。不使用 `...`、伪代码或省略关键导入；学习者参考后亲手实现。
- 教学代码直接放在对话流的普通代码块中，不新建讲义、补丁文件或用文件链接代替展示，不用 Git Diff 的加减号与行号作为教学格式。
- 只有新建文件才给完整文件；任何已有文件（无论长短）只给需改动的完整代码段、类型、函数或组件，明确查找与替换位置，以及必要导入。跨文件任务必须把所有相关文件一次列全。
- 只有学习者明确说本课“完成了”后，教练才检查其实际改动，并创建对应测试文件、补齐测试命令、样板与机械性配套代码；这些配套由教练实现，不交给学习者抄写，不重复请求代写确认；学习者只亲手实现本课有学习价值的核心逻辑。测试仍须完整并覆盖成功与失败路径，教练简述覆盖点和真实验证结果。
- 核心业务实现默认只供讲解和对照，不代写，除非学习者明确授权；测试及机械性配套已获长期代写授权，但必须等学习者明确说本课“完成了”才开始，不能提前创建测试或开展代码正确性验收。学习者完成后再检查实际 Diff、运行验证并解释差异。
- 用户要求在 main 学习：使用已检出 main 的主仓库目录执行后续读取、配套改动和验收。开始前核实分支与工作区；若对话初始目录是独立 worktree，不把 detached HEAD 冒称为 main，也不强制多工作树共享同一分支。
- 不重复已经完成的内容，始终围绕 AI Agent 应用开发，并发挥前端 / 全栈产品工程优势。
- 每课结束不要求学习者当堂回答掌握检查题，不以口头答题作为课程完成或继续学习的门槛。工程验收通过后，教练主动筛选重要面试知识点，将题目、参考答案、失败场景、取舍和项目证据归档至 `interview-questions/` 对应分类，优先更新已有题目并同步索引。标记复习优先级与“参考答案已整理、尚未模拟”；等用户要求模拟面试时再抽取重要题目，不把归档视为学习者已通过面试考核。

## 回归测试范围规则（长期有效）

- 每课回归只包含本课新增测试，以及当前代码修改实际影响到的既有功能测试。不默认运行全量后端、全量前端或整个业务领域测试集。
- 选择既有测试前，先说明“修改点 → 受影响功能 → 对应测试”的依据；同目录、相邻模块或历史上一起运行过，不构成回归理由。测试文件包含无关功能时，优先按测试用例或名称筛选。
- 只有发现具体跨模块影响、测试失败或未解决风险时才扩大范围，并说明新增范围与原因；不能以“保险起见”无限扩大。已有检查通过且代码未变化时，不重复运行。
- 类型检查、静态检查和浏览器验收同样按实际影响选择；没有前端改动或跨端协议影响时，不例行运行前端测试或浏览器验收。
- 新增测试仍须完整覆盖本课成功与失败路径。范围收敛不代表减少必要覆盖；必须等学习者明确说本课“完成了”后才创建测试和执行正确性验收。

## 数据库测试规则

数据库测试统一使用 PostgreSQL + psycopg，与开发环境保持相同数据库技术栈；复用 `apps/api/tests/conftest.py` 的独立测试库/私有 schema 配置，不新增 SQLite 测试或连接开发业务表。事务测试应允许真实提交，测试结束自动清理本轮资源；运行方式见 `ENVIRONMENT.md`。

## 学习收尾文档规则

- “更新对应文档”只指更新本次实际变化的事实来源，不表示每课都修改所有 learning 文档、环境说明和 README。总结当日进度也遵守此规则，不把回复全文复制进仓库。

| 文档 | 唯一职责 | 何时更新 |
|---|---|---|
| `LEARNING_HANDOFF.md` | 当前快照、唯一下一课、必要边界、最近验收摘要、未解决问题 | 课程完成或接续状态改变；替换原内容，不追加历课记录 |
| `LEARNING_PLAN.md` | 岗位目标、技术路线、周目标与整周状态 | 路线/里程碑或整周状态变化；不写分课完成清单 |
| `LEARNING_CURRICULUM.md` | 任务、验收标准、关键概念/坑及阶段能力状态 | 课程内容变化或能力条目状态变化；更新原行，不逐课追加 |
| `LEARNING_COACH_GUIDE.md` | 授课模板与补充设计判断 | 教学方法变化；不重复 AGENTS 规则或当前进度 |
| `ENVIRONMENT.md` | 当前安装、配置、启动、迁移、隔离测试方法、可复用故障处理 | 环境操作实际变化；禁止追加逐课测试数量/耗时/实现流水账 |
| `interview-questions/` | 重要问题、答案、取舍与项目证据 | 确有重要新增考点；优先补已有题目 |
| `week-learning/week-XX/REVIEW.md` | 已结束周次的阶段复盘和必要验收证据 | 周结束时归档；不成为跨周持续更新的运行说明 |

- 同一事实只维护一处，其他文档使用链接。只允许简短导航摘要；“唯一下一课”只在 HANDOFF 维护，其他文档不得再写一份。README 不保存动态课名。
- 每课验收在对话中说明真实结果；HANDOFF 只保留最近一课的范围、结论、证据入口和未验证边界。详细证据优先沿用测试/报告/题库，重要阶段结论在周结束进入复盘；不把历次测试数加总冒称全量结果，不丢失重要但尚未归档的证据。
- HANDOFF 目标不超过 100 行且 6000 字符；单行塞入整段历史不算精简。ENVIRONMENT 目标不超过 250 行；其余按职责精简，不为限长删课程任务、成功/失败验收、教学约束、待办或重要未验证边界。超出时先去重、归位和改链接；必要内容确实超限须说明原因。
- 默认不新建“今日总结”、daily log、逐课讲义或逐课历史快照。`docs/history/*through-2026-09-23.md` 是本次精简的一次性冻结档案，后续禁止追加或仿照生成每课副本；按主题检索即可。
- 文档收尾前检查：是否重复追加、是否还有过期下一课/已解决问题、当前完成状态是否与证据一致、链接/命令入口是否有效。文档精简不能降低教学完整度，也不能把“教练实现/工程通过”改成“学习者独立掌握”。

## 新会话上下文规则

- 新的学习会话只把本文件与 `LEARNING_HANDOFF.md` 作为固定启动上下文，然后检查 Git 分支、工作区和当前课程相关代码。
- 不为恢复进度而完整读取 `LEARNING_PLAN.md`、`LEARNING_CURRICULUM.md`、`LEARNING_COACH_GUIDE.md`、`ENVIRONMENT.md` 或 `docs/codex-like-agent-scope.md`；先按标题/关键词定位，只读取当前任务需要的章节。历史档案、周复盘、题库和测试报告不作为固定上下文批量加载。
- `LEARNING_HANDOFF.md` 的“唯一下一课”是课程接续事实来源；历史实现细节优先从相关代码、测试、Git 历史和周复盘中按需检索。
- 按最小充分上下文推进：先确认当前目标、约束、验收基线和未解决问题，证据足够后停止扩展读取。

## 项目 Skills

题库筛选补充：只归档重要功能、关键设计取舍与工程难点；简单样式、文案、卡片展示等课程无需单独入库，也不要求每课都有题目。没有新增重要考点时直接完成课程收尾。

遇到下列变更前，先读取对应 `SKILL.md`；它们记录本项目的非通用工程约束，而不是替代正常的设计判断。

- FastAPI、Next.js BFF、浏览器流式渲染或 Agent 运行状态相关改动：`skills/agent-streaming/SKILL.md`。
- 工具定义、参数校验、工具调度或 Agent Loop 相关改动：`skills/agent-runtime/SKILL.md`。
