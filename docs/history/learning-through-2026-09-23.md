# 历史学习记录（截至 2026-09-23）

本文件是本次精简的一次性冻结档案，不再追加，不作为启动上下文。它保存旧交接及移出的课程完成记录，供查询学习者/教练分工、设计取舍和历史限制；其中“当前”“下一课”“尚未完成”均指当时，不得用来安排新课。实时进度只见 [LEARNING_HANDOFF.md](../../LEARNING_HANDOFF.md)，有效教学规则只见 [AGENTS.md](../../AGENTS.md)。

历史测试结果见 [验收证据](verification-through-2026-09-23.md)。先按主题搜索，再读取相关章节；重复历史叙述不再同步维护。

## 原交接快照

# AI Agent 学习交接

更新时间：2026-09-23（Asia/Shanghai），第5周清理待办只读诊断已接入工作台并完成PC只读验收；下一课转回隔离样例仓库的Sandbox只读挂载。正式进度仍为4/12。

这是新会话的唯一动态进度入口，长期规则见自动加载的 AGENTS.md。详细历史与验证记录按需查看，不再整篇加载计划或课程大纲。

2026-09-23本课直接实现并验收：当前PC工作台右栏新增清理待办只读诊断面板，只有用户点击或键盘触发才经同源BFF读取；七种固定分类由浏览器再次校验资源后展示，失败保留未知，取消、收起与Task切换使旧响应失效。组件Chrome专项通过；直接受影响的既有真实工作台专项通过，覆盖双面板按需GET、收起/Task切换、旧响应隔离、无写请求与1366/1920视口。另用隔离Chrome→Next BFF→FastAPI→随机PostgreSQL/自有待办样例链路验证来源Task的identity_matches_record、兄弟/未知Task 404、非法查询422及no-store；读取前后文件字节/inode/mtime、提案、Workspace与来源记录不变，页面无错误/写请求，样例、服务与随机库/schema均清理。TypeScript、定向ESLint与diff check通过；匹配只是诊断快照，绝非清理、恢复或重试授权。未运行全量回归，具体证据见ENVIRONMENT.md。正式进度仍4/12，第5周进行中。

2026-09-23本课直接实现并验收：Next.js同源BFF新增GET /api/workspaces/{workspaceId}/tasks/{taskId}/sample-cleanup-preflight；仅在local模式、本机访问与服务端凭证边界内代理上游只读查询。请求拒绝非法标识、查询和正文；上游200须资源标识匹配且result是七种固定分类之一，浏览器响应只重建workspace_id、task_id、result并no-store。上游403/404/422保留状态但统一脱敏，其他状态、非法正文、错资源、取消和超时为502；上游Cookie、路径、dev/ino与句柄均不透传，也不自动重试或赋予清理权。新增BFF契约测试51条通过，TypeScript、定向ESLint和diff check通过。上游fetch受控模拟，本课未接工作台或真实浏览器；具体证据见ENVIRONMENT.md。正式进度仍4/12，第5周进行中。

2026-09-23本课直接实现并验收：GET /workspaces/{workspace_id}/tasks/{task_id}/sample-cleanup-preflight把内部read_cleanup_preflight的七种固定分类公开；请求仍须通过local模式和来源Task重新授权，严格限制路径、查询与正文，成功响应只有workspace_id、task_id、result。未知/越权及同Workspace兄弟Task均为404 workspace_not_accessible，非法输入为422 invalid_sample_cleanup_preflight_input，读取失败为500 sample_cleanup_preflight_read_failed；设置no-store，不把读取故障降级成安全分类。目录观察仍发生在服务的短数据库事务结束后；响应不含目录路径、dev/ino、句柄或令牌，不提供清理、恢复或重试能力。identity_matches_record只是读取时刻的诊断快照，不证明目录可安全清理。新增HTTP测试32条及直接受影响旧用例13条通过；定向Ruff、compileall和diff check通过，随机隔离测试库遗留数为0。实际请求链与资源清理证据见ENVIRONMENT.md；第5周正式验收仍未完成。

2026-09-23本课完成（用户授权直接实现）：内部清理待办预检在可信临时父目录fd下以O_DIRECTORY/O_NOFOLLOW只读打开候选目录，交叉核对打开前目录项、候选fd和退出前目录项的身份/属主/0700形态，并前后复核父目录。首次缺失仍报directory_missing；观察期间替换、消失或I/O/关闭失败报inspection_unavailable；稳定但与持久身份不符报evidence_inconsistent。新来源稳定数值匹配才报identity_matches_record，旧来源即使本轮描述符一致仍identity_unverifiable。候选fd先于父fd关闭，不读取内容、不恢复原进程句柄、不执行清理；检查后替换和inode重用仍无法由只读预检排除。仅运行新增与直接受影响的隔离PostgreSQL/真实临时目录测试24条通过，9条无关用例未选；定向Ruff、compileall与diff check通过，随机测试库无遗留。无模型、迁移、HTTP或前端变化，未运行全量或浏览器回归；具体证据见ENVIRONMENT.md。

2026-09-23本课完成（学习者实现核心，教练验收修复）：新建受限样例在发布路径前核对父/子目录描述符身份，登记沿用核验值；bind与Workspace.root_path、来源记录同事务保存parent/root的dev/ino四值。93e4c2d7a10b仅加四个可空列与完整性约束，旧来源不回填；有身份记录时降级拒绝丢证据。内部预检对新记录返回当前候选数值匹配或错配，旧记录仍报身份未知；匹配不是CAS、执行权或清理许可。教练恢复误删的预检结果类型、去除重复描述符校验，并按用户反馈修正静态类型收窄。新增及直接受影响的隔离PostgreSQL/真实临时目录测试123条通过，类型修正后定向复核1条通过；Ruff、compileall、diff check与开发库Alembic结构检查通过。开发库从82d14f5b09ad迁至93e4c2d7a10b，来源记录迁移前后均为0，Workspace仍为1；无HTTP/前端改动，未运行浏览器或全量回归。详细边界和命令见ENVIRONMENT.md。

2026-09-23上一课完成（当时状态）：TaskSampleBindings新增仅供可信服务端调用的read_cleanup_preflight；先检查本地进程并重新授权来源Task，在短事务内核对Workspace与持久待办，事务退出后才只读观察当前服务端临时父目录下的规范样例名字。冻结结果只有固定分类：来源缺失、非待办、证据不一致、目录缺失、候选目录身份无法确认或检查不可用；不返回路径/句柄，不恢复执行或清理。该课当时数据库尚未持久化创建时目录dev/ino，故同名、同属主和0700权限的目录仍不能证明是原对象；当前临时父目录与记录不同时也不探查旧路径。新增19条隔离PostgreSQL/自有临时目录测试通过，定向Ruff和diff check通过；无HTTP/前端协议变化，未运行全量或浏览器回归。main工作区保留原有和该课未提交改动。

2026-09-23本课完成（用户授权直接实现）：样例登记状态增加必填、脱敏的sealed_reason，只有重新授权的来源Task在持久来源为cleanup_pending时看到cleanup_pending；其他sealed均为unavailable，missing/busy/ready为null。FastAPI严格验证状态/原因组合，BFF与浏览器再次校验并只投影四个公开字段；PC工作台说明持久待办，不推断样例目录现状，也不提供恢复或重试操作。定向PostgreSQL状态/HTTP/清理待办52条通过，补充最终提交不确定边界2条通过；前端契约57条、TypeScript、定向ESLint和PC Chrome组件通过。真实链路及清理证据见ENVIRONMENT.md。未运行全量回归。main工作区保留先前及本课未提交改动。

2026-09-23清理待办课完成（用户授权直接实现）：WorkspaceSampleOrigin新增active/cleanup_pending持久状态及82d14f5b09ad迁移，旧来源自动保持active。close先同事务解绑Workspace并置待办，确认后安全清理自有目录，清理确认后再以第二事务删除来源；清理失败、中断、延期或最终数据库确认失败保守封锁。待办阻止样例和普通目录重新绑定，也继续保护来源Task不被删除，GET只报sealed。定向新增及直接受影响PostgreSQL测试分批3、4、68、8、2条通过；Ruff、diff check、Alembic check通过，本地开发库已迁至新head。测试临时文件与随机库/schema均清理；未做全量或浏览器回归。第二次删除来源的提交若已成功但回执丢失，记录可能已消失，此时文件已确认清理，旧进程登记仍封锁，不能把回执失败推断为未清理。当前main工作区仍有未提交改动。

2026-09-23新课完成（用户授权直接实现）：read_status在重新授权后即使没有进程登记，也读取Workspace的持久样例来源；无内存登记而有来源时保守sealed，包含同Workspace来源Task错配与目录错配，无来源的普通Workspace仍missing，读取失败不伪造状态。未恢复进程句柄、文件操作或执行权。新增/受影响PostgreSQL状态与HTTP测试分批35+1条通过；隔离真实浏览器→Next BFF→FastAPI返回sealed/missing/404/422，样例文件与提案数据库状态不变，PC双宽度无页面错误，自有样例、服务与随机测试库/schema均清理。首次浏览器运行发现旧夹具把同Workspace的兄弟Task误当无来源，已改用独立普通Workspace并重跑通过；无全量回归。下一课处理关闭后文件清理失败时来源证据先被删除的问题，仍不自动清理。

2026-09-23接续：状态查询已接通同源BFF、工作台只读面板和定向浏览器验收。随后完成受限样例来源持久登记：新增workspace_sample_origins表，服务端绑定时与Workspace.root_path同事务写入；关闭已由上方课程改为先置待办再清理；借用与状态读取核对来源，来源Task删除返回明确409。旧普通项目目录不回填。定向新增及受影响测试、PostgreSQL迁移与前端冲突提示验证通过，详情见ENVIRONMENT.md。持久记录不能恢复进程句柄；当时新进程会把有来源但无内存登记的样例报为missing，已由上方只读诊断课修正。当前改动留在main工作区，未提交推送。

2026-09-22收尾：今天学习结束。将本轮提案审批、受限样例应用完整链路、Workspace目录/路由整理、PC与真实端到端验收、登记状态服务及HTTP接口统一归档main。各课“未提交推送”为当时的历史状态；本次提交与远端同步以Git记录为准。验证证据按课记录于ENVIRONMENT.md，不将不同批次相加为全量回归，也不重复已通过的测试。当时接续课为“受限样例登记状态BFF代理”；新对话可先给完整核心参考，等待学习者后续动手，不延续逐课代写授权。

- 受限样例登记状态HTTP接口课完成（用户授权直接实现）：新增GET /workspaces/{workspace_id}/tasks/{task_id}/sample-status，独立samples路由复用WorkspaceRoute、CurrentUser及进程get_sample_bindings。严格冻结TaskSampleStatusResponse仅公开workspace_id/task_id/status；路径严格校验，拒绝任何查询参数或正文，响应统一no-store。未知/越权404，非法输入422；查询/内部状态/响应失败统一500 sample_status_read_failed，不降级missing/ready。同步服务在线程池执行，路由不持数据库事务或访问文件。新增20条与共享边界直接相关57条共77条通过（8.29s），Ruff/diff check通过；独立PostgreSQL测试库/schema自动清理，无BFF/UI接入或全量回归，未提交推送。

- 受限样例登记状态只读查询课完成（用户授权直接实现）：TaskSampleBindings新增read_status与冻结TaskSampleStatus，只返回missing/busy/sealed/ready。先检查local模式/进程身份，再按进程锁→数据库归属锁顺序重新授权Workspace/Task/Conversation，包含无登记情况；根目录绑定不匹配仅报告sealed，不修复或改变登记。查询不创建/借用/关闭样例、不访问文件、不提交数据库，退出短事务后返回快照。ready不证明文件仍有效、不预留执行权，不依据提案审批/应用状态恢复内存登记。新增18条及同类生命周期直接影响23条共41条通过（3.08s），Ruff/diff check通过；无HTTP/BFF/UI变更、无全量回归，未提交推送。

- 受限样例应用真实端到端验收完成（用户授权直接实现）：新增execution-e2e隔离API启动夹具、Next/BFF启动器和真实Chrome脚本。使用独立PostgreSQL库/schema及真实Alembic迁移；API内部创建本机用户/任务、自建样例并生成批准提案，页面仅获公开标识。真实页面→BFF→API→门禁→文件替换→数据库登记通过：old\n变new\n，idle变applied，HTTP200公开回执replaced/applied/cleanup_complete=true；UI请求一次且刷新不重发，非法额外path返回422。直接重复HTTP返回200但code=proposal_application_claim_unconfirmed，文件内容/inode/mtime均不变并封锁登记，正常close拒绝。仅测试夹具在请求结束且核对applied后解绑并清理自有样例；生产保护未改。最终样例目录删除、Workspace解绑、进程及测试库/schema清理确认，pageerror=0；Ruff/定向ESLint/diff check通过，无全量回归，未提交推送。

- 受限样例应用组件PC浏览器验收完成（用户授权直接实现）：新增test/browser/execution-component隔离编译/页面/验证脚本，复用真实组件、按钮、全局样式及React StrictMode；仅监听127.0.0.1随机端口，API请求全部受控拦截，漏拦截时服务器503拒绝。真实Chrome在1366×900与1920×1080下7组专项通过，7次受控请求，pageerror为0；覆盖键盘勾选提交、StrictMode不重发、刷新回执、停止等待、卸载重挂、资源切换及迟到响应、unknown恢复、非法回执和存储不可用。三张截图已检查，内容区宽度/滚动宽度均720px无越界。TypeScript、定向ESLint及diff check通过，组件无须修改。无真实后端/数据库/文件写入，无既有或全量回归，未提交推送。

- 受限样例提案应用交互组件课完成（用户授权直接实现）：新增独立ProposalExecutionActions，按资源key重建生命周期与确认状态，effect内装配真实sessionStorage、fetch和请求控制器；首次checking避免水合差异，勾选后才允许提交，停止等待不撤销写入。清理先取消订阅再dispose，未知结果保留防重复标记，合法回执复用既有只读展示。新增14条静态渲染/受控Hook调度测试通过，包含真实控制器的点击、取消、卸载及迟到结果；TypeScript、定向ESLint通过。尚未挂通用工作台，未做真实浏览器验收、真实后端或文件写入、既有或全量回归；未提交推送。

- 提案应用客户端请求状态管理课完成（用户授权直接实现）：新增独立createProposalExecutionRequest控制器，按资源快照管理idle/submitting/receipt/uncertain/unavailable；请求前同步保存本标签页防重复标记，重建实例重新读取，存储不可用拒绝提交。25秒浏览器等待预算，取消/切换/销毁不清除标记，代次隔离迟到响应；合法公开回执持久保存并保留unknown/uncertain，回执保存失败仍展示已核对证据。保守策略：所有非200响应均保留未确认且禁止重提，不提供解锁/自动重试。新增25条分批通过，TypeScript及定向ESLint通过；未改既有模块、未接页面、未访问真实后端/文件、无既有或全量回归，未提交推送。

- 受限样例提案应用BFF代理课完成：学习者核心与参考一致，无需修正。新增同源apply路由及proposalExecutionProxy，复用本机访问边界和公开回执解析；严格动作/资源/查询校验，服务端凭证隔离，状态与错误码白名单，20秒上游预算覆盖响应读取。转发前取消标记尚未转发，转发后取消/超时/解析失败均保持结果未确认，不重试；合法unknown/uncertain回执原样保留。新增106条受控上游测试通过（0.70s），TypeScript、定向ESLint、diff check通过。未访问真实后端、数据库或文件，未做浏览器或全量回归，未提交推送。

- 受限样例提案应用HTTP课验收完成，并按用户建议整理Workspace：服务分为directory/files/edits/proposals/samples/metadata，测试同步分组；原1437行路由拆成项目、目录、任务、提案和样例应用入口，复用统一安全边界，聚合入口9行，最大模块342行。27个服务业务AST、27个路由函数/类AST及完整OpenAPI与拆分前一致；保留全部接口，无旧路径转发壳。新增23条HTTP测试、直接受影响386条既有共409条通过，Ruff/diff check通过。使用隔离PostgreSQL及自有临时文件；未全量回归、未运行浏览器、未提交推送。其他聊天大文件已在docs/project-structure.md记录拆分方向，未扩大本次实现范围。

## 2026-09-22 接续

- 样例授权门禁与提案执行器装配课完成（用户授权直接实现）：新增execute_sample_proposal，在可信TaskSampleBindings借用内重新授权提案及目录/固定文件范围后调用执行器，借用持续到执行结束。临时样例保存创建时目录dev/ino；执行器及预检增加可选期望目录/文件约束，替换原语在打开实际父目录后、访问文件前核对期望身份。回执经既有严格Schema验证，unknown/uncertain、非法回执和中断使登记封锁，保留目录/原文件证据，无自动重试。19条新增与24条直接受影响既有共43条不同用例通过，Ruff/diff check通过。真实隔离PostgreSQL及临时文件验证成功、错误范围、数据库改绑/目录替换、登记故障、重复执行及清理拒绝；无HTTP/前端/浏览器/全量回归，未提交推送。

- 临时样例与用户任务授权绑定课完成（用户授权直接实现）：新增内部TaskSampleBindings，local模式下按用户/Workspace/Task建立私有登记，只接收资源标识不接受路径/句柄；创建前授权，创建后持项目→任务→会话锁重新授权，仅绑定未绑定Workspace，复用整个项目的running/uncertain占用保护。借用重新授权及核对目录绑定，文件使用期间无数据库事务；忙时拒绝关闭。正常关闭先确认解绑提交再清理。提交尝试前失败清理未发布样例；绑定/解绑commit结果未知及使用异常保留目录并封锁登记，不自动恢复或重试。23条分批通过（18条1.62s、补充5条0.67s），Ruff/diff check通过。无既有模块修改、HTTP/执行器接入、前端/浏览器/全量回归，未提交推送。

- 临时样例服务端登记与失效边界课完成（用户授权直接实现）：新增TemporarySampleRegistry，只经内部自建流程登记，不提供任意路径/对象注册。随机内部句柄、最多32项、独占借用；close立即撤销新借用，活动借用退出后清理，正常归还允许后续借用。借用异常撤销并清理，清理失败保留失效登记/容量和现场，不自动重试；目录/父目录身份及权限变化拒绝。新实例不恢复旧登记，PID变化在碰锁前拒绝。18条分批通过（16条0.10s、补充2条0.07s），Ruff/diff check通过；包含真实线程关闭、创建失败、失效与异常保留。未改旧模块，无数据库/HTTP/用户资源授权接入、既有或全量回归，未提交推送。

- 服务端临时样例目录生命周期课完成（用户授权直接实现）：新增temporary_proposal_sample上下文管理器，无外部路径参数，macOS服务端临时目录内独占创建0700目录和0600固定example.txt（old\n）；持有父/子目录描述符核对身份，支持短写，退出仅清理已知单硬链接普通样例文件（允许可信原子替换），拒绝未知内容/链接/身份或权限变化，不递归删除。异常和中断释放描述符；清理失败保留现场，以固定错误报告或给原异常追加note。21条最终0.10s通过，Ruff/diff check通过。无HTTP/数据库/注册门禁接入，无既有或全量回归，未提交推送。路径对象不是授权凭证，必须作用域内结束使用且无外部写者，身份核对不是CAS。

- 提案应用请求契约课完成：学习者实现与参考一致，仅补类间空行。新增严格冻结FileEditProposalExecutionRequest，只接受必填action=apply，禁止额外身份/路径/正文/令牌/权限字段。新增32条0.10s通过，覆盖字典与JSON合法/非法输入、精确动作、冻结及Schema必填说明，Ruff/diff check通过。执行范围仅完成设计约束：未来实验入口须由服务端创建并登记临时样例，APP_MODE=local或浏览器声明不构成范围授权；可信样例登记与门禁尚未实现。未接路由、无文件执行/数据库/前端/既有或全量回归，未提交推送。

- 提案应用执行结果的只读展示组件课完成（用户授权直接实现）：新增ProposalExecutionResultPanel，展示边界重新解析资源及协议，分别表达文件结果、登记确认、临时资源清理；区分领取未确认与登记未确认，非法回执不推断未写入，不展示令牌/路径/正文。独立无状态组件，无fetch/执行/重试按钮，尚未接工作台页面。新增25条静态渲染测试0.88s通过，覆盖17种合法回执、非法输入/错资源、切换隔离及敏感字段；TypeScript、定向ESLint、diff check通过。未做浏览器视觉验收、既有回归或全量回归，未提交推送。

- 提案应用执行结果的前端解析契约课完成：学习者实现与参考一致，无核心修正。纯解析器校验三个资源标识、自有必填字段、严格类型及17种合法组合，显式投影公开字段，保留replaced/unknown证据，不查询/执行/重试。新增51条通过（0.78s），含调用真实后端Schema逐项对照240种组合（17接受/223拒绝）、缺字段与原型链、错误资源/状态/清理值、冻结输入不变及敏感字段隔离。TypeScript、定向ESLint、diff check通过。无HTTP/BFF/UI接入、数据库/浏览器/既有全量回归，未提交推送。

- 提案应用执行结果的公开响应契约课完成：学习者核心与参考一致，教练仅补类间空行。新增严格冻结Schema与显式投影，校验三个公开标识、回执提案匹配、文件/登记/清理状态及固定错误码组合；拒绝额外字段、隐式类型转换和未知协议。新增57条0.94s通过，包含240种组合穷举（17合法/223拒绝）、敏感字段隔离、缺字段/错误对象及无执行/数据库/文件I/O调用检查。Ruff/diff check通过。未接HTTP路由、未改执行器，无既有回归/数据库/前端/浏览器/全量回归，未提交推送。

- 临时样例提案应用完整链路验收完成：新增7条服务串联测试，经真实创建、审批、执行和授权查询，不直接改审批状态。隔离PostgreSQL及真实临时文件验证有/无ACL、普通二进制/空xattr与完整可见元数据保留（不滤provenance）、正文精确替换、临时文件清理、文件I/O前关闭事务、一次领取及重复不写；验证批准后基线过期、pending/rejected拒绝、终态登记提交前/后故障下文件成功与记录确认分离。7 passed（1.83s），Ruff/diff check通过。仅新增测试与文档，无业务代码修改，无既有回归/前端/浏览器/模型调用/开发库操作，未提交推送。

- 元数据保护装配扩展属性复制课完成（临时样例）：FileMetadata纳入完整可见xattr，两轮ACL/xattr快照及前中后stat核对；属组→权限→xattr→ACL→完整回读。拒绝同inode、目标额外属性和源/目标变化，失败保留临时目标部分修改并交调用方清理。学习者核心与参考一致；教练修正contextmanager返回标注为Generator[int, None, None]及空行。删除测试代理的provenance过滤，真实临时样例ACL/xattr复制、替换及隔离PostgreSQL执行登记通过。16条新增与40条直接受影响测试共56条分批通过；Ruff/diff check通过。仍非CAS，不保证不可见属性、不同provenance值可复制或断电持久化，无HTTP/UI写入口、全量回归、提交推送。

- 受限扩展属性复制原语课完成（用户授权直接实现，临时样例）：新增copy_file_xattrs，校验源快照及源/目标对象身份，拒绝同inode、目标多硬链接/非当前属主/非零flags/特殊权限位，目标额外属性在写前拒绝。复制仅使用当次有界读取值，相同属性跳过设置，不删除属性/绕过权限；最终回读完整可见快照并复核源与文件信息。失败可留下部分修改，目标清理由可信调用方负责，不关闭调用方描述符、不执行replace。新增26条分批通过（21条0.14s、补充5条0.08s），Ruff/diff check通过；真实临时文件验证普通属性复制及重复调用零设置、正文/偏移不变，受控测试覆盖返回成功但不生效、源变化、目标额外属性、部分失败和中断。不保证临时性/独占性/原子快照，provenance原本相同不证明不同值复制能力。未接现有替换入口，无数据库/前端/浏览器/全量回归，未提交推送。下一课元数据保护装配扩展属性复制（临时样例）。

- 扩展属性复制能力探测课完成（临时样例）：学习者核心与参考一致，仅合并临时文件上下文并整理缩进。探测函数只创建自有临时文件，分别记录matched_before/set_accepted/readback、目标额外可见属性与最终快照一致性，不输出属性值/摘要，不删除系统属性、不修改替换策略。新增19条0.08s通过，Ruff/diff check通过；真实macOS三个普通样例从目标缺失到回读相同，provenance源/目标原本相同且调用成功/回读相同，不能据此证明不同值复制能力。受控测试覆盖调用失败但原本相同、成功却缺失/不同、额外属性、源变化、读取失败、创建/清理/中断路径及资源关闭。无数据库/前端/浏览器/既有整套/全量回归，未提交推送。下一课受限扩展属性复制原语（临时样例）。

- 有界扩展属性读取课完成：学习者核心与参考一致，无需修正。新增macOS描述符级只读xattr模块，固定两轮快照及前中后fstat、名称集合复核，预算128项/16KiB名称/64KiB单值/256KiB每轮逻辑总量；空值合法，异常不降级为空，名称和值不进入repr。新增39条0.13s通过，Ruff/diff check通过；真实临时文件验证二进制/空值/中文名称及内容、元信息、描述符偏移不变，受控ABI验证预算分配前拒绝、数量/总量边界、非法名称、变化/权限/平台失败及固定轮数。没有修改现有替换策略、无数据库/前端/浏览器/既有整套/全量回归，未提交推送。读取只覆盖当前进程可见属性，不是原子快照/CAS或复制许可。下一课扩展属性复制能力探测（临时样例）。

- 提案应用状态按需展示课完成（用户授权直接实现）：新增独立查询组件接入提案详情，五状态、加载/错误、重新查询和取消；按三资源标识key隔离实例，卸载abort并通过controller身份阻止旧响应。审批标签改为已批准，不再从审批状态推断尚未应用。新增8条与受影响详情8条共16条通过（1.41s），TypeScript/定向ESLint/启动器Ruff/脚本语法通过。PC真实BFF/API/隔离PostgreSQL验证idle，另外四状态和异常响应受控；收起卸载后的延迟响应隔离、显式取消、跨任务导航、键盘查询、1366/1920无横向溢出及截图视查通过，原文件不变，提案写请求为0。无执行入口/全量回归/真实模型调用，未提交推送。下一课有界扩展属性读取。

- 提案应用状态查询BFF代理课完成（用户授权直接实现）：新增四字段状态解析器、服务端代理与同源GET application-status路由；local访问边界、内部凭证隔离、严格三标识与额外查询拒绝、五状态及资源匹配、公开字段投影、no-store和20秒取消/超时预算覆盖正文读取。未知协议安全502，不默认idle、不自动重试或触发执行。55条新增测试通过（0.59s），TypeScript/定向ESLint/diff check通过。上游fetch受控模拟，无真实HTTP/数据库/浏览器/后端/既有整套/全量回归，无UI或执行入口，未提交推送。下一课提案应用状态按需展示。

- 提案应用状态查询HTTP课及workspace路由整理完成：学习者接口核心与参考一致。新增GET application-status返回四字段严格Schema，复用local身份边界、统一404与no-store，非法标识/额外查询422，未知状态及响应异常安全500。用户要求整理workspace.py，已合并排序导入，集中请求/标识类型，按安全边界、工作空间/目录、任务/运行历史、提案审阅/审批/状态分区并统一格式；全部函数/类含装饰器AST与整理前一致，无业务逻辑调整。新增34条和直接受影响既有18条共52条分批通过（首批33通过，1条OpenAPI测试缺本地凭证；补凭证后修正1条+既有18条共19条2.95s通过），Ruff/diff check通过。隔离PostgreSQL与真实HTTP验证；无文件执行/前端/浏览器/模型/全量回归，未提交推送。下一课提案应用状态查询BFF代理。

- 提案应用状态只读查询课完成：学习者核心与参考一致，教练仅补仓库函数间空行和末尾换行。单次联合查询重新授权Workspace/Task/Conversation/Proposal，仅投影三资源标识与application_status；五种状态严格校验，不读取文件、不领取/登记/重试，未知状态拒绝。新增29条分批通过（首轮24通过/5条因测试令牌与公开项目编号碰撞误报；修正独立令牌后5条1.22s通过），Ruff/diff check通过。隔离PostgreSQL验证归属、仅SELECT/无锁/无flush/无commit、私有字段不加载、会话关闭、绑定变化和文件删除后历史查询及无缓存读取。无既有全量/文件替换/前端/浏览器/迁移，未提交推送。下一课提案应用状态查询HTTP接口。

- 临时样例中的提案应用执行装配课完成：学习者实现与参考一致，核心无需修正。执行器按单次领取、重新授权/占用令牌与绑定核对、只读preflight、再次快照核对、受限替换、单次终态登记运行，文件I/O前释放数据库事务。回执分开表达文件证据与已确认数据库状态；登记回执丢失返回unknown，不回滚文件或重试，BaseException传播并保留占用。新增37条分批通过（35条4.70s、补充2条1.00s），Ruff/diff check通过；真实隔离PostgreSQL验证提交前/后故障与并发仅一个写入者。成功文件路径沿用仅过滤provenance的测试模拟，额外验证无模拟真实xattr拒绝和核对后外部编辑；不宣称当前宿主完整无模拟写入或CAS。仅新增测试，无既有整套/数据库迁移/前端/浏览器/模型调用，未提交推送。下一课提案应用状态的只读查询。

- 文件替换元数据保护课完成（临时样例范围）：学习者已完成元数据模块及大部分接入，用户授权教练补齐替换后核对与错误分类。macOS描述符ACL读取/复制/回读，保留mode/uid/gid，检测可见扩展属性或非零文件标志即拒绝；替换前拒绝与替换后uncertain沿用原提交边界。新增28条与直接受影响既有25条共53条分批通过，Ruff/diff check通过。当前宿主新建文件自动带com.apple.provenance且删除后仍存在；真实扩展属性拒绝已验证，成功路径仅过滤该系统属性模拟支持环境，ACL与文件操作仍为真实调用，不宣称当前宿主无模拟成功写入或完整元数据保留。仍非CAS，未接真实项目/数据库/HTTP/工具，无全量回归、未提交推送。下一课为临时样例中的提案应用执行装配。

- 受限文件替换原语课完成（临时样例范围）：学习者核心与参考一致，教练仅补参考遗漏的BLE001说明注释。实现描述符逐级无跟随打开、双次字节基线核对、同目录独占临时文件、短写入循环、基本权限/属主属组保留、同步与replace后核对；结果区分not_replaced/replaced/uncertain，清理失败独立表达。新增67条分批通过（首批60通过/1测试环境特殊权限位失败；核对发现setuid被清除后补平台分支元信息注入，修正1条及新增6条共7条0.80s通过），Ruff/diff check通过。测试直接证明最后检查到replace之间仍可覆盖外部编辑，不是CAS；ACL/扩展属性等元数据尚未保护，仅临时样例可用，不接真实项目执行入口。无数据库/前端/浏览器/模型/全量回归，未提交推送。下一课文件替换的元数据保护。

- 应用占用期间的资源删除与目录变更保护完成（用户授权直接实现）：持有已授权Workspace锁时检查running/uncertain，Task删除仅检查目标任务，绑定目标变更检查整个项目；同一规范路径重复绑定保持可用。现有产品无Workspace删除/解绑/覆盖重绑入口，未新增。HTTP与三处BFF安全映射409 proposal_application_busy，删除UI归类明确拒绝并保留任务和草稿。后端20新增+6受影响共26条通过（3.46s），BFF新增3条分批通过；类型/定向ESLint/Ruff/diff check通过。真实数据库4组锁等待验证两种顺序，PC单场景双宽度通过。无新迁移、真实模型或全量回归，未提交推送。下一课受限文件替换原语。

- 提案应用状态与执行占用完成（用户授权直接实现）：审批status保持原契约，新增application_status=idle/running/applied/not_applied/uncertain及内部随机令牌；固定归属锁序重新授权，只允许idle领取一次、匹配令牌的running登记终态，所有已领取状态均不可重领，无超时释放或自动重试。新增48条及直接受影响15条共63条分批通过（61条7.09s，提交后响应失败2条1.00s），Ruff/diff check通过。开发库升级60d32ae695cd，旧列业务数据逐行不变，新字段idle/null，Alembic结构与启动就绪通过。无实际文件应用、HTTP/UI/模型/全量回归，未提交推送。下一课应用占用期间的资源删除与目录变更保护。

- 已批准提案的应用前授权与基线核对完成（用户授权教练实现）：新增内部授权投影与只读preflight服务，要求approved、绑定相同、Diff完整及完整新内容摘要有效；受限读取前再次比较预期绑定，按原始UTF-8字节核对基线，读取后新Session重新授权并比对快照。返回不可变公开核对结果，不带正文/宿主路径、不写文件或状态，不是写入授权。新增42条、直接受影响既有10条共52条分批通过；首批48通过，3条测试准备被数据库约束拒绝后改为内部边界注入，补跑这3条及1条新增只读仓库测试通过。Ruff/diff check通过，隔离库清理。无迁移/HTTP/UI/浏览器/模型/全量回归，未提交推送。下一课提案应用状态与执行占用；当前快照不防ABA或后续外部修改。

- 历史运行提案审阅与审批入口完成（用户明确授权教练实现）：TaskRunPanel仅将成功提案结果接入既有ToolResult，使用当前Workspace/Task范围，未知协议安全回退，历史事件不自动查询或提交。新增8条与直接受影响详情8条共16条通过（1.30s），TypeScript/定向ESLint/脚本语法通过。真实PC四场景覆盖刷新后批准、截断拒绝、未知结果刷新防重、切换离开后返回防重；1366/1920无横向溢出，截图视查通过。隔离PostgreSQL恰好4条逐ID匹配approved/rejected/pending/pending，所有源文件字节不变，临时资源清理。上一课历史入口缺口已补齐；无全量回归或真实模型，未提交推送。下一课已批准提案的应用前授权与基线核对，本次代写授权不自动延续。

- 文件修改提案审批交互课完成（当前运行入口）：学习者核心无需修正；新增Actions组件接入详情，pending可决策、截断仅可拒绝，提交前保存sessionStorage防重标记，未知结果及卸载保留标记，已核对回执更新状态。新增11条及直接受影响详情8条，共19条通过；TypeScript、定向ESLint、浏览器脚本语法与启动器Ruff通过。PC六场景分两批通过，原文件字节不变，后续三场景额外完成隔离PostgreSQL精确状态/数量核对。刷新仅验证标记保留，历史运行尚无结构化审阅入口，这是参考方案遗漏，下一课补齐；不宣称刷新后审批界面已恢复。未运行全量回归，未提交推送。

- 文件修改提案审批BFF代理课完成：学习者三个新文件与参考一致，核心无需修改。新增同源POST、严格决策解析与公开回执核对；使用服务端凭证和已校验Origin，拒绝额外正文/查询，状态和错误码双重白名单。转发前取消表示尚未转发，转发后取消/超时/异常为结果未确认，不自动重试；20秒预算覆盖上游请求及响应正文。新增93条专项通过（0.66s），TypeScript/新增文件及测试ESLint/diff check通过。只运行新增测试，上游fetch受控，无后端/数据库/浏览器/既有整套/全量回归，未提交推送。下一课文件修改提案审批交互。

- 文件修改提案审批HTTP接口课完成：学习者Schema、路由及安全错误分支与参考一致，核心无需修正，仅补新增函数间空行。POST决策入口使用可信身份、严格正文与三标识，复用local/Origin/JSON边界；固定映射404/409/422，未知异常及响应失败统一500结果未确认，成功/失败no-store。新增60条、按共享错误分支选取既有11条，共71个不同用例通过（58条首批、14条补充批次，其中1条调整后重跑）；Ruff/diff check通过。真实隔离PostgreSQL与HTTP测试覆盖提交后详情核对和文件不变；无前端/浏览器/事务服务整套/迁移整套/全量回归，无开发库变更，未提交推送。下一课文件修改提案审批BFF代理。

- 后续回归严格限于新增测试及本次修改实际影响的既有功能；按修改点说明选测依据，不按整个领域例行回归。详细长期规则见AGENTS.md“回归测试范围规则”。审批HTTP课按此范围完成验收。

- 文件修改提案审批状态与决策服务课完成：学习者核心与参考一致，无需业务修正，仅补文件末尾换行与函数间空行。pending只能转换一次为approved/rejected，固定Workspace→Task→Conversation→Proposal锁序重新授权；批准要求目录绑定一致且Diff完整，拒绝允许绑定失效或截断。审批不读写文件，也不检查实时文件基线。新增后端53条、前端9条；后端直接相关208条分批通过，前端172条通过，Ruff/TypeScript/定向ESLint/diff check通过。开发库升级5fc219d584bc，既有业务数据逐行一致，Alembic结构与启动就绪检查通过。未运行浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案审批HTTP接口。

## 2026-09-21 当日收尾与新会话交接（历史）

今天停止学习。本次将main工作区累计课程代码、测试及已有文档统一归档：Docker attach异步连接与命令执行/清理/恢复证据链、请求级命令工具、文件查找、纯预览、待审批提案保存与查询、HTTP/BFF及PC按需详情展示。各节“未提交推送”是当时的历史状态，本次统一提交推送；提交编号及远端同步状态以Git记录为准。

正式进度仍4/12，第5周进行中；当前没有批准/应用入口，也没有实际项目文件写入。唯一下一课为“文件修改提案审批状态与决策服务”。新会话使用已检出main的主仓库，先读AGENTS.md和本文件，再按需读下一课相关代码；今天不开新课，用户要求继续时再给完整核心参考。用户逐课代写授权不自动延续，默认学习者亲手实现，明确“完成了”后教练补测试及机械配套。

最近验收：详情按需展示相关163条通过，TypeScript/定向ESLint通过；PC5场景和真实BFF/API/隔离PostgreSQL查询、受控404重试/延迟取消、双宽度及原文件不变通过。跨任务挂起响应未单独注入，不能当作已完成的专项证据；本次收尾不重复全量回归。各课证据见ENVIRONMENT.md，重要考点已更新既有题库。开发库迁移为4eb108c473ab，其他电脑需自行升级。

## 2026-09-21 接续

- 文件修改提案详情按需展示课完成：学习者核心已实现；教练修正参考遗漏的selection.task可空判断为selection?.task，并仅整理该段缩进。新增5条初始展示测试，相关163条通过（1.26s），tsc/定向ESLint通过。更新真实组件夹具与隔离启动器详情路由；PC5场景通过，真实BFF/API详情与DOM Diff一致，键盘读取/聚焦、256px限高/长Diff滚动、双宽度、刷新不自动读详情或重复创建、文件不变及数据库3条pending核对通过。额外受控404→真实重试、挂起旧响应→取消→新请求验证通过；任务切换清理由key/卸载代码核对，本轮未单独做跨任务挂起响应注入。截图视查、临时资源清理，无批准/写入/真实模型/全量回归，未提交推送。下一课文件修改提案审批状态与决策服务。

- 文件修改提案详情BFF代理课完成：学习者三文件核心与参考一致，无需修改。新增同源GET路由、local凭证代理及公开详情解析，三标识匹配、拒绝额外查询参数、只转发公开字段、no-store和20秒取消/超时覆盖正文读取。新增56条，连同相邻运行代理/回执解析共169条通过（1.02s），tsc/定向ESLint/diff check通过。覆盖凭证隔离、本地边界、错误脱敏、错误正文取消、无重试、三个阶段取消/超时、Unicode截断Diff与非法响应。上游fetch受控，无真实HTTP/数据库/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情按需展示。

- 文件修改提案详情HTTP接口课完成：学习者Schema和路由已实现；教练修正参考遗漏的str到Literal[pending]类型衔接，使用model_validate显式公开字段并保留运行时状态校验，不强转或硬编码pending。新增24条HTTP专项，直接相关81条分批通过（首批80通过/1条测试断言误判，修正后24条3.67s通过），Ruff/diff check通过。覆盖真实local身份/授权查询、越权与不存在统一404、非法标识422、本地边界403、未知状态/异常安全500、no-store、公开字段、历史文件删除及截断。身份依赖含幂等INSERT，提案业务查询仅SELECT；隔离库清理，无开发库变更/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情BFF代理。

- 文件修改提案回执结构化展示课完成（用户授权教练实现）：新增公开协议解析与提案卡片，显示创建时待审批/尚未写入、路径、编号、带时区时间及折叠双摘要；截断独立提示，未知协议通用回退。新增34条，连同预览/命令共102条通过（1.26s），tsc/定向ESLint通过。真实PC5场景通过，1366/1920无横向溢出，键盘摘要展开/收起、回执精确字段、刷新不重复、原文件不变及数据库3条pending核对通过；成功/截断截图视查，临时资源清理。无批准/应用按钮，无真实模型/全量回归，未提交推送。下一课文件修改提案详情HTTP接口；本课无新增重要考点独立入库。

- 文件修改提案PC闭环验收完成（用户授权教练完成）：新增proposal-tools.mjs/proposal_model.py，沿用隔离启动器，仅模型决策受控，浏览器/BFF/API/工具/文件/数据库真实。成功、删除至空、纯预览、歧义拒绝、截断5场景全部通过；1366/1920无横向溢出，键盘发送、回执字段/双摘要、消息事件持久化和刷新不重发通过。每场景逐文件核对原始字节不变；数据库最终恰好3条pending，完整新内容和授权查询一致，预览/失败无提案。成功1366和截断1920截图已视查；临时目录/服务/测试库清理。Ruff/脚本语法/diff check通过，无真实模型/Docker/全量回归，未提交推送。下一课文件修改提案回执结构化展示。

- 文件修改提案工具适配与注册课完成：学习者三文件核心与参考一致，无需修改；新增独立create_file_edit_proposal工具，复用严格替换参数，身份来自可信context，返回pending创建回执，数据库异常映射proposal_save_unconfirmed且不重试。新增79条专项，直接相关230条分批通过（工具194条1.41s，2条Docker未选；隔离PostgreSQL闭环与服务36条3.79s）。旧能力集合断言同步，Ruff/diff check通过；真实工具保存→授权查询、越权拒绝、预览不保存及原文件字节不变通过。数据库/schema自动清理，无浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案PC闭环验收。

- 文件修改提案授权查询课完成（用户授权教练实现）：同一SELECT关联提案/Task/Workspace/Conversation并校验两处用户归属，仅投影公开审阅字段；只读服务返回不可变快照，不加载bound_root或proposed_content、不读文件、不加锁或提交。新增15条，连同保存服务18条共33条通过（3.72s），Ruff/diff check通过。覆盖跨用户/项目/任务及缺失资源统一拒绝、会话/项目归属变化、任务删除、Session清理、无自动flush、截断Diff精确保留，以及文件删除/解绑后历史快照仍可查询。隔离PostgreSQL自动清理，无迁移/浏览器/模型/全量回归，未提交推送。下一课文件修改提案工具适配与注册。

- 文件修改提案持久化课完成：学习者模型、迁移、仓库与服务核心通过验收，仅补文件末尾换行。提案保存原文摘要、完整新内容及其摘要、有界Diff、目录绑定与pending状态；先结束查询事务再读取文件，保存事务按Workspace→Task→Conversation加锁重新授权。新增37条专项，连同直接相关共109条分批通过（107条9.06s、锁竞争2条1.20s）。真实迁移往返、约束、提交回滚、归属/绑定变化、删除级联与双向锁竞争通过，原文件不变。开发库升级至4eb108c473ab，原业务数据逐行一致，启动就绪和Alembic结构检查通过。无HTTP/工具接入、批准或文件写入，无浏览器/模型/全量回归，未提交推送。下一课文件修改提案授权查询。

- 文件修改预览结构化展示课完成：学习者四文件核心与参考一致，无需修改。解析公开协议、只读状态/路径/字节数/折叠摘要、有界彩色纯文本Diff及独立截断提示；异常协议回退、原命令卡片兼容。新增38条，连同命令/摘要卡片共82条通过（1.04s），tsc/定向ESLint通过。更新真实组件测试夹具与浏览器断言，4场景PC通过，1366/1920无横向溢出，键盘摘要展开/收起和Diff聚焦、0字节、长Diff滚动、刷新不重放及原文件不变均通过。成功/截断截图视查，临时目录/服务/数据库清理；无真实模型/全量回归，未提交推送。下一课文件修改提案持久化，本课无新增重要考点独立入库。

- 文件修改预览PC闭环验收完成：新增preview-tools.mjs/preview_model.py，模型受控、真实浏览器/BFF/API/文件服务/隔离数据库。成功(BOM/CRLF)、删除预览、歧义拒绝、长Diff截断4场景通过，每场景核对全部夹具原始字节不变；SHA-256由Node独立计算，公开字段精确核对无updated_content/宿主路径。1366/1920宽度正常，刷新不重放、答案及结果/错误事件持久化通过，无页面JS错误。成功/截断截图视查；仍为通用JSON展示，下一课结构化预览卡片。临时目录/服务/测试库schema清理，Ruff/脚本语法/diff check通过，无真实模型/Docker/全量回归，未提交推送。

- 文件修改预览工具课完成：学习者适配器和注册与参考一致；errors.py漏加8项固定文案，教练补齐机械配套，否则EditPreviewError会转成未知安全错误码。新增69条专项，直接相关172条通过（1.36s，2条Docker未选），同步旧能力集合断言。覆盖严格参数/UTF-8字节、公开字段不含完整新文件、截断、全部安全映射/回退、context过滤、取消传播及6组模型往返。Ruff/diff check通过；底层服务和模型受控，无数据库/浏览器/真实模型/全量回归，未提交推送。下一课文件修改预览PC闭环验收。

- 授权文件预览与基线摘要课完成：学习者workspace_file_preview.py与参考一致，无需修改。只读取一次已授权文本，预览与SHA-256均来自同一内容，保留规范相对路径及嵌套完整预览；不写入/保存审批。新增28条，连同直接相关预览65/读取35共128条通过（1.89s），含真实文件/BOM/换行、隔离PostgreSQL授权和Session关闭、外部读取后修改不混用基线。Ruff/diff check通过，测试库/schema自动清理，无Docker/浏览器/模型/全量回归，未提交推送。下一课文件修改预览工具适配与注册。

- 单文件文本替换预览课完成：学习者workspace_edit_preview.py与参考一致，核心无需修改。精确唯一匹配含重叠检测，原始换行保留，256KiB文本/4000行/16384字符Diff限制，完整新内容与截断展示分离；Diff使用JSON行表示，仅供审阅，不能git apply。新增65条纯内存测试通过（0.11s），测试码点表示及断言可读性调整后相关25条通过（0.06s）；Ruff/diff check通过。无文件写入、数据库、Docker、浏览器、模型或全量回归，未提交推送。下一课授权文件预览与基线摘要。

- 文件查找PC闭环验收完成（用户授权教练完成）：新增find-tools.mjs/find_model.py，真实浏览器→BFF→API→授权/文件服务→隔离数据库，仅模型受控。嵌套查找→读取、空结果、51匹配返回50并截断、越界拒绝、未绑定5场景通过；每场景1366/1920宽度无横向溢出，刷新不重执行，答案与结果/错误事件持久化核对。成功/截断/越界截图已视查，正文与外部哨兵不变，无页面JS错误。临时目录、服务及数据库/schema已清理；Ruff/脚本语法/diff check通过，无真实模型/Docker/全量回归，未提交推送。下一课单文件文本替换预览。

- 文件查找工具适配与注册课完成：学习者三文件核心与参考一致，无需修改；find_files严格参数/可信context/固定错误映射/结果不完整语义接入请求级能力。新增54条专项，相关148条分批通过：首批147通过、1条旧能力集合断言需新增find_files，更新后该组3条通过。Ruff/diff check通过。模型与底层服务受控，覆盖查找→读取及空/截断/安全错误/未知错误/非法参数/无context；无真实模型、数据库、Docker或浏览器，未提交推送。下一课文件查找PC闭环验收。

- 受限跨目录文件查找课完成：学习者workspace_find.py与参考一致，核心无需修改。按文件名字面量匹配、描述符无跟随递归，固定2000条扫描/50条结果/8层深度/4096字符路径；truncated表示未完整覆盖或返回，不承诺还有匹配。新增55条专项，连同直接相关路径/枚举共144条分批通过（139条2.69s，追加5条见ENVIRONMENT.md）；隔离PostgreSQL授权先行、Session先关闭、仅SELECT通过，资源自动清理。Ruff/diff check通过，无浏览器/Docker/模型/全量回归，未提交推送。下一课文件查找工具适配与注册。

- 命令结果结构化展示课完成（用户授权教练实现）：新增command-result-view.ts与tool-result.tsx，校验公开结果、区分调用完成与进程成功、展示退出码/OOM/daemon未知事实/耗时，双路独立截断提示及有界文本滚动，异常协议通用回退。新增30条专项，相关共48条通过；tsc/定向ESLint/diff check通过。真实PC浏览器5场景通过，1366/1920宽度无横向溢出，键盘聚焦、截断区域限高/可滚动、刷新不重执行验证通过；截图视查。模型受控，4个容器确认全部缺失，临时服务及隔离库/schema清理。无全量回归，未提交推送；下一课受限跨目录文件查找。

- 命令工具PC闭环课完成（用户授权教练验收）：新增command-tools.mjs/command_model.py，沿用隔离启动器，仅模型决策受控，浏览器→BFF→API→Runtime→Docker与持久化真实。成功/退出码7/10万字节截断/参数拒绝/用户取消5场景通过，1366及1920宽屏无横向溢出，刷新不重执行且结果事件可查询。4个真实容器最终全部缺失；取消记录cancelled/start_attempted=True/stop_confirmed=True，1个取消目标由夹具按身份清理。截图已视查，JSON大输出详情过长留下一课结构化展示。修正终态后工具卡片仍显示运行中为结果未确认，无真实模型/全量回归；临时服务、隔离库/schema清理，未提交推送。

- 请求级命令工具注册课完成：学习者五文件核心与参考一致，无需修改。tools_for_execution构造本请求能力快照，绑定ChatExecution.execute_command；模型与Runtime使用同一tuple，空集合禁用能力，默认调用兼容，全局注册表不变。local流式入口且有授权上下文时开放固定无挂载沙箱，普通/chat保持原流程。新增14条常规及2条真实Docker；常规定向分批94条通过（67条1.31s、7条0.05s、20条2.63s），真实Docker2条1.41s，退出码0/7及删除缺失确认。旧线程断连测试改从请求级工厂注入工具后通过，隔离PostgreSQL库/schema清理；Ruff/diff check通过，无浏览器/真实模型/全量回归，未提交推送。

- 恢复记录作用域装配课完成：应用lifespan创建CommandRecoveryStore，最多32次运行，每次journal最多16次命令；ChatExecution首次命令时登记并注入，关闭阻止新命令，完成/空记录释放，异常/取消/pending保留到应用存储。学习者核心与参考一致，仅补参考遗漏的Ruff异常捕获说明。新增29条分批通过（27条1.30s、2条1.03s），聊天生命周期/预算18条通过（2.48s，PostgreSQL隔离库/schema自动清理），合计47条。补齐绕过lifespan的旧测试装配；Ruff/diff check通过。无真实Docker/模型/浏览器/全量回归，未提交推送。

- 超时与取消分类课完成（用户授权教练实现）：新增agent/tool_wait.py，Runtime统一调用wait_for_tool。用等待完成事实区分自身预算超时和执行器异常，不依赖取消子类转换；超时请求一次取消并等待收尾，外部/重复取消优先传播且不重复打断子任务。新增14条，相关68条通过（1.50s，2条数据库专项未选中），覆盖自定义取消/吞取消返回/收尾异常、内部TimeoutError、重复取消、真实适配器恢复记录与流式/非流式Observation。Ruff/diff check通过，无Docker/数据库/模型/浏览器/全量回归，未提交推送；代写授权仅本课。

- 命令恢复证据接收课完成：学习者核心与参考一致，无需修改。新增CommandRecoveryJournal，固定16次尝试、执行前预留、不可变请求快照、关闭后允许在途收尾；适配器在错误/取消离开前保存记录。新增34条并更新旧适配测试，直接相关86条通过（0.93s），Ruff/diff check通过。实测本机Python 3.12.13的wait_for只转换精确CancelledError，自定义SandboxCommandCancelled在超时后仍传播；两种路径记录均保留，下一课先处理分类再注册。无Docker/数据库/浏览器/模型/全量回归，未提交推送。

- Runtime分派课完成：学习者改动与参考一致，无需修改。按is_async选择execute_async或原线程入口，统一等待与原Observation协议；未注册命令工具。新增28条，直接相关82条通过（1.32s，2条数据库占用专项未选中）；覆盖非流式/流式、跟踪器有无、异步成功/安全错误/未知错误/返回类型、前置拒绝、上下文透传、超时及外部取消等待finally、执行器主动取消。Ruff/diff check通过，无Docker/数据库/浏览器/模型/全量回归，未提交推送。

- 工具定义异步支持课完成：学习者ToolDefinition核心与参考一致，无需修改。同步executor与async_executor互斥，共用参数/上下文校验，execute_async直接await并保留异常/取消，旧同步注册及位置参数兼容。新增39条，直接相关分批共104条通过：首批102条（1.56s），两条PostgreSQL占用测试因沙箱连接受限中断后单独放行重跑通过（1.08s），隔离测试库/schema自动清理。Ruff/diff check通过；本课未改Runtime调度或注册命令工具，无Docker/模型/浏览器/全量回归，未提交推送。

- 受限命令适配课完成（用户授权教练实现）：新增tools/run_command.py，复用CommandRequest唯一Schema，纯校验先于执行，正常只返回公开命令字段；errors.py增加固定错误码/文案。按创建/执行/超时/适配/清理失败分类，CommandToolExecutionError内部保留recovery及已有命令结果，取消原样传播，未知错误脱敏。新增24条，连同统一入口共57条通过（0.12s，2条真实专项跳过），Ruff/diff check通过。本课仅异步适配器，未向同步注册器注册或向模型开放，无真实Docker/数据库/浏览器/模型/全量回归，未提交推送。

- 失败执行只读核对课完成（用户授权教练实现）：新增sandbox_command_reconciliation.py，按原请求/恢复身份只读核对；已知ID通过成功缺失查询才返回absent，否则重新校验身份及一致状态；未知ID仅复用created-only核对，再按发现的完整ID重新查询。不重试、不写、不将状态快照当删除授权或命令结果，取消保留恢复证据。新增36条常规；首批31条连同相关核对/停止共134条通过（0.17s），追加5条后新文件36条通过（0.08s）。1条真实Docker通过（0.55s），created/running/exited/absent核对及临时容器清理确认；定向Ruff/diff check通过，详情见ENVIRONMENT.md。未提交推送，代写授权仅本课。

- 统一编排课完成：sandbox_command.py核心与参考一致，无需修改。入口复制请求、生成独立token，协调创建/执行/适配/清理；非零退出仍清理，命令结果与清理凭据分离。各阶段错误/取消保留阶段及已知身份、启动/停止/删除证据，清理失败保留已确认命令结果，不重试或无条件删除。新增33条常规，直接相关共192条通过（0.45s，13条真实专项默认跳过）；本课2条真实Docker通过（0.68s），退出码0/7端到端及删除后缺失确认，临时容器无残留。定向Ruff/diff check通过，无数据库/浏览器/模型/全量回归，未提交推送。

- 结果适配课完成：学习者核心与参考一致，无需修改。CommandResult新增可空OOM/daemon错误事实与保守succeeded判定，sandbox_command_result.py仅转换已确认执行结果，保留输出/截断/耗时，不暴露内部身份或把异常转为完整结果。新增67条，连同直接相关契约/缓冲共228条通过（0.19s，-W error）；定向Ruff及diff check通过。仅纯内存验证，无真实Docker/数据库/浏览器或全量回归，未提交推送。

- 已退出容器清理课完成（用户本课授权教练实现）：sandbox_cleanup.py新增cleanup_exited_sandbox及携带身份/delete_attempted的取消异常，原created-only入口不变。冻结请求，按完整ID重新核对身份和一致exited/Pid=0后非强制删除，回执通过且查询缺失才成功；错误/取消不重试、不停止、不把缺失或查询失败当成功。新增50条常规，连同旧清理41/客户端33共124条通过（1.09s）；4条真实Docker通过（1.45s），退出码7清理、删除成功后模拟响应丢失/查询失败/取消，错token拒绝及重复请求不再次rm，临时容器确认无残留。Ruff/diff check通过，无数据库/浏览器/全量回归，未提交推送；代写授权仅本课。

- 执行协调课完成（用户本课授权教练实现）：新增sandbox_execution.py。冻结请求，attach前及start前复核完整ID/身份/非TTY/隔离策略，先握手并启动读取任务，再尝试start；TaskGroup并发排空与退出轮询，确认EOF和退出后返回输出/退出事实/编排耗时。30秒总等待，失败/超时/取消后先回收读取及连接，再按原身份停止；重复取消等待收尾，错误保留token/ID/start_attempted/stop_confirmed，不返回部分成功、不重试或删除。新增44条常规，直接相关共377条通过（2.00s）；5条真实Docker通过（6.63s），覆盖快速退出两路输出、非零退出、大输出双路截断、忽略SIGTERM的超时/取消停止。临时容器均按身份/停止证据非强制删除并查询缺失。Ruff/diff check通过，无数据库/浏览器/全量回归，未提交推送；代写授权仅本课。

- attach连接课完成（用户本课授权教练实现）：新增docker_attach_connection.py。固定本机socket、完整ID请求、10秒connect/发送/握手总预算；成功后仅借出reader，退出独立关闭任务有界等待2秒，重复取消等待收尾，关闭失败abort并明确报告，保留业务异常/取消。27条新增常规通过，连同HTTP/读取共172条通过（分两批：170条0.86s、追加2条0.10s）；2条真实Unix socket/Docker握手通过（0.21s）。本机Docker 29.7.2支持API 1.40～1.55，固定1.45兼容；真实created容器握手成功，未启动，已按身份清理并查询缺失。Ruff/diff check通过。无数据库/浏览器/全量回归，未提交推送；代写授权仅本课。

- attach请求构造课完成：build_docker_attach_request核心与参考一致，仅补两处顶层函数间空行。新增34条专项，同文件HTTP响应70条共104条通过（0.64s，-W error）；两文件Ruff及diff check通过。覆盖精确请求字节、完整小写ID、非法类型/短ID/大小写/空白/路径/换行注入拒绝、固定策略与关键字契约、环境变量不覆盖目标/API版本及无文件/socket调用。固定API 1.45，本机daemon兼容尚未核对；无真实连接或全量回归，未提交推送。

- 用户已确认将 Obsidian 接入自研 Agent 的学习计划，尚未实现：Sandbox 执行链路完成后，第 6 周做授权 Vault 的只读检索与引用，第 8 周在 Diff/审批/冲突检测具备后做笔记写入；关联与语义检索后续按需增强。详细范围与验收统一见 LEARNING_CURRICULUM.md“Obsidian Vault 接入实践”，路线已同步 LEARNING_PLAN.md。当前唯一下一课及正式进度不变。

- attach HTTP响应课完成：docker/docker_attach_http.py核心与参考一致，无需修改。新增70条专项，连同直接相关读取41条共111条通过（0.71s，-W error）；两文件Ruff及diff check通过。覆盖头/首帧同块、字段大小写与token、状态/非法/重复字段拒绝、8192字节边界、提前EOF、读取契约、异常原样传播、总超时/滴流/立即读取、外部取消及并发隔离。仅内存流，无真实Docker/网络/数据库/浏览器或全量回归，未提交推送。

- attach异步读取课完成：docker/docker_attach_stream.py核心与参考一致，无需修改。新增41条内存流/受控读取专项，连同91条解析器测试共132条通过（0.13s，-W error）；两文件Ruff及diff check通过。覆盖短读取、跨帧UTF-8、通道交错、1MiB帧、超限后后续通道、半帧EOF、读取契约错误、异常原样传播、协议失败停止读取、延迟EOF、等待及持续立即返回时取消、流所有权与并发实例隔离。无Docker/网络/数据库/浏览器或全量回归，未提交推送。

## Runtime 目录整理（2026-09-20）

按用户要求，将29个运行时服务模块分入 `agent/`、`execution/`、`command/`、`sandbox/`、`docker/`，31个对应测试同步分组，导入、浏览器夹具与环境命令已更新；导航见 `docs/project-structure.md`。业务逻辑保持原样，下一课仍为 attach 异步读取与收尾，相关实现放 `services/runtime/docker/`。本次是工程整理，不推进课程或延续核心代写授权；本轮整理随代码与配套文档统一归档至 main，提交及远端同步以 Git 记录为准。验证结果见 `ENVIRONMENT.md` 末尾。

## 2026-09-20 当日收尾

今天停止学习，本次将累计第5周改动统一归档到主仓库main：Workspace只读文件/目录/单文件搜索与可信工具上下文、local聊天接入及PC闭环；命令契约/输出缓冲/环境；Sandbox策略、创建/核对/清理/停止/启动/退出结果；Docker attach帧解析及对应测试、验收记录、题库。

各课定向验收结果见下方及ENVIRONMENT.md，本次收尾不重复全量回归。下方“未提交推送”均为各课完成时的历史状态，本次统一提交归档；提交与远端同步以Git记录为准。正式进度仍4/12，第5周进行中。下一会话从“唯一下一课：attach异步读取与收尾”开始，今天不继续上课。用户的逐课代写授权不自动延续，恢复核心由学习者实现、完成后教练补测试；只回归新增及直接影响部分。

## 2026-09-20 接续

- attach帧解析课完成：docker_attach_parser.py核心与参考一致，无需修改；新增91条纯字节专项通过（0.09s），定向Ruff/diff check通过。覆盖全单切分/双切分、固定种子随机分块、通道交错/空帧/跨帧UTF-8、独立捕获限额与后续帧、1MiB帧边界、非法头/通道/保留字节/长度、半帧EOF、失败保持、误用不消费、完成幂等与实例隔离。无Docker/网络/数据库/浏览器/全量回归，未提交推送。

- 退出结果课完成（本课用户授权教练实现）：新增sandbox_exit.py，严格身份及一致exited/Pid=0后解析ExitCode/OOMKilled/Error，返回不可变结果；仅退出码0且无OOM/daemon错误时succeeded，不从137推断OOM或从143推断取消。原始daemon错误只保留布尔标记，不伪造输出/耗时。新增75条纯解析通过（0.09s），3条真实Docker验证0/7/主动137退出，均非OOM，临时资源已清理。Ruff/diff check通过，仅本课专项，无全量回归，未提交推送；代写授权仅本课。

- 容器启动课完成（本课用户授权教练实现）：新增sandbox_start.py及完整ID start适配。冻结原请求副本，先按同一响应核对身份/执行/隔离/created状态，再启动并重新查询running或快速exited。尝试start后的异常/取消衔接停止核对，shield收尾抵抗重复取消；错误/取消携带token/ID和stop_confirmed，不删除、不重试start。新增30条常规，直接相关客户端33条共63条通过（0.91s）；4条真实Docker通过（1.23s），覆盖running/快速exited/真实启动后人工响应丢失/取消后停止，临时资源无残留。定向Ruff/diff check通过；无数据库/浏览器/全量回归，未提交推送，本课代写授权不延续。

- 容器停止课完成（用户本课授权教练直接实现）：抽出read_sandbox_identity复用严格归属解析，保留原created检查；新增sandbox_stop.py及按完整ID inspect/stop适配。只接受一致created/running/exited状态，异常中间态拒绝；先授权再stop(SIGTERM/2秒)，重新查询非运行/Pid=0才确认，不自动删除或重试。新增53条常规，连同身份95/客户端33共181条通过（0.97s）；1条真实Docker专项通过（2.98s），父子忽略SIGTERM且子进程独立会话，daemon报告exited/Pid=0并拒绝exec，临时容器清理无残留。定向Ruff/diff check通过，无数据库/浏览器/全量回归，未提交推送；代写授权仅本课。

- 隔离与资源配置复核课完成：sandbox_isolation_policy.py核心与参考一致，无需修改。新增218条纯校验通过（0.16s），1条真实Docker专项通过（0.22s），真实created容器符合当前HostConfig/挂载策略；仅在响应副本改Privileged验证拒绝。临时容器已由清理服务删除并查询无残留，定向Ruff/diff check通过。仅新增测试，无启动/数据库/浏览器/全量回归，未提交推送；下一课先补停止与停止确认再推进执行生命周期。

- 执行配置复核课完成：sandbox_execution_policy.py核心与参考一致，仅给两处隐式字符串拼接加括号以满足Ruff（参考格式遗漏）。新增71条纯校验通过（0.08s），1条真实Docker专项通过（0.22s），复核未启动容器实际用户/入口/Cmd/Env/交互标志，篡改仅发生在响应副本；临时容器已按既有清理服务删除并确认无残留。定向Ruff/diff check通过；本课仅新增测试，无全量回归。网络/挂载/权限/资源配置尚未复核，不接启动，未提交推送。此前candidate_id类型收窄修复保留，相关145条测试已在修复回合通过，未重复运行。

- 显式清理课完成（本课用户授权教练直接实现）：新增sandbox_cleanup.py及Docker非强制rm/删除后缺失查询适配。按原上下文/完整ID重新核对created身份，核对删除回执并成功查询缺失才返回完成快照；失败保留token/ID且不重试，取消传播。新增41条常规专项，连同客户端33条共74条通过（0.90s）；2条真实Docker专项通过（0.48s），正常删除和删除生效后模拟响应丢失均验证，错ID不删、重复请求不再次rm，临时容器无残留。定向Ruff/diff check通过，无启动/数据库/浏览器/全量回归，未提交推送；本课代写授权不延续至下一课。

- 只读核对课完成：sandbox_reconciliation.py及身份恢复函数核心与参考一致，仅补sandbox_identity.py函数间空行和末尾换行。新增50条常规专项，连同直接相关身份解析95条共145条通过（0.13s）；1条真实Docker专项通过（0.27s）。验证未知ID发现、已知ID严格约束、错ID拒绝后目标仍为created、删除后查询仍保留未确认；临时容器按确认的完整ID清理，查询无残留。定向Ruff/diff check通过；无启动、数据库、浏览器或全量回归，未提交推送。

- Sandbox创建装配课完成：sandbox_creation.py及create适配核心与参考一致；仅补docker_client.py函数间空行和末尾换行。新增33条常规专项、2条显式Docker集成；常规连同直接相关客户端33条共66条通过（0.89s），真实Docker2条通过（0.27s）。验证正常创建与创建成功后模拟响应丢失仍保留容器且不重试，均确认created身份后按完整ID清理并查询无残留。定向Ruff/diff check通过；无容器启动、数据库、浏览器或全量回归，未提交推送。

- 有界Docker客户端课完成：docker_client.py核心与参考一致，仅补两处有原因的BLE001豁免（原参考静态检查遗漏），不改变行为。新增33条专项，连同直接相关双路捕获12条共45条通过（0.87s，-W error）；定向Ruff/diff check通过。覆盖固定程序/目标/环境、参数拒绝、失败脱敏、输出限额、启动及读取期间超时/取消、重复取消、kill竞态与reap、捕获失败；其中5条使用真实Python子进程/OS管道。只读Docker版本29.7.2及随机不存在容器inspect验证通过，未创建容器。无数据库/浏览器或全量回归，未提交推送。

- 创建响应/身份确认前置课完成：sandbox_identity.py核心与参考一致，无需修改。新增test_sandbox_identity.py 95条通过（0.07s，-W error），两文件Ruff及diff check通过。覆盖完整ID及行结束符、异常/超长/深嵌套JSON、重复字段/非标准数值拒绝、身份串用、必需字段/类型、镜像/标签/created状态及严格PID、额外字段兼容、不可变快照和安全错误。仅本课纯解析专项，无Docker/数据库/浏览器/全量回归。实际创建调用尚未实现，未提交推送。

- Sandbox参数构造课完成：sandbox_spec.py核心与参考一致，仅给两处tmpfs隐式字符串拼接加括号以满足Ruff（参考格式遗漏）。新增test_sandbox_spec.py 44条通过（0.05s，-W error），两文件Ruff及diff check通过。覆盖固定Docker策略/环境分层、参数原样保留、程序位置防env赋值、token/镜像拒绝、未挂载目录拒绝、请求重校验与不可变快照；仅本课纯参数专项，无Docker/数据库/浏览器或全量回归。未提交推送。

- Sandbox最小验证完成：infra/sandbox/compose.yaml与参考一致，无需修改。发现本机尚无指定镜像/练习容器，教练拉取python:3.12-slim-bookworm并按实际摘要固定；新增scripts/verify_sandbox.py，在随机Compose项目完成3组真实验收：权限/文件/网络/资源/主进程环境、忽略SIGTERM且独立会话的子孙进程随容器停止、重启tmpfs清空及宿主哨兵不变。临时容器清理完成，Ruff/diff check通过。网络额外隧道接口均DOWN，修正测试“仅lo”的过强假设后外连确实拒绝。仅本课专项，无数据库/浏览器/全量回归；镜像保留本地，未提交推送。

- 命令环境白名单课完成：command_environment.py核心与参考一致，无需修改。新增test_command_environment.py 62条通过（0.05s，-W error），两文件Ruff及diff check通过。覆盖精确11字段白名单、宿主敏感/注入变量不继承且不读取os.environ、两目录严格类型及POSIX路径边界、输入保留、额外覆盖拒绝、独立返回字典与无文件系统调用。仅该纯函数专项，无外部命令/数据库/浏览器或全量回归。环境白名单不是Sandbox；未提交推送。

- 双路输出课完成：command_capture.py核心与参考一致，无需修改。新增test_command_capture.py 12条通过（0.05s，-W error），两文件Ruff及diff check通过；测试显式导入builtins.ExceptionGroup以适配当前Ruff目标版本。覆盖真实内存流/独立输出限额、成对契约、同流拒绝、双方启动屏障、单路先EOF、双向故障取消并等待异步收尾、整体取消等待两路、同时失败异常组，终态无遗留读取任务。仅本课专项，无外部命令/真实管道/数据库/浏览器或全量回归。未提交推送。

- 单路异步输出课完成：command_stream.py核心与参考一致，无需修改。新增test_command_stream.py 31条通过（0.06s，-W error），两文件Ruff及diff check通过；仅本课专项，内存StreamReader/受控读取器验证，无外部命令、真实管道、数据库、浏览器或全量回归。覆盖EOF/短块/大输出、额度耗尽继续读取、无效策略在读取前拒绝、读取契约检查、异常原样传播、等待及连续立即返回时取消、流不被主动关闭、并发实例隔离。未提交推送。

- 输出缓冲课完成：command_output.py核心与参考一致，仅按Ruff排序__slots__（参考代码的格式遗漏）。新增test_command_output.py 41条通过（0.05s，-W error），两文件Ruff及diff check通过；仅本课纯内存专项，无命令进程/管道/数据库/浏览器或全量回归。覆盖硬上限、精确边界、持续超限保存量不增长、所有切分位置UTF-8、非法及末尾不完整字节、双截断、完成幂等及拒绝写入、独立两路与CommandResult组装。未提交推送。

- 命令契约课完成：command_contracts.py核心与参考一致，仅清理末尾空白。新增test_command_contracts.py 120条通过（0.10s，-W error），两文件Ruff及diff check通过；仅该单文件专项，无数据库/浏览器/命令进程或全量回归。验证argv/路径语法边界、策略字段拒绝、非零及信号退出码、超时/取消、启动失败一致性、独立输出上限/截断及JSON往返。格式合法不代表执行授权，资源常量尚未接执行器，Sandbox和子进程停止证据待实现。未提交推送。

- 只读工具链PC闭环4组通过：键盘发送→真实BFF/API/隔离PostgreSQL/临时文件→列目录、搜索定位、读取并回答；刷新答案与3条工具结果事件恢复且无新聊天POST；越界读取和未绑定目录安全失败。1366/1920成功页及两种失败页截图已视查，Ruff/Node语法/diff check通过。仅新增readonly-tools.mjs、readonly_model.py和隔离模型分支，未改业务代码；模型决策模拟，真实模型效果未验收。只跑本课浏览器专项，无全量回归；临时资源清理完成，未提交推送。

- 搜索工具适配与注册完成：严格参数、可信上下文、安全错误及定位/截断JSON保留已验收。检查发现新增搜索条目覆盖了原get_current_time注册，教练恢复原有条目；搜索核心无需其他修改。新增45条工具测试，与直接相关适配/注册回归共122条通过（0.91s，-W error），定向Ruff通过。受控模型验证列目录→搜索→行号回答及失败路径，底层服务模拟；未跑数据库、文件系统、浏览器、真实模型或全量回归。未提交推送。

- 学习者完成workspace_search.py，核心与参考一致，无需修改。新增test_workspace_search.py 43条通过（1.07s，-W error），本课两文件Ruff及diff check通过；仅本课单文件专项，无读取/聊天/Runtime全量回归。覆盖查询规则、字面量/大小写/每行首次匹配、Unicode列号、换行、50/51行与片段预算、错误原样传播，以及3条隔离PostgreSQL+真实文件成功/跨用户/非法UTF-8集成。无浏览器/模型调用，未提交推送。
- 搜索最多返回50个匹配行，每行片段最多200字符；字符列号从1开始，片段截断与匹配行截断独立，不提供总匹配数或JSON/Token精确预算。服务复用256KiB读取边界，目前尚未注册搜索工具。

- 学习者完成list_directory工具适配、目录错误白名单及requires_context注册，核心与参考一致；教练仅补errors.py类间空行、同步旧工具可见列表断言。新增test_list_directory_tool.py 31条，与直接相关读取适配/注册回归共77条通过（0.71s，-W error），定向Ruff及diff check通过。未运行数据库/文件系统/浏览器或聊天全量，模型与底层服务模拟。
- 受控模型实际根据目录Observation中的名称发起第二次读取，验证列目录→读文件→回答；默认“.”、额外能力参数拒绝、truncated保留、11类错误映射、未知异常脱敏及无上下文强行调用拒绝已覆盖。当前local流式聊天自动可见两种项目工具，普通/chat与无上下文演示入口不扩展能力。未提交推送。

- 学习者完成workspace_listing.py，核心与参考一致，无需修改。教练新增test_workspace_listing.py 26条通过（1.11s，-W error）；仅本课单文件专项和两文件Ruff，diff check通过。真实目录/链接/FIFO、200/201及250条限量迭代、子项消失/目录变化、无跟随打开、错误脱敏、资源清理及隔离PostgreSQL授权已覆盖。未跑读取/Runtime/聊天/全量回归，无浏览器或模型调用；未提交推送。
- 目录服务只返回单层有限子集，不暴露链接目标；truncated不是稳定分页或全局排序前缀，不提供原子快照/完整Sandbox。下一课接目录工具注册与安全错误，当前模型仍只有文件读取而无目录枚举工具。

- 学习者完成stream_chat_reply本地上下文装配，核心与参考一致，无需修改。新增test_chat_tool_context.py 7条通过（0.42s）；受影响流式回归9条（0.08s）、取消/断线生命周期4条（1.05s）通过，共20条分组验证。定向Ruff及diff check通过。只回归直接影响部分，无全量。
- 教练同步旧流式Runtime替身的tool_context签名，并把旧生命周期夹具新增上下文服务的SessionLocal接入隔离库；两处配套缺失曾分别造成RUN_ERROR和等待保存阶段超时，修复后通过。新增真实链路从隔离PostgreSQL任务上下文到临时文件读取、工具事件、消息和Run终态持久化，模型模拟；未运行浏览器或真实模型。普通/chat仍直接文本请求，账号流式不装配文件上下文；未提交推送。

- 学习者完成read_text_file工具适配、SafeToolExecutionError固定文案、注册及按上下文过滤模型工具列表；核心与参考一致，教练仅补新文件末尾换行并更新旧注册列表测试断言。新增test_read_file_tool.py 37条，与直接相关工具/API/模型适配/上下文派发回归合计83条通过（0.93s，-W error）；定向Ruff、diff check通过，无数据库/文件系统/浏览器/真实模型回归。
- 模拟模型完整往返验证成功JSON、安全错误与未知错误Observation、无上下文强行调用拒绝；模型请求不携带上下文身份，默认TOOLS仅时间/面积。当前聊天入口仍未传上下文，因此用户聊天尚不能读取项目文件；下一课装配。未提交推送。

- 学习者完成ToolDefinition.requires_context、保留参数防覆盖、execute二次检查及Runtime前置拒绝/显式透传；核心与参考一致，教练仅补类间空行。新增test_tool_context_dispatch.py 20条，与23条直接相关工具/线程/超时/取消/事件回归合计43条通过（1.17s，-W error）；本课三文件Ruff及diff check通过。未连接数据库、运行浏览器或调用模型，无领域全量回归。
- 缺失上下文沿用tool_execution_failed，固定details=tool_context_required且duration_ms=None；参数校验仍先执行，直接execute也拒绝缺失上下文。流式/非流式、真实跟踪线程和并发隔离已覆盖。上下文尚未从聊天入口装配，文件工具尚未注册；未提交推送。

- 学习者完成 tools/context.py 与 services/runtime/agent/tool_execution_context.py，核心与参考一致；教练仅补末尾换行。新增test_tool_execution_context.py共24条通过（1.20s，-W error），本课三文件Ruff及diff check通过。真实隔离PostgreSQL验证两模式同等归属条件、不同会话/项目定位、无Task拒绝、未绑定目录允许、仅SELECT无commit、SQL错误与Session关闭；对象不可变及不接受独立资源参数也已覆盖。
- 本轮仅运行上述单个测试文件，无Runtime/账号/路径/读取或全量回归；独立测试资源自动清理，未操作开发业务表/迁移，未调用模型。上下文工厂已完成，Runtime透传和文件工具注册仍未接入。未提交、未推送。

- 受限文本读取课：学习者完成 workspace_file.py，核心与参考一致，教练仅补末尾换行；新增35条读取测试，与路径边界63条合计98条通过（1.69s，-W error），本课两文件Ruff及diff check通过。真实描述符覆盖普通/空/中文/上限文件、非法文本、特殊文件、打开阶段替换、读取期间修改、有界读取与异常关闭；两条隔离PostgreSQL验证真实授权及Session先关闭。未运行领域/后端全量、浏览器或模型，未操作开发业务表/迁移；未提交推送。
- 后续回归遵循用户最新要求：只运行新增及直接受影响测试，不默认按整个领域或后端全量扩展；确有跨模块影响或异常证据才说明原因后扩大。

- 第5周路径边界课：学习者完成 workspace_path.py，核心与参考一致；教练只补末尾换行。新增63条专项通过（1.54s），Workspace/Task/local相关回归659条通过（46.18s，-W error），全量后端Ruff及diff check通过。覆盖跨平台输入规则、真实链接逃逸、隔离PostgreSQL授权先行、只读查询、会话先关闭及异常清理。
- 首次专项53条通过后因沙箱阻止本机数据库连接而中断；放行测试连接后重跑通过，独立测试库/schema已自动清理。未操作开发业务表/迁移，未调用模型或运行浏览器，Windows未实机验证。当前仅路径快照，不是读取权限、TOCTOU防护或Sandbox。下一课见“唯一下一课”；本课改动未提交/推送。

- 最新收尾：按用户授权完成有历史任务删除，以及本机异常退出恢复的服务/HTTP/BFF/UI。后端最终全量1411条通过（105.58s），Workspace718/聊天86、类型检查、ESLint/Ruff通过；真实子进程kill、事务失败回滚、旧数据迁移与浏览器恢复闭环均已覆盖。详情见 ENVIRONMENT.md 最后章节。
- 第4周按原生本机、进程内工具线程范围验收完成，正式4/12（33.3%）。旧版无执行者身份、外机或权限不足的记录保守拒绝恢复；不按TTL清理，不自动续跑工具。第5周Shell须扩展子进程树停止证明，第7周再做checkpoint续跑，第9周做跨进程预算/交付。
- 开发库已显式迁移至3da097b362fa，alembic check无差异；未删除开发业务数据。本次按用户要求将累计源码、测试、迁移与文档统一提交并推送 origin/main；推送后在同一主仓库 main 创建新任务，继续第5周路径边界课。以下为本日各课当时的阶段记录，不代表当前提交状态。

- 学习者完成 ConversationExecutionStatusResponse 及 GET /sessions/{session_id}/execution，核心与参考一致；教练仅补定义之间空行。复用 CurrentUser 和 ChatRoute，在线程中执行同步查询，响应只含公开三字段，成功与错误均 no-store。
- 新增 tests/chat/test_conversation_execution_api.py 共 13 条真实应用/隔离 PostgreSQL 专项，随查询服务、本地访问/任务边界及聊天占用生命周期共 90 条通过（7.70s，-W error）；Ruff/diff check 通过。覆盖空闲/占用/释放、四类授权拒绝、本地访问拦截、身份伪造、线程执行、数据库故障脱敏、Run 状态独立和业务数据不变。
- 首次测试因 PostgreSQL 未监听而停在夹具初始化；确认 Docker 并启动 Compose PostgreSQL/Redis 后健康，重跑通过。未修改开发业务表或迁移；本课未运行前端/浏览器，未调用模型。
- 学习者完成占用查询解析器、代理及 Next.js GET 路由，核心与参考一致；教练仅补末尾换行。新增 conversation-execution-route.test.ts 60 条通过，Workspace 全量 672 条通过（1.79s）；类型检查、ESLint/diff check 通过。上游 fetch 模拟，验证服务端凭证隔离、公开字段重建、错误白名单、无重试及请求/正文阶段取消与超时。
- 学习者完成 ConversationExecutionPanel 和右侧详情接入，核心无需修改，教练仅补末尾换行。新增 8 条真实 refresh 函数测试，Workspace 680 条、聊天状态85条、类型/ESLint/diff check 通过。
- 新增 conversation-execution.mjs 浏览器 5 组验收通过：真实 BFF/API/PostgreSQL 占用与取消释放，键盘查询、错误与契约失败重试、切换任务和关闭详情的迟到响应隔离。1366/1920 PC 截图已视查；隔离服务及数据库已清理。模型模拟，不宣称验证强制停机恢复。
- 学习者完成 ExecutionBudget，核心无需修改。新增23条专项，随 ExecutionThreads 共35条通过（0.07s，-W error），Ruff/diff check通过。验证20个竞争者仅3个获准、满额拒绝不误归还、异常/取消归还、内层收尾期间保留容量、重复退出不误释放、实例独立及跨循环拒绝。未运行数据库/浏览器，尚未接聊天入口。
- 学习者完成配置、请求依赖与安全错误映射；检查发现 main.py 漏了启动装配，教练补共享预算 lifespan，并更新测试夹具与 .env.example。新增配置/生命周期17条、真实ASGI容量6条；加强既有数据库提交/工具断线测试的预算保持断言。后端全量1371条通过（111.88s，-W error）。
- 认证/BFF207条、聊天状态86条、类型/ESLint/Ruff通过。不同会话共享预算，满额503且无执行副作用，预算包住会话占用和请求收尾；本地主线先授权，账号扩展保留原会话准备行为。
- 容量1的真实PC浏览器2组通过：不同会话收到503，取消后第二会话恢复执行与刷新历史；1366/1920截图已视查，临时服务及隔离库清理完成。
- 学习者完成 historical-run-summary.ts 与历史详情卡片接入，核心与参考一致，教练仅补末尾换行。新增23条摘要专项，Workspace703/聊天86、类型/ESLint/diff check通过；未知指标不补零，金额不转number，仅匹配唯一终态恢复摘要。
- 历史摘要PC浏览器4组通过：真实持久化摘要刷新/重选恢复；受控失败、缺失及冲突数据正确显示。1366/1920截图已视查，隔离服务/数据库已清理。未调用真实模型。
- 第4周范围核对已完成：复核当前删除/占用事务与外键、课程目标和既有验收证据，修正文档中摘要恢复及并发预算的过期待办。本轮仅改文档，未重跑功能测试；删除服务保护随后按用户授权直接实现；占用冲突现已按用户授权接通 API/BFF/UI。累计改动未提交、未推送。

## 2026-09-18 接续

- 检查运行链路后，将占用接入先拆出后台线程跟踪这一前置课：asyncio.to_thread 的等待取消或超时不代表线程结束，不能立即释放占用。
- 学习者完成 services/runtime/execution/execution_threads.py：每次执行独立跟踪任务，shield 隔离调用方取消，关闭后拒绝新工作，等待全部线程工作结束后再传播收尾期间的取消。核心与参考一致，教练仅补末尾换行。
- 教练新增 12 条真实线程专项；随既有 Agent Loop 回归共 28 条通过（0.82s，-W error），后端 app/tests Ruff 与 diff check 通过。未调用模型、数据库或浏览器，未修改运行入口。
- 学习者已完成 conversation_execution_scope.py，采用 AsyncGenerator[ExecutionThreads, None] 标注。核心无需修正，教练仅补换行及说明预期异常捕获的 Ruff 豁免。新增 17 条隔离 PostgreSQL 专项，Runtime 共 165 条通过（12.45s，-W error），Ruff/diff check 通过。验证获取期间取消、线程收尾、重复取消、AnyIO 取消域、提交确认丢失和精确释放。
- 学习者已完成 Agent Loop 的 execution_threads 可选参数、工具调用转交及非流式兼容函数透传，核心无需修正。新增 12 条工具接入测试，Runtime 共 177 条通过（13.70s，-W error）；Ruff/diff check 通过。两条真实 PostgreSQL 集成验证超时/取消后占用保留至工具线程结束。
- 本课用户明确授权教练直接实现：两个聊天入口共用 request 级依赖，覆盖响应发送、Run 创建、生成器与取消监听器关闭、后台数据库/工具线程排空、缓存失效及精确释放；API/BFF/UI 接通安全 409，保留账号扩展兼容。
- 新增 ASGI/PostgreSQL 专项 12 条通过，后端全量 1300 条通过（99.38s，-W error）；BFF 37、聊天状态 85、TypeScript/ESLint/Ruff 通过。旧状态测试夹具补 setCreationError，避免误把夹具缺依赖当成业务失败。PC 浏览器并发忙提示、取消后恢复及刷新历史两组通过，1366/1920 截图已视查，隔离服务及数据库已清理。
- 学习者完成 conversation_execution_query.py，核心与参考一致，教练仅补末尾换行。新增 12 条隔离 PostgreSQL 专项，Runtime 共 189 条通过（13.96s，-W error），Ruff/diff check 通过。覆盖占用获取/释放快照、先授权、旧占用与 Run 独立、仅 SELECT 且不读 token、不提交及查询异常传播。
- 下一课为会话执行占用状态查询 HTTP 接口。今日改动未提交或推送；上一轮 main 提交为 8c0fb84。此前聊天入口直接实现授权不扩展至后续课次。

## 2026-09-17 接续

- 学习者明确“完成了”后，已检查创建幂等事务服务。核心与参考一致；教练仅按 Ruff 合并等价嵌套 if、补文件末尾换行，并补测试和收尾文档。
- 已实现：先授权并锁项目、规范化标题和服务端 SHA-256、按用户/项目/键查询请求记录、同键重放/内容冲突/删除后拒绝重建、重新核对任务和会话归属、三条记录同事务提交。重放返回当前公开资料。
- 新增 37 条专项，随 Task/Workspace/local 相关回归共 545 条通过（41.45s，-W error）；后端全量 Ruff 与 diff check 通过。没有重复执行账号专项、前端或浏览器验收。
- 已启动 Docker Desktop，项目 PostgreSQL/Redis 健康；测试仅使用自动清理的随机独立库/schema，未迁移或修改开发业务表。详细命令与初次环境阻塞见 ENVIRONMENT.md。
- 本课 HTTP 接入核心与参考一致，无需修正。请求键暂时可选，严格正文校验；两种冲突返回安全 409，非法键返回 422，未知失败保持创建结果未确认。新增 37 条 HTTP 专项通过（3.83s），Task/Workspace/local 相关回归 582 条通过（42.15s，-W error）；Ruff/diff check 通过，隔离资源已清理。
- BFF 课按用户“这个你直接完成吧”授权由教练实现：严格校验并原样转发可选请求键，补 409/422 白名单，保留缺省/null 兼容和未确认语义，不生成键或自动重试。创建路由 92 条（新增 33）、Workspace 全量 612 条、TypeScript/ESLint/diff check 通过；上游 fetch 模拟，未运行浏览器/数据库。
- UI 课用户再次明确授权直接实现并要求解释。教练已接入工作台内存创建意图、首发请求键、冻结输入重试、冲突/删除拒绝及明确新意图；重试成功读历史，不自动发送消息。新增 4 组 PC 浏览器场景通过；提示修正后重跑关键恢复场景 1 组及既有首发/切换/列表找回/历史隔离 4 组通过，浅深色 PC 截图已视查。Workspace 612/聊天状态84、TypeScript/ESLint/diff check通过，临时服务及隔离数据库已清理。
- 本课学习者完成 ConversationExecutionSlot 模型与迁移，核心无需修正，只补迁移末尾换行；新增13条专项随迁移目录56条通过（7.42s），后端全量1205条通过（82.51s，-W error）；Ruff/diff check通过。开发库从0a7b64e039cd升级至1b8c75f140de，alembic check无结构差异。
- 本课学习者完成 conversation_execution_service.py，核心与参考一致，无需修正；教练新增 42 条专项，随 runtime/tasks/local 回归 508 条通过（36.91s，-W error），Ruff/diff check 通过。验证授权先行、短事务、真实锁等待、提交确认丢失及旧 token 不误释放；尚未接入聊天运行。
- 下一课是聊天执行生命周期中的占用接入；此前 UI 代写授权仅限对应课次，后续仍由学习者实现核心。继续在主仓库 main 学习，一课一个任务；本次按用户要求将当日累计源码、测试、迁移和对应文档统一提交并推送至 origin/main；正式进度仍为 3 / 12。

本次收尾范围：Task 创建幂等事务服务及 HTTP/BFF/UI 全链路、会话执行占用模型/迁移及获取/释放服务。验收证据已归档 ENVIRONMENT.md，课程范围与状态已同步 LEARNING_CURRICULUM.md、LEARNING_PLAN.md，重要考点已更新题库及索引。今天不继续接入运行生命周期；下次从“唯一下一课”开始。

## 当前目标与约束

- 正式进度：第 1～4 周完成，4 / 12（33.3%）；第 5 周进行中，已完成只读工具链与 Sandbox 生命周期基础、attach 帧解析。
- 主产品：本地优先 PC Coding Agent。Web UI/API/执行服务在用户电脑，模型由用户配置；无需产品注册登录。账号模式作为既有扩展保留，不主动推进访客系统或账号专项课程。
- 技术路线仍为 Next.js + FastAPI + PostgreSQL + Redis。域名用于展示、文档和分发；第 9 周保留独立受限样例的云部署学习。
- 用户要求在已检出 main 的主仓库学习。每次确认分支与工作区，不覆盖累计未提交改动。
- 一课一个可运行小任务，完整参考直接展示在对话中；已有文件只给完整改动段/函数，新文件才给整文件；四空格缩进，关键逻辑前说明原因和边界。
- 用户明确“完成了”后才检查实现、补测试和验收。测试和机械性配套由教练完成；口头答题不是课程门槛。
- 本轮用户直接授权了对话式侧栏/标题/历史、18px 样式和目录整理；后续课程恢复学习者编写核心实现，不能将这次授权当成永久代写授权。

## 当前界面与行为

- Codex 风格项目/任务侧栏，项目行有笔形新建入口和项目设置；目录选择及绑定放在设置中，不常驻项目 ID、时间和手填标题表单。
- 笔形入口只打开草稿，首次发送才创建 Task/Conversation；同任务后续消息使用同一 conversation_id。列表支持分页，选回任务读取真实历史。
- 第一轮完成后异步总结标题，失败保留首条消息摘录；后端重新授权并按旧标题条件更新，迟到结果不覆盖后来标题。
- 创建响应未确认时停止自动发送，可以从列表找回已创建的空任务；创建服务、HTTP、BFF 和 UI 已接通请求键；未确认时同键重试找回任务，读历史后等待明确发送。
- 空白态引导与输入框居中相邻，有消息后输入框固定底部；详情默认收起，可恢复三栏。左右布局开关独立于任务身份，收起导航不丢草稿。
- 选中任务和首次创建后同步 URL；刷新及新标签页打开链接自动恢复任务与历史，继续使用原会话。草稿清除任务参数；不持久化未发送输入，不建立任务切换的浏览器前进/后退历史，历史运行列表、时间线与有效终态摘要可重新读取；不自动续跑中断执行。
- 全页正文、控件、提示基线 18px，标题更大；左侧栏 320px。右侧详情默认 400px，支持 320～720px 拖动与方向键/Home/End 调宽、双击重置、localStorage 保存偏好，并根据窗口为中栏保留空间；当前运行与摘要改为分区/行式指标，数值不拆行。继续复用共享控件和主题。
- 任务删除：Provider 状态、ref 防重、精确详情确认、迟到结果隔离及 URL 清理已接通；本轮扩展到无占用且运行均结束的历史任务，并更新确认文案。教练按用户两张 Codex 截图调整任务行悬停删除图标、应用内确认框和项目菜单；保留 18px 字号。项目菜单仅接入新建任务、项目设置和刷新任务，不伪造归档/置顶/移除能力。
- Enter 发送，Shift+Enter 换行；输入法 composition/229 不误发。历史读取失败阻止发送，旧请求不会污染新任务。

## 已验收的后端与运行基线

- 第 1～3 周：FastAPI/模型调用、会话持久化、BFF NDJSON 透传、流式事件解析、六态聊天、工具注册/参数校验/Agent Loop、超时取消、最大步骤、run/event 时间线、指标与预算。具体边界见 docs/agent-ui-events.md 和 week-learning/week-03/REVIEW.md。
- 账号扩展：注册、登录、身份、退出与资源所有权。local 模式依赖稳定本机身份、Loopback Host、来源检查及服务端内部凭证；身份不替代资源授权。
- Workspace：创建、列表、原生目录选择和绑定/读取。选择目录不等于获得任意文件写入或命令执行权限。
- Task：模型、创建事务、HTTP/BFF、列表/分页/历史/标题。Task 与 Conversation 同事务创建；提交后响应失败可能返回错误但记录已存在，不能自动重发。
- Task 详情 HTTP：TaskDetailResponse、task_detail 和 GET /workspaces/{workspace_id}/tasks/{task_id}。返回项目资料及任务/会话公开标识；定位不依赖列表第一页，检查项目和会话归属，只读且不返回 ORM 对象。
- Task 详情 BFF：学习者完成同源 Task 详情 BFF、readTaskDetail 与 taskProxy 的 detail 分支。校验 URL 项目/任务与响应匹配、会话标识格式，重建公开字段；复用本地凭证、取消超时、错误脱敏和 no-store，兼容列表/messages/title。

- URL 恢复：学习者完成 task-url.ts、工作台 URL 恢复及恢复状态 UI。选中/创建后 replaceState 更新定位参数，刷新或复制链接通过详情恢复项目/任务，再由 TaskChat 读取历史；详情失败不回退成草稿，旧详情不得覆盖新选择，创建后保持组件 key。教练修正参考中 effect 同步 setState 的 lint 问题，补测试与隔离夹具。

- 空任务删除事务：学习者完成 task_deletion_service.py，仅删除没有消息、没有任何状态 Run 的空任务；按项目→任务→会话授权加锁，先删会话再删任务，同事务提交/回滚，保留项目与目录。核心无需修改，仅补末尾换行。现已接通 HTTP/BFF/UI；不代表已完成通用删除或运行生命周期收口。

- 本地会话保护：学习者收紧 conversation_repository.py：local 模式只允许已有 Task 会话，联查 Conversation/Task/Workspace 归属；创建 Run 前锁会话且不执行会话 INSERT，普通聊天在缓存/模型调用前重新检查。账号分支保留兼容。核心无需修正，仅补末尾换行。

- 最新完成：学习者实现空任务 DELETE HTTP，教练按本轮明确授权实现同源 DELETE BFF。成功 204 空正文；404 不可访问、409 已有历史；状态与错误码白名单脱敏，转发后超时/取消/异常提示删除结果未确认，不自动重试。该授权仅限本轮 BFF，后续核心课程仍由学习者实现。

- 任务运行历史查询：学习者完成 task_run_query.py 与两种响应结构；复用 owned_task 授权后按 Run ID 倒序读取 limit + 1 条，返回概要和下一页游标，不加载事件，不写入或提交。未知状态保留原值，结束时间为空则最终耗时为空。查询服务、HTTP 与 BFF 已验收，列表 UI 已接通（手动刷新，不订阅实时状态）。

## 当前目录入口

后端按“层级 → 领域”组织，目录导航见 docs/project-structure.md。不要再向 routers/services/repositories 根层平铺新模块。

- HTTP：apps/api/app/routers/workspace/router.py
- Task 服务：apps/api/app/services/tasks/task_service.py、task_workspace.py
- Workspace 服务：apps/api/app/services/workspace/
- 模型与运行：apps/api/app/services/model/、services/runtime/
- 仓储：apps/api/app/repositories/{auth,chat,workspace,runtime}/
- 响应结构：apps/api/app/schemas.py；ORM：apps/api/app/models.py
- Task 测试：apps/api/tests/tasks/，公共夹具仍为 apps/api/tests/conftest.py
- 前端详情代理基础：apps/web/src/app/api/_shared/task-proxy.ts
- 工作台状态：apps/web/src/features/workbench/workbench-session.tsx
- 浏览器夹具：apps/web/test/browser/run-isolated.py、chat_test_app.py

目录整理移动了 97 个源码/测试文件，同步 Python 导入、测试包路径、浏览器脚本、迁移测试相对路径和文档引用；没有旧路径转发壳。应用启动入口仍为 app.main:app，不需要数据库迁移。

## 最近验证

- 2026-09-16 Markdown 展示修复：接入 react-markdown 10.1.0 + remark-gfm 4.0.1，新增 MarkdownMessage，历史助手消息与流式回复统一解析，用户消息保留纯文本。覆盖标题/强调/列表/任务列表/引用/代码/表格，代码表格内部滚动；禁用原始 HTML，沿用 URL 校验，图片显示显式链接。浏览器 3 组通过（逐块模拟流、格式/安全/滚动、历史恢复/浅深色），截图已视查；TypeScript、ESLint、聊天84/Workspace579及 diff check 通过。模型输出模拟，隔离服务和测试库已清理，下一课不变。

- 2026-09-16 右侧栏：按用户截图修正摘要依赖屏幕断点导致的三列挤压，改为标签/数值行式布局，移除当前运行嵌套卡片。右栏默认 400px，拖动/方向键/Home/End/双击调宽，持久化偏好并限制中栏空间。浏览器 9 组通过（原 8 组 + 真实聊天摘要/调宽），用模拟指标复现 757 Token、¥0.00128420、1499 ms，320px 下无断行；拖动保留草稿与节点、刷新恢复宽度。1366px 窄/宽截图已视查；Workspace 579、聊天 84、TypeScript、ESLint 通过，临时资源已清理。

- 2026-09-16 启动迁移职责：按用户明确授权移除 API 启动 create_all，新增 check_database_ready，只读比较迁移 heads，不一致拒绝启动；迁移 env 支持显式连接，浏览器隔离环境先执行真实 upgrade。新增 7 条专项通过，后端全量 1101 条通过（79.95s，-W error），Ruff/diff check 通过。开发库仅只读检查版本并通过，未迁移或写入。

- 2026-09-16 历史详情 UI：按用户明确授权完成 TaskRunPanel、列表选择入口及独立历史详情状态。返回保留列表，重试/返回/任务切换清理旧请求，文本由 React 转义。浏览器原 5 组列表 + 新增 3 组详情共 8 组通过，1366/1920×900 截图已视查；Workspace 579 条、聊天状态 84 条、TypeScript、全量 ESLint 和 diff check 通过。真实空任务链路，其余分页/详情/故障模拟；临时服务和隔离数据库已清理，无真实模型调用。

- 2026-09-16 运行详情 BFF：用户明确授权直接完成 GET /api/runs/[runId]、run-detail-proxy.ts 与 run-detail-data.ts。新增 61 条、Workspace 全量 579 条及聊天状态 84 条通过，TypeScript、ESLint、夹具 Ruff 与 diff check 通过。复用流事件解析器，兼容持久化开始/取消事件，未知事件保留信封但 payload 置空；核对 Run ID、事件 ID 严格递增和时间/耗时，重建公开字段。测试直接调用路由、模拟上游，无浏览器/数据库/模型调用；隔离启动器已补复制新路由。

- 2026-09-16 运行历史 UI：学习者完成右侧概要列表、分页、刷新、失败重试及任务切换取消/旧回调隔离。教练恢复被替换掉的 RunSummaryCard 并补文件换行；隔离启动器补复制 runs 路由。新增浏览器 5 组全部通过（真实空任务 BFF/API，其余分页和故障响应模拟），1366/1920 PC 截图已视查；Workspace 518 条、聊天状态 84 条、TypeScript、ESLint、夹具 Ruff 和 diff check 通过。临时服务及独立数据库/schema 自动清理。

- 2026-09-16 运行列表 BFF：按用户本轮明确授权完成 task-run-data.ts、task-run-proxy.ts 和 runs/route.ts；新增 79 条路由专项、Workspace 全量 518 条通过，TypeScript、全量 ESLint 与 diff check 通过。覆盖内部凭证隔离、参数重复/越界、项目/任务匹配、排序/游标、公开字段重建、错误脱敏及请求/正文/JSON 完成阶段取消超时。直接调用真实 GET 路由，上游 fetch 模拟；未运行浏览器、数据库或模型。授权仅限本课 BFF，后续核心仍由学习者实现。

- 2026-09-16 运行列表 HTTP：核心实现与参考一致，无需修改。新增 test_task_run_api.py 42 条通过（4.53s）；Task/runtime/local/workspace 共 602 条通过（44.08s），-W error；全量后端 Ruff 与 diff check 通过。覆盖真实分页、跨会话隔离、空任务授权、非法参数、访问边界、SQL/响应异常脱敏与 no-store；隔离库/schema 自动清理。未接 BFF/UI，未运行浏览器或模型。

- 2026-09-16 运行列表查询：新增 test_task_run_query.py，44 条隔离 PostgreSQL 专项通过（2.67s），覆盖分页、归属、状态时间、无事件读取/写入/commit、真实 SQL 错误与 Session 清理。核心无需修正，仅补文件末尾换行。Task/runtime/local/workspace 共 560 条通过（38.31s），-W error；后端全量 Ruff 与 diff check 通过。独立测试库/schema 已清理，命令见 ENVIRONMENT.md。

- 2026-09-16 删除 UI：新增 task-delete.mjs 共 10 个场景分别验收通过；最终布局调整后重复验证删除/菜单 5 组及既有首发/项目切换/历史失败与旧请求隔离 3 组，全部通过。列表 DOM 保持/延迟骨架专项 1 组通过；Workspace 439 条、聊天状态 84 条、TypeScript、全量 ESLint 与 diff check 通过。原删除核心仅修正两处缩进，后续按明确授权调整样式和加载体验。浅/深色 PC 截图已视查，临时服务与独立数据库已清理，命令见 ENVIRONMENT.md。

- 2026-09-16 删除 HTTP/BFF：HTTP 新增 42 条通过，后端 tasks/workspace/local 共 422 条通过（30.91s），-W error 与领域 Ruff 通过；BFF 新增 43 条、Workspace 全量 439 条通过，TypeScript 与 ESLint 通过。真实浏览器删除链路 1 组通过（空任务 204、重复 404、历史任务 409 并保留消息），隔离资源已清理；命令见 ENVIRONMENT.md。

- 2026-09-16 本地会话课：新增 40 条隔离 PostgreSQL 专项通过（4.65s），受影响 local/tasks/chat API/stream/run repository 共 189 条通过（14.21s），-W error；Ruff、修改脚本 ESLint/语法及 diff check 通过。真实工作台 first send 1 组与本地模式 3 组浏览器通过，模型出口模拟，临时服务和测试库自动清理。旧 local-mode.mjs 已补先创建项目、进入草稿、展开运行详情的配套。
- 2026-09-16 空任务删除课：新增隔离 PostgreSQL 专项 27 条（2.14s），Task 领域共 116 条通过（9.14s），-W error；领域 Ruff 与 diff check 通过。独立连接验证提交事实，真实 SQL 错误验证回滚；pg_blocking_pids 验证 Message/Run 插入与删除的锁竞争，覆盖删除提交/回滚及插入先提交。测试库/schema 自动清理，未改开发业务表、迁移或前端。
- 2026-09-16 URL 课：新增 URL 单元测试 13 条，Workspace 全量 396 条、聊天状态 84 条通过；TypeScript、全量 ESLint、夹具 Ruff、diff check 通过。真实 BFF/API/隔离 PostgreSQL 的 PC 浏览器 8 组全部通过，覆盖自动刷新/新标签页恢复、同会话续聊、首发不中断、恢复失败与重试、分页外定位、项目列表失败独立恢复、旧详情/历史竞态。模型出口模拟；1366/1920 截图已视查。临时服务和测试库已清理。Docker Desktop 与项目 PostgreSQL/Redis 已启动并健康，未执行开发库迁移或重置。
- 2026-09-16 详情 BFF 新增 58 条，连同既有读取代理共 73 条通过；Workspace 全量 383 条通过，TypeScript、全量 ESLint 和 diff check 通过。核心无需修正，仅补新路由末尾换行。直接调用实际 GET 路由，上游 fetch 模拟；取消/超时覆盖请求、正文读取与 JSON 完成阶段。本轮未运行浏览器、数据库或模型调用。
- 详情课新增 18 条，与相关 Task 测试共 62 passed（5.41s）；核心无需修正，仅文件末尾换行。
- 目录整理后后端全量 899 passed（58.07s），-W error、Ruff 通过；前端 TypeScript 与 Workspace 325 条测试通过。目录迁移后浏览器主流程定向复跑 1 组通过，隔离资源已清理；命令见 ENVIRONMENT.md。
- 隔离 PostgreSQL 与 psycopg，使用随机独立测试库/私有 schema，允许真实提交，自动清理；禁止 SQLite 或测试开发业务表。
- 浏览器此前 4 组任务交互通过；18px 样式在 1366×768、1920×1080、2560×1318 验证。聊天/标题模型出口模拟，不宣称测试了真实模型生成质量。
- 开发库迁移 head=1b8c75f140de；本课验证 alembic check 无差异。Python 3.12.13 与环境细节见 ENVIRONMENT.md；跨电脑需各自安装依赖与迁移。

本课验收（2026-09-16）：修正模型表名缺少 s、primary_key 参数拼写；新增请求迁移专项 17 条，迁移目录 43 条通过。后端全量 1118 条通过（75.03s，-W error），Ruff 与 diff check 通过。开发库已从 f16a53d928bc 升级至 0a7b64e039cd，alembic check 无结构差异；回退只在隔离测试库演练。请求键在真实空任务删除后保留。交接、课程、计划与重要题库已同步。

## 第 4 周范围核对（2026-09-20）

本次核对当前代码与既有验证记录，不将历史测试通过描述成本轮重新执行。证据命令及原始范围保留在 ENVIRONMENT.md。

| 范围 | 当前结论 | 验收边界 |
|---|---|---|
| 本地 Workspace/目录、Task、PC 工作台 | 已验收 | 目录绑定不授予文件写入或 Shell 权限；源码启动仍需 PostgreSQL/Redis |
| URL/消息/运行列表/时间线/终态摘要恢复 | 已验收，只读恢复 | 刷新后重读持久化事实；未发送草稿、创建重试键与执行检查点不随刷新恢复 |
| Task 创建幂等 | HTTP/BFF/UI 已接通 | 页面内重试复用请求键；响应未确认不自动发送消息，删除后原键拒绝重建 |
| 单会话执行互斥及状态查询 | 两个聊天入口与只读查询均已验收 | 占用保留到必要线程/响应收尾完成；快照和 Run 终态不能证明执行者已停止 |
| 执行并发预算 | 已验收 | 单进程共享实例、两个聊天入口；不覆盖多进程总额与独立工具演示接口 |
| 历史成功/错误摘要 | 已验收 | 唯一且匹配的终态才展示；未知指标不补零；不计辅助标题调用费用 |
| Task 删除 | 空任务及已结束历史任务已验收 | 占用、未结束/未知Run或缺结束时间时拒绝；历史与Task同事务删除，保留目录和创建回执；未知结果不自动重试 |
| 异常停止与执行恢复 | 本机进程退出恢复已验收 | 占用与Run各自记录主机/PID，确认原进程已退出才恢复；无身份/外机/权限失败拒绝；不做checkpoint续跑或外部子进程恢复 |

最近相关证据：后端全量1371条（预算接入课）、前端认证/BFF207条、Workspace703条、聊天状态86条；PC 浏览器会话互斥2组、占用查询5组、预算2组、历史摘要4组。各组模型出口模拟；并非本轮一次性重跑全套。这是范围核对时的基线；最终收尾新增历史删除与进程退出恢复，证据见本页首及ENVIRONMENT.md。

短期状态当前为持久化消息/运行事实加可失效内存缓存，不是执行检查点。第7周原计划继续承接 checkpoint/暂停恢复，第9周承接 Worker/跨进程容量与交付；这些不能反向算作第4周已实现。第4周删除生命周期及异常停止的可诊断边界已补齐；未知来源记录仍不可强制清理。

## 唯一下一课

**第5周：隔离样例仓库的Sandbox只读挂载规格与核对。**

目标：只对服务端自建的隔离样例仓库定义只读bind挂载规格，创建前核对宿主源目录身份，创建后核对实际Mounts、WorkingDir和原有网络/权限/资源隔离。用真实Docker验证容器可读样例、写入被拒，错源、链接替换或额外挂载均不能作为合格Sandbox，结束清理自有容器与样例。此课不接用户Task、普通Workspace或模型run_command，也不开放文件写入/Apply Patch；后续再把经过授权的项目范围接入命令链。只回归新增及直接受影响范围。核心默认学习者实现，明确说“完成了”后教练补测试；本课“直接完成”的授权不延续到下一课。

命令链路已接通请求级工具注册、异步Runtime、恢复记录作用域和PC浏览器闭环。run_command仅执行固定无项目挂载/无网络沙箱；公开结果使用结构化卡片，工具调用完成与进程成功分别表达。只有exited、退出码0、OOM及daemon错误均明确False才显示命令成功；未知不补成False，输出截断单独展示。

恢复记录仍是单进程内存：最多32个运行scope，每个journal最多16条尝试，异常/取消保留，重启丢失；没有公开恢复/清理接口。取消停止证据不等于完整命令结果，正常清理需删除后确认缺失。当前只适配本机macOS Docker Desktop固定socket/API 1.45，无项目目录挂载、文件写入或checkpoint续跑。

受限跨目录文件查找服务已完成：find_task_files先授权再关闭Session，逐级无跟随打开后递归，按文件名字面量大小写敏感匹配。固定扫描2000项/返回50项/进入8层/路径4096字符，子项链接与特殊文件跳过，失败不返回部分成功；排序仅覆盖已取得子集，无全局顺序/分页/硬超时或原子快照保证。55条新增及相关测试分批共144条通过。

文件查找工具已注册：参数仅query和可选relative_path，requires_context=True，模型描述及Runtime共用现有请求能力集合；底层服务重新授权。结果保留paths/scanned_entries/truncated，未知服务分类安全回退，未知执行异常由Runtime脱敏。54条新增专项和直接相关148条分批通过；该工具课为模型与服务受控验证；后续真实文件/授权/浏览器闭环已完成，见本页首及ENVIRONMENT.md。

单文件纯内存替换预览已完成：preview_text_replacement要求旧文本非空且唯一命中，重叠候选也拒绝；不归一化大小写/空格/换行。原文、片段、新内容均最多256KiB UTF-8，Diff输入最多4000行，输出最多16384字符；updated_content完整保留，diff_truncated只描述展示。JSON转义行让CR/LF/CRLF与末尾无换行可见，该Diff不能直接git apply。65条专项通过，无外部I/O。

授权文件预览与基线摘要已完成：preview_task_file_replacement复用read_task_text_file读取一次，在Session/句柄关闭后生成预览，并从同一原文UTF-8字节计算SHA-256。不重新读文件、不归一化BOM/换行、不写入；摘要不是授权/审批或对象身份凭据。新增28条连同相关共128条通过，覆盖读取后外部修改仍不混用基线与预览。

文件修改预览工具已注册：preview_file_edit参数只有relative_path/old_text/new_text，严格类型与UTF-8字节校验；身份由可信context提供，服务重新授权。公开结果status=preview_only，仅含路径、原文摘要、字节数、Diff及截断标记，不回传updated_content。已补齐8项安全错误文案，69条新增与相关172条通过。真实PC闭环已完成，见本页首；不保存审批或可执行修改计划。

文件修改提案持久化已完成，记录通过Task继承Workspace/用户归属；创建服务只生成pending，完整内容与截断Diff分别保存。重复调用产生独立提案，尚无请求幂等；提交结果不确定时不能自动重试。开发库迁移4eb108c473ab已应用。

文件修改提案授权查询已完成：按workspace_id/task_id/proposal_id单条联表查询重新校验Workspace和Conversation归属，返回路径、摘要、Diff、截断、状态与时间等公开字段。只读历史审阅，不校验当前文件基线或绑定是否仍适用；不存在/无权访问统一拒绝。

文件修改提案工具已注册：create_file_edit_proposal与preview_file_edit独立，创建回执只含提案编号、pending、路径、双摘要、截断和时间；不重复查询或返回完整正文/Diff。数据库异常保守映射保存未确认，未知异常与取消沿用Runtime处理；描述中的禁止自动重试不等于后端幂等，等待超时不保证同步事务停止。

文件修改提案PC闭环已验收：5场景、双PC宽度、刷新不重发、真实提案数量/正文/摘要及授权查询、原文件不变均通过。模型回答受控，不能据此证明真实模型始终准确表达待审批状态。回执已结构化展示，详情HTTP、BFF及按需审阅界面已完成，尚无审批/应用入口。

提案回执卡片已完成：严格解析pending/编号/摘要/含时区日期/截断标记，明确这是创建时回执，不保证当前状态或基线有效；卡片无Diff正文及批准/应用按钮，未知协议回退。

提案详情HTTP已完成：GET /workspaces/{workspace_id}/tasks/{task_id}/file-edit-proposals/{proposal_id}沿用CurrentUser与WorkspaceRoute，显式公开响应、no-store及安全错误；model_validate在HTTP边界验证实际状态，不伪造pending。只读历史提案，不检查当前文件基线。

提案详情BFF已完成：同源/api/workspaces/{workspaceId}/tasks/{taskId}/file-edit-proposals/{proposalId}调用真实后端路径，只用服务端内部凭证；校验三标识、共有回执协议及Diff，显式投影公开字段。成功/失败no-store，未知上游状态/格式/网络/取消统一安全502，不自动重试；本课测试fetch受控。

提案详情按需展示已完成：从当前工作台Workspace/非空Task注入范围，点击后经BFF查询；显示保存的Diff/摘要/截断状态，支持错误重试、收起/取消。controller引用过滤旧响应，三标识key使切换卸载；仅查询，不批准/写入。真实PC闭环及受控失败/取消注入见ENVIRONMENT.md。

审批状态与决策服务已完成：数据库允许pending/approved/rejected，并拒绝approved+截断Diff；decide_task_file_edit_proposal使用新Session事务按项目→任务→会话→提案加锁，重新授权后只允许pending转换一次。重复或相反决策均冲突；批准要求目录绑定一致且Diff未截断，拒绝允许失效提案。服务不读写文件，不证明实时基线有效；详情HTTP/BFF/UI支持三个真实状态，创建回执仍只接受pending。开发库迁移5fc219d584bc；没有审批时间/历史审计字段。

审批HTTP已完成：POST /workspaces/{workspace_id}/tasks/{task_id}/file-edit-proposals/{proposal_id}/decision，只接受decision=approved/rejected，无额外正文或查询字段；身份来自CurrentUser。404隐藏资源存在性，409区分状态/绑定/截断，422参数校验，未知服务错误、提交异常及响应校验失败均安全500 proposal_decision_uncertain。决策后GET详情核对持久化事实；500不意味着未提交，查询到pending也不证明仍在运行的旧请求已经停止。无自动重试或文件应用。

审批BFF已完成：同源POST /api/workspaces/{workspaceId}/tasks/{taskId}/file-edit-proposals/{proposalId}/decision通过严格正文解析、服务端本地凭证和可信Origin转发。成功回执核对三标识及本次决定，安全错误按HTTP状态+错误码白名单重建；无自动重试。转发前取消499 proposal_request_cancelled只说明本次请求未转发；转发后499/504/502及后端500均保留proposal_decision_uncertain。上游预算20秒，不含浏览器请求正文接收；取消不证明后端事务已停止。

当前运行与历史运行均可按需读取提案详情及审批。历史事件只复用成功提案的公开回执，当前Workspace/Task范围传入既有详情组件，服务端仍重新授权；创建回执与查询时状态分开。sessionStorage只保护当前标签页，不是跨标签页锁或服务端权威。未知结果重新查询pending仍不解锁，卸载取消不证明服务端事务回滚。

应用前只读核对已完成：file_edit_proposal_preflight.check_task_file_edit_proposal先读取内部授权快照并关闭Session，检查批准/绑定/完整内容；read_task_text_file携带expected_bound_root，在路径解析前重新授权并比较绑定，严格字节摘要核对后再次查询归属及快照。结果只说明某次读取时匹配，不保留句柄、不保证文件对象身份，不检测ABA，不消除返回后权限/文件变化。真正应用不能直接信任缓存的核对结果。

应用占用已完成：审批决定不变，内部应用状态idle只能领取一次，事务提交后返回隐藏于repr的随机令牌；running仅可由匹配令牌的可信执行器登记applied/not_applied/uncertain。登记结果不是服务自己验证文件副作用，not_applied要求执行器有未写入证据，applied要求真实成功证据。无HTTP/模型入口、超时释放或终态重试；领取提交结果未知时禁止启动文件写入。迁移60d32ae695cd拒绝丢失已领取记录的降级。

资源变更保护已接入现有服务：Task删除和目录绑定目标变化在Workspace锁内检查应用占用，与领取/完成串行，running/uncertain明确409拒绝；Task只检查本任务，目录变更检查整个项目。同一路径无修改绑定保持可用；选择器等待期间的并发绑定/领取在保存时重查。当前没有Workspace删除、解绑或覆盖重绑入口，未来新增必须复用同一锁内保护，不能绕过服务直接级联删除。数据库直接管理操作和外部文件编辑不在此应用层保护范围。

受限文件替换原语已在临时样例验收：workspace_file_replace.py使用当前用户的单硬链接普通文件，拒绝路径链接与特殊目标；按字节摘要核对，独占临时文件写入与同步后执行os.replace。替换前拒绝为not_replaced，开始调用replace后的异常保守uncertain，成功完成检查为replaced，清理失败单独返回。67条新增用例覆盖真实文件、故障与资源释放；测试明确复现最后核对后外部编辑被覆盖，不能把原子替换称为条件比较交换。该原语不是数据库授权或写入令牌，不直接开放给模型/HTTP。

下一课受限样例提案应用HTTP接口：复用本机访问边界、严格action=apply正文、服务端长生命周期样例绑定实例和公开回执投影，只让已登记当前用户任务的样例进入门禁；普通项目/空登记拒绝，客户端不能传路径或登记凭据。明确同步执行、无自动重试、响应失败不等于未写入及no-store，不把原始执行器直接暴露为HTTP。默认先给完整参考，完成后补测试；收尾直接说明下一课名称及目标。

文件查找浏览器验收使用受控模型解释真实结果，不能据此证明真实模型总能准确描述截断。通用工具详情仍为有界JSON文本，文件查找专用结果卡片不在本课范围。

正式进度仍4/12，第5周进行中。下一课恢复学习者亲手实现核心：先给完整参考，明确“完成了”后补测试与验收；本课代写授权不自动延续。只回归新增及直接影响部分。

## 尚未解决的问题

- URL 自动选中与消息历史恢复已完成；空任务删除服务、HTTP/BFF/UI 已完成。本地会话已禁止隐式重建；取消终态仍不等于协程停止；有历史任务删除与本机进程退出恢复已完成；持久化checkpoint续跑仍留到第7周。
- 按用户明确要求修复加载闪烁：三栏 WorkbenchShell 移到 keyed TaskChat 外层，右侧详情通过 Portal 跟随当前任务；ProjectGroup 改为稳定项目 key，保留展开状态及已有列表，revision 重置分页并后台刷新；确认删除后排除旧列表目标。会话仍重新读取真实历史，250ms 后才显示静态骨架，快请求不显示加载文字；未引入会话缓存，读取失败仍禁止发送。
- 删除结果未确认时保留查询入口并阻止再次删除；详情 200 也不证明之前的删除已经终止，刷新页面仍会丢失该客户端提示。
- 创建幂等端到端已接通；执行占用已接入普通/流式聊天生命周期，占用只读诊断与进程内预算已完成；有本机退出证据的遗留占用/Run可手动恢复；无身份旧记录仍拒绝。跨进程总限额和持久化执行检查点尚未实现。刷新/关闭会丢失内存中的创建重试键及未发送内容，不能将跨刷新恢复视为已完成。
- API 启动已改为只读迁移版本检查，结构变更须显式 Alembic upgrade。版本一致不保证没有手工结构漂移，不能直接 stamp 跳过缺失结构；此前 Task 空表修复过程保留在 ENVIRONMENT.md。
- 数据库提交与 Redis 通知不属于同一事务，重复取消不补发通知。同会话并发现由占用拒绝重叠，普通聊天收尾会失效缓存；跨系统提交恢复仍待处理。
- 隔离取消曾出现 ASGI callable returned without completing response / Next failed to pipe response；页面停止和授权测试通过，但传输层有序关闭未排查完。
- 当前模型决策使用 stream=False，不可称为逐 Token 生成；文本出现后终态前断网的浏览器验证仍待完成，见 docs/chat-browser-fault-validation.md。
- 辅助标题调用费用尚未计入 AgentRun 指标。真实模型措辞质量没有在模拟浏览器测试中验证。
- 统一安装/依赖准备/迁移启动入口仍需交付阶段完善；源码版目前依赖 PostgreSQL/Redis，不宣称体验者已能零依赖安装。

## 按需文档

- 目录导航：docs/project-structure.md
- 环境、命令与验证证据：ENVIRONMENT.md
- 周大纲：LEARNING_CURRICULUM.md 第 4 周章节
- 教学规则：AGENTS.md；必要时查 LEARNING_COACH_GUIDE.md 对应章节
- 技能：FastAPI/BFF/流式状态先读 skills/agent-streaming/SKILL.md；工具/Agent Loop 先读 skills/agent-runtime/SKILL.md
- 面试题：interview-questions/；优先更新重要已有题目，参考答案已整理不等于已通过模拟面试。

## 原第 4 周实现与学习分工

| 内容 | 学习与工程结果 | 当前边界 |
|---|---|---|
| Workspace 与目录 | 项目创建/列表、系统选择目录并绑定；理解本地身份、归属检查及目录边界 | 绑定目录不代表获得任意文件读写或命令执行权限 |
| Task 创建 | 学习者完成创建事务、HTTP/BFF 及原创建表单；理解 Task/Conversation 同事务与提交结果未确认 | 原手填标题表单已被对话式入口替换；创建幂等已端到端接入；刷新恢复重试信息仍不在范围 |
| 对话式工作台 | 教练按明确授权完成项目/任务侧栏、笔形新建入口、首发创建、固定会话续聊、历史读取和首轮标题总结 | 原工作台由教练按授权实现；本次 URL 刷新恢复由学习者实现 |
| 异步一致性 | 首轮标题失败保留消息摘录，条件更新防止迟到总结覆盖后来标题；旧历史请求不污染新任务 | 辅助标题费用未计入 AgentRun 指标 |
| 任务详情读取 | 学习者完成 TaskDetailResponse、详情服务与 HTTP；理解直接定位不依赖列表分页、项目/会话双重归属、只读事务和关闭 Session 后序列化 | HTTP 新增 18 条，相关 62 条通过；BFF 与 URL 自动恢复已接入 |
| 任务详情 BFF | 学习者完成详情路由、公开字段解析与项目/任务匹配；复用本地凭证、no-store、取消超时及错误脱敏 | BFF 新增 58 条；本次真实浏览器恢复链路已覆盖 |
| URL 选中与刷新恢复 | 学习者完成 URL 定位、独立详情恢复、错误态禁止发送、取消隔离及创建后保留组件 key；教练修正 effect 挂载时机并补测试 | URL 单元新增 13 条，Workspace 396 条、聊天 84 条、真实浏览器 8 组通过；未发送草稿与历史运行状态不在本课范围 |
| 空任务删除事务 | 学习者实现授权加锁、空历史检查、会话/任务同事务删除及回滚；理解取消终态不能证明执行停止 | 新增 27 条，Task 领域 116 条通过；仅支持无消息且无任何 Run 的任务，当时仅完成服务，现已接通 HTTP/BFF |
| 空任务删除 HTTP/BFF | 学习者完成 DELETE HTTP；教练按明确授权实现同源 BFF，204 空正文、错误白名单与删除结果未确认语义 | HTTP 42 条；后端相关回归 422 条；BFF 43 条、Workspace 439 条通过 |
| 空任务删除 UI | 学习者完成 Provider 删除状态、同步防重、迟到结果隔离、详情确认及 URL 清理；教练按截图授权调整悬停图标、确认框与项目菜单 | 仅支持空任务；未知结果不重试 DELETE，详情 200 仍保留未确认态；刷新页面不保留操作状态 |
| 本地任务会话边界 | 学习者实现本地模式只读取已有 Task 会话、双重归属校验、Run 创建事务锁与缓存前授权；账号模式保留兼容 | 新增 40 条，相关后端 189 条、工作台 1 组及本地模式 3 组浏览器通过；普通非流式在途模型调用不自动取消 |
| 任务运行历史查询 | 学习者完成概要响应与只读服务；先授权再查询，ID 倒序游标、多取一条判断下一页、关闭 Session 前构造普通数据 | 新增 44 条专项通过；已接 HTTP/BFF/UI，不读取事件正文，不将未知状态归类为终态 |
| 运行历史 HTTP | 学习者接入 GET runs、参数校验、404/422/500 安全映射及 no-store | 新增 42 条 HTTP 专项通过；已接 BFF/UI，复用本地身份和访问边界 |
| 运行历史 BFF | 教练按明确授权完成同源路由、凭证隔离、分页输入与概要响应校验、取消超时及错误脱敏 | 新增 79 条、Workspace 518 条通过；TypeScript/ESLint 通过，UI 已接入 |
| 运行历史 UI | 学习者实现右侧概要列表、分页刷新、保留旧结果重试及任务切换隔离；教练恢复原运行摘要 | 浏览器 5 组、Workspace 518 条、聊天 84 条通过；PC 截图视查，手动刷新快照 |
| 运行详情 BFF | 教练按明确授权接入同源 GET，验证 Run 与事件结构，复用流解析器并兼容取消事件；未知 payload 不透传 | 新增 61 条、Workspace 579 条和聊天 84 条通过；详情 UI 已接入 |
| 历史运行详情 UI | 教练按明确授权接入选择、只读事件时间线、返回保留列表、错误重试和旧请求隔离；与实时聊天状态分离 | 浏览器 8 组含原列表回归通过；完整历史费用摘要/执行恢复尚未实现 |
| 数据库启动职责 | 教练按明确授权移除启动自动建表，读取源码/数据库 migration heads，不一致拒绝启动；浏览器夹具使用真实迁移 | 新增 7 条专项、后端全量 1101 条通过；开发库只读就绪检查通过 |
| PC 右侧栏体验修正 | 按截图改为行式摘要、可拖动/键盘调宽、宽度记忆；保留 18px 基线和聊天挂载状态 | 浏览器 9 组及 1366px 窄/宽截图通过，320px 长指标无拆行 |
| Markdown 回复展示 | 按用户问题直接接入统一助手 Markdown 组件，覆盖历史和流式内容；GFM 表格/列表、代码内部滚动、安全 HTML/链接边界 | 浏览器3组、聊天84/Workspace579及类型/lint通过；不计新增周进度 |
| 创建幂等模型与迁移 | 学习者完成请求键作用域、指纹、唯一约束和 Task 删除 SET NULL；教练修正表名与主键参数拼写 | 新增 17 条专项、迁移目录 43 条通过；开发库升级并检查无结构差异；事务服务现已验收 |
| 创建幂等事务服务 | 学习者完成授权加锁、规范化指纹、作用域查询、重放/冲突/删除拒绝及三记录同事务；教练仅做等价 lint 修正和补换行 | 新增 37 条专项，Task/Workspace/local 545 条通过；覆盖真实锁等待、提交确认丢失、归属重查和 Session 缓存；HTTP/BFF/UI 现已接入 |
| 创建幂等 HTTP | 学习者完成可选请求键校验与转交、安全 409/422 映射，保留本地访问边界及未确认语义；核心无需修正 | 新增 37 条真实 HTTP 专项通过；覆盖重放、删除、SQL 回滚及提交后响应失败的同键找回；BFF/UI 现已接入 |
| 创建幂等 BFF | 教练按本课明确授权实现可选键严格校验/原样转发、409/422 错误白名单，保留凭证隔离和未确认语义；不生成键或自动重试 | 创建路由 92 条（新增 33），Workspace 全量 612 条、TypeScript/ESLint 通过；fetch 模拟，UI 现已发送键 |
| 创建幂等 UI | 教练按本课明确授权实现创建意图内存保存、同键重试、切换恢复草稿、明确换键和历史读取后手动发送；用户要求完成后解释实现 | 新增浏览器4组及既有流程4组通过，关键恢复复跑1组通过；Workspace612/聊天84及类型/lint通过；刷新/关闭不保存未发送内容与键，创建幂等不等于消息幂等 |
| 会话执行占用模型与迁移 | 学习者实现单会话主键占用、持有者 token、数据库时间与非级联外键；区分 Run 终态与执行结束 | 新增13条专项、迁移目录56条通过；占用存在时真实删除回滚；运行链路尚未使用占用 |
| 会话执行占用事务服务 | 学习者完成先授权加锁、原子获取、持有者精确释放及独立事务边界；核心无需修正 | 新增 42 条专项，runtime/tasks/local 共 508 条通过；覆盖真实竞争、回滚、提交确认丢失与迟到释放；尚未接入聊天运行 |
| 执行后台线程跟踪 | 学习者完成 ExecutionThreads：强引用跟踪、取消隔离、关闭后拒绝新工作、等待全部结束后传播取消；核心无需修正 | 新增 12 条真实线程专项，随 Agent Loop 共 28 条通过；未接入工具、聊天入口或占用释放 |
| 会话执行作用域 | 学习者组合短事务获取、后台线程收尾和精确释放，兼容 asyncio 与 AnyIO 取消收尾；修正参考返回标注为 AsyncGenerator | 新增 17 条隔离 PostgreSQL 专项，Runtime 共 165 条通过；尚未接入真实聊天入口 |
| Agent Loop 工具线程接入 | 学习者让两种 Runtime 入口传递同一 ExecutionThreads，登记真实工具执行，保留原超时/错误协议与兼容调用；核心无需修正 | 新增 12 条，Runtime 共 177 条通过；真实 PostgreSQL 验证超时/取消后占用保留至工具结束；聊天入口尚未接入 |
| 聊天入口执行占用 | 教练按明确授权接入 request 级依赖、两个入口、全链路线程跟踪、监听器/生成器收尾、Run 兜底终态和缓存失效；API/BFF/UI 安全 409 | 新增 ASGI/PostgreSQL 12 条、后端全量 1300 条通过；BFF 37/聊天状态85、类型及 lint 通过；PC 并发/取消恢复两组通过，截图已视查；详情见 ENVIRONMENT.md |
| 会话占用只读查询 | 学习者完成先授权、只选公开时间字段及不可变快照；教练仅补末尾换行 | 新增 12 条隔离 PostgreSQL 专项，Runtime 189 条通过；验证无写入/提交、无 token 暴露、Run 状态独立及异常传播；HTTP/UI 尚未接入 |
| 会话占用查询 HTTP | 学习者完成公开响应模型及 GET 接口；复用身份和错误边界，线程执行同步查询 | 新增 13 条真实 HTTP/PostgreSQL 专项，相关回归 90 条通过；占用仍返回 200，安全错误与成功均 no-store，查询不改业务记录；BFF/UI 待接入 |
| 会话占用查询 BFF | 学习者完成解析器、代理及 GET 路由；可信本地凭证、公开契约重建和错误脱敏 | 新增 60 条，Workspace 672 条、类型/ESLint 通过；模拟上游覆盖取消/超时、不重试与 no-store；UI 待接入 |
| 会话占用查询 UI | 学习者接入右侧只读查询/手动刷新；会话 key、请求取消及回调身份防迟到覆盖 | 新增组件异步测试8条，Workspace680/聊天85、类型/ESLint通过；PC浏览器5组通过，含真实占用与释放查询、失败重试及切换/关闭隔离；1366/1920截图已视查 |
| 进程内执行并发预算组件 | 学习者完成正整数容量、循环绑定、同步准入与作用域归还；核心无需修正 | 新增23条，随线程跟踪共35条通过；验证真实协程竞争及异常/取消/跨循环边界，尚未装配到聊天入口 |
| 聊天入口并发预算 | 学习者完成配置/依赖/错误映射；教练补漏掉的 lifespan 共享实例装配与测试配套 | 新增后端23条、全量1371条通过；认证/BFF207、聊天86、类型及lint通过；预算保留至响应与后台收尾，单进程两个聊天入口共享 |
| 历史运行摘要恢复 | 学习者复用唯一终态事件与既有摘要卡片恢复步骤/用量/耗时/成本，不补零或拼接不同事件 | 新增23条，Workspace703/聊天86、类型/ESLint通过；PC浏览器4组及1366/1920截图验收通过；非法详情契约仍由既有解析器拒绝 |
| PC 体验 | 教练按授权优化空白页/聊天布局、项目菜单、悬停删除图标、18px 字号、键盘发送与中文输入法保护；三栏保持挂载，后台刷新保留列表，历史加载延迟显示骨架 | 保持 PC 三栏方向，复用共享组件；样式不单列面试考点 |
| 工程组织 | 教练按授权迁移 97 个源码/测试文件，按层级和领域归类，同步导入、脚本与文档 | 未改变接口或数据库结构；目录整理不增加正式周完成数 |

目录整理后后端全量 899 条、前端 Workspace 相关 325 条及浏览器主流程定向场景通过。具体命令和各轮范围归入 ENVIRONMENT.md，重要设计取舍与参考答案归入现有 interview-questions/backend/web/task-model.md，不把整理答案视为已通过模拟面试。


## 原第 5 周分课完成记录

2026-09-22 受限扩展属性复制原语课完成（用户授权直接实现，临时样例）：新增copy_file_xattrs，校验源快照及源/目标对象身份，拒绝同inode、目标多硬链接/非当前属主/非零flags/特殊权限位，目标额外属性在写前拒绝。复制仅使用当次有界读取值，相同属性跳过设置，不删除属性/绕过权限；最终回读完整可见快照并复核源与文件信息。失败可留下部分修改，目标清理由可信调用方负责，不关闭调用方描述符、不执行replace。新增26条分批通过（21条0.14s、补充5条0.08s），Ruff/diff check通过；真实临时文件验证普通属性复制及重复调用零设置、正文/偏移不变，受控测试覆盖返回成功但不生效、源变化、目标额外属性、部分失败和中断。不保证临时性/独占性/原子快照，provenance原本相同不证明不同值复制能力。未接现有替换入口，无数据库/前端/浏览器/全量回归，未提交推送。下一课元数据保护装配扩展属性复制（临时样例）。

2026-09-22 扩展属性复制能力探测课完成（临时样例）：学习者核心与参考一致，仅合并临时文件上下文并整理缩进。探测函数只创建自有临时文件，分别记录matched_before/set_accepted/readback、目标额外可见属性与最终快照一致性，不输出属性值/摘要，不删除系统属性、不修改替换策略。新增19条0.08s通过，Ruff/diff check通过；真实macOS三个普通样例从目标缺失到回读相同，provenance源/目标原本相同且调用成功/回读相同，不能据此证明不同值复制能力。受控测试覆盖调用失败但原本相同、成功却缺失/不同、额外属性、源变化、读取失败、创建/清理/中断路径及资源关闭。无数据库/前端/浏览器/既有整套/全量回归，未提交推送。下一课受限扩展属性复制原语（临时样例）。

2026-09-22 有界扩展属性读取课完成：学习者核心与参考一致，无需修正。新增macOS描述符级只读xattr模块，固定两轮快照及前中后fstat、名称集合复核，预算128项/16KiB名称/64KiB单值/256KiB每轮逻辑总量；空值合法，异常不降级为空，名称和值不进入repr。新增39条0.13s通过，Ruff/diff check通过；真实临时文件验证二进制/空值/中文名称及内容、元信息、描述符偏移不变，受控ABI验证预算分配前拒绝、数量/总量边界、非法名称、变化/权限/平台失败及固定轮数。没有修改现有替换策略、无数据库/前端/浏览器/既有整套/全量回归，未提交推送。读取只覆盖当前进程可见属性，不是原子快照/CAS或复制许可。下一课扩展属性复制能力探测（临时样例）。

2026-09-22 提案应用状态按需展示课完成（用户授权直接实现）：新增独立查询组件接入提案详情，五状态、加载/错误、重新查询和取消；按三资源标识key隔离实例，卸载abort并通过controller身份阻止旧响应。审批标签改为已批准，不再从审批状态推断尚未应用。新增8条与受影响详情8条共16条通过（1.41s），TypeScript/定向ESLint/启动器Ruff/脚本语法通过。PC真实BFF/API/隔离PostgreSQL验证idle，另外四状态和异常响应受控；收起卸载后的延迟响应隔离、显式取消、跨任务导航、键盘查询、1366/1920无横向溢出及截图视查通过，原文件不变，提案写请求为0。无执行入口/全量回归/真实模型调用，未提交推送。下一课有界扩展属性读取。

2026-09-22 提案应用状态查询BFF代理课完成（用户授权直接实现）：新增四字段状态解析器、服务端代理与同源GET application-status路由；local访问边界、内部凭证隔离、严格三标识与额外查询拒绝、五状态及资源匹配、公开字段投影、no-store和20秒取消/超时预算覆盖正文读取。未知协议安全502，不默认idle、不自动重试或触发执行。55条新增测试通过（0.59s），TypeScript/定向ESLint/diff check通过。上游fetch受控模拟，无真实HTTP/数据库/浏览器/后端/既有整套/全量回归，无UI或执行入口，未提交推送。下一课提案应用状态按需展示。

2026-09-22 提案应用状态查询HTTP课及workspace路由整理完成：学习者接口核心与参考一致。新增GET application-status返回四字段严格Schema，复用local身份边界、统一404与no-store，非法标识/额外查询422，未知状态及响应异常安全500。用户要求整理workspace.py，已合并排序导入，集中请求/标识类型，按安全边界、工作空间/目录、任务/运行历史、提案审阅/审批/状态分区并统一格式；全部函数/类含装饰器AST与整理前一致，无业务逻辑调整。新增34条和直接受影响既有18条共52条分批通过（首批33通过，1条OpenAPI测试缺本地凭证；补凭证后修正1条+既有18条共19条2.95s通过），Ruff/diff check通过。隔离PostgreSQL与真实HTTP验证；无文件执行/前端/浏览器/模型/全量回归，未提交推送。下一课提案应用状态查询BFF代理。

2026-09-22 提案应用状态只读查询课完成：学习者核心与参考一致，教练仅补仓库函数间空行和末尾换行。单次联合查询重新授权Workspace/Task/Conversation/Proposal，仅投影三资源标识与application_status；五种状态严格校验，不读取文件、不领取/登记/重试，未知状态拒绝。新增29条分批通过（首轮24通过/5条因测试令牌与公开项目编号碰撞误报；修正独立令牌后5条1.22s通过），Ruff/diff check通过。隔离PostgreSQL验证归属、仅SELECT/无锁/无flush/无commit、私有字段不加载、会话关闭、绑定变化和文件删除后历史查询及无缓存读取。无既有全量/文件替换/前端/浏览器/迁移，未提交推送。下一课提案应用状态查询HTTP接口。

2026-09-22 临时样例中的提案应用执行装配课完成：学习者实现与参考一致，核心无需修正。执行器按单次领取、重新授权/占用令牌与绑定核对、只读preflight、再次快照核对、受限替换、单次终态登记运行，文件I/O前释放数据库事务。回执分开表达文件证据与已确认数据库状态；登记回执丢失返回unknown，不回滚文件或重试，BaseException传播并保留占用。新增37条分批通过（35条4.70s、补充2条1.00s），Ruff/diff check通过；真实隔离PostgreSQL验证提交前/后故障与并发仅一个写入者。成功文件路径沿用仅过滤provenance的测试模拟，额外验证无模拟真实xattr拒绝和核对后外部编辑；不宣称当前宿主完整无模拟写入或CAS。仅新增测试，无既有整套/数据库迁移/前端/浏览器/模型调用，未提交推送。下一课提案应用状态的只读查询。

2026-09-22 文件替换元数据保护课完成（临时样例范围）：学习者已完成元数据模块及大部分接入，用户授权教练补齐替换后核对与错误分类。macOS描述符ACL读取/复制/回读，保留mode/uid/gid，检测可见扩展属性或非零文件标志即拒绝；替换前拒绝与替换后uncertain沿用原提交边界。新增28条与直接受影响既有25条共53条分批通过，Ruff/diff check通过。当前宿主新建文件自动带com.apple.provenance且删除后仍存在；真实扩展属性拒绝已验证，成功路径仅过滤该系统属性模拟支持环境，ACL与文件操作仍为真实调用，不宣称当前宿主无模拟成功写入或完整元数据保留。仍非CAS，未接真实项目/数据库/HTTP/工具，无全量回归、未提交推送。下一课为临时样例中的提案应用执行装配。

2026-09-22 受限文件替换原语课完成（临时样例范围）：学习者核心与参考一致，教练仅补参考遗漏的BLE001说明注释。实现描述符逐级无跟随打开、双次字节基线核对、同目录独占临时文件、短写入循环、基本权限/属主属组保留、同步与replace后核对；结果区分not_replaced/replaced/uncertain，清理失败独立表达。新增67条分批通过（首批60通过/1测试环境特殊权限位失败；核对发现setuid被清除后补平台分支元信息注入，修正1条及新增6条共7条0.80s通过），Ruff/diff check通过。测试直接证明最后检查到replace之间仍可覆盖外部编辑，不是CAS；ACL/扩展属性等元数据尚未保护，仅临时样例可用，不接真实项目执行入口。无数据库/前端/浏览器/模型/全量回归，未提交推送。下一课文件替换的元数据保护。

2026-09-22 应用占用期间的资源删除与目录变更保护完成（用户授权直接实现）：持有已授权Workspace锁时检查running/uncertain，Task删除仅检查目标任务，绑定目标变更检查整个项目；同一规范路径重复绑定保持可用。现有产品无Workspace删除/解绑/覆盖重绑入口，未新增。HTTP与三处BFF安全映射409 proposal_application_busy，删除UI归类明确拒绝并保留任务和草稿。后端20新增+6受影响共26条通过（3.46s），BFF新增3条分批通过；类型/定向ESLint/Ruff/diff check通过。真实数据库4组锁等待验证两种顺序，PC单场景双宽度通过。无新迁移、真实模型或全量回归，未提交推送。下一课受限文件替换原语。

2026-09-22 提案应用状态与执行占用完成（用户授权直接实现）：审批status保持原契约，新增application_status=idle/running/applied/not_applied/uncertain及内部随机令牌；固定归属锁序重新授权，只允许idle领取一次、匹配令牌的running登记终态，所有已领取状态均不可重领，无超时释放或自动重试。新增48条及直接受影响15条共63条分批通过（61条7.09s，提交后响应失败2条1.00s），Ruff/diff check通过。开发库升级60d32ae695cd，旧列业务数据逐行不变，新字段idle/null，Alembic结构与启动就绪通过。无实际文件应用、HTTP/UI/模型/全量回归，未提交推送。下一课应用占用期间的资源删除与目录变更保护。

2026-09-22 已批准提案的应用前授权与基线核对完成（用户授权教练实现）：新增内部授权投影与只读preflight服务，要求approved、绑定相同、Diff完整及完整新内容摘要有效；受限读取前再次比较预期绑定，按原始UTF-8字节核对基线，读取后新Session重新授权并比对快照。返回不可变公开核对结果，不带正文/宿主路径、不写文件或状态，不是写入授权。新增42条、直接受影响既有10条共52条分批通过；首批48通过，3条测试准备被数据库约束拒绝后改为内部边界注入，补跑这3条及1条新增只读仓库测试通过。Ruff/diff check通过，隔离库清理。无迁移/HTTP/UI/浏览器/模型/全量回归，未提交推送。下一课提案应用状态与执行占用；当前快照不防ABA或后续外部修改。

2026-09-22 历史运行提案审阅与审批入口完成（用户明确授权教练实现）：TaskRunPanel仅将成功提案结果接入既有ToolResult，使用当前Workspace/Task范围，未知协议安全回退，历史事件不自动查询或提交。新增8条与直接受影响详情8条共16条通过（1.30s），TypeScript/定向ESLint/脚本语法通过。真实PC四场景覆盖刷新后批准、截断拒绝、未知结果刷新防重、切换离开后返回防重；1366/1920无横向溢出，截图视查通过。隔离PostgreSQL恰好4条逐ID匹配approved/rejected/pending/pending，所有源文件字节不变，临时资源清理。上一课历史入口缺口已补齐；无全量回归或真实模型，未提交推送。下一课已批准提案的应用前授权与基线核对，本次代写授权不自动延续。

2026-09-22 文件修改提案审批交互课完成（当前运行入口）：学习者核心无需修正；新增Actions组件接入详情，pending可决策、截断仅可拒绝，提交前保存sessionStorage防重标记，未知结果及卸载保留标记，已核对回执更新状态。新增11条及直接受影响详情8条，共19条通过；TypeScript、定向ESLint、浏览器脚本语法与启动器Ruff通过。PC六场景分两批通过，原文件字节不变，后续三场景额外完成隔离PostgreSQL精确状态/数量核对。刷新仅验证标记保留，历史运行尚无结构化审阅入口，这是参考方案遗漏，下一课补齐；不宣称刷新后审批界面已恢复。未运行全量回归，未提交推送。

2026-09-22 文件修改提案审批BFF代理课完成：学习者三个新文件与参考一致，核心无需修改。新增同源POST、严格决策解析与公开回执核对；使用服务端凭证和已校验Origin，拒绝额外正文/查询，状态和错误码双重白名单。转发前取消表示尚未转发，转发后取消/超时/异常为结果未确认，不自动重试；20秒预算覆盖上游请求及响应正文。新增93条专项通过（0.66s），TypeScript/新增文件及测试ESLint/diff check通过。只运行新增测试，上游fetch受控，无后端/数据库/浏览器/既有整套/全量回归，未提交推送。下一课文件修改提案审批交互。

2026-09-22 文件修改提案审批HTTP接口课完成：学习者Schema、路由及安全错误分支与参考一致，核心无需修正，仅补新增函数间空行。POST决策入口使用可信身份、严格正文与三标识，复用local/Origin/JSON边界；固定映射404/409/422，未知异常及响应失败统一500结果未确认，成功/失败no-store。新增60条、按共享错误分支选取既有11条，共71个不同用例通过（58条首批、14条补充批次，其中1条调整后重跑）；Ruff/diff check通过。真实隔离PostgreSQL与HTTP测试覆盖提交后详情核对和文件不变；无前端/浏览器/事务服务整套/迁移整套/全量回归，无开发库变更，未提交推送。下一课文件修改提案审批BFF代理。

2026-09-22 文件修改提案审批状态与决策服务课完成：学习者核心与参考一致，无需业务修正，仅补文件末尾换行与函数间空行。pending只能转换一次为approved/rejected，固定Workspace→Task→Conversation→Proposal锁序重新授权；批准要求目录绑定一致且Diff完整，拒绝允许绑定失效或截断。审批不读写文件，也不检查实时文件基线。新增后端53条、前端9条；后端直接相关208条分批通过，前端172条通过，Ruff/TypeScript/定向ESLint/diff check通过。开发库升级5fc219d584bc，既有业务数据逐行一致，Alembic结构与启动就绪检查通过。未运行浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案审批HTTP接口。

2026-09-21 文件修改提案详情按需展示课完成：学习者核心已实现；教练修正参考遗漏的selection.task可空判断为selection?.task，并仅整理该段缩进。新增5条初始展示测试，相关163条通过（1.26s），tsc/定向ESLint通过。更新真实组件夹具与隔离启动器详情路由；PC5场景通过，真实BFF/API详情与DOM Diff一致，键盘读取/聚焦、256px限高/长Diff滚动、双宽度、刷新不自动读详情或重复创建、文件不变及数据库3条pending核对通过。额外受控404→真实重试、挂起旧响应→取消→新请求验证通过；任务切换清理由key/卸载代码核对，本轮未单独做跨任务挂起响应注入。截图视查、临时资源清理，无批准/写入/真实模型/全量回归，未提交推送。下一课文件修改提案审批状态与决策服务。

2026-09-21 文件修改提案详情BFF代理课完成：学习者三文件核心与参考一致，无需修改。新增同源GET路由、local凭证代理及公开详情解析，三标识匹配、拒绝额外查询参数、只转发公开字段、no-store和20秒取消/超时覆盖正文读取。新增56条，连同相邻运行代理/回执解析共169条通过（1.02s），tsc/定向ESLint/diff check通过。覆盖凭证隔离、本地边界、错误脱敏、错误正文取消、无重试、三个阶段取消/超时、Unicode截断Diff与非法响应。上游fetch受控，无真实HTTP/数据库/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情按需展示。

2026-09-21 文件修改提案详情HTTP接口课完成：学习者Schema和路由已实现；教练修正参考遗漏的str到Literal[pending]类型衔接，使用model_validate显式公开字段并保留运行时状态校验，不强转或硬编码pending。新增24条HTTP专项，直接相关81条分批通过（首批80通过/1条测试断言误判，修正后24条3.67s通过），Ruff/diff check通过。覆盖真实local身份/授权查询、越权与不存在统一404、非法标识422、本地边界403、未知状态/异常安全500、no-store、公开字段、历史文件删除及截断。身份依赖含幂等INSERT，提案业务查询仅SELECT；隔离库清理，无开发库变更/浏览器/模型/全量回归，未提交推送。下一课文件修改提案详情BFF代理。

2026-09-21 文件修改提案回执结构化展示课完成（用户授权教练实现）：新增公开协议解析与提案卡片，显示创建时待审批/尚未写入、路径、编号、带时区时间及折叠双摘要；截断独立提示，未知协议通用回退。新增34条，连同预览/命令共102条通过（1.26s），tsc/定向ESLint通过。真实PC5场景通过，1366/1920无横向溢出，键盘摘要展开/收起、回执精确字段、刷新不重复、原文件不变及数据库3条pending核对通过；成功/截断截图视查，临时资源清理。无批准/应用按钮，无真实模型/全量回归，未提交推送。下一课文件修改提案详情HTTP接口；本课无新增重要考点独立入库。

2026-09-21 文件修改提案PC闭环验收完成（用户授权教练完成）：新增proposal-tools.mjs/proposal_model.py，沿用隔离启动器，仅模型决策受控，浏览器/BFF/API/工具/文件/数据库真实。成功、删除至空、纯预览、歧义拒绝、截断5场景全部通过；1366/1920无横向溢出，键盘发送、回执字段/双摘要、消息事件持久化和刷新不重发通过。每场景逐文件核对原始字节不变；数据库最终恰好3条pending，完整新内容和授权查询一致，预览/失败无提案。成功1366和截断1920截图已视查；临时目录/服务/测试库清理。Ruff/脚本语法/diff check通过，无真实模型/Docker/全量回归，未提交推送。下一课文件修改提案回执结构化展示。

2026-09-21 文件修改提案工具适配与注册课完成：学习者三文件核心与参考一致，无需修改；新增独立create_file_edit_proposal工具，复用严格替换参数，身份来自可信context，返回pending创建回执，数据库异常映射proposal_save_unconfirmed且不重试。新增79条专项，直接相关230条分批通过（工具194条1.41s，2条Docker未选；隔离PostgreSQL闭环与服务36条3.79s）。旧能力集合断言同步，Ruff/diff check通过；真实工具保存→授权查询、越权拒绝、预览不保存及原文件字节不变通过。数据库/schema自动清理，无浏览器/真实模型/全量回归，未提交推送。下一课文件修改提案PC闭环验收。

2026-09-21 文件修改提案授权查询课完成（用户授权教练实现）：同一SELECT关联提案/Task/Workspace/Conversation并校验两处用户归属，仅投影公开审阅字段；只读服务返回不可变快照，不加载bound_root或proposed_content、不读文件、不加锁或提交。新增15条，连同保存服务18条共33条通过（3.72s），Ruff/diff check通过。覆盖跨用户/项目/任务及缺失资源统一拒绝、会话/项目归属变化、任务删除、Session清理、无自动flush、截断Diff精确保留，以及文件删除/解绑后历史快照仍可查询。隔离PostgreSQL自动清理，无迁移/浏览器/模型/全量回归，未提交推送。下一课文件修改提案工具适配与注册。

2026-09-21 文件修改提案持久化课完成：学习者模型、迁移、仓库与服务核心通过验收，仅补文件末尾换行。提案保存原文摘要、完整新内容及其摘要、有界Diff、目录绑定与pending状态；先结束查询事务再读取文件，保存事务按Workspace→Task→Conversation加锁重新授权。新增37条专项，连同直接相关共109条分批通过（107条9.06s、锁竞争2条1.20s）。真实迁移往返、约束、提交回滚、归属/绑定变化、删除级联与双向锁竞争通过，原文件不变。开发库升级至4eb108c473ab，原业务数据逐行一致，启动就绪和Alembic结构检查通过。无HTTP/工具接入、批准或文件写入，无浏览器/模型/全量回归，未提交推送。下一课文件修改提案授权查询。

2026-09-21 文件修改预览结构化展示课：学习者四文件核心与参考一致。独立协议校验、仅预览状态、零字节显示、折叠SHA-256与纯文本Diff着色/截断提示完成。新增38条与相关82条通过，tsc/ESLint通过；真实PC4场景核对键盘操作、Diff文本及滚动、双宽度、刷新无重放和文件不变，临时资源清理。无需为样式新增独立面试题。下一课文件修改提案持久化，不自动延续核心代写授权。

2026-09-21 文件修改预览PC闭环课：教练配套验收完成。真实文件成功/删除预览/歧义/截断4场景，独立摘要核对、所有夹具字节不变、双PC宽度、刷新无重放及持久化事件通过；模型受控。新增浏览器脚本与决策夹具，生产代码未修改。临时资源清理，下一课结构化展示有界Diff和仅预览状态，不添加写入或批准能力。

2026-09-21 文件修改预览工具课：适配器与注册由学习者完成，教练补遗漏的8项固定错误文案及旧能力断言。公开结果显式选择字段，不含完整新文件，status=preview_only；不写文件或批准修改。新增69条与相关172条通过（1.36s），模型/服务受控，无数据库/浏览器。下一课PC预览闭环验收。

2026-09-21 授权文件预览与基线摘要课：学习者组合授权读取、纯内存预览及SHA-256，核心无需修改。原文摘要与预览共用单次读取，不重读、不写入。新增28条与相关共128条通过（1.89s），含真实临时文件、BOM/换行、读取后外部修改、隔离PostgreSQL授权/事务关闭及失败传播。下一课文件修改预览工具适配与注册，仍不提供写入。

2026-09-21 单文件文本替换预览课：学习者完成纯函数与不可变结果，精确唯一替换、删除、重叠歧义拒绝、UTF-8大小/行数/展示限额、原始换行及完整新内容保留。新增65条纯内存通过，相关测试表达调整后25条复验通过，核心无需修改。Diff是JSON行审阅表示，不是可应用补丁；无文件读写或工具注册。下一课授权文件预览与基线摘要。

2026-09-21 文件查找PC闭环课（用户授权教练验收）：新增受控决策夹具与浏览器脚本，真实文件/授权/BFF/API/数据库通过成功查找读取、空结果、截断、越界、未绑定5场景；双PC宽度、刷新无重放、历史答案与工具事件通过，截图视查。原文件和外部哨兵不变，临时资源清理。未改生产代码，无真实模型/全量回归。下一课单文件文本替换预览，先做纯内存契约/唯一匹配/Diff，不写文件。

2026-09-21 文件查找工具课：学习者find_files适配器、错误映射及注册与参考一致；教练新增54条，直接相关148条分批通过，更新旧能力集合断言以包含新工具。请求级模型/Runtime能力一致，查找→读取和错误Observation链路受控验证；无真实模型、数据库或浏览器。下一课PC闭环验收。

2026-09-21 受限跨目录文件查找课：学习者实现workspace_find.py，核心与参考一致。固定预算、描述符递归、目录身份/版本核对和失败清理；区分文件名匹配与路径匹配、范围不完整与确定有更多匹配。新增55条专项，直接相关分批144条通过，含隔离PostgreSQL授权/只读事务；无浏览器或模型。下一课文件查找工具适配与注册。

2026-09-21 命令结果结构化展示课（用户授权教练实现）：公开结果校验与卡片分层，明确区分工具调用完成和命令成功，展示退出码、错误事实、命令耗时及独立截断提示。stdout/stderr纯文本转义并限高滚动，旧结果未知事实不补False，协议异常与其他工具保留通用文本。新增30条专项，连同摘要卡片/耗时共48条通过；PC验收证据见ENVIRONMENT.md。此课无新增重要面试题，不为样式展示单独入库。下一课受限跨目录文件查找。

2026-09-21 命令工具PC闭环课（用户授权教练验收）：成功、非零退出、截断、参数拒绝与取消5场景经过真实浏览器/BFF/API/Docker，模型受控；双PC视口、刷新不重执行及持久化事件通过。取消恢复证据保留，临时容器及数据库清理。新增配套验收代码并修正取消后工具状态文案；下一课改进原始JSON命令结果的结构化展示。

2026-09-21 请求级命令注册课：学习者完成请求绑定能力集合、模型/Runtime共用快照、local流式入口开放及默认兼容，核心无需修改。新增14条常规、2条真实Docker；相关常规分批94条通过，Docker退出码0/7并确认无残留。尚未浏览器或真实模型验收；下一课受限命令工具PC闭环验收。

2026-09-21 恢复作用域装配课：学习者完成应用有界存储与ChatExecution注入，32运行×16命令上限、归属隔离、请求结束后的异常证据保留和正常记录释放；仅补参考遗漏的Ruff说明。新增29条，聊天生命周期/预算18条，共47条分批通过。下一课请求级命令工具注册与能力开放，当前命令尚未开放给模型。

2026-09-21 超时与取消分类课（用户授权教练实现）：统一等待器按预算来源产生ToolWaitTimeout，内部TimeoutError按执行失败，外部取消优先，重复取消等待拥有的子任务收尾；恢复证据不依赖异常转换。新增14条，相关68条通过。下一课命令恢复记录作用域装配，命令仍未注册，恢复学习者核心实现。

2026-09-21 命令恢复证据接收课：学习者实现有界作用域记录及适配器保存，原请求快照、容量前置、在途收尾与取消证据保留完成。新增34条，相关86条通过，核心无需修改。发现本机wait_for对取消子类不转换的版本边界，下一课异步工具超时与取消分类；命令未注册。

2026-09-21 Runtime分派课：学习者完成is_async分支，异步直接交给统一等待边界，同步保留线程跟踪路径；原错误/成功Observation及耗时不变。新增28条，相关82条通过，核心无需修改。下一课命令工具内部恢复证据接收，尚未注册命令工具。

2026-09-21 工具定义异步支持课：学习者完成互斥执行器定义、共用参数/上下文边界、直接await及结果字符串契约；同步注册保持兼容。新增39条，直接相关分批104条通过，含两条PostgreSQL线程占用回归。核心无需修改。原调度与注册拆课，下一课仅接Runtime同步/异步分派，命令工具尚未注册。

2026-09-21 受限命令适配课（用户授权教练实现）：新增异步适配器和安全错误映射，复用唯一参数契约，正常仅输出命令事实，失败内部保留恢复证据，取消原样传播。新增24条，直接相关57条通过；未注册到现有同步调度器。下一课异步工具调度与受限命令注册，恢复学习者实现核心。

2026-09-21 失败执行只读核对课（用户授权教练实现）：复用创建核对与身份/状态校验，区分当前状态、成功查询缺失与未确认，保留原历史证据与取消语义，不执行恢复写操作。新增36条常规，分批定向验收通过；真实验证及命令见ENVIRONMENT.md。下一课受限命令工具适配与安全错误映射，恢复学习者核心实现。

2026-09-21 统一编排课：学习者完成sandbox_command.py，独立token及请求复制、正常执行后清理、阶段恢复证据与取消语义已接通；命令结果和清理凭据分离，失败不自动删除或重试。新增33条常规，直接相关192条通过；2条真实Docker验证退出码0/7并确认清理，无需修改核心。下一课失败执行的只读状态核对，证据见ENVIRONMENT.md。

2026-09-21 结果适配课：学习者完成CommandResult的OOM/daemon三态事实和保守成功判定，并新增纯函数适配器。已确认退出结果保留输出、双路截断与耗时，内部身份不进入公开协议；异常与清理未确认保持独立。核心无需修改，新增67条，直接相关共228条通过；下一课创建、执行与清理的统一编排，证据见ENVIRONMENT.md。

2026-09-21 已退出容器显式清理课（用户本课授权教练实现）：新增独立exited入口，重新核对身份/停止状态、非强制删除、确认缺失及取消恢复信息；原created-only入口保持不变。50条新增，直接相关共124条通过；4条真实Docker验证删除成功后的确认丢失与取消边界，资源清理完成，证据见ENVIRONMENT.md。下一课执行结果到CommandResult的适配，恢复学习者实现核心。

2026-09-21 执行协调课（用户本课授权教练实现）：已有容器的先订阅后启动、并发排空/退出确认、总预算与失败停止收尾完成；44条新增常规，直接相关377条通过，5条真实Docker验证通过且临时容器清理。保留未确认结果与停止证据，不自动删除或开放模型Shell。下一课已退出容器显式清理，恢复学习者实现核心；详情见ENVIRONMENT.md。

2026-09-21 attach连接课（用户本课授权教练实现）：固定socket、握手总预算、reader借用与关闭所有权、失败/取消收尾已实现；27条新增常规及2条真实Unix socket/Docker验证通过，详细证据见ENVIRONMENT.md。未启动测试容器，已清理；尚未实现启动前订阅协调及整体命令执行。下一课恢复学习者亲手实现核心，正式进度仍4/12。

2026-09-21 attach请求构造课完成：固定API/HTTP升级请求与输出订阅策略，完整容器ID严格校验；核心由学习者实现，教练仅补空行。新增34条，同文件HTTP专项共104条通过，详情见ENVIRONMENT.md。下一课固定socket的attach连接生命周期；尚未验证本机API兼容或建立连接。

2026-09-21 attach HTTP响应课完成：有界头部读取、严格升级契约、首帧保留、总超时与取消传播。学习者核心无需修改；70条新增及直接相关41条共111条通过。尚未发送请求或连接真实daemon；下一课请求构造与连接生命周期，证据见ENVIRONMENT.md。

2026-09-21 attach异步读取与收尾课完成：固定块排空、EOF完成检查、异常/取消传播与主动调度；核心由学习者实现，教练无需修正。新增41条专项，连同解析器共132条通过；详细证据见ENVIRONMENT.md。下一课有界HTTP attach握手，正式进度仍4/12。

工程整理（2026-09-20）：按用户授权，将 Runtime 服务与对应测试按 agent、execution、command、sandbox、docker 职责分组，同步导入、阅读导航和验证命令；核心逻辑不变，不增加课程完成数。目录与依赖规则见 `docs/project-structure.md`，本轮定向验证证据见 `ENVIRONMENT.md`。

attach帧解析课：非TTY原始帧有界增量解析、双路捕获与永久协议失败状态完成，91条纯字节专项通过（0.09s），核心无需修改。不连接Docker，无全量回归；下一课异步读取与收尾。

退出结果课（用户授权教练实现）：严格exited身份/停止证据及ExitCode/OOMKilled/Error解析，不可变结果和成功判定完成，不暴露daemon错误正文、不伪造输出。新增75条纯解析（0.09s）及3条真实退出码0/7/137专项通过，临时容器已清理。下一课attach输出帧解析，尚未接完整命令执行器。

容器启动课（用户授权教练实现）：冻结请求、启动前身份/执行/隔离复核、完整ID启动与running/快速exited查询完成；尝试启动后异常或取消先停止核对并保留结果，重复取消不打断收尾。新增30条常规，直接相关63条通过（0.91s）；4条真实Docker通过（1.23s）并清理资源。命令退出码/输出及运行中整体生命周期仍待接；下一课退出结果确认。

容器停止课（用户授权教练实现）：严格归属解析与阶段状态判断分离，原created契约保留；按完整ID核对→停止→重新查询确认，不自动删除。新增53条，直接相关共181条通过（0.97s）；1条真实Docker验证忽略SIGTERM的父子进程停止、exited/Pid=0/exec拒绝（2.98s），资源已清理。下一课启动与启动结果确认，代写授权不延续。

隔离与资源复核课：网络/命名空间、权限/只读根/禁止提权、资源整数限额、禁额外设备与挂载、固定tmpfs、重启和日志策略纯校验完成；218条专项通过（0.16s），1条真实created容器验收通过（0.22s），已清理资源。核心无需修改，不接启动；下一课停止与停止状态确认。

执行配置复核课：用户/工作目录/入口/完整Cmd、入口环境基线、严格交互布尔及无健康检查纯校验完成；新增71条专项通过（0.08s），1条真实未启动容器验收通过（0.22s），临时资源已清理。只修正参考字符串拼接括号；HostConfig/挂载/资源复核待下一课，不接启动。

显式清理课（用户授权教练实现）：原上下文/完整ID复核created身份，按ID非强制删除、核对回执并查询缺失；错误统一清理未确认，取消传播，不自动补偿。新增41条常规，直接相关共74条通过（0.90s），2条真实Docker专项通过（0.48s）并清理资源。启动前安全配置复核待下一课，直接实现授权不延续。

只读核对课：未知ID候选发现后复用严格身份核对，已知ID保持原约束，查询失败统一未确认且取消传播；新增50条常规专项，与身份解析共145条通过（0.13s）。1条真实Docker专项验证两种ID路径、错ID拒绝无副作用、删除后仍未确认（0.27s），临时容器已清理。核心无需修改，仅补空行/换行；显式安全清理入口待下一课。

Sandbox创建装配课：创建前校验、固定规格create调用、完整ID解析及inspect身份确认已接通，普通异常报告未确认且保留执行标识，取消传播；不自动重试或删除。新增33条常规专项，直接相关共66条通过（0.89s）；2条真实Docker验证正常创建及创建成功后模拟响应丢失，临时容器均核对后清理（0.27s）。核心无需修改，仅补客户端空行/换行；只读核对恢复服务待下一课。

有界Docker客户端课：固定宿主程序/socket/环境、argv调用、有界双路捕获、超时/取消与重复取消收尾已完成；新增33条，直接相关回归共45条通过（0.87s），含5条真实Python子进程测试；Docker只读版本及不存在容器inspect通过。仅补两处解释性Ruff豁免，未改核心行为；未创建容器，创建与身份装配待下一课。

创建响应/身份确认课：鉴于Docker调用生命周期基础尚缺，先完成sandbox_identity.py纯解析；完整ID、严格JSON、名称/标签/批准镜像引用及created状态一致性已验收，95条专项通过（0.07s），核心无需修改。未调用Docker，实际创建与响应丢失恢复待接；身份确认失败不代表容器不存在。

Sandbox参数构造课：固定镜像批准列表、执行token/名称/标签、Docker权限与资源选项、env分层、请求重新校验和不可变参数快照已完成；44条纯参数专项通过（0.05s）。仅修正参考代码两处字符串拼接括号，未调用Docker；实际创建/身份确认待下一课。

Sandbox最小验证课：独立Docker Desktop Linux/ARM64容器3组真实专项通过，验证非root/权限、只读根与tmpfs、网络、cgroup限制、主进程白名单环境及带子孙进程停止和重启临时数据清空。核心Compose无需修改；通用执行器、项目挂载和故障恢复尚未接入，正式进度仍4/12。

命令环境课：从空映射构造POSIX目标环境，固定PATH/语言环境、执行器目录及XDG派生路径，拒绝任意env覆盖；62条纯函数专项通过（0.05s），核心无需修改。没有继承宿主环境或访问文件系统，尚未接执行器；固定PATH不等于程序允许列表，环境清理不等于Sandbox。

双路输出课：TaskGroup并发读取、同流拒绝、两路成功返回、故障/取消等待兄弟任务收尾及异常组保留已完成；12条专项通过（0.05s），核心无需修改。仅内存流及受控读取器，不代表进程树停止或Sandbox完成。

单路异步输出课：固定4096字节读取、超限继续排空、EOF快照、异常与取消传播及主动让出执行权已完成；31条专项通过（0.06s），核心无需修改。仅内存流/受控读取器，无命令进程或真实管道；双路生命周期仍待下一课。

输出缓冲课：有界字节前缀、UTF-8统一替换解码、独立字节/字符截断、结束快照及两路隔离已完成；41条纯内存专项通过（0.05s），仅修正参考代码的__slots__排序。尚未接入真实管道或进程，不代表完整进程内存上限或Sandbox。

命令契约课：严格argv/相对工作目录、服务端策略常量、四类执行结果及跨字段一致性已完成；120条纯校验专项通过（0.10s），只清理末尾空白。未启动进程，语法校验不等于执行授权，字节/字符上限尚需执行器实施。

只读工具链PC验收：4组专项通过，真实BFF/API/隔离PostgreSQL/临时文件验证三工具串联、刷新无重放、越界和未绑定安全错误；1366/1920截图已视查。模型决策受控，无真实模型效果验证，业务代码无需修改。仅本课专项，无全量回归；完整Sandbox、递归搜索和外部命令执行尚未完成。

搜索工具适配课：严格参数、可信上下文、固定安全错误、定位与截断结果已接通；恢复误覆盖的时间工具注册。新增45条，直接相关回归共122条通过（0.91s）；模型及底层服务模拟，后续PC闭环已验收。

单文件搜索服务课：复用受限读取，字面量/大小写敏感、字符行列号、每行首次命中、50行与200字符片段限制及独立截断标记已完成；43条专项通过（1.07s）。后续搜索工具已注册，不提供仓库递归搜索。

目录工具课：严格默认相对目录参数、身份独立注入、截断JSON与安全错误、按上下文注册已完成；新增31条，直接相关回归共77条通过（0.71s）。受控模型验证先列目录再依据结果读文件，底层服务模拟，未宣称真实模型或浏览器验收。

受限目录枚举课：单层200项上限、第201项检测截断、不跟随子项链接、描述符清理与变化检测已完成；26条专项通过（1.11s），后续目录工具适配与注册已完成。

聊天上下文装配课：local流式入口在模型前加载并传递同一上下文，账号模式保留无文件能力；新增7条、流式9条、生命周期4条分组通过。隔离数据库与真实临时文件链路验证，模型模拟，未做浏览器验收。

文件工具适配与能力过滤课：严格relative_path参数、固定安全错误、模型按上下文选择工具已完成；新增37条，直接相关回归共83条通过（0.93s）。后续local流式聊天入口已完成装配，可调用文件工具；普通/chat保持直接文本请求；该入口已完成装配，受限目录枚举服务已完成，目录工具适配与注册已完成，受限单文件文本搜索服务已完成，单文件搜索工具适配与注册已完成，只读工具链PC闭环已验收，受限命令请求与结果契约已完成，命令输出有界缓冲已完成，单路异步排空已完成，双路并发排空与失败收尾已完成，命令环境白名单已完成，Docker Sandbox最小隔离已验收，Docker Sandbox创建参数已完成，创建响应解析与身份确认已完成，有界Docker客户端调用已完成；Sandbox创建与身份确认装配已完成；创建结果未确认后的只读核对已完成；已确认未启动容器的显式清理已完成；启动前执行配置复核已完成；Sandbox隔离与资源配置复核已完成；容器停止与停止状态确认已完成；容器启动与启动结果确认已完成；命令退出结果与退出码确认已完成；Docker attach输出帧解析已完成；下一课attach异步读取与收尾。

2026-09-20：路径边界服务已完成，由已授权任务取得绑定根目录，统一相对路径协议并拒绝上级/符号链接逃逸。后续受限文本读取课也已完成：逐级无跟随打开、普通文件及身份检查、256KiB限制、UTF-8/NUL策略、变化检测与句柄清理。新增35条，连同路径边界共98条通过；尚未注册工具，不提供完整Sandbox或原子快照，可信上下文构造也已完成（24条专项，1.20s）：由会话查询用户/Task/Workspace归属并返回不可变对象，不含根目录。执行器上下文透传与缺失拒绝也已完成（20条新增，定向回归共43条，1.17s）；只读文件工具适配与能力过滤已完成，流式聊天上下文装配已完成，受限目录枚举服务已完成，目录工具适配与注册已完成，受限单文件文本搜索服务已完成，单文件搜索工具适配与注册已完成，只读工具链PC闭环已验收，受限命令请求与结果契约已完成，命令输出有界缓冲已完成，单路异步排空已完成，双路并发排空与失败收尾已完成，命令环境白名单已完成，Docker Sandbox最小隔离已验收，Docker Sandbox创建参数已完成，创建响应解析与身份确认已完成，有界Docker客户端调用已完成；Sandbox创建与身份确认装配已完成；创建结果未确认后的只读核对已完成；已确认未启动容器的显式清理已完成；启动前执行配置复核已完成；Sandbox隔离与资源配置复核已完成；容器停止与停止状态确认已完成；容器启动与启动结果确认已完成；命令退出结果与退出码确认已完成；Docker attach输出帧解析已完成；下一课attach异步读取与收尾。正式进度仍为4/12，第5周进行中。


## 原大纲收尾记录

2026-09-20收尾：第5周累计只读工具链、命令边界、Sandbox生命周期基础及attach帧解析随源码/测试统一归档；正式4/12，第5周进行中。下一课attach异步读取与收尾，接续以LEARNING_HANDOFF.md为准。各课验收不合并为全量回归结论。

2026-09-21当日收尾：累计完成第5周attach异步连接、命令工具执行与恢复证据、文件查找、预览、提案持久化/授权查询及HTTP/BFF/PC审阅详情。正式4/12，第5周进行中；批准/拒绝决策与文件应用尚未实现。下一课为提案审批状态与决策服务，具体边界以LEARNING_HANDOFF.md唯一下一课为准。源码、测试和文档统一归档main；历史分课验收不合并为全量回归结论。

- 2026-09-22：元数据保护装配扩展属性复制课完成（临时样例）：FileMetadata纳入完整可见xattr，两轮ACL/xattr快照及前中后stat核对；属组→权限→xattr→ACL→完整回读。拒绝同inode、目标额外属性和源/目标变化，失败保留临时目标部分修改并交调用方清理。学习者核心与参考一致；教练修正contextmanager返回标注为Generator[int, None, None]及空行。删除测试代理的provenance过滤，真实临时样例ACL/xattr复制、替换及隔离PostgreSQL执行登记通过。16条新增与40条直接受影响测试共56条分批通过；Ruff/diff check通过。仍非CAS，不保证不可见属性、不同provenance值可复制或断电持久化，无HTTP/UI写入口、全量回归、提交推送。

- 2026-09-22：临时样例提案应用完整链路验收完成：新增7条服务串联测试，经真实创建、审批、执行和授权查询，不直接改审批状态。隔离PostgreSQL及真实临时文件验证有/无ACL、普通二进制/空xattr与完整可见元数据保留（不滤provenance）、正文精确替换、临时文件清理、文件I/O前关闭事务、一次领取及重复不写；验证批准后基线过期、pending/rejected拒绝、终态登记提交前/后故障下文件成功与记录确认分离。7 passed（1.83s），Ruff/diff check通过。仅新增测试与文档，无业务代码修改，无既有回归/前端/浏览器/模型调用/开发库操作，未提交推送。

- 2026-09-22：提案应用执行结果的公开响应契约课完成：学习者核心与参考一致，教练仅补类间空行。新增严格冻结Schema与显式投影，校验三个公开标识、回执提案匹配、文件/登记/清理状态及固定错误码组合；拒绝额外字段、隐式类型转换和未知协议。新增57条0.94s通过，包含240种组合穷举（17合法/223拒绝）、敏感字段隔离、缺字段/错误对象及无执行/数据库/文件I/O调用检查。Ruff/diff check通过。未接HTTP路由、未改执行器，无既有回归/数据库/前端/浏览器/全量回归，未提交推送。

- 2026-09-22：提案应用执行结果的前端解析契约课完成：学习者实现与参考一致，无核心修正。纯解析器校验三个资源标识、自有必填字段、严格类型及17种合法组合，显式投影公开字段，保留replaced/unknown证据，不查询/执行/重试。新增51条通过（0.78s），含调用真实后端Schema逐项对照240种组合（17接受/223拒绝）、缺字段与原型链、错误资源/状态/清理值、冻结输入不变及敏感字段隔离。TypeScript、定向ESLint、diff check通过。无HTTP/BFF/UI接入、数据库/浏览器/既有全量回归，未提交推送。

- 2026-09-22：提案应用执行结果的只读展示组件课完成（用户授权直接实现）：新增ProposalExecutionResultPanel，展示边界重新解析资源及协议，分别表达文件结果、登记确认、临时资源清理；区分领取未确认与登记未确认，非法回执不推断未写入，不展示令牌/路径/正文。独立无状态组件，无fetch/执行/重试按钮，尚未接工作台页面。新增25条静态渲染测试0.88s通过，覆盖17种合法回执、非法输入/错资源、切换隔离及敏感字段；TypeScript、定向ESLint、diff check通过。未做浏览器视觉验收、既有回归或全量回归，未提交推送。

- 2026-09-22：提案应用请求契约课完成：学习者实现与参考一致，仅补类间空行。新增严格冻结FileEditProposalExecutionRequest，只接受必填action=apply，禁止额外身份/路径/正文/令牌/权限字段。新增32条0.10s通过，覆盖字典与JSON合法/非法输入、精确动作、冻结及Schema必填说明，Ruff/diff check通过。执行范围仅完成设计约束：未来实验入口须由服务端创建并登记临时样例，APP_MODE=local或浏览器声明不构成范围授权；可信样例登记与门禁尚未实现。未接路由、无文件执行/数据库/前端/既有或全量回归，未提交推送。

- 2026-09-22：服务端临时样例目录生命周期课完成（用户授权直接实现）：新增temporary_proposal_sample上下文管理器，无外部路径参数，macOS服务端临时目录内独占创建0700目录和0600固定example.txt（old\n）；持有父/子目录描述符核对身份，支持短写，退出仅清理已知单硬链接普通样例文件（允许可信原子替换），拒绝未知内容/链接/身份或权限变化，不递归删除。异常和中断释放描述符；清理失败保留现场，以固定错误报告或给原异常追加note。21条最终0.10s通过，Ruff/diff check通过。无HTTP/数据库/注册门禁接入，无既有或全量回归，未提交推送。路径对象不是授权凭证，必须作用域内结束使用且无外部写者，身份核对不是CAS。

- 2026-09-22：临时样例服务端登记与失效边界课完成（用户授权直接实现）：新增TemporarySampleRegistry，只经内部自建流程登记，不提供任意路径/对象注册。随机内部句柄、最多32项、独占借用；close立即撤销新借用，活动借用退出后清理，正常归还允许后续借用。借用异常撤销并清理，清理失败保留失效登记/容量和现场，不自动重试；目录/父目录身份及权限变化拒绝。新实例不恢复旧登记，PID变化在碰锁前拒绝。18条分批通过（16条0.10s、补充2条0.07s），Ruff/diff check通过；包含真实线程关闭、创建失败、失效与异常保留。未改旧模块，无数据库/HTTP/用户资源授权接入、既有或全量回归，未提交推送。

- 2026-09-22：临时样例与用户任务授权绑定课完成（用户授权直接实现）：新增内部TaskSampleBindings，local模式下按用户/Workspace/Task建立私有登记，只接收资源标识不接受路径/句柄；创建前授权，创建后持项目→任务→会话锁重新授权，仅绑定未绑定Workspace，复用整个项目的running/uncertain占用保护。借用重新授权及核对目录绑定，文件使用期间无数据库事务；忙时拒绝关闭。正常关闭先确认解绑提交再清理。提交尝试前失败清理未发布样例；绑定/解绑commit结果未知及使用异常保留目录并封锁登记，不自动恢复或重试。23条分批通过（18条1.62s、补充5条0.67s），Ruff/diff check通过。无既有模块修改、HTTP/执行器接入、前端/浏览器/全量回归，未提交推送。

- 2026-09-22：样例授权门禁与提案执行器装配课完成（用户授权直接实现）：新增execute_sample_proposal，在可信TaskSampleBindings借用内重新授权提案及目录/固定文件范围后调用执行器，借用持续到执行结束。临时样例保存创建时目录dev/ino；执行器及预检增加可选期望目录/文件约束，替换原语在打开实际父目录后、访问文件前核对期望身份。回执经既有严格Schema验证，unknown/uncertain、非法回执和中断使登记封锁，保留目录/原文件证据，无自动重试。19条新增与24条直接受影响既有共43条不同用例通过，Ruff/diff check通过。真实隔离PostgreSQL及临时文件验证成功、错误范围、数据库改绑/目录替换、登记故障、重复执行及清理拒绝；无HTTP/前端/浏览器/全量回归，未提交推送。

- 2026-09-22：受限样例提案应用HTTP课完成（学习者核心实现，教练验收及授权重构）：严格apply请求、本机身份/来源/模式边界、进程样例门禁与安全公开回执接通；无登记拒绝，写入后响应失败仍报告未确认。新增23条及直接影响386条共409条通过；Workspace服务和测试按六类职责分组，路由按操作拆分且保留统一边界，OpenAPI/业务AST不变。当前无公开登记、BFF或UI执行入口；下一课为受限样例提案应用BFF代理。

- 2026-09-22：受限样例提案应用BFF代理课完成：学习者核心与参考一致，无需修正。新增同源apply路由及proposalExecutionProxy，复用本机访问边界和公开回执解析；严格动作/资源/查询校验，服务端凭证隔离，状态与错误码白名单，20秒上游预算覆盖响应读取。转发前取消标记尚未转发，转发后取消/超时/解析失败均保持结果未确认，不重试；合法unknown/uncertain回执原样保留。新增106条受控上游测试通过（0.70s），TypeScript、定向ESLint、diff check通过。未访问真实后端、数据库或文件，未做浏览器或全量回归，未提交推送。下一课为提案应用客户端请求状态管理。

- 2026-09-22：提案应用客户端请求状态管理课完成（用户授权直接实现）：新增独立createProposalExecutionRequest控制器，按资源快照管理idle/submitting/receipt/uncertain/unavailable；请求前同步保存本标签页防重复标记，重建实例重新读取，存储不可用拒绝提交。25秒浏览器等待预算，取消/切换/销毁不清除标记，代次隔离迟到响应；合法公开回执持久保存并保留unknown/uncertain，回执保存失败仍展示已核对证据。保守策略：所有非200响应均保留未确认且禁止重提，不提供解锁/自动重试。新增25条分批通过，TypeScript及定向ESLint通过；未改既有模块、未接页面、未访问真实后端/文件、无既有或全量回归，未提交推送。下一课为受限样例提案应用交互组件。

- 2026-09-22：受限样例提案应用交互组件课完成（用户授权直接实现）：新增独立ProposalExecutionActions，按资源key重建生命周期与确认状态，effect内装配真实sessionStorage、fetch和请求控制器；首次checking避免水合差异，勾选后才允许提交，停止等待不撤销写入。清理先取消订阅再dispose，未知结果保留防重复标记，合法回执复用既有只读展示。新增14条静态渲染/受控Hook调度测试通过，包含真实控制器的点击、取消、卸载及迟到结果；TypeScript、定向ESLint通过。尚未挂通用工作台，未做真实浏览器验收、真实后端或文件写入、既有或全量回归；未提交推送。下一课为受限样例应用组件的PC浏览器验收。

- 2026-09-22：受限样例应用组件PC浏览器验收完成（用户授权直接实现）：新增test/browser/execution-component隔离编译/页面/验证脚本，复用真实组件、按钮、全局样式及React StrictMode；仅监听127.0.0.1随机端口，API请求全部受控拦截，漏拦截时服务器503拒绝。真实Chrome在1366×900与1920×1080下7组专项通过，7次受控请求，pageerror为0；覆盖键盘勾选提交、StrictMode不重发、刷新回执、停止等待、卸载重挂、资源切换及迟到响应、unknown恢复、非法回执和存储不可用。三张截图已检查，内容区宽度/滚动宽度均720px无越界。TypeScript、定向ESLint及diff check通过，组件无须修改。无真实后端/数据库/文件写入，无既有或全量回归，未提交推送。下一课为受限样例应用真实端到端验收。

- 2026-09-22：受限样例应用真实端到端验收完成（用户授权直接实现）：新增execution-e2e隔离API启动夹具、Next/BFF启动器和真实Chrome脚本。使用独立PostgreSQL库/schema及真实Alembic迁移；API内部创建本机用户/任务、自建样例并生成批准提案，页面仅获公开标识。真实页面→BFF→API→门禁→文件替换→数据库登记通过：old\n变new\n，idle变applied，HTTP200公开回执replaced/applied/cleanup_complete=true；UI请求一次且刷新不重发，非法额外path返回422。直接重复HTTP返回200但code=proposal_application_claim_unconfirmed，文件内容/inode/mtime均不变并封锁登记，正常close拒绝。仅测试夹具在请求结束且核对applied后解绑并清理自有样例；生产保护未改。最终样例目录删除、Workspace解绑、进程及测试库/schema清理确认，pageerror=0；Ruff/定向ESLint/diff check通过，无全量回归，未提交推送。下一课为受限样例登记状态的只读查询。

- 2026-09-22：受限样例登记状态只读查询课完成（用户授权直接实现）：TaskSampleBindings新增read_status与冻结TaskSampleStatus，只返回missing/busy/sealed/ready。先检查local模式/进程身份，再按进程锁→数据库归属锁顺序重新授权Workspace/Task/Conversation，包含无登记情况；根目录绑定不匹配仅报告sealed，不修复或改变登记。查询不创建/借用/关闭样例、不访问文件、不提交数据库，退出短事务后返回快照。ready不证明文件仍有效、不预留执行权，不依据提案审批/应用状态恢复内存登记。新增18条及同类生命周期直接影响23条共41条通过（3.08s），Ruff/diff check通过；无HTTP/BFF/UI变更、无全量回归，未提交推送。下一课为受限样例登记状态HTTP接口。

- 2026-09-22：受限样例登记状态HTTP接口课完成（用户授权直接实现）：新增GET /workspaces/{workspace_id}/tasks/{task_id}/sample-status，独立samples路由复用WorkspaceRoute、CurrentUser及进程get_sample_bindings。严格冻结TaskSampleStatusResponse仅公开workspace_id/task_id/status；路径严格校验，拒绝任何查询参数或正文，响应统一no-store。未知/越权404，非法输入422；查询/内部状态/响应失败统一500 sample_status_read_failed，不降级missing/ready。同步服务在线程池执行，路由不持数据库事务或访问文件。新增20条与共享边界直接相关57条共77条通过（8.29s），Ruff/diff check通过；独立PostgreSQL测试库/schema自动清理，无BFF/UI接入或全量回归，未提交推送。下一课为受限样例登记状态BFF代理。

2026-09-22收尾：本轮课程完成范围已推进至受限样例登记状态HTTP接口，正式进度4/12、第5周进行中。提案审批/应用服务、BFF与独立交互组件、PC及真实样例文件链路验收、Workspace职责分组均已归档；普通项目写入与生产工作台应用入口仍未开放。后续从登记状态BFF代理继续，唯一接续与约束见LEARNING_HANDOFF.md。

- 2026-09-23：受限样例登记状态BFF、工作台只读展示及定向浏览器验收已完成，状态只作快照，错误不降级为missing/ready；相关未提交改动仍在main工作区。

- 2026-09-23：受限样例来源持久登记课完成（用户授权直接实现）：新增WorkspaceSampleOrigin及Alembic迁移，旧普通目录不回填；绑定/关闭与Workspace.root_path同事务提交，借用及已有登记状态读取重新核对来源；来源Task删除由服务端拒绝并在BFF/工作台明确显示409。新增迁移、服务、API、BFF和浏览器场景，定向运行新增及实际影响测试、Ruff、TypeScript、ESLint、Alembic check，均通过；隔离PostgreSQL测试资源已清理，本地开发库迁移至71c43e9a8f02。持久证据不等于进程内执行权；重启后无内存登记仍被报告missing，下一课改为只读sealed诊断，不恢复执行或清理。未做全量回归，未提交推送。

- 2026-09-23：进程重启后的受限样例只读诊断课完成（用户授权直接实现）：read_status重新授权后总是读取来源；无内存登记但Workspace有持久来源，包括来源Task/目录错配，保守sealed；无来源的普通Workspace仍missing，来源读取失败直接失败，执行句柄不恢复。定向35+1条PostgreSQL状态/HTTP测试通过；隔离浏览器→Next BFF→FastAPI真实链路验证sealed/missing/404/422、页面与双PC宽度、文件/提案不变及资源清理。旧浏览器夹具将同Workspace兄弟Task误作missing，改为独立无来源Workspace后重跑通过。Ruff、Node语法及diff check通过，无全量回归，未提交推送。下一课为关闭清理失败的持久待办记录。

- 2026-09-23：受限样例关闭清理待办课完成（用户授权直接实现）：新增WorkspaceSampleOrigin.lifecycle_state及82d14f5b09ad迁移，旧来源默认active，数据库拒绝非法状态及有待办时回退。close先同事务解绑并置cleanup_pending，确认后安全清理文件，再以第二事务撤销来源；清理失败/中断/延期与提交结果未知时封锁进程登记，待办阻止样例、普通目录重新绑定和来源Task删除。状态HTTP仍只公开sealed，不恢复句柄或自动清理。新增与直接受影响测试分批3、4、68、8、2条通过；Ruff、diff check、Alembic check通过，本地开发库已迁至82d14f5b09ad。隔离PostgreSQL与自有临时文件自动清理，无全量或浏览器回归，未提交推送。下一课为清理待办原因的只读公开展示。

- 2026-09-23：清理待办原因的只读公开展示课完成（用户授权直接实现）：重新授权并按来源Task限定cleanup_pending原因，其他sealed只报unavailable，非封锁状态原因必为null；服务、FastAPI、BFF与PC工作台都只传严格公开字段，组件明确只反映持久记录快照，不提供清理/重试。定向隔离PostgreSQL状态/HTTP/待办52条及补充提交不确定2条通过；前端契约57条、TypeScript、定向ESLint与PC Chrome组件通过，真实端到端链路和资源清理记录见ENVIRONMENT.md。未运行全量回归，正式进度4/12、第5周进行中；下一课为清理待办的只读处置预检。

- 2026-09-23：清理待办内部只读处置预检课完成（用户授权直接实现）：新增固定分类的TaskSampleCleanupPreflight和read_cleanup_preflight。先按本地进程/来源Task重新授权，在短事务中核对持久待办与Workspace；只有一致待办才在事务外用可信临时父目录fd无跟随观察规范样例名字。候选目录即使同名/同权限也只能标记身份无法确认，目录缺失只是观察快照；不读取内容、不调用登记借用/关闭、不返回路径、不公开HTTP/前端或执行清理。新增19条隔离PostgreSQL与真实临时目录用例通过，定向Ruff/diff check通过，无全量/浏览器回归。正式进度4/12、第5周进行中；下一课为新建样例的目录身份持久证据。

- 2026-09-23：新建受限样例的目录身份持久证据课完成（学习者实现核心，教练验收修复）：创建与登记沿用已持有描述符核验的父/子目录dev/ino，绑定时和Workspace.root_path、来源同事务提交四个可空身份字段。迁移93e4c2d7a10b不回填旧来源，约束只允许四列全空或完整有效；有身份记录禁止无损降级。内部预检对新待办数值匹配返回identity_matches_record、错配返回evidence_inconsistent，旧来源仍identity_unverifiable；所有结果只是只读观察，不授权清理或执行。新增与直接受影响的迁移、创建、登记、绑定、来源、待办与预检测试123条通过，类型收窄后定向1条通过；开发库迁至新head并通过alembic check，正式进度4/12、第5周进行中。下一课为候选目录的描述符级只读复核，唯一接续见LEARNING_HANDOFF.md。

- 2026-09-23：清理待办候选目录的描述符级只读复核课完成（用户授权直接实现）：在可信父目录fd下无跟随打开候选目录，对打开前目录项、候选fd与退出前目录项做身份及形态核对，前后复核父目录，候选fd先于父fd关闭。稳定的新记录数值匹配保持identity_matches_record，旧记录仍identity_unverifiable；观察中替换/消失或I/O/关闭失败为inspection_unavailable，稳定持久错配为evidence_inconsistent。只读快照不构成文件系统CAS、执行或清理许可。新增及直接影响的隔离PostgreSQL/真实临时目录用例24条通过、9条无关用例未运行；定向Ruff、compileall、diff check通过，无迁移、HTTP、前端或浏览器改动。正式进度4/12、第5周进行中；下一课为清理待办只读诊断HTTP接口，唯一接续见LEARNING_HANDOFF.md。

- 2026-09-23：清理待办只读诊断HTTP接口课直接实现并验收：GET /workspaces/{workspace_id}/tasks/{task_id}/sample-cleanup-preflight将内部预检的七种固定分类投影为来源Task重新授权后的workspace_id/task_id/result，限定local模式和请求形态，统一no-store与脱敏错误；未知/越权或兄弟Task为404，非法输入422，读取失败500。响应不含路径、目录身份、句柄或执行/清理能力；匹配结果仅为只读观察快照。新增HTTP测试32条、直接受影响旧用例13条通过，定向静态检查与隔离库清理见ENVIRONMENT.md；正式进度仍4/12、第5周进行中，唯一下一课为清理待办只读诊断BFF代理，见LEARNING_HANDOFF.md。

- 2026-09-23：清理待办只读诊断BFF代理课直接实现并验收：新增同源GET、独立代理与严格解析器，只使用服务端本机凭证，拒绝非法资源标识/查询/正文；上游200必须对应同一Workspace/Task及七种固定分类，只向浏览器重建三个公开字段并no-store。上游403/404/422保留状态但统一脱敏，其他状态、非法JSON/分类、错资源、网络/取消/超时返回安全502，不重试或透传内部正文。新增51条BFF契约测试通过，TypeScript、定向ESLint和diff check通过；上游受控模拟，无后端/数据库/浏览器访问，也未改现有共享代理，无直接影响的旧测试。正式进度仍4/12、第5周进行中；下一课为工作台按需展示与PC只读验收，见LEARNING_HANDOFF.md。

- 2026-09-23：清理待办只读诊断工作台按需展示与PC只读验收课直接实现：真实面板只在用户点击后查询，按Workspace/Task切换隔离快照，卸载与取消阻止迟到结果回写；七种固定分类使用明确的只读文案，失败显示状态未知，不把目录缺失或数值匹配当作清理、恢复、重试许可。隔离Chrome组件专项通过，覆盖键盘查询、取消/卸载、资源切换、错误与双PC宽度；直接受影响的既有工作台专项重跑通过，核对双面板按需GET、收起/Task切换与旧响应隔离，未发送写请求。真实Chrome→Next BFF→FastAPI专项在随机PostgreSQL库/schema和自有待办样例上通过：来源Task只读返回identity_matches_record，同Workspace兄弟与未知Task为404，非法查询422，响应no-store；GET前后文件字节/inode/mtime、提案与来源/Workspace记录均不变，页面无写请求或错误，1366/1920无横向溢出；请求后自有样例、来源、服务及测试库/schema均清理。匹配只证明查询时刻的数值观察，不能授权文件操作。正式进度仍4/12、第5周进行中；唯一下一课见LEARNING_HANDOFF.md。

## 第 2 周原始逐日计划

本节保留第 2 周当时执行的后端底座计划；该周已完成。第 3 周及之后的逐日计划与当前勾选状态见 [LEARNING_CURRICULUM.md](../../LEARNING_CURRICULUM.md)。

### Day 1：配置管理

- 学习：配置是什么，为什么不能散落在路由代码中。
- 实践：使用 Pydantic Settings 管理 API Key、模型名和数据库地址。
- 验收：缺少必要配置时提示清楚，`POST /chat` 仍然正常。

### Day 2：项目分层与依赖

- 拆分 routes、schemas、services、repositories、tools 和 config。
- 一次只移动一个职责，每次移动后都启动验证。

### Day 3：PostgreSQL 与 SQLAlchemy

- 学习表、主键、外键、索引和事务。
- 创建 users、conversations、messages 和 run_events 表。
- 将聊天记录从 SQLite 迁移到 PostgreSQL。

### Day 4：Alembic 迁移

- 创建、升级和回退数据库迁移。
- 验收新电脑能通过迁移得到相同表结构。

### Day 5：pytest

- 测试配置、工具函数、数据库读写和主要接口错误。
- 不在自动测试中真实消耗模型额度。

### Day 6：Docker Compose 跨平台环境

- 使用同一份 Compose 启动 PostgreSQL + pgvector 和 Redis。
- 在 macOS 验证后，保证 Windows 只需要相同命令即可启动。

### Day 7：复盘

- 更新环境文档和启动命令。
- 画出 Next.js → FastAPI → Agent → 数据库/模型的架构图。
- 提交本周成果，记录仍未解决的问题。
