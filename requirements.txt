# AI Agent 后端项目依赖（跨周共享）
# 安装方式：uv pip install -r requirements.txt

# Web 框架（写接口 @app.post / @app.get 等）
fastapi==0.141.1
# Starlette 1.6.0 TestClient 仍引用 anyio.abc.BlockingPortal；
# AnyIO 4.15 开始对此发出弃用警告。待 Starlette 修复并验证后再升级。
anyio==4.14.2
# 数据校验（BaseModel 定义请求/响应结构）
pydantic==2.13.4
# 配置管理（从环境变量和 .env 读取并校验配置）
pydantic-settings==2.15.0
# 读取 .env 环境变量
python-dotenv==1.2.3
# 密码哈希与验证（Argon2id）
argon2-cffi==25.1.0
# 调用 DeepSeek（OpenAI 兼容）LLM API
openai==3.3.1
# ORM：先在 SQLite 上练习数据建模，之后可平滑切换到 PostgreSQL
SQLAlchemy==2.0.52
# PostgreSQL 驱动：让 SQLAlchemy 能通过 psycopg 连接本地 pgvector 数据库
psycopg[binary]==3.3.5
# 数据库迁移：把表结构变更记录为可升级、可回退的版本
alembic==1.19.1
# 自动化测试：验证工具、数据库和接口逻辑，避免测试时调用模型 API
pytest==9.1.1
# FastAPI 接口测试客户端的 HTTP 传输依赖
httpx==0.28.1
# ASGI 服务器（启动 FastAPI 应用用）
uvicorn==0.52.4
# Redis 异步客户端：跨进程传递 Agent Run 的取消信号
redis==8.1.0
# 代码格式化工具（VS Code 已配置为默认格式化器）
ruff==0.16.4
