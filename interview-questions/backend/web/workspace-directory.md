# Workspace 目录规范化为何不能替代文件访问授权？

复习优先级：高。参考答案已整理、尚未模拟。

## 参考答案与取舍

项目入口要求本机绝对路径，并用 resolve(strict=True) 得到已存在的真实目标，再检查目录类型。保留合法空格，不展开 ~ 或环境变量，避免输入被隐式解释；拒绝文件系统根目录，限制过大的项目范围。入口允许符号链接，保存其规范目标。

用户选择目录入口与工具访问目录内文件是两层检查。入口解析成功不代表目录内软链接都可访问；文件工具必须根据实际目标检查是否落在绑定根目录内，不能只按字符串前缀判断。路径检查和打开文件之间可能发生替换，即 TOCTOU；真正的执行边界还需要操作时校验、适当的文件描述符策略与进程隔离，不能声称一次 resolve 已实现 Sandbox。

## 失败场景

目录不存在、普通文件、路径中间不是目录、失效/循环链接、权限不足与其他文件系统故障要区分。使用稳定业务错误码，响应不直接携带原始系统异常；验证成功只描述检查时刻，不保证之后仍存在或可写。

## 项目证据与限制

app/services/workspace/directory/workspace_directory.py 与 tests/workspace/directory/test_workspace_directory.py：本课 29 条通过，真实临时目录/链接验证路径语义，故障注入验证 resolve/stat 的异常分类。当前尚未实现目录绑定、文件范围授权、竞态防护或 Sandbox；Windows 未实机验证。不能把这些未来能力写成已完成成果。

## 增量迁移如何兼容已有工作空间？

新增可空 Text 字段，NULL 明确表示未绑定，不设置默认项目或回填路径；旧应用省略此字段的创建仍可成功。模型与迁移同步维护 CHECK，但数据库仅拒绝空字符串，路径存在性仍属于服务层。迁移只能向前新增版本，不修改已执行的旧文件。历史迁移测试要对照历史结构快照，最新结构由新迁移测试验证。

本课 e05f42c817ab 的隔离 PostgreSQL 测试验证旧数据保持、真实提交、CHECK 拒绝、降级保留旧列记录与再升级；相关专项 66 passed。回退会丢失绑定字段的数据，结构可回退不等于数据可恢复。开发库仅执行向前升级，head 和结构检查一致。目录绑定事务与文件授权仍未实现。复习优先级高，参考答案已整理、尚未模拟。

## 并发绑定如何避免后来的请求覆盖项目目录？

按 Workspace 标识及归属 SELECT FOR UPDATE，锁内读取最新绑定值，再校验规范路径；首次绑定保存，同一规范路径重复成功，不同目录拒绝覆盖。所有绑定入口遵循该事务规则，不能用普通查询后无条件 UPDATE 替代。populate_existing 防止 Session 身份映射里的旧值掩盖数据库新值；no_autoflush 避免查询刷新其他待写数据。服务在 try 外拒绝已有事务，避免错误回滚调用方工作，成功前复制普通结果，commit 成功才返回。

项目证据：test_workspace_binding.py 的两类并发测试使用 pg_blocking_pids 确认实际锁等待，验证后来的请求读取首个已提交目录。16 条绑定专项加相关测试共 84 passed；提交故障在真实 flush 之后注入，独立连接验证回滚和恢复。此处验证提交前明确失败，不证明连接中断后的提交结果可判定。重复请求会重新验证目录，目录被删时不能承诺仍成功。目录校验期间持有行锁，因此不能在此事务内加入递归扫描或模型调用。

绑定服务已完成，HTTP/BFF/页面与文件授权尚未接入。参考答案已整理、尚未模拟。

## 绑定接口返回 500 是否说明绑定没有发生？

不能。事务可能已提交，随后在构造响应时发生错误。HTTP 边界要使用“结果未确认”，前端不能自动按未提交处理或任意换绑；重复同一规范路径仍经过校验。服务异常与请求校验应转换为稳定错误码，不能反射原始路径、SQL 或异常正文。

证据：test_workspace_binding_api.py 在真实提交之后注入响应构造异常，验证安全 500、身份与业务 Session 关闭，以及独立连接读取的已保存路径。本课 HTTP 32 条，相关回归 78 passed。后端 HTTP 已完成，BFF/页面与文件授权仍待实现。参考答案已整理、尚未模拟。

## BFF 为什么必须匹配响应中的工作空间标识？

格式正确不代表响应属于当前操作。绑定 BFF 同时检查 external_id 与请求 ID 完全一致，仅复制 external_id/name/root_path，不透传内部字段或 Set-Cookie。内部凭证只由服务端注入，不采用浏览器伪造值。已知错误需同时匹配 HTTP 状态和自有错误码，用 Object.hasOwn 排除原型属性；使用本地文案，不反射异常正文。

证据：binding-route.test.ts 60 条通过，覆盖不同 ID、白名单、错误码错配、Unicode/路径，以及请求和响应正文阶段超时/取消且不自动重试。BFF 已完成，真实前后端集成及工作台入口尚待接入；取消代理并不回滚后端事务。参考答案已整理、尚未模拟。


## 为什么选择目录要经过本地服务，取消请求又不等于撤销保存？

浏览器目录控件提供相对路径或目录句柄，不能直接充当 Python Runtime 的本机绝对路径。当前本地 Web 产品通过严格本地 POST 请求打开系统目录窗口，选中后校验并保存；原生调用使用固定参数、不插入用户命令，先校验资源归属、释放数据库连接，再等待用户选择，保存时重新检查行锁与归属。窗口取消是正常 204；网络中断或返回失败则可能发生在提交之后，页面显示结果未确认并通过 GET 回读，不能自动重复选择或覆盖目录。

证据：test_directory_picker.py、test_workspace_selection_api.py 与 directory-select-route.test.ts 覆盖平台输出、超时/忙碌、取消无写入、本地访问边界、等待期间 Session 关闭、并发绑定冲突和白名单响应。原先 HTTP/BFF/页面待接入的限制已由目录选择交互补齐；文件授权与 Sandbox 仍未实现。复习优先级高，参考答案已整理、尚未模拟。


## 为什么文件工具的根目录必须来自已授权任务，resolve 后还不能直接宣称安全读取？

复习优先级：高。参考答案已整理、尚未模拟。

模型只提供相对路径，服务端执行上下文提供身份和任务定位。先通过 owned_task 联查项目、任务与会话归属，再复制数据库中的绑定目录并关闭 Session；不存在或不可访问的任务不能触发文件系统探测。未绑定目录明确拒绝，不回退到 cwd。只读查询无需 commit，文件系统解析不能占着数据库事务等待。

工具协议统一用正斜杠；同时识别 POSIX 根路径、Windows 盘符/根路径/UNC，拒绝盘符相对路径 C:foo、所有 .. 段、反斜杠、替代数据流冒号及设备名。代价是部分 Unix 合法文件名不受支持，换取各平台一致的输入语义。PureWindowsPath 规则测试不等于 Windows 实机验收。

重新校验保存的规范根目录，检测它是否改为指向别处的链接；对目标 strict resolve 后按路径层级判断 is_relative_to，而不是字符串 startswith。项目内链接可用，链接到项目外与同名前缀兄弟目录均拒绝。不存在、断链、循环链接和权限异常采用稳定错误分类，不回传底层路径。

失败场景与限制：返回 Path 后，目录或目标仍可能被另一个进程替换。根目录规范字符串相同也不证明目录对象身份相同；本课没有持有目录/文件描述符，不提供原子检查加打开，也没有验证硬链接来源、挂载边界、可读性或文件内容。真正读取时必须另行设计打开阶段边界和资源限制，不能简单把 resolve + open 当成完整 Sandbox。

项目证据：apps/api/app/services/workspace/directory/workspace_path.py 与 apps/api/tests/workspace/directory/test_workspace_path.py，63 条专项通过；真实临时文件/目录/符号链接及隔离 PostgreSQL 验证授权先行、Session 关闭、仅 SELECT、无 commit 和数据库失败清理。权限与通用系统异常采用故障注入；未做 Windows 实机或文件读取竞态验收。此前章节中的“文件授权未实现”是阶段历史，本次已补路径解析授权，实际安全读取与 Sandbox 仍待实现。


## 为什么有界文本读取不能只用 resolve + read_text？

复习优先级：高。参考答案已整理、尚未模拟。

resolve只确定检查时刻的路径。当前受限读取从规范路径的根逐段使用dir_fd和O_NOFOLLOW打开目录，末端打开前stat、打开后fstat核对普通文件与设备/inode/大小/时间；O_NONBLOCK避免替换为FIFO后打开阻塞，ExitStack在所有分支关闭描述符。它防止规范路径打开时跟随新链接，但不等于完整文件系统隔离，也不阻止目录搬移或同一路径对象替换。

大小检查只是提前拒绝，实际读取仍须最多上限+1字节，避免文件增长后突破内存/输出预算；短读不代表EOF，空字节串才结束。严格UTF-8并拒绝NUL是明确接受策略，不是通用二进制鉴定；超限不返回部分结果。读取后比较元信息可发现常见变化，但不能声称有原子快照。普通文件非阻塞标记也不提供磁盘I/O硬超时。

项目证据：workspace_file.py与test_workspace_file.py，新增35条，与路径课63条共98条通过（1.69s）；真实替换与句柄跟踪、读取增长/缩小/同长度修改、边界大小及两条PostgreSQL授权集成。Windows缺少所需能力时拒绝，不回退普通open；未做Windows实机验证。历史“实际读取待实现”已由本课补齐，工具注册、完整Sandbox与快照仍未实现。


## 为什么工具上下文不可变仍不能替代执行时授权？

复习优先级：高。参考答案已整理、尚未模拟。

可信来源与不可变是不同问题：frozen dataclass防止正常字段赋值，不阻止不可信调用者构造对象。身份来自服务端认证，当前会话经Conversation→Task→Workspace联查，同时约束会话和项目所有者；工具模型参数不承载user_id、Task、Workspace或root_path。同用户多个项目也必须由当前会话推导范围，不能让模型自由挑选。

只复制普通标识并关闭Session，不持有ORM对象或跨模型等待的事务/锁；上下文可以在未绑定目录时构造，因为任务定位不等于文件能力。后续读取再查归属和绑定，避免把已过期快照当作永久访问许可。模式切换不能删除文件工具需要的所有权条件。

证据：test_tool_execution_context.py 24条通过（1.20s），隔离PostgreSQL验证两模式授权、会话/项目定位、未绑定目录、SQL错误及只读清理；本课仅完成构造服务，执行器上下文传递和工具能力控制仍待接入。


## 为什么 Runtime 前置检查后，execute 还要检查上下文？

复习优先级：高。参考答案已整理、尚未模拟。

Runtime前置检查避免无上下文调用启动线程；ToolDefinition.execute再次检查覆盖绕开Runtime的直接调用，两者职责不同。模型参数与context分开传参，不通过字典合并决定身份来源。注册时拒绝context字段及Schema别名，执行时再次拒绝导出payload中的context；无上下文工具不接收不必要身份字段，保持原签名。类型检查不证明来源可信，服务端仍须通过授权工厂构造上下文。

项目证据：test_tool_context_dispatch.py新增20条，连同直接相关回归43条通过（1.17s）；包含两种入口、真实线程跟踪及并发隔离。缺失上下文沿用tool_execution_failed，details固定为tool_context_required，未执行时duration_ms为空。下一步仍需模型工具可见性过滤和聊天入口装配，不能把执行器守卫当成已接通文件工具。


## 为什么工具注册、模型可见性与执行授权要分别处理？

复习优先级：高。参考答案已整理、尚未模拟。

注册表说明服务端有哪些实现；按上下文生成模型工具列表，只展示本次执行可用的能力；Runtime/execute的上下文守卫和读取服务的再次授权负责实际阻止越权。模型可能猜到未展示的工具名，所以隐藏Schema不能替代执行检查。每次生成独立列表及Schema，避免并发任务互相修改能力描述；上下文本身不进入模型消息。

已知文件故障转换成白名单SafeToolExecutionError，Runtime生成失败Observation，code沿用tool_execution_failed，details承载固定业务分类。不能返回含error的成功字符串，也不能反射底层异常正文；未知异常仍走通用安全失败，取消不吞掉。

证据：test_read_file_tool.py新增37条，直接相关回归83条通过（0.93s）。模拟模型完整往返验证实际工具列表、成功/安全失败/未知失败及无上下文强行调用拒绝；读取服务模拟，未重复验证文件系统。文件工具已注册，聊天入口上下文装配仍待完成。


## 聊天入口为什么先加载上下文，再准备历史？

复习优先级：高。参考答案已整理、尚未模拟。

_prepare_chat_messages会追加本轮用户消息。先在已有ExecutionThreads中查询可信上下文，失败即可在调用模型和修改待处理历史前退出；查询完成关闭Session后，才把同一上下文交给模型可见工具选择与Runtime执行。取消等待不代表数据库线程停止，必须由现有执行作用域等待它结束再释放占用，不能另建未跟踪的后台查询。

项目证据：test_chat_tool_context.py 7条，相关流式9条、取消/断线生命周期4条分组通过。local流式入口已装配，账号流式保持无文件能力；普通/chat仍直接文本调用。真实隔离数据库到临时文件及消息/Run事件持久化通过，模型模拟，未做浏览器和真实模型验收。


## 为什么限量目录枚举不能先完整排序再截断？

复习优先级：高。参考答案已整理、尚未模拟。

完整排序需要先扫描全部目录，输出上限不等于扫描和内存上限。当前只获取最多201项，返回200项，以额外一项确认truncated；仅排序已获取子集，因此不承诺全局字母顺序前缀或稳定分页。子项stat(follow_symlinks=False)只分类，不读链接目标或正文；断链可正常报告为symlink，特殊文件为other。scandir迭代器和调用方目录fd分别负责关闭。

目录变化检测用于发现常见并发变化，子项消失不能悄悄跳过后声称完整。打开路径逐段O_NOFOLLOW与dir_fd仍不构成挂载/并发搬移隔离或原子快照；列出名称也不意味着名称符合后续工具路径协议或已经获得访问许可。

证据：test_workspace_listing.py 26条通过（1.11s），真实限量迭代、链接/FIFO、替换及资源清理，3条隔离PostgreSQL授权；仅服务完成，目录工具注册尚待下一课。


目录工具接入补充（2026-09-20）：list_directory已注册为requires_context工具，参数只接受relative_path且默认“.”，不允许模型扩大递归/数量能力。成功结果保留truncated，不把部分结果伪装成完整目录或错误；后续读取仍重新授权。test_list_directory_tool.py新增31条，相关回归77条通过（0.71s），受控模型根据目录Observation选择文件后读取，底层服务模拟；此前“目录工具注册尚待”现已完成。参考答案已整理、尚未模拟。


跨目录查找补充（2026-09-21，复习优先级高，参考答案已整理、尚未模拟）：递归需要整次调用共享扫描预算，不能让每层各自重置上限；返回50项也不能限制无匹配时的扫描量。当前分别限制2000个检查条目、50个结果、8层深度及4096字符路径。深度或路径限制只跳过分支，全局扫描/结果预算耗尽停止搜索；truncated只表示覆盖或返回不完整，不保证存在更多匹配。只排序有限结果，不提供稳定分页。子项无跟随stat后使用父dir_fd打开目录并核对身份，finally关闭子fd，ExitStack关闭祖先，异常不冒充空搜索。起始解析允许项目内链接，递归子项链接不跟随；目录搬移/高权限并发变更仍不属于该服务的隔离保证，也没有硬超时。证据：workspace_find.py及test_workspace_find.py，55条新增和直接相关分批共144条通过，含授权先于扫描及查询Session关闭验证。


## 单文件搜索如何同时限制结果数量并保持定位准确？

复习优先级：中。参考答案已整理、尚未模拟。

先复用已有256KiB受限读取，避免再实现一套权限和打开逻辑；查询按字面量、区分大小写，拒绝空字符串/换行/NUL。只将CRLF/CR/LF作为行边界，每行只返回第一次命中，行列号从1开始且列号按Python字符而非UTF-8字节或显示宽度计算。

最多50匹配行，发现第51行才标记整体truncated；每个片段最多200字符，匹配前最多40字符、查询最长128，保证完整匹配落在片段内。snippet_start_column保留片段原始位置，snippet_truncated与结果集截断独立。10000字符正文预算不等于序列化JSON或Token预算；无匹配是正常空结果，读取失败必须传播，不能吞成“找不到”。

证据：test_workspace_search.py 43条通过（1.07s），含Unicode坐标/长行/边界/错误传播和3条真实文件及隔离数据库集成。搜索工具适配及注册现已完成；新增45条工具测试，直接相关回归共122条通过（0.91s）。受控模型根据目录Observation选择文件并返回搜索行号；身份来自服务端上下文，模型不能扩大搜索能力。不提供正则、递归搜索或原子快照，PC闭环现已完成4组：三工具串联、刷新无重放、越界与未绑定失败，真实隔离数据库/文件，模型决策受控。工具失败与整轮完成分别验证；不将模拟决策通过视为真实模型效果。


## 命令参数数组与结果校验能否代替 Sandbox？

复习优先级：高。参考答案已整理、尚未模拟。

不能。argv数组保留参数边界，避免自行拼接命令字符串，但sh -c或解释器仍能执行任意代码；相对cwd仅为语法与定位输入，不限制进程访问范围。模型请求只提供argv和相对工作目录，身份、环境、超时与输出策略由服务端控制。后续还须实现执行授权、Sandbox和进程树停止证据。

结果要区分已退出、超时、取消与启动失败：非零退出可表示测试失败，不等于无法启动；POSIX信号退出可能为负值。启动失败必须有固定分类，不能同时声称存在退出码或进程输出。超时/取消状态本身不证明子进程已停止，执行器需先清理。原始输出按字节限制捕获，展示字符串按字符限制；不能先无限读取再用Pydantic拒绝。stdout/stderr分别记录截断，丢弃输出后仍需排空管道，避免子进程被管道阻塞。

项目证据：command_contracts.py与test_command_contracts.py，120条专项通过（0.10s），只验证数据契约，尚未执行命令或实现Sandbox。取舍是先固定数据边界再接运行机制，不把模型可表达的请求当成已授权的动作。


输出边界补充证据：command_output.py及41条专项（0.05s）验证有限字节前缀、所有UTF-8分块位置和双截断。统一解码避免多字节字符跨块产生错误替换；捕获截断造成末尾不完整字符时明确使用U+FFFD。非法编码替换本身不计为额度截断。达到上限不等于已经丢弃字节，必须收到额外数据才标记；stdout/stderr独立保存，finish后拒绝写入。当前只完成内存缓冲，真实管道排空与进程回收仍待实现。


异步排空补充证据：command_stream.py及31条专项（0.06s）验证固定4096字节读取、额度耗尽仍读到EOF、异常与取消传播。短块不是EOF；不能在finally返回部分结果，否则会掩盖取消/失败。read在已有缓存时可能立即返回，循环显式sleep(0)让其他任务和取消得到调度；测试用有限立即返回源验证这一点。单路读取者不拥有进程或流的关闭权，双路并发收尾和进程树终止尚未实现。参考答案已整理、尚未模拟。


双路收尾补充证据：command_capture.py及12条专项（0.05s）验证TaskGroup同时排空stdout/stderr，避免串行等待造成管道互相阻塞；普通失败取消兄弟读取任务并等待收尾，整体取消等待两个读取任务退出，两路失败保留ExceptionGroup。事件屏障验证真实并发与收尾等待，终态无遗留读取任务。子任务CancelledError不作为普通失败触发兄弟取消；不向外暴露内部任务句柄。读取生命周期收尾不代替进程树终止，尚未启动外部命令。参考答案已整理、尚未模拟。


环境白名单补充：从空映射加入固定PATH/语言环境和执行器提供的目标HOME/临时目录，比复制宿主环境后删除已知密钥更不易遗漏新增变量；不接受模型env覆盖，启动时仍必须完整传入该映射，不能再与os.environ合并。目录仅做语法校验，容器内路径不能混用宿主挂载源路径。固定PATH不是可执行程序白名单，改变HOME也不能阻止主动访问宿主文件；文件、网络和进程隔离仍需Sandbox。command_environment.py及62条专项（0.05s）验证精确字段、宿主不继承、无环境读取/文件系统调用及独立字典；实际执行后端尚未实现。复习优先级：高；参考答案已整理、尚未模拟。


Sandbox最小运行证据：独立Compose与scripts/verify_sandbox.py三组真实专项覆盖非root、capabilities清零、禁止提权、默认seccomp、只读根、限额tmpfs、禁用外连、cgroup及主进程白名单环境；不挂载宿主项目或Docker Socket。网络验证不能只数接口名，本机内核有DOWN状态隧道接口，应检查当前网络命名空间状态与外连行为。停止容器后daemon报告停止/Pid=0并拒绝exec，覆盖忽略SIGTERM的独立会话子孙进程；只依赖父PID消失不够。重启tmpfs清空。边界：可信daemon下的最小验证，不等于逃逸防护证明或通用执行器已完成；模型工具、项目挂载、daemon失联及恢复仍待实现。复习优先级：高；参考答案已整理、尚未模拟。


创建参数分层补充证据：sandbox_spec.py及44条专项（0.05s）验证服务端固定Docker选项在镜像前、容器参数在镜像后，env用-i/--并限制程序首项为不含等号的绝对路径，防止启动参数被解释成额外环境赋值。镜像摘要还需批准列表，摘要格式正确不代表已授权使用。可变CommandRequest需重新校验并复制到不可变tuple。执行token生成名称和标签，但不替代容器ID/归属确认；创建操作尚未接入，不能声称已处理提交后响应丢失。参考答案已整理、尚未模拟。


创建身份解析补充证据：sandbox_identity.py及95条专项（0.07s）核对完整容器ID、inspect单元素对象、名称、执行标签、批准镜像引用和created/Running=false/Pid整数零。严格拒绝重复JSON字段、非标准数值及超长响应，允许无关额外字段；失败输出固定安全文案。无法确认创建不等于创建未发生，不能据此盲目重试或删除；快照不代替后续HostConfig/挂载核对和启动授权。当前只完成纯解析，真实Docker调用及不确定结果恢复仍待实现。参考答案已整理、尚未模拟。


Docker客户端生命周期补充：为什么超时后不能直接返回或重试create？外层取消可能发生在进程创建已开始但句柄尚未返回时；shield保留启动任务，finally取回句柄、kill、持续排空管道并wait，重复取消延迟至收尾完成后传播。管道EOF也不代表进程退出。10秒限制正常等待而非清理总耗时；客户端被停止不等于daemon撤销操作，create可能已经成功，需保留token供后续确认，不能盲目重试/删除。固定可执行程序、socket和白名单环境是宿主控制边界，不开放模型任意宿主命令。项目证据：docker_client.py新增33条专项，含5条真实OS子进程/管道测试；直接相关合计45条通过，Docker版本及缺失容器inspect只读检查通过。真实创建/daemon响应丢失恢复尚未实现。复习优先级：高；参考答案已整理、尚未模拟。


创建装配补充：为什么把规格构造放在副作用try之外？调用前校验失败能明确本次未发起create，但不能推断历史同token目标不存在。开始调用后，create失败、响应ID损坏、inspect失败或身份不一致统一报告“创建结果未确认”，不返回半成品，也不自动重试/删除。取消继续传播，调用方必须提前持有token；只有内存token不能支持进程崩溃恢复。代码证据sandbox_creation.py与新增35条专项（含2条真实Docker），直接相关常规共66条通过。真实创建后人工注入响应丢失验证容器仍在且没有重复create；测试核对身份后按ID清理。边界：未实现恢复服务或完整HostConfig授权，参考答案已整理、尚未模拟；复习优先级高。


只读核对补充：创建ID丢失时可以从inspect取得候选，但必须继续校验完整ID格式、名称、执行/用途标签、批准镜像引用和created状态。候选ID与自身相等不构成独立证据；未知ID路径只能确认当前匹配目标，不能证明目标从未被宿主高权限程序替换。已知原ID时必须保留约束，不能在不匹配后降级发现。通用Docker失败不足以区分不存在和连接失败，继续报告未确认，不自动重试create或删除。sandbox_reconciliation.py与身份恢复函数新增50条常规专项，直接相关共145条通过，另1条真实Docker验证两路径、错ID拒绝无副作用及删除后未确认；临时资源已清理。参考答案已整理、尚未模拟；复习优先级高。


显式清理补充：为什么删除要用完整ID且不能把超时当作失败未生效？删除前按原token/完整ID重新确认created身份，随后非强制rm只作用于该ID，避免名称被替换后误删新对象；核对删除回执并成功查询缺失后才返回完成快照。查询与删除非原子，仍有高权限并发修改边界；不强制删除或自动停止。删除超时可能已经生效，保留token/ID并报告清理未确认，不自动重试。已有目标不存在也不能靠通用inspect错误宣称幂等成功。sandbox_cleanup.py新增41条常规专项，直接相关共74条通过，另2条真实Docker验证正常删除、错ID拒绝、删除后响应丢失与重复请求无再次rm。参考答案已整理、尚未模拟；复习优先级高。


执行配置复核补充：env -i为何不能替代Config.Env检查？入口env自身启动时已收到镜像/容器环境，最终命令白名单只在入口执行后生效。因此同时固定批准镜像Env基线并精确比较Cmd中的-i/--/环境赋值/原命令。环境顺序可不同，但列表排序比较保留重复项，不能用集合掩盖重复变量。身份与配置从同一份严格解析响应核对；用户/入口/工作目录和交互布尔也要确认，额外健康检查作为其他自动执行入口拒绝。sandbox_execution_policy.py新增71条纯校验及1条真实未启动容器测试通过；只在响应副本注入环境字段验证拒绝。HostConfig/资源隔离及启动尚未接入。复习优先级高；参考答案已整理、尚未模拟。


隔离配置复核补充：创建argv表达意图，inspect表达daemon返回的配置；对同一响应先核对执行身份，再核对HostConfig网络/权限/只读根/资源及挂载。MemorySwap是内存加swap总额，与正Memory相等才表示禁用swap；不能把bool/float或字符串当作整数限额。HostConfig.Tmpfs声明和顶层Mounts摘要需要同时检查，摘要空不能代替声明，禁止其他挂载入口及镜像匿名卷。测试218条纯校验及1条真实created容器通过，恶意变化只注入响应副本，临时容器已清理。范围只覆盖显式列出的策略，不证明内核已实施或消除检查后修改窗口；启动与停止生命周期仍待接。复习优先级高；参考答案已整理、尚未模拟。


### 只读根文件系统能代替 bind 挂载只读吗？

复习优先级：高。参考答案已整理、尚未模拟。

不能。根文件系统与 bind 挂载是独立边界：规格显式声明 readonly、rprivate、非递归；核对 HostConfig.Mounts 中 Type/Source/Target/ReadOnly/BindOptions 与顶层 Mounts 的 Type/Source/Destination/RW/Propagation，同时保留网络、权限、资源、WorkingDir、tmpfs 和匿名卷检查。不能从 inspect 读出来源再把它当作期望值，期望来源必须由服务端持有的样例确定。默认入口继续拒绝 bind，样例分支只允许唯一固定目标。

如何证明写入失败来自挂载？样例置于宿主 0700 私有父目录，挂载子目录允许容器访问；无敏感内容的探针文件设置 0666，实机要求读取成功且写入返回 EROFS，并核对 statvfs 只读标志。EACCES 只能说明普通权限不足，不能单独证明只读挂载。宿主字节/inode/mtime 不变是副作用证据，不能代替容器返回的退出事实。

失败场景：目录路径未变但 inode 已替换、任一路径段变为链接、错误源、额外挂载或重复目标都拒绝；同字段值的伪造样例对象也不能冒充本进程登记。来源检查与 Docker 操作不是原子事务，当前限定服务端自建且无外部写者的样例；没有消除恶意同 UID/高权限主体造成的 TOCTOU，也没有提供重启后的执行权。容器确认缺失前不能释放样例，连接失败不等于缺失，部分清理失败保留现场。

项目证据：`sandbox_sample.py`、`build_sample_sandbox_create_spec()`、`sandbox_isolation_policy.py`；自动测试见 `apps/api/tests/runtime/sandbox/test_sandbox_sample.py` 和 `test_sandbox_sample_probe.py`，实机入口见 `scripts/verify_sandbox_sample.py`。最新验收范围与未验证边界只维护在 [交接](../../../LEARNING_HANDOFF.md#最近验收摘要)。

### 为什么 Task 样例 ready 仍不能直接作为命令来源？

复习优先级：高。参考答案已整理、尚未模拟；本课由教练单课授权实现。

ready 是查询时的登记快照，不预留来源，也不证明当前会话仍属于原 Task。内部命令来源入口只接服务端身份与当前会话，从数据库关系推导 Workspace/Task；随后通过 TaskSampleBindings 独占借用、重新核对归属与绑定，并再次查询会话关系，拒绝两次查询间可观察的迁移。授权事务在使用来源前关闭，不持数据库锁等待文件操作。两次读取不是原子租约，无法检测 ABA 或阻止之后的外部写者。

正常退出只归还占用，不删除 Task 来源；异常/取消由底层保留目录、封锁登记，同时释放 busy。来源视图退出或进程变化后不能再读取路径，但无法撤销已复制的 Path，所以可信调用方仍必须保证后台使用不越过作用域。此来源视图不是公开授权凭证，也不直接执行 Docker；模型命令通过后续独立快照桥接。Task 样例 0700/0600 与 Sandbox 的 UID 10001 不能直接互用。现有桥接在借用内通过逐级无链接目录描述符读取固定 example.txt，检查根目录身份、文件类型/单链接/权限和读取前后版本，实际最多读取 256 KiB 加一个判超限字节。归还正常结束后才创建独立快照，不放宽原 Task 权限。快照文件 0444 不替代容器 readonly 挂载，原 EROFS 探针继续使用 0666。

所有权取舍：归还失败时尚无目标；复制失败时 Task 已归还，异常只携带目标部分现场且不登记成功。正常目标与 Task 独立，Task 后续改写/关闭不影响快照，未来容器未确认缺失时须保留目标而非继续借用 Task。字节保留 BOM/CRLF/非 UTF-8/NUL，不以文本解码或截断伪造完整快照；身份及 mtime/ctime 核对不保证恶意并发写入的原子一致性。证据：[来源读取](../../../apps/api/tests/runtime/command/test_task_command_source_read.py)、[快照工厂](../../../apps/api/tests/runtime/sandbox/test_sandbox_snapshot.py)、[PostgreSQL 桥接](../../../apps/api/tests/runtime/sandbox/test_task_sample_snapshot.py)。本次桥接核心由学习者参考实现，测试由教练补齐；参考答案已整理、尚未模拟。

证据：[可信来源借用专项](../../../apps/api/tests/runtime/command/test_task_command_source.py)，真实隔离 PostgreSQL 与临时目录，覆盖绑定/会话变化、独占、异常封锁和作用域失效。


Task 快照执行补充：`run_task_sample_command` 将独立快照交给共享 Sandbox 生命周期，原 Task 借用不会持续到容器退出。同步准备在工作线程运行，asyncio 取消无法停止线程；因此准备任务受 shield 保护，重复取消也先等待完成，收回完整快照或部分创建定位，再传播取消且不启动容器。只等待线程、不读取其结果会丢失资源所有权。准备尚未尝试 create 的恢复与“已知容器 ID”清理是不同边界，不能混用。

本课由教练按单课授权实现。证据：[编排与准备取消专项](../../../apps/api/tests/runtime/sandbox/test_task_sample_command.py)、[真实 PostgreSQL/Docker 专项](../../../scripts/verify_task_sample_command.py)。真实链路覆盖快照后原文件改写、读取/非零退出/超时/取消、create/rm 回执丢失与显式收尾。参考答案已整理、尚未模拟。


Task 快照恢复记录补充（复习优先级高，参考答案已整理、尚未模拟）：为什么要在准备前预留记录？准备线程本身就可能创建完整快照或部分现场，先执行再尝试记账会在容量不足时丢失资源所有权。新 journal 按服务端身份/会话限制内部使用，在纯策略校验后预留最多 16 条；参数转成 tuple，执行从记录构造新请求。成功保存命令 JSON，非零退出仍为 completed；异常/取消先同步保存完整恢复对象，再传播，二者之间没有 await。

关闭仅拒绝新预留，不中断在途工作、不清理目标、不丢弃记录；终态只允许从 pending 写入一次。恢复对象和命令结果均冻结，快照句柄须保留工厂登记的原对象，不能深复制成一个看似相等却无效的新句柄。容量满时拒绝，不按时间或终态自动淘汰尚需处置的资源。作用域匹配不是授权，真正执行仍经过 Task 借用；请求层仍持有 journal，现已新增独立 TaskSampleRecoveryStore 按服务端用户/会话/Run 保存作用域。证据：[43 个记录与编排组合用例](../../../apps/api/tests/runtime/sandbox/test_task_sample_command_journal.py)。


应用级作用域补充（复习优先级高，参考答案已整理、尚未模拟）：请求函数结束不代表资源消失，因此 store 保留 journal 与原快照句柄。获取同 Run 时必须匹配用户与会话，不能仅凭 Run ID 取得资源；同归属复用原对象，不替换已关闭 journal 来绕过预算。关闭作用域要求本 store 持有的原对象，并只封闭新预留；已预留执行仍可补齐终态。每应用最多 32 个作用域，不自动淘汰未知或已完成记录，容量满保守拒绝。

FastAPI lifespan 独立建立 store，退出时封闭登记、释放 app.state 引用，不调用 Docker/文件清理；单进程内存所有权不等于持久恢复，旧引用存在也不能承诺重启找回资源。归属元组用于防内部错配，真正业务权限仍需数据库重新授权。证据：[作用域组合测试](../../../apps/api/tests/runtime/execution/test_task_sample_recovery_store.py)、[应用生命周期](../../../apps/api/tests/runtime/execution/test_task_sample_store_lifespan.py)。


内部工具适配补充（复习优先级高，参考答案已整理、尚未模拟）：工具输出与恢复记录必须分开。成功只序列化现有 CommandResult，非零退出仍返回真实结果；succeeded 为内部计算属性，不额外改写 JSON 契约。身份、会话、Run、store 和绑定服务由冻结内部上下文注入，不进入 CommandRequest Schema。模型参数只描述命令，不能选择宿主目录或资源归属。

失败映射依据阶段事实：准备可能留下部分目标，不能说“无副作用、可重试”；有 command 的清理失败不能冒称命令未执行；无证据异常保持结果未知。新适配的 SafeToolExecutionError 不附带 recovery，仅固定 code/message 可进入公开输出，完整证据已在底层 journal 保存。取消保持取消语义，输出投影失败也不能覆盖已保存的 completed。命令 stdout/stderr 是正文，本层不提供任意内容脱敏保证。证据：[35 个适配组合用例](../../../apps/api/tests/tools/test_task_sample_command_tool.py)。


请求级注册补充（复习优先级高，参考答案已整理、尚未模拟）：能力展示是请求快照，不能把 ready 当作执行许可。本地聊天 ready 绑定 Task 快照执行器，missing 绑定普通无挂载执行器，busy/sealed 不提供命令能力；查询失败不降级。模型与 Runtime 使用同一工具定义，身份、Run 和 store 均由服务端注入，模型 Schema 不变。

为什么执行前再查一次还不够？会话可能在复核后、借用前改绑 Task；必须把请求原上下文传到实际借用入口比较，借用中的原有二次核对也保留。占用变化、迁移或读取失败拒绝执行，不静默换成普通命令。作用域首次执行才分配，请求结束先拒绝新调用，流关闭抛错也在 finally 封闭并保留 journal；关闭不是清理或删除恢复证据。

证据：[请求装配与关闭竞争 16 例](../../../apps/api/tests/tools/test_request_task_sample_tools.py)、[真实 PostgreSQL 聊天路由集成 5 例](../../../apps/api/tests/chat/test_task_sample_chat_integration.py)。集成验证 NDJSON、Run/事件持久化及复核后迁移拒绝，模型与 Docker 为受控替身；不等于浏览器、HTTP 传输或真实 Docker 联合验收。

联合证据：[PC Task 样例命令专项](../../../apps/web/test/browser/run-task-sample-command.py)通过真实浏览器/BFF/API/PostgreSQL/Docker 验证读取、退出码 7、删除回执丢失和取消，刷新无重放。非零退出仍是 completed 工具结果，UI 显示命令失败；实际删除后响应丢失仍是 unconfirmed，不能用测试夹具知道“已删除”改写生产记录；取消先观察真实 attach 就绪，再确认内部 stop_confirmed，不用 UI 的 aborted 推断容器停止。请求关闭后 journal/快照仍在，测试拥有者随后显式清理，原 Task 字节与权限不变。模型与取消通知受控，不证明 Redis 通知、真实模型或其他断流模式。参考答案已整理、尚未模拟。



### 为什么命令失败后不能无条件删除样例目录？

复习优先级：高。参考答案已整理、尚未模拟。本次编排由教练按单课授权实现，不代表学习者已独立掌握。

文件系统与 Docker 没有共同事务。create 响应丢失时，daemon 可能已经创建容器但调用方没有 ID；取消或 CLI 退出也不证明容器已停止。样例拥有者必须保留 token、已知完整 ID、请求快照、来源句柄及各阶段已确认事实，不能在 finally 无条件释放仍可能被容器使用的来源，也不能盲目重试 create。

正常路径先完成 attach 排空与退出核对，再重读归属和 exited/Pid=0，按完整 ID 非强制删除、成功查询缺失，最后清理样例。非零退出也有完整命令事实，可以正常释放资源；反过来，输出成功但删除失败，应保留 command 和“清理未确认”，不能把命令改成未执行。停止确认、删除尝试、缺失确认、样例清理是不同证据。异常路径只完成执行器拥有的连接/读取与必要停止收尾，后续恢复需再次授权；创建前验证、attach 前验证和启动前验证分别覆盖不同等待窗口。

样例工厂中途失败也要保存独占创建后的现场位置；mkdir 名称碰撞不能认领旧目录。进程内记录不是持久 checkpoint，旧快照不能授予新的执行或删除权。当前实现以调用方保存恢复信息换取不盲目回滚的保守策略，代价是失败后可能保留资源；已有内部只读诊断及显式清理。显式清理先校验完整登记来源，再按完整 ID 重核容器归属及 created/exited 状态，非强制删除并成功查询缺失后才释放来源；运行中不自动停止，未知 ID 不按名称接管。删除回执丢失或取消保留来源与 delete_attempted，拥有者再次明确调用仍须重新查询，不能信任旧 absent 标志。文件清理失败保留已确认的容器缺失事实；部分来源现场拒绝盲重试。

取舍：清理入口只供原内部拥有者使用，要求一个样例仅供原命令使用且没有并发执行/外部写者；恢复对象不是公开授权凭据。同事件循环清理互斥只防重复调用，不是跨进程租约。容器不存在也不证明任意其他容器没有使用该来源，故不支持将来源转借或复制记录接管资源。证据：[显式清理专项](../../../apps/api/tests/runtime/sandbox/test_sandbox_sample_cleanup.py)、[真实 Docker 六场景](../../../scripts/verify_sandbox_sample_cleanup.py)。参考答案已整理、尚未模拟。

证据：`sandbox_sample_command.py`、`test_sandbox_sample_command.py`、`test_sandbox_sample_execution.py` 与 `scripts/verify_sandbox_sample_command.py`。实机通过 attach 读取/双路截断/非零退出/超时/取消；创建和启动响应丢失在真实操作成功后本地注入，不冒称实际断网。


### 为什么失败恢复诊断需要分别报告容器与来源状态？

复习优先级：高。参考答案已整理、尚未模拟。本课由教练单课授权实现，不标记独立掌握。

文件系统和 Docker 的读取不在同一事务中：容器可查但来源不可读，或者来源身份匹配但 daemon 不可达。分侧报告能保留已取得的事实，又不把异常降级成“资源不存在”或“可以重试”。身份匹配只说明目录/文件身份与结构符合记录，不是文件内容快照；它也没有重验运行中容器的完整隔离策略，不能作为执行或删除权限。

已知完整 ID 只按该 ID 查询，哪怕同名容器已重建也不能接管。未知 ID 可从原名称发现 created 候选，但须核对完整身份后按 ID 复核；正在运行或已经退出的同名目标不能凭名称认领。成功列举确认的 absent 不等于历史删除成功，更不补造命令退出结果。旧 stop_confirmed/container_absent 与当前观察分开保存，不改原恢复记录。

来源路径明确不存在才报 missing；链接/身份/声明结构变化报 identity_changed；权限或未知读取故障报 unconfirmed。没有本进程登记的对象报 unregistered，不凭原始路径探测宿主目录。取消继续抛出取消异常并保留恢复上下文，不返回一份看似完整的“未知”结果冒充成功查询。

证据：`sandbox_sample_reconciliation.py`、`observe_sandbox_sample()`、`test_sandbox_sample_reconciliation.py` 和 `scripts/verify_sandbox_sample_reconciliation.py`；实机比较每个只读观察区间的文件元数据/字节、登记及容器状态/配置不变，同名重建也保持旧 ID 边界。权限/daemon 读取失败用本地注入验证，不能声称真实网络故障已验收。


停止确认补充：停止回执与客户端退出都不是容器停止证据。先按已知完整ID和原执行标签核对归属，再发固定SIGTERM/2秒宽限stop，重新inspect确认一致created或exited、Running=false/Pid整数0且Paused/Restarting/Dead=false。已停止目标无需发stop；运行中必须Running=true/Pid>0；中间态保守拒绝。停止阶段允许exited不应放宽创建检查，故共享身份解析与阶段状态判断分离。新服务53条常规、直接相关181条通过，1条真实Docker验证忽略SIGTERM的父子（子独立会话）停止后daemon报告及exec拒绝。超时/取消仍未确认，不自动删除或重试，运行资源释放需依赖后续收尾证据。优先级高；参考答案已整理、尚未模拟。


启动收尾补充：请求先复制，防止await期间调用方修改原参数使停止核对失效；启动前复核同一响应的身份/执行/隔离及created，start后查询running或快速exited，不能将回执当成运行证据。start_attempted放在await前，失败/取消后独立shield停止核对，重复取消延迟到收尾完成后继续传播。stop_confirmed与启动结果独立：停止确认不意味着命令从未执行。前置复核失败不能随意停止未知目标；不自动重试start/删除。新增30条常规，直接相关63条通过；4条真实Docker覆盖运行/快退/响应丢失/取消收尾。启动成功后运行责任交给调用方，不等于完整命令超时或输出采集。优先级高；参考答案已整理、尚未模拟。


退出结果补充：非运行/Pid=0不足以证明命令执行过，created也满足停止条件，只有一致exited才解释退出码。退出码0且OOMKilled=false、Error为空才标记进程结果成功；非零退出是已确认失败，不等于结果未确认。137可由程序主动返回，不能据此推断OOM；143也不能证明用户取消，编排层需保留原因。daemon原始Error可能泄漏路径/参数，只公开布尔标记。sandbox_exit.py新增75条纯解析及3条真实0/7/137退出验证通过；OOM实际耗尽未测，只覆盖解析。未采集输出不能伪造空stdout/stderr。优先级高；参考答案已整理、尚未模拟。


attach分帧补充：网络read边界不是消息边界。非TTY Docker原始流以8字节头标识通道和大端uint32正文长度，增量解析只缓存未完成头与剩余长度，正文分块送入有界通道缓冲；保存额度耗尽仍必须消费后续字节以保持帧对齐。跨帧UTF-8最后按通道统一解码。项目单帧1MiB上限不同于协议uint32范围；非法头或半帧EOF后实例永久失败，不返回部分成功。finish只能证明帧边界完整，不能证明连接正常结束或输出全部到达，不能用于TTY或CLI已解复用流。docker_attach_parser.py及91条纯字节专项通过；HTTP/异步读取/启动协调未接。优先级高；参考答案已整理、尚未模拟。


文件查找工具接入补充（2026-09-21）：模型Schema只含query和relative_path，严格类型/额外字段拒绝；身份由可信context注入，服务重新授权。无context时请求能力集合不包含工具，强行调用产生unknown_tool；直接调用已注册定义仍由context检查拒绝。空结果与截断是正常Observation，不能转换为“完整搜索无结果”；固定错误码不反射异常正文。test_find_files_tool.py新增54条，相关148条分批通过，受控模型消费查找路径后调用读取工具。底层服务模拟，无浏览器/真实文件授权链路重验；该链路留下一课PC验收。复习优先级高，参考答案已整理、尚未模拟。


文件查找PC证据补充（2026-09-21）：find-tools.mjs/find_model.py在真实浏览器/BFF/API/授权/临时文件/隔离PostgreSQL中完成嵌套查找→读取、空匹配、51匹配截断、越界与未绑定5场景。双PC宽度、刷新无重执行及持久化答案/工具结果/错误核对通过，临时资源清理。模型决策及回答受控，只证明链路和事实传递，不证明真实模型始终准确解释截断。参考答案已整理、尚未模拟。


## 为什么文本替换预览要分离完整新内容与审阅Diff？

复习优先级：高。参考答案已整理、尚未模拟。

精确替换先验证旧文本非空且唯一命中；第二次find从首次位置+1开始，以拒绝aaa中aa这样的重叠歧义。不能用默认非重叠count判断唯一，也不能模糊匹配或自动选首处。256KiB输入/输出和4000行约束Diff计算规模，16384字符只限制展示；截断Diff不代表完整修改，不能据其完成审批。updated_content必须独立完整保留。

保留CR、LF、CRLF与EOF事实；审阅表示使用每行JSON转义，避免换行差异被隐藏，因此不是可直接git apply的补丁。预览不等于写入，原文内容基线、授权复核、审批和并发冲突仍须后续实现。规模限制不是硬CPU超时。

证据：workspace_edit_preview.py/test_workspace_edit_preview.py，新增65条纯内存通过（0.11s），覆盖删除、Unicode字节、重叠、多种换行、末尾换行变化、输入/修改后超限、4000行边界、Diff精确及分块边界、不可变和重复调用；无文件写入。


统一 Diff 解析补充（复习优先级高，参考答案已整理、尚未模拟）：为什么不能只校验旧行号？前一块增删会改变后续新位置，必须同时验证旧原文切片和新内容累计位置。零行范围表示行间间隙，转换零基索引时不能按普通起点减一；重复插入同一位置要求合并，拒绝重叠、倒序和模糊偏移。

无末尾换行标记作用于紧邻数据行，不计入行数；不能把无换行新行与后续未修改内容直接拼接。失败时不能返回已经应用前几块的部分候选，全部匹配和输出预算通过才构造结果。补丁、原文、候选及块数分别有界，字符数限制不替代 UTF-8 字节数。首版明确拒绝 CRLF、Git 扩展头、文件新增/删除和多文件；这是严格子集，不声称兼容任意 git diff。路径语法与候选内容都不代表业务授权或写入许可。

证据：[补丁专项](../../../apps/api/tests/workspace/edits/test_workspace_unified_patch.py)113 例通过，包含一例 2070 组 difflib 生成补丁与目标文本的往返核对，以及失败不构造候选、纯内存调用约束。核心由学习者参考实现完成，教练补测试验收，尚未模拟面试。


授权预览基线补充（2026-09-21，复习优先级高，参考答案已整理、尚未模拟）：预览与SHA-256应来自同一次授权读取，不能生成预览后重读文件计算摘要，否则可能把旧修改计划绑到新内容。严格UTF-8解码后未做归一化，重新编码保留BOM、CR/LF/CRLF及末尾换行；摘要对应原文而非修改后内容。数据库事务和文件句柄在Diff计算前结束；摘要仅描述读取内容，不能代替访问授权、审批或对象身份检查，预览之后修改由未来写入阶段冲突检测处理。workspace_file_preview.py/test_workspace_file_preview.py新增28条，相关128条通过（1.89s），包含读取后模拟外部修改、单次读取、实际字节摘要、文件不变及隔离PostgreSQL授权拒绝。


授权补丁预览证据：同一份原文同时驱动补丁匹配、候选计算、审阅 Diff 和 SHA-256；不能按补丁文件头另读一个目标，也不能在候选生成后重读文件计算基线。先使用服务端 Task 上下文授权读取，再将读取服务返回的规范相对路径与文件头精确匹配。解析/审阅失败不返回成功对象，截断只影响 Diff，不影响完整候选；基线不是文件锁，也不能保证预览时磁盘未变。

[授权补丁专项](../../../apps/api/tests/workspace/edits/test_workspace_patch_preview.py)33 例通过，含 14 个隔离 PostgreSQL/真实文件用例，验证读取后外部改写不会混用摘要，计算前事务关闭、SQL 只读、正常文件字节/权限不变，以及越权/越界/编码/超限/冲突拒绝。复用上一课补丁核心，本课组合服务由教练获单课授权实现；工具注册与实际写入尚未接入。复习优先级高，参考答案已整理、尚未模拟。


预览工具适配补充（2026-09-21）：内部结果和模型可见结果需分别设计，不能直接asdict整个预览对象，否则完整updated_content会重复进入模型上下文。公开status=preview_only及有限Diff/摘要/字节数；工具成功不等于文件已写入，也不代表批准。模型不能提供身份、基线或approved，new_text必填但允许空表示删除，UTF-8字节限制不能仅依赖字符max_length。安全错误码必须同步白名单，本课实际发现漏文案会触发ValueError，已补齐。test_preview_file_edit_tool.py新增69条，相关172条通过（1.36s），模型/服务受控，无真实浏览器。复习优先级高，参考答案已整理、尚未模拟。


补丁适配补充：模型参数只有 relative_path/patch，严格拒绝额外字段，UTF-8 字节上限独立于字符上限，拒绝 CR/NUL 但不自动修正文法；读取与补丁语义仍由服务验证。可信上下文类型只能防内部错接，不能让模型构造一个相似对象就取得授权。公开预览复用既有字段，仅显式选择 Diff/摘要/字节数；完整候选留在内部，Diff 正文不等于已脱敏。

未知普通异常、未来未识别错误码及投影失败都映射固定不可用，不回显原异常；CancelledError 保持取消。新增安全分类要同时进入白名单并检验全部映射，不能因漏文案制造二次异常。[补丁工具专项](../../../apps/api/tests/tools/test_preview_file_patch_tool.py)68 例通过，包含真实解析/审阅与受控读取组合；旧预览安全映射 18 例回归通过。该组适配测试不证明模型/聊天接入，接入证据见下文。复习优先级高，参考答案已整理、尚未模拟。


补丁请求能力与往返证据：注册使用同一 Pydantic 参数模型，requires_context 同时约束模型可见能力和 Runtime 执行；没有上下文或显式空能力集时，伪造工具调用不进入文件服务。类型门禁不替代后续真实数据库授权。同步预览在线程执行，生成器使用同一请求能力集合；成功是 preview_only，不是写入或提案。

[注册往返10例](../../../apps/api/tests/tools/test_patch_tool_registration.py)与[真实聊天服务集成3例](../../../apps/api/tests/chat/test_patch_preview_chat.py)通过，覆盖成功/截断/错误、参数拒绝、文件不变、零提案及事件/消息持久化。测试发现默认 ValidationError.json 会回显 input，现去掉 input/ctx；这不代表 TOOL_CALL_START 中的原始参数也被过滤，也不保证任意错误字段/正文脱敏。集成消费真实 NDJSON 生成器，模型受控，未经过浏览器/BFF/HTTP传输。复习优先级高，参考答案已整理、尚未模拟。


预览PC证据补充（2026-09-21）：preview-tools.mjs/preview_model.py在真实浏览器/BFF/API/授权/临时文件/隔离PostgreSQL验收成功(BOM/CRLF)、删除预览、歧义拒绝及16384字符Diff截断。Node独立计算SHA-256和UTF-8字节数，每场景后逐文件核对原始字节不变；公开字段无updated_content/宿主路径，双PC宽度和刷新无重放、答案/结果/错误持久化通过。模型决策/回答受控，不证明真实模型措辞质量或批准/写入实现；临时资源已清理。参考答案已整理、尚未模拟。

补丁 PC 联合证据：[浏览器专项](../../../apps/web/test/browser/patch-preview-tools.mjs)通过正常/BOM/LF、删除至空、冲突、截断四场景，真实文件逐轮不变，Node 独立核对摘要/字节数；4个运行、各1次调用、零提案由[数据库夹具](../../../apps/web/test/browser/patch_preview_model.py)独立确认。当前和历史只读卡片均可恢复，刷新无重放，PC双宽度/键盘/截断提示验证通过。模型与取消通知受控，不能推定真实模型质量或已获得批准；预览成功、调用完成、文件写入是不同事实。已有题目证据补充，参考答案已整理、尚未模拟。



### 文件修改提案为什么分两段事务，为什么保存前仍需授权？（2026-09-21）

复习优先级：高。参考答案已整理、尚未模拟。

参考答案：先查询归属并复制内部Task/Workspace编号和绑定目录，关闭Session后读取文件和计算Diff；耗时I/O不占用数据库行锁。保存时新建写事务，按Workspace→Task→Conversation锁序重新检查当前归属，再比对身份和绑定，插入pending提案。仓库只flush，事务由服务提交，成功退出事务后才返回结果；与删除使用相同锁序，Task外键CASCADE使已提交提案随任务删除。

失败场景与取舍：预览期间项目/会话归属或绑定改变、任务被删均不能沿用旧授权；flush成功但commit前失败仍须回滚，不返回成功。文件系统不参与数据库事务，保存提案不能承诺文件基线仍有效，应用前必须重新检查。完整proposed_content单独保存，diff_truncated只描述审阅展示；pending与摘要均不是批准凭据。当前无请求幂等，重复调用会新建提案，提交响应不确定时不能盲目重试。当前无更新入口，并非数据库层强制不可变。

项目证据：test_file_edit_proposal_service.py与test_file_edit_proposal_migration.py共37条，相关共109条分批通过；含真实PG约束/迁移往返、双向行锁竞争、故障回滚、删除级联、外部编辑不覆盖。开发库升级后旧业务数据逐行一致。尚无提案HTTP/工具入口和实际文件写入。


补丁共用存储补充（2026-09-24，高优先级）：为什么不为补丁另建提案表或事务？两种输入都先生成授权的完整候选，再共享 pending 状态、双摘要、Diff、锁序与提交边界；复用应发生在保存流程，不能允许模型绕过预览提交任意候选。保存前仍重新授权与复核绑定；文件系统不参与数据库事务，外部编辑不会被覆盖，也不意味着保存的基线仍有效。绑定字符串复核不能检测变更后又恢复的 ABA 竞态。

[补丁提案专项](../../../apps/api/tests/workspace/proposals/test_file_patch_proposal_service.py)21例及原保存服务18例通过。after_commit 事件注入异常后，独立查询仍看到1条记录，说明调用失败不等于未提交；提交前异常则0条，两个场景均不返回成功或自动重试。该注入验证提交后异常语义，不冒称真实网络故障；当前没有请求幂等。测试还验证完整候选、截断 Diff、双摘要、授权查询兼容、并发变更拒绝和文件不变。参考答案已整理、尚未模拟。

提案查询补充（2026-09-21）：为什么不能仅凭proposal_id查记录？随机编号只提供定位，查询应同时约束Task/Workspace编号和Workspace、Conversation的用户归属，并通过实际外键连接提案；不存在/无权访问返回相同安全错误。单条SELECT使授权与结果使用同一语句快照，但不是后续应用的授权凭证。显式投影公开列避免加载bound_root/proposed_content，冻结普通结果避免Session关闭后的隐式查询。Diff仍包含编辑片段，不能宣称内容完全脱敏。文件删除或解绑后可查询历史提案，应用时仍须重新授权、复核绑定和内容基线。test_file_edit_proposal_query.py新增15条，相关33条通过，真实PG验证跨用户/项目/任务/会话边界与只读事务。复习优先级高，参考答案已整理、尚未模拟。


提案工具补充（2026-09-21）：为何保存异常不能统一表述为“未保存”？连接可能在提交确认阶段失败，工具映射proposal_save_unconfirmed并禁止自动重试提示；超时也不保证同步事务停止。当前无请求幂等，模型描述只是协议约束，不能当作重复写入防护。创建工具与纯预览工具分开，创建成功直接返回已提交回执，不追加查询使已成功保存被查询失败掩盖。79条新增专项、直接相关230条通过，含真实工具保存/授权查询、越权拒绝、预览不保存及文件字节不变；模型往返受控，无浏览器证据。复习优先级高，参考答案已整理、尚未模拟。


补丁提案工具补充（2026-09-24，高优先级）：保存已提交，但构建JSON回执失败时应如何报告？必须返回“结果未确认，可能已保存”，不能说未保存或自动重复创建。适配器不拥有事务，不在提交后追加查询；成功回执显式选取字段并校验实际 pending 状态，完整候选与宿主目录不进入回执。参数模型复用严格补丁校验，身份仅取可信上下文，但上下文类型门禁仍不能替代底层授权。

[适配专项](../../../apps/api/tests/tools/test_create_file_patch_proposal_tool.py)73例与[真实数据库专项](../../../apps/api/tests/tools/test_file_patch_proposal_integration.py)9例通过。提交前失败0条，提交后确认/投影失败1条，均返回安全未确认错误；取消原样传播。故障注入只替换工具序列化出口，不能替换全局JSON函数干扰预览后误称提交后失败。测试未包含真实网络或模型往返，工具尚未注册；参考答案已整理、尚未模拟。

补丁提案模型往返补充（2026-09-24，高优先级）：能力可见不等于授权；模型和Runtime必须使用同一请求工具快照，缺少上下文或显式空能力时都不得进入保存。工具错误作为Observation返回模型，不能将RUN_FINISHED/done误读为提案创建成功。超时或提交确认丢失可能已保存；工具说明要求不自动重试，但没有幂等键与执行门禁就不能保证真实模型不会再调用。

[注册专项](../../../apps/api/tests/tools/test_patch_proposal_registration.py)10例与[真实聊天生成器专项](../../../apps/api/tests/chat/test_patch_proposal_chat.py)5例通过。提交后事件注入故障时，模型收到proposal_save_unconfirmed，运行持久化工具错误且数据库保留1条pending，文件不变；成功回执编号对应数据库提案。请求参数拒绝、无上下文及空能力快照均不执行保存。模型受控，不包含浏览器/HTTP传输或真实模型遵循说明的证明；参考答案已整理、尚未模拟。

提案PC证据补充（2026-09-21）：proposal-tools.mjs经真实浏览器/BFF/API/Runtime完成成功、删除至空、纯预览、歧义拒绝及截断5场景。刷新不重发，并在隔离数据库最终核对恰好3条pending及完整内容/双摘要/授权查询，失败与预览无记录；每场景所有源文件字节不变。模型受控，不证明真实模型措辞或后端请求幂等。双PC宽度与截图视查通过，临时资源清理。参考答案已整理、尚未模拟。


补丁提案PC证据补充（2026-09-24）：[浏览器专项](../../../apps/web/test/browser/patch-proposal-tools.mjs)真实链路验证正常、删除至空、冲突、截断和提交后未确认。未确认时UI只显示安全错误，不能凭RUN完成伪造成功回执；[数据库核对](../../../apps/web/test/browser/patch_proposal_model.py)仍确认该场景有1条pending，五场景合计4条，无刷新重复创建。当前/历史详情均按当前可信任务范围读取，公开字段不包含完整候选或宿主目录；原文件逐轮不变，PC双宽度和键盘操作通过。模型与提交后异常受控，不证明真实网络恢复或幂等；参考答案已整理、尚未模拟。

提案HTTP边界补充（2026-09-21）：服务status:str与响应Literal[pending]之间应通过model_validate校验真实字段，而不是cast掩盖类型或硬编码pending伪造事实。错误状态触发安全500，公开字段显式投影，未知/无权访问统一404且no-store。GET的业务语义只读不等于整条依赖链无任何写SQL：本地身份仍含幂等INSERT初始化，提案查询才是单SELECT。24条新增HTTP专项及相关81条分批通过；参考答案已整理、尚未模拟。


提案详情BFF补充（2026-09-21）：200且JSON可解析仍需核对workspace/task/proposal三标识、状态及有界Diff；显式投影避免私有扩展字段泄漏。本机凭证只能从服务端配置生成，不能透传浏览器的同名头。取消预算覆盖fetch和正文读取，错误流主动cancel，不盲目重试；取消后的502只是代理安全失败，不代表后端执行已停止。file-edit-proposal-route.test.ts新增56条，相关169条通过，上游fetch受控，未当作真实浏览器证据。复习优先级高，参考答案已整理、尚未模拟。


## 文件修改提案的并发审批（2026-09-22）

复习优先级：高。参考答案已整理、尚未模拟。

**问题：两个请求同时批准和拒绝同一提案，怎样避免后提交者覆盖？为什么批准后还不能直接写文件？**

参考答案：为每次决策创建独立事务，按Workspace→Task→Conversation→Proposal固定顺序取得行锁并重新检查归属。在锁内读取最新状态，仅允许pending转换为approved/rejected；终态重复提交或反向提交都返回冲突。成功结果必须等事务提交后返回，flush只验证SQL与约束。未知提交结果不自动重试，应查询已持久化状态。数据库CHECK约束限制状态集合并禁止approved与截断Diff组合，但不替代服务端的合法转换检查。

批准只记录对保存内容的决策。批准时检查目录绑定相同且Diff完整，仍不能证明当前文件未被外部编辑，也不能证明同一路径仍指向同一目录对象。实际应用必须再授权并验证基线；本课完全不读写文件。拒绝可关闭目录已解绑或Diff截断的提案。创建回执保留创建时pending，详情显示查询时状态，不能把历史回执伪装成当前状态。

失败场景与取舍：先读pending再加锁会让竞争请求使用旧判断；加锁后重新读取避免覆盖。统一锁序与删除流程兼容；目前项目级锁也会串行化同项目其他提案，优先保证边界清晰，尚未优化锁粒度。状态列没有审批人/时间或变更历史，不能声称完整审计。降级只允许全部pending；存在决策时拒绝，不能静默抹掉用户决策。

项目证据：tests/workspace/proposals/test_file_edit_proposal_decision.py中的6组真实双线程竞争，通过pg_blocking_pids确认阻塞，再验证持锁者提交/回滚后的结果；独立连接确认最终状态。迁移专项8条验证保留历史数据、约束及拒绝有损降级。后端直接相关208条、前端172条分批通过，完整命令与证据边界见[历史验收记录](../../../docs/history/verification-through-2026-09-23.md)本课章节。


审批HTTP补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：HTTP 500为什么不能说明决策未提交？服务事务提交和HTTP响应生成是两个阶段；提交后代码异常、Pydantic公开响应校验失败、FastAPI响应校验失败都可能使已提交决策最终返回500。因此依靠覆盖依赖/路由/响应阶段的WorkspaceRoute统一返回proposal_decision_uncertain，不反射异常，不自动重试，保留GET详情核对持久化事实。一次查询pending也不能证明仍在运行的原请求已停止。新增HTTP专项以真实SQL失败、提交前失败、提交后异常及两级响应校验故障验证同一500可对应pending或approved；60条新增及11条受影响既有用例通过，范围与命令见[历史验收记录](../../../docs/history/verification-through-2026-09-23.md)。


审批BFF补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：代理错误语义必须保留“是否已经开始转发”的边界。调用fetch前取消可以确认本次请求未转发；调用后无论499取消、504超时、502响应错误或后端500，都不能推断数据库未提交，禁止自动重试。BFF只使用服务端凭证并转发已验证Origin，200回执核对三标识和本次决定，错误按HTTP状态与错误码共同白名单重建，不透传上游文案。新增93条测试验证12组转发后取消/超时阶段与4组转发前取消、公开投影及错误配对；上游受控，无真实网络中止或数据库证据。后续UI查询到pending也不能证明原请求已停止。


审批UI补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么查询到pending后不能立即恢复提交？旧请求仍可能在运行，读取发生在提交前；AbortController只能中止客户端等待，卸载不等于事务回滚。提交前先同步落sessionStorage未知结果标记，再发请求；写入失败则不发送，已确认未执行的白名单错误才清除标记，未知失败和终态冲突保留。busyRef阻止同一事件循环重复点击，三标识加决定的回执校验和aliveRef隔离迟到响应。sessionStorage只保护当前标签页，服务端授权及事务状态转换仍是权威。真实浏览器覆盖双击、键盘拒绝、截断、未知结果、存储失败及切换后的迟到响应；刷新仅证明标记保留，历史审阅入口尚待下一课接入。批准仍不写文件。


历史入口证据补充（2026-09-22）：已通过TaskRunPanel复用既有ToolResult补齐上一课历史入口缺口。事件中的pending是创建事实，当前状态由带当前Workspace/Task范围的详情请求重新授权读取；事件payload不能提供可信归属。未知协议回退文本，失败事件不能开启审批。刷新/离开返回后未知结果防重与真实终态展示经PC四场景验证，隔离数据库4条精确核对，无重复创建、源文件不变。新增8条分派/非法协议/范围测试与8条既有详情测试通过。参考答案已整理、尚未模拟。


应用前核对补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么approved加一次SHA-256比较仍不是写入许可？批准只记录决定，必须重新授权归属及目录绑定，并验证保存的完整新内容摘要；文件读取前比较内部expected_bound_root，防止绑定切换后读到另一目录。数据库只读事务在文件I/O前关闭，之后再新建Session查询并比较可观察变化，不持锁等待文件操作。UTF-8往返保留BOM/CRLF，不能归一化文本再比较摘要。失败不修改审批状态或源文件。

取舍与失败场景：两次快照无法检测中间变化又恢复的ABA；相同字节不证明目录/文件对象身份相同，返回后外部编辑仍可能发生。因此核对返回值不带可直接应用的正文/宿主路径，不能缓存为写入凭据。后续需要单次应用占用和写入时重新校验；文件替换与数据库提交的未知结果不能随意重试。测试专门模拟读取后外部改写，证明成功返回只对应已读取快照，不能承诺未来覆盖安全。42条新增及10条直接受影响用例分批通过，真实隔离PostgreSQL、临时文件、双归属授权与跨阶段绑定/状态/正文变化均覆盖；未接文件写入。


应用占用补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：审批状态与应用状态为何分开？approved只是用户决定；idle→running表示某次执行已持久领取，只有事务成功返回令牌后才能进入未来执行器。running→applied/not_applied/uncertain通过重新授权和令牌匹配完成；所有已领取状态均不能再次领取，not_applied也不自动重试。随机令牌隔离迟到执行结果，但不是权限凭据，Workspace/Task/Conversation归属仍需核对。

文件副作用和数据库事务不原子：提交异常不一定回滚。测试在after_commit注入异常，验证领取已持久为running或完成已持久为applied，调用方却收到失败；因此不得因异常重新执行。终态由未来可信执行器依据真实证据登记，本服务本身不证明文件写入。无超时回收/结果核对恢复协议，也不是完整审计。迁移拒绝丢失执行记录的降级。真实双连接竞争验证仅一个领取/完成成功，未把线程起跑屏障当成数据库锁等待证据。48条新增及15条受影响测试通过。

尚待闭环：事务行锁结束后，现有资源删除仍可级联清理提案；单次领取不等于整个执行期间的资源保护。下一课先补running/uncertain期间的服务端删除与目录变更约束，再推进真正写入。


资源保护补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：上段“尚待闭环”的现有入口保护已补齐。领取、完成、Task删除、目录绑定共用Workspace锁，在锁内检查running/uncertain并保持至提交，防止先查空闲后并发领取。目标任务删除不受兄弟任务占用影响，项目绑定变化则检查整个项目；同一路径无写入的重复绑定无需拒绝。未知结果不能按超时当作已停止，应用占用检查必须在DELETE前且授权后完成。

证据：4组真实双连接竞争通过pg_blocking_pids确认等待；领取先提交则变更拒绝，删除先提交则领取不可访问，原绑定冲突先回滚则领取成功。选择器等待期间发生另一绑定/占用时保存重查，HTTP及BFF固定409不反射私有文案，UI不把明确拒绝当成删除未知。后端26条、BFF3条及PC单场景通过。保护仅覆盖现有应用服务，不能防管理员直接SQL或外部编辑器；未来Workspace删除/解绑入口也必须接入相同协议。


受限替换补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么临时文件加os.replace仍不能承诺不覆盖外部编辑？独占创建同目录临时文件、循环处理短写入、同步新内容和目录项，解决的是部分写入及替换可见性；摘要检查与rename之间不是一个条件原子操作。专项在replace包装器中先让外部编辑器修改目标，再执行真实replace，验证结果仍可为replaced且覆盖编辑。O_NOFOLLOW、目录描述符和目录身份复核也不能锁住整个外部目录树。

结果边界：尚未调用替换可返回not_replaced，但可能产生临时文件；一旦尝试replace，异常即保守uncertain，即使异常发生在真正系统调用之前，也不能让上层据此自动重试。成功替换后同步/核对失败不得回滚目标，避免覆盖新的外部修改。清理失败独立于目标结果，close不盲目重试；KeyboardInterrupt清理后传播，不伪造操作结果。新inode会丢失未复制的ACL/xattr等元数据，目前只保留基本模式与属主/属组，所以本课仅临时样例，下一课补平台元数据策略后再讨论真实项目接入。

证据：67条新增专项分批通过，真实UTF-8/BOM/CRLF/空内容/256KiB、链接与FIFO/硬链接拒绝、短写入、源文件与临时文件变化、替换前后故障、清理失败、目录移动和描述符释放；当前环境chmod 04640实测仅保留0640，特殊权限位分支采用条件元信息注入，不声称实机setuid保留证据。参考答案已整理、尚未模拟。


元数据保护补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么正文和mode正确仍不意味着替换安全？rename替换的是新inode，ACL、扩展属性及文件标志不会自动从旧对象完整继承。当前策略通过描述符读取/复制/核对macOS ACL，同时回读mode/uid/gid；拒绝可见xattr和非零flags，不静默丢弃无法处理的语义。临时文件可能继承目录ACL，必须覆盖并验证。原生对象限作用域释放，ACL导出长度有预算，不反序列化外部输入。

工程取舍：严格拒绝意味着兼容性降低，当前环境新文件自带provenance便不能成功应用；不能为通过测试放宽生产策略。成功测试只模拟缺少该属性，真实ACL与继承覆盖、真实xattr拒绝分别提供证据。查询只覆盖当前进程可见属性，前后快照不是CAS；写入后核对失败保留uncertain，不盲目回滚。28条新增与25条直接受影响用例分批通过，仍仅临时样例。


执行装配补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：文件成功替换但数据库登记失败，为什么不能返回not_applied或回滚文件？文件系统与PostgreSQL没有统一事务；数据库提交后也可能丢失回执。执行器保留file_status=replaced，同时application_status=unknown，禁止自动重领/重试/回滚。领取回执失败没有令牌就不进入文件操作；正常核对失败已消耗领取次数，登记not_applied而不恢复idle；清理未完成登记uncertain保留资源保护。中断不捕获为普通成功/失败，而传播并保留running。

证据与取舍：37条新增隔离PostgreSQL/临时文件测试分批通过，真实before_commit和after_commit故障分别验证领取和登记；两个并发调用仅一个到达写入。文件I/O前会话关闭，不持行锁做磁盘操作，所以不承诺外部修改CAS或跨系统原子性。未接公开执行入口，provenance仍是生产严格拒绝条件。


应用状态查询补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：回执unknown与持久化uncertain有何不同？unknown表示调用方无法确认数据库登记结果，不是数据库状态；授权查询只返回idle/running/applied/not_applied/uncertain。running不证明进程存活，applied不证明当前文件字节未被后来修改，not_applied不恢复执行资格。查询不是恢复动作。单次联合查询验证归属，仅投影公开标识和状态，不加载令牌/正文/绑定路径；绑定变化或文件消失不应阻止历史记录查询。29条新增测试分批通过，覆盖权限、状态协议、事务无副作用和资源释放。


状态查询HTTP证据补充（2026-09-22）：严格四字段Schema、三标识长度/字符检查，查询参数不能替代路径/可信身份，未知应用状态和响应生成错误返回专用安全500而非idle。统一边界覆盖依赖、路由与序列化，成功失败均no-store；GET只提供持久化快照。新增34条及受影响18条共52条分批通过；接口未开放执行能力。参考答案已整理、尚未模拟。


状态查询BFF证据补充（2026-09-22）：浏览器不提供内部凭证；代理只使用服务端配置，核对三个资源标识与五种持久化状态，显式投影公开字段。unknown/错误协议不能变成idle。取消/20秒预算覆盖fetch和正文解析，失败不自动重试，不触发应用。55条受控fetch测试通过，未宣称真实浏览器链路。复习优先级高，参考答案已整理、尚未模拟。


状态展示工程证据补充（2026-09-22）：审批已批准不能推出尚未应用；应用状态单独按需查询。资源key触发卸载，AbortController节省请求，controller身份比较负责防止旧响应更新；取消、收起和跨任务导航的迟到响应均经PC专项验证。查询错误不回退为idle，五状态说明各自证据边界。组件新增8条+受影响8条通过；浏览器真实idle、受控其余状态、双PC宽度及零提案写请求通过。参考答案已整理、尚未模拟。


有界xattr读取补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么先探测长度再分配仍不够？探测与读取之间名称和值可能变化，所以必须先检查预算、核对实际长度与回读长度，读取结束复核名称集合，并用固定两轮结果与fstat发现可观察变化；不能无限重试。空值与不存在/权限拒绝不同，失败不能变成空属性。名称排序避免系统返回顺序变化误报，属性值保持bytes且不进入repr。查询仅覆盖当前进程可见属性，两轮仍不防ABA，不代表可复制。39条新增专项通过，含真实macOS二进制/空值/中文属性读取、描述符偏移及文件不变证据。


xattr复制探测补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：为什么set成功加回读一致仍不能证明复制能力？目标可能原本就与源相同，需要独立记录matched_before、set_accepted和readback；失败但原本相同也不能归功于本次设置。最终完整可见快照比较还要考虑目标额外属性，不能静默删除。当前macOS真实临时样例中普通三属性由缺失变相同，provenance原本相同，因此不能证明不同值复制能力。19条新增测试通过，覆盖调用/回读分离和清理失败，现有替换仍拒绝xattr。


受限xattr复制补充（2026-09-22，优先级高，参考答案已整理、尚未模拟）：文件描述符不能证明临时性和独占性，因此复制原语的调用方必须持有独占临时目标，不能作为公共写接口。源与目标同inode必须拒绝，否则复制可能修改源；目标多硬链接也不能当作独占。写前拒绝额外属性，不自动删除；相同属性跳过设置仍须最后核对。逐属性设置不是事务，失败可留下部分修改，调用方清理目标，不回滚源。26条新增测试分批通过，含无provenance过滤的真实临时复制、空/二进制值、重复调用零设置和描述符保留。

### 元数据复制为什么需要顺序与最终整体回读？

复习优先级：高。参考答案已整理、尚未模拟。

属组设置可能影响权限位，因此先属组后权限；最终ACL可能限制属性写入，因此先xattr后ACL。调用成功不证明最终属性保留，ACL步骤也可能改变属性，必须重新读取mode/uid/gid/ACL/完整可见xattr并整体比较，同时复核源版本及目标正文信息。目标额外属性直接拒绝，不删除；部分失败不回滚，由可信调用方清理独占临时目标。两轮快照仍不是CAS，不能防止最后检查后的外部变化，也不证明不可见元数据或任意系统属性都能复制。证据：test_workspace_metadata_xattrs.py、test_workspace_file_metadata.py及两条执行器定向测试；真实系统属性不再过滤。

完整链路证据补充（2026-09-22）：`test_proposal_application_lifecycle.py`经真实审批服务串联创建/审批/执行/查询。批准不写文件；文件替换成功与终态登记确认是两个边界。登记before_commit故障保留running，after_commit故障已是applied，但执行回执均为unknown；禁止从回执未知推断未写入或自动重试。新增7条通过，不直接修改审批状态，不过滤系统属性。该证据补充既有事务边界题，复习优先级高，参考答案已整理、尚未模拟。

公开契约补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：字段各自符合枚举不代表组合合法。执行回执replaced/unknown/True保留文件成功但登记未确认的证据；not_replaced/applied必须拒绝。领取未确认只允许not_attempted/None。显式投影防止内部新增敏感字段自动泄露；纯转换失败也不能推断副作用未发生或触发重试。证据：test_proposal_execution_response.py，57条通过，其中240种组合17合法/223拒绝；转换不是资源授权。

前端契约证据补充（2026-09-22）：TypeScript声明不能替代运行时解析；执行回执unknown与查询持久化状态不同，解析失败也不能推断文件未写入。proposal-execution-data.test.ts以真实后端Schema交叉核对240种组合，双方17接受/223拒绝；前端显式投影额外字段而后端Schema禁止额外字段，两端职责不同。51条新增通过。沿用既有公开契约题，高优先级，参考答案已整理、尚未模拟。

临时样例生命周期证据（2026-09-22，高优先级，参考答案已整理、尚未模拟）：为什么不能对临时路径直接rmtree？路径可能被替换，目录可能出现未知内容。服务端独占创建、持有目录描述符和核对dev/ino，将清理限定为固定普通文件及空目录；发现未知内容/链接/身份变化保留现场。返回路径对象本身不是授权凭证，后台使用不能跨越清理边界。原异常遇到清理失败追加固定note而非被覆盖。21条测试覆盖异常、中断、替换、链接与资源释放；身份检查仍非CAS。

样例登记补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：为什么撤销登记不应立即删除正在使用的目录？close先停止新借用，现有独占借用结束才清理，避免使用中删除；不依靠超时推断使用已停止。失败不可重新开放，清理失败保留容量和现场；新进程不能信任旧句柄。内部登记只证明创建来源和生命周期，不替代用户/任务授权，路径对象可复制也无法自动撤销。18条新增验证并发关闭与失败边界。

样例授权绑定补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：为什么commit抛错不能直接删除刚创建目录？提交可能已成功，数据库仍引用目录。只在尚未尝试提交时清理未发布样例；提交未知保留并封锁。正常关闭先授权/占用检查并确认解绑提交，再删除目录，避免清理与绑定状态相矛盾。创建前和创建后重复授权，借用时再核对归属及绑定；事务不跨越文件执行。23条真实隔离PostgreSQL用例覆盖绑定/解绑提交前后故障及活动占用。授权快照不是跨系统锁或文件系统CAS。

样例执行门禁补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：外层授权一次为何不足？后续数据库路径或文件系统目录对象可能变化，因此将可信样例根目录、固定文件名和创建时dev/ino传入执行链，预检前拒绝其他路径，实际替换在打开目录后复核身份。借用覆盖完整执行，未知/不确定回执通过异常退出封锁登记但保留回执事实和目录。19条新增加24条受影响既有验证；不是文件系统CAS，原内部执行器不直接开放给HTTP。

样例HTTP补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：POST返回500为何不能据此重试？写入或数据库提交可能已发生，失败也可能仅发生在响应构造；返回安全的proposal_execution_uncertain并保留现场，不能描述成未执行。请求里的apply只表达意图，可信范围来自服务端样例登记及重新授权，不接受客户端路径。新增23条真实HTTP用例验证重复不再次写入、提交前后故障、归属变化及响应失败。证据：`apps/api/tests/workspace/proposals/test_sample_execution_api.py`。

样例BFF补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：AbortSignal取消为何不能当作回滚？它只中断网络等待，不能撤销后端已经发生的文件或数据库操作；仅转发前取消可声明本次尚未转发。上游预算应覆盖响应正文读取，合法unknown回执保留证据，未知HTTP状态/错误码/损坏回执统一报告未确认且不重试。106条新增BFF测试覆盖各取消阶段、凭证隔离与错误脱敏，证据：`apps/web/test/features/workspaces/proposal-execution-route.test.ts`。

客户端应用请求补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：为何仅React状态或AbortController不足以防止重复写入？状态更新不一定同步，卸载/重建也会丢失实例状态；应在发请求前同步保存按资源隔离的标记，提交时重读共享标记。取消不能回滚，迟到响应还需代次检查；持久回执只保存公开投影。存储故障时拒绝提交，写回执失败不否定当前已核对证据。sessionStorage仅覆盖同标签页，不替代服务端并发保护。证据：proposal-execution-request.test.ts新增25条，参考答案已整理、尚未模拟。

真实样例端到端证据（2026-09-22，高优先级，参考答案已整理、尚未模拟）：HTTP200为何不等于一次新执行成功？重复领取在本项目返回合法claim_unconfirmed回执，file_status=not_attempted/application_status=unknown，并封锁内存登记；实际文件内容/inode/mtime不变。首次调用以独立文件读取和数据库applied交叉验证，不能仅凭UI截图判断写入成功。生产close拒绝封锁登记，隔离测试通过确认状态后的专用收尾清理，不代表产品具有自动恢复能力。项目证据：apps/web/test/browser/execution-e2e及本地evidence.json。

样例状态查询补充（2026-09-22，高优先级，参考答案已整理、尚未模拟）：为什么ready快照不能作为执行权限？归属、绑定和文件在查询后均可能改变，查询既不借用登记也不预留执行权；执行仍须重新授权。即使内存没有登记也先检查归属，防止越权被误当合法missing；查询发现绑定不符仅报告sealed，不隐式修复或封锁原对象。按既有进程锁→数据库锁顺序读取，避免反向锁序死锁。证据：test_task_sample_status.py新增18条，包括真实跨线程busy与查询不恢复封锁。

样例来源持久化补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为什么数据库中保存了样例来源，重启后仍不能据此执行或删除目录？来源记录只证明服务端曾把Workspace、来源Task与创建时根目录一同登记，进程内句柄、目录描述符和独占生命周期无法从字符串路径安全恢复。绑定时来源与Workspace.root_path同事务写入，关闭时先同事务解绑并置cleanup_pending，清理确认后才删除来源；借用仍要重新核对归属；有来源时来源Task不能直接删除。旧普通目录不回填，迁移回退不能丢弃非空来源。提交回执丢失或清理失败仍可能留下现场，不能自动恢复或递归清理。证据：test_sample_origin.py、test_workspace_sample_origin_migration.py及Task删除API/BFF专项。只读诊断现已完成：重启后无内存登记但Workspace仍有来源时sealed，同Workspace来源Task错配也不降级missing；真正无来源的普通Workspace仍missing。真实浏览器→BFF→API和隔离PostgreSQL验证查询无文件/提案写入；来源读取失败直接失败。

样例清理待办补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为什么关闭时不能在解绑提交中直接删除来源？PostgreSQL与文件系统没有共同事务；解绑后文件清理若失败，来源一并删除就会失去可诊断的根目录快照。现在先原子解绑并写cleanup_pending，确认提交后才通过原有独占句柄安全清理；清理确认后再删来源。第一提交回执未知不碰文件；清理失败、中断或延期时保留待办、封锁登记，并拒绝来源Task删除及普通目录/样例重新绑定。第二提交若回执丢失，来源可能已删，但文件已安全清理，不能推断记录仍在。待办不是自动重试许可，也不证明目录当前仍存在。证据：test_sample_cleanup_pending.py、test_sample_cleanup_pending_migration.py及状态/目录绑定HTTP专项。

公开原因补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为什么已授权的同Workspace兄弟Task不能直接看到cleanup_pending？查询身份与来源归属不同：先重新授权请求Task，仍须核对其为持久来源Task，才把清理待办投影为公开原因；其他sealed只返回unavailable，避免透露兄弟Task的生命周期。HTTP/BFF/浏览器均拒绝状态与原因错配，并只投影标识、状态和原因。原因取自持久记录而非文件是否存在：安全清理已完成但最终删除记录的提交失败时，仍可能显示cleanup_pending；回执丢失且记录已删时只显示unavailable。任何公开原因都不是再次执行、清理或重试的许可。证据：test_task_sample_status_api.py、test_sample_cleanup_pending.py及sample-status浏览器专项。

只读处置预检补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为什么目录同名、属主正确且权限0700仍不能安全清理？旧来源只有路径，创建时的父/子目录身份并未持久化；现在新建样例把描述符核验过的父/子目录dev/ino与Workspace绑定及来源同事务写入四个可空字段，旧行仍全NULL，不从现存路径伪造回填。内部预检先重新授权来源Task并核对cleanup_pending，再在事务外限定当前临时父目录和规范样例名：从可信父fd无跟随读取目录项，以O_DIRECTORY/O_NOFOLLOW打开候选fd，交叉核对打开前目录项、fd和退出前目录项的身份/属主/0700形态，并复核父目录；候选fd先于父fd关闭。旧记录即使本轮描述符一致仍报identity_unverifiable，新记录稳定数值相符才报identity_matches_record，稳定持久错配报evidence_inconsistent；首次缺失只是观察时刻，观察中替换/消失或I/O/关闭故障报inspection_unavailable。即使三次读数相符，inode仍可能重用，数据库与文件系统不是共同快照，检查完成后也可替换，不能恢复旧fd、执行或清理。检查不调用registry.borrow/close，也不删除文件。证据：test_cleanup_preflight.py及test_sample_directory_identity_migration.py的隔离PostgreSQL/真实临时目录专项。

只读预检HTTP投影补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为什么内部诊断分类不能直接作为清理授权公开？GET /workspaces/{workspace_id}/tasks/{task_id}/sample-cleanup-preflight在local模式下重新授权来源Task后，只公开workspace_id、task_id和七种固定result之一；同Workspace兄弟Task与未知/越权资源同为404 workspace_not_accessible，避免透露来源归属。路径、查询和正文形态错误为422 invalid_sample_cleanup_preflight_input；服务读取或响应失败为500 sample_cleanup_preflight_read_failed，不伪装成directory_missing或identity_matches_record。响应no-store且不带路径、dev/ino或句柄；诊断是查询时刻的快照，不能凭identity_matches_record自动清理或重试。新增HTTP专项32条及直接受影响回归13条通过，实际链路与隔离证据见[历史验收记录](../../../docs/history/verification-through-2026-09-23.md)。

只读预检BFF补充（2026-09-23，高优先级，参考答案已整理、尚未模拟）：为何FastAPI已有严格响应模型，Next.js仍要重新解析？浏览器只能调用同源BFF，不能取得服务端本机凭证；BFF必须校验上游200的资源标识和七种分类并重新构建三个公开字段，避免错Task响应和内部字段/Set-Cookie穿透。上游故障、非法分类、取消或超时是查询失败，不得猜成目录缺失或身份匹配，也不能自动重试清理。工作台只在用户触发后查询，取消、收起、Task切换先使旧请求失效；前端再次核对资源和分类，失败保留未知，展示中的identity_matches_record也不提供清理按钮。证据：task-sample-cleanup-preflight-route.test.ts新增51条、Chrome组件专项、直接受影响的真实工作台专项，以及隔离PostgreSQL/自有待办样例的Chrome→Next BFF→FastAPI只读链路；查询前后文件与来源记录不变，详情见[历史验收记录](../../../docs/history/verification-through-2026-09-23.md)。

补丁候选与受限样例应用补充（2026-09-24，高优先级）：为何统一补丁不需要单独的写入器？补丁只是生成完整候选的一种输入形式；保存后的候选、原文摘要、候选摘要和审阅Diff可复用原审批与执行协议。批准仅说明接受保存内容，写入前仍检查归属、可信样例登记、绑定/目录对象与字节基线，并以单次应用占用阻止重复写入。原子替换不是文件条件比较交换，不能据此声称消除外部并发修改。

[补丁样例应用专项](../../../apps/api/tests/workspace/samples/test_patch_sample_application.py)22例通过，覆盖正常/BOM/空内容/多块修改、未批准/截断/基线与候选摘要冲突、归属/绑定/目录替换和重复执行。文件已替换而终态登记失败时，数据库可能running或applied，公开应用结果仍unknown并封锁样例，不能回滚文件或解释为未写入；真实文件新内容是独立证据。此组为隔离PostgreSQL与临时样例集成，不含真实崩溃恢复。参考答案已整理、尚未模拟。

[PC联合专项](../../../apps/web/test/browser/patch-application.mjs)另验收四场景：工作台保存/审批，独立应用组件经真实BFF/API执行，再回历史查询状态；成功applied、冲突not_applied、未批准/截断idle与文件证据一致。刷新无重复POST且字节/inode/mtime未变。应用组件只由隔离启动器挂载，不能冒称生产工作台已有应用按钮；测试夹具清理封锁样例也不能当作产品恢复能力。双PC宽度截图与键盘操作通过，模型受控；参考答案已整理、尚未模拟。

### Git状态为什么不能按行解析或把空输出当干净仓库？（2026-09-24）

复习优先级：高；参考答案已整理、尚未模拟。

参考答案：porcelain v1 -z使用NUL分隔，文件名可以含换行、空格和箭头；重命名/复制双路径是目标在前、原路径在后，不能套用可读短格式。普通XY表示暂存区与工作区，合并冲突XY表示双方相对祖先的状态，不能混为普通暂存修改。解析只判断语法，空字节可能来自失败命令、过滤输出或中途取消；采集层必须确认成功、输出完整和选项范围，才可解释没有条目。

失败与取舍：先限制总字节，再拆分NUL；条目数和单路径字节也须有界。截断、超限和非法UTF-8整体失败，不返回看似完整的部分列表；严格UTF-8意味着暂不支持任意Unix文件名字节。解析路径只是展示数据，不授予读写权限，后续工具必须重新授权。v1将子模块变化合并为M，不能推导完整子模块状态。协议依据：[Git官方文档](https://git-scm.com/docs/git-status#_porcelain_format_version_1)。

项目证据：[纯解析专项](../../../apps/api/tests/workspace/git/test_status_parser.py)77例通过，覆盖状态、重命名/复制顺序、特殊文件名、恶意/不完整记录与实际字节/条目预算。没有运行Git或读取项目文件，不作为真实命令安全性证据。

受控采集补充（2026-09-24，高优先级）：Git status为何仍需隔离环境？默认状态查询可刷新索引，宿主GIT_DIR/INDEX/CONFIG变量可改变目标，本地配置也可能改变行为；不能因命令名看似只读就忽略边界。本项目只支持自建临时句柄，固定git-dir/work-tree、配置与环境，禁用可选锁/fsmonitor，冻结初始化配置；不是任意不可信仓库沙箱，配置检查也不是防并发替换的原子授权。参考：[Git环境约定](https://git-scm.com/docs/git)。

stdout的NUL协议必须保留原始字节，stderr单独预算；两路并发排空、退出码0及EOF齐备后才调用解析器。管道EOF不代表进程退出，超时还覆盖wait；超限/异常/中断必须杀进程组并回收，不能将保留前缀当完整快照。[真实采集专项](../../../apps/api/tests/workspace/git/test_status_capture.py)23例通过，包含实际Git状态、项目/索引不变以及真实子进程的超时/超限/中断回收。仅临时夹具可准备add/commit，用户项目没有提交；不含Workspace授权、Windows或并发写者保证。参考答案已整理、尚未模拟。

Task Git样例授权补充（2026-09-24，高优先级）：注册时验证过身份，为什么读取时仍需验证？资源归属可变化，外部编号也可能被复用；登记保存内部Workspace/Task/Conversation身份，每次读取重新授权并比对，再关闭Session执行Git。借用标记负责跨查询/采集/回收阶段阻止关闭，不把数据库事务或行锁延伸到子进程等待。该设计不是与数据库资源删除共享的原子撤权协议。

[Task Git样例专项](../../../apps/api/tests/workspace/git/test_task_git_samples.py)17例通过，包含真实数据库归属变化/同编号会话重建、初始化竞态、线程借用竞争、句柄过期与清理失败封锁。独立样例不改项目root_path，不能把它的空状态解释成用户项目干净。宿主shutdown根据本实例创建的句柄收尾，用户接口close仍重新授权；shutdown已接应用生命周期，删除Task不会自动回收登记。进程重启无持久恢复能力；参考答案已整理、尚未模拟。

Git样例工具公开协议补充（2026-09-24，高优先级）：为何空参数工具仍需验证与来源说明？空对象也要extra=forbid，防止模型暗传目录、命令、身份或预算；manager由服务端绑定，任务上下文只传可信身份，但真正查询仍需底层重新授权。source明确为task_git_sample，子模块忽略范围单独说明，不能把空条目包装成用户项目干净。

公开JSON有独立1MiB预算，不能只依赖256KiB源字节上限：控制字符经JSON转义会膨胀。超限/序列化失败整体返回固定错误，取消与中断继续传播；字段显式投影不等于扫描路径正文中的敏感内容。[适配39例](../../../apps/api/tests/tools/test_git_sample_status_tool.py)及[真实集成5例](../../../apps/api/tests/tools/test_git_sample_status_integration.py)通过，覆盖来源/额外参数/身份/错误边界与文件不变。已接请求级模型能力及受控PC验收；参考答案已整理、尚未模拟。

应用托管与取消补充（2026-09-24，高优先级）：为什么不能在finally里直接删掉manager引用？manager持有临时目录上下文，借用未结束或清理失败时丢掉最后引用可能触发隐式清理；应先封闭获取入口及旧manager新操作，再等待真实清理结果，失败保留封闭owner并拒绝新启动覆盖。进程内保留不是持久恢复，也不代表可安全重试。

asyncio.to_thread的等待被取消不意味着底层线程停止；shield并保留worker，连续取消仍等待结果，成功释放owner后传播取消，清理失败则传播故障并保留现场。取舍是关闭可能等待线程，而不会伪报资源已释放；在途采集忙碌时明确失败，不自动等待或重试清理。其他应用资源通过独立finally收尾。[应用生命周期专项](../../../apps/api/tests/workspace/git/test_application_samples.py)10例覆盖真实临时目录、借用竞争、异常和重复取消；授权使用桩，不替代数据库专项。参考答案已整理、尚未模拟。

请求级Git能力补充（2026-09-24，高优先级）：为什么不把带manager的执行器放进全局注册表？manager属于应用，任务上下文属于请求；全局绑定会跨应用或跨任务混用能力。请求从Request.app取得当前owner，工具描述与执行定义共用同一快照，执行时要求原上下文对象并再次加载会话归属；底层manager仍核对Task，能力可见不代表授权永久有效。未登记样例明确失败，不因用户询问状态而自动创建样例或改查用户项目。

同步Git执行复用Runtime受跟踪线程，超时/取消只是等待结束，不能提前释放请求占用或把空结果当成功。失败只回灌固定Observation，参数校验错误不回显input。证据：[请求注册19例](../../../apps/api/tests/tools/test_git_status_registration.py)、[隔离数据库与真实Git聊天4例](../../../apps/api/tests/chat/test_git_status_chat.py)，覆盖上下文变化、关闭、线程收尾、模型往返及事件持久化；PC受控闭环已验证，真实模型措辞仍未验证。参考答案已整理、尚未模拟。

PC证据补充（2026-09-24，高优先级）：Run完成为什么不能推导工具成功？Agent可以消费失败Observation后解释失败并正常结束；四场景中缺失登记/配置变化两轮Run同为done，但只有TOOL_CALL_ERROR，界面须展示失败而非空成功。历史页的开始/结束事件会重复出现工具名，不等于重复调用；用POST次数、持久TOOL_CALL_START数量及原文件证据独立判断重放。[PC专项](../../../apps/web/test/browser/git-status.mjs)四场景、两种桌面宽度及刷新历史通过，[服务端夹具](../../../apps/web/test/browser/git_status_model.py)确认文件/项目绑定未变、未自动登记和生命周期清理。结果仍为通用JSON，样例来源可见；受控模型的正确措辞不能替代真实模型验收。参考答案已整理、尚未模拟。
