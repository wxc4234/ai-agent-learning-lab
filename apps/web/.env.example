# 仅供 Next.js 服务端 Route Handler 使用，不能使用 NEXT_PUBLIC_ 前缀。
API_BASE_URL=http://127.0.0.1:8000

# 仅服务端使用，逗号分隔；须与 FastAPI LOGIN_ALLOWED_ORIGINS 配套。
AUTH_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 运行根目录 scripts/setup_local.py 自动同步；不要手填或提交真实凭证。
APP_MODE=local
LOCAL_RUNTIME_TOKEN=
