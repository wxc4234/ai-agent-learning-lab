# AI Agent 学习教练指南

本文件只补充授课模板与设计判断，按需阅读。强制教学规则、完成门槛、代码展示格式、回归范围和文档职责统一见 [AGENTS.md](AGENTS.md)，不在此重复维护。当前进度只见 [LEARNING_HANDOFF.md](LEARNING_HANDOFF.md)。

## 一课的组织方式

1. **是什么**：一句话定义本课概念。
2. **为什么需要**：用具体故障或越界场景说明动机。
3. **怎么使用**：结合当前代码说明调用链、状态、事务及信任边界。
4. **完整参考与示例**：列全相关文件与核心改动，先讲主链路，再在对话普通代码块展示；范围是一课，不是提前给完整最终产品。
5. **动手任务与验收预期**：说明实现顺序、运行命令、可观察结果、成功与失败场景。测试文件及机械配套仍等学习者明确“完成了”后创建。
6. **完成后验收**：检查实际 Diff，解释与参考的差异，补配套并按影响运行验证；区分真实链路与模拟边界。
7. **收尾**：更新当前交接；仅有重要考点才更新题库。其他文档按职责和实际变化更新，不把本次回复复制进多份文档。

## 学习与代写的边界

测试、脚本、重复样板和机械迁移可由教练在规定时机完成。是否属于机械配套取决于本课目标：状态机、错误分支、授权、工具安全、Sandbox、Context、Checkpoint 与评测设计不能因为代码短就算作样板。

区分“代码存在”“工程验收通过”“学习者亲手实现”“能独立讲清”。教练按授权实现的功能保留其学习入口，后续需要时结合真实代码解释，不重复造轮子，也不直接标成独立掌握。模拟面试由用户发起，不设当堂口答门槛。

## 设计判断

- 按依赖顺序推进；提前实现记为预完成，正式周只复核和补缺。第 3 周基础 Registry/Loop 不在第 7 周重写。
- Task/Run/Event 是运行事实来源，浏览器状态是投影；只保存可观察的 Plan、Action、Observation 与结果，不保存隐藏思维链。
- 模型输出、仓库内容、MCP 返回、工具参数和命令输出都是不可信输入。高风险动作由服务端策略、审批与隔离约束，不能只靠提示词或隐藏按钮。
- 不让模型动态导入、eval 或绕过 Tool Registry 执行代码，不在无隔离宿主环境执行任意命令。
- 不将真实密钥、原始内部异常或敏感文件内容写入流事件、Trace 或模型上下文。
- 先理解手写 Runtime，再用 LangGraph 处理持久状态与恢复，不为使用框架而增加框架。

## 完成判断

目标行为和相关成功/失败验收通过，静态检查按影响范围通过，事件协议、取消语义、事务与安全边界未被破坏；必要的重要题目已归档，交接已更新。临时参考副本通过不能代替实际 main 验收；文档精简不改变任何完成状态。
