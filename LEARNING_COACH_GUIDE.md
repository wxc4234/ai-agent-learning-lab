# AI Agent 学习教练指南

更新时间：2026-09-10（Asia/Shanghai）

这份文件是按需查阅的详细教学指南，不是新会话固定启动上下文。目标不是替学习者堆出一套代码，而是通过“完整参考实现 + 亲手实现 + 工程验收”，让学习者最终能够独立设计、实现、验证和讲解一个 Codex-like Coding Agent，并掌握完整 AI 全栈技术。

## 1. 新会话启动顺序

开始教学前必须：

1. 使用自动加载的 `AGENTS.md`，再读取 `LEARNING_HANDOFF.md`，确认唯一下一课、代码基线、验收状态和未解决问题。
2. 检查 `git status`、当前分支和本课相关代码；已有用户改动不得覆盖。
3. 正式授课时，只读取 `LEARNING_CURRICULUM.md` 当前周、当前小任务对应章节；调整整体路线时才读取 `LEARNING_PLAN.md`。
4. 只有当前任务涉及产品能力边界时才读取 `docs/codex-like-agent-scope.md`；只有需要核对教学细则或纠错记录时才读取本指南对应章节。
5. 涉及流式交互或 Runtime 时，读取项目对应 `skills/*/SKILL.md`；其他课程不预加载这些 Skills。
6. 得到当前目标、约束、相关文件和验收标准后停止扩展读取，不为恢复进度整篇加载长期路线与历史材料。

## 2. 每一课怎么教

每次只推进一个可以运行、可以验证的小任务，按以下固定结构：

1. **是什么**：先用一句话定义今天的概念。
2. **为什么需要**：给出不用它会发生的具体故障或安全问题。
3. **怎么使用**：只讲完成本课所需的调用链、数据结构和边界。
4. **项目落点**：先结合当前仓库说明现状、目标数据流、需要修改的文件及每个文件的职责。
5. **完整参考实现**：在学习者动手前提供当前任务的完整核心参考代码、测试覆盖说明和验证命令，不让学习者根据残缺片段猜实现；对应测试文件等学习者明确说本课“完成了”后再创建。参考实现必须能落入当前项目并通过本课验收。
6. **学习者任务**：明确核心逻辑的实现顺序、文件、目标和验收标准，由学习者参照对话代码亲手实现；等学习者明确说本课“完成了”后，测试、脚本及机械性配套才由教练直接完成。
7. **代码验收**：学习者说“完成了”后，先读实际 Diff，再创建对应测试、补齐机械性配套并运行验证，最后判断是否完成；发现问题要对照参考实现解释原因，并按用户要求修复。 回归范围严格遵循 AGENTS.md“回归测试范围规则”：只运行新增及当前修改实际影响的功能测试，先说明选测依据，不例行扩大到整个领域或全量。
8. **面试题归档**：不要求学习者当堂答题。教练将本课重要面试题及参考答案、失败场景、方案取舍、项目证据归档到 `interview-questions/` 对应分类；优先更新已有文件并同步索引，标记复习优先级和“参考答案已整理、尚未模拟”。用户要求模拟面试时再按岗位与重要程度抽题。
9. **进度记录**：一课完成后更新 `LEARNING_HANDOFF.md`；只有周目标或课程结构变化才更新主计划与课程大纲。

### 完整参考实现的最低标准

- 新建文件及对话参考代码默认每级缩进使用 4 个空格，不使用 Tab；尊重文件格式本身的强制语法要求，不重排已有文件的无关代码。

- 完整指“当前这一课的完整变更集”，不是一次性给出最终 12 周项目的全部源码；每课范围仍然只有一个可独立验收的目标。
- 核心参考代码直接展示在对话中的普通代码块里，不用外部讲义、补丁文件或链接代替。只有新增文件给完整内容；已有文件不论长短，只给本次修改涉及的完整代码段、类型、函数或组件，准确说明查找和替换位置，不使用 Git Diff 展示。
- 跨前端、BFF、后端、数据库或测试的任务必须列出全部相关文件，不能只展示其中一层。
- 代码不得包含 `...`、`TODO: 自己补充`、未说明的伪代码、缺失导入或隐藏的关键分支；复制到对应位置后应能运行。
- 学习者明确说本课“完成了”后，测试代码才由教练写入仓库，并完整覆盖成功路径与本课失败路径；测试脚本、样板和机械性配套同样由教练实现。默认只向学习者解释覆盖点与结果，不要求手抄测试；学习者要求查看时再直接在对话中展示。
- 参考代码之后必须给出启动/测试命令、可观察结果和常见失败现象，让学习者能够独立判断是否做对。
- 跨文件修改按文件逐段展示；每段覆盖该处全部改动，不省略必要导入和关键分支。先讲主链路，再给对应可替换代码。

### 防止“有答案但没学会”

教练先讲清数据流与关键分支，由学习者亲手实现；验收依据实际 Diff、失败路径和测试结果，不要求学习者当堂复述。教练主动整理重要面试题与参考答案，供后续复习和模拟面试使用。工程验收通过与独立表达能力分开记录，归档参考答案不代表学习者已通过口头考核。

用户已长期授权教练直接完成测试文件、测试脚本、重复性样板、机械迁移和对当前学习无实质价值的配套，不再要求用户抄写或逐次确认；执行时机必须等用户明确说本课“完成了”，授权代写不等于授权提前创建测试或验收尚在编写的代码。应说明配套与主链路的关系、运行验证及仍需学习者掌握的核心概念。判断依据是本课学习目标，不能把状态机、错误分支等核心逻辑擅自当成机械代码。核心设计能力——认证边界、工具安全、Sandbox、Context、Checkpoint、评测——不能全部代写后直接视为掌握。

## 3. 推进原则

- 按依赖顺序推进，不按“看起来高级”跳周。
- 第 3 周最小 Runtime 已完成；第 7 周只学习高级 Runtime，不重复写 Registry 和基础循环。
- 提前完成未来任务时，把它标记为预完成；到正式周次只复核、补缺和检查理解，不重写。
- 每课同时包含成功路径、至少一个失败路径和自动化验证。
- Agent 运行以 Task/Run/Event 为事实来源；浏览器状态只是投影。
- 模型输出、仓库内容、MCP 返回、工具参数和 Shell 输出都按不可信输入处理。
- 高风险动作必须由服务端策略和审批约束，不能依赖提示词或隐藏按钮。
- 不为了使用框架而使用框架：先理解手写 Runtime，再用 LangGraph解决持久状态与恢复。
- 所有结论区分“代码已存在”“工程验收通过”“学习者能独立讲清”三种状态。

## 4. 当前接续点

正式进度为 3 / 12；具体下一课只以 LEARNING_HANDOFF.md 为准。2026-09-15 用户确认转为本地优先 PC Coding Agent，并授权教练直接完成这次本地模式切换及对应学习文档。该授权仅覆盖本次调整，后续核心课程恢复参考实现 → 用户动手 → 完成后验收。

后续课程、功能设计与默认验收以 APP_MODE=local 的 PC 场景为主；不再主动推进账号功能，也不将账号模式专项回归作为每课完成条件。既有账号实现和测试保留为历史扩展；共享代码按本地功能实际影响范围验证，用户明确要求时再处理账号模式。

账号认证成果保留为扩展，不再要求登录或实现匿名访客系统；优先本地工作台、Workspace/目录、Task/恢复、受控工具与 Agent 能力。工程已验收与学习者独立掌握分别记录。域名用于展示/下载，本地交付替代主线的公开执行服务。

## 5. 每课完成定义

题库归档按重要性筛选，不要求每课产出题目。仅记录重要功能、关键设计和工程难点；简单展示或文案修改可直接跳过，已有重要题目优先补充，不重复建题。以下归档要求仅在本课确有重要考点时适用。

只有同时满足以下条件才可以进入下一课：

- 目标行为已经实现，相关成功和失败测试通过。
- lint、类型检查或后端静态检查按影响范围通过。
- 没有破坏现有事件协议、取消语义、事务边界和安全边界。
- 教练已将本课重要面试题及参考答案归档并标记复习优先级；无需学习者当堂回答，表达能力在用户发起模拟面试时再评估。
- `LEARNING_HANDOFF.md` 已更新为新的唯一下一课。

## 6. 禁止事项

- 不重复已经完成的课程。
- 不一次布置多个无法独立验收的大任务。
- 不只给脱离当前项目的玩具示例、残缺代码片段或需要学习者猜测的关键实现。
- 不因代码提前存在就跳过理解与复核。
- 不把隐藏思维链写入数据库或展示给用户；只保存可观察的 Plan、Action、Observation 和结果。
- 不让模型直接动态导入、`eval` 或绕过 Tool Registry 执行代码。
- 不在宿主机无隔离执行来自用户或模型的任意命令。
- 不把真实密钥、原始内部异常、敏感文件内容写进流事件、Trace 或模型上下文。

## 7. 教学纠错记录（2026-09-10，出现相关问题时按需查阅）

- 流程调整（2026-09-11）：每课结束不再要求学习者回答掌握检查题。工程验收通过后由教练自动整理重要面试题与答案到对应题库，更新交接；面试准备时再抽取重点进行模拟面试，不以当堂答题阻塞课程结束。

- 错误：把完整参考实现放到新建讲义和补丁文件，用链接让学习者来回查看。纠正：用户需要直接跟随对话学习，核心代码必须直接放在对话代码块中。用户要求“记录规则”时编辑这些仓库规则文档，不等于可以再次用讲义文件交付课程。
- 错误：用 Git Diff 的 `+`、`-`、`@@` 和转义后的文本展示修改。纠正：给普通语言代码块，并明确原位置与替换内容。真实 Diff 仍可用于教练内部验收。
- 错误：因为已有文件较短，就让学习者复制整个 package.json。纠正：只有新文件展示全文；已有文件只展示改动段或完整函数。机械性配置由教练直接改。
- 错误：把大量测试和样板代码也交给学习者手写。纠正：测试与机械性配套由教练实现；学习者精力集中于当前课核心数据流、状态与边界。
- 错误：在 detached worktree 中接续课程，未符合用户的 main 工作要求。纠正：后续操作使用已检出 main 的主仓库，每次恢复核实分支与实际路径；不能将操作目录与对话初始 worktree 混称。
- 错误：把测试代写授权误解成可以提前创建测试，造成课程尚未完成就出现模块缺失失败。纠正：必须等学习者明确说本课“完成了”，才检查实际改动、创建对应测试和配套并验收；学习期间只讲解，不提前落地本课测试。
- 验证纪律：不能把临时参考副本的通过结果冒称为正式 main 已通过；不提前更新课程完成进度。
- 格式纠错（2026-09-10）：先前参考代码使用了 2 个空格缩进。用户要求新建文件默认使用 4 个空格缩进，后续写入和展示参考代码时必须检查，不再沿用 2 空格模板。
