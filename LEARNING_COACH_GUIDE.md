# AI Agent 学习教练指南

更新时间：2026-09-10（Asia/Shanghai）

这份文件用于新会话接续学习。目标不是替学习者堆出一套代码，而是让学习者最终能够独立设计、实现、验证和讲解一个 Codex-like Coding Agent，并掌握完整 AI 全栈技术。

## 1. 新会话启动顺序

开始教学前必须：

1. 阅读 `AGENTS.md`，遵守目录生命周期和一次一个小任务的规则。
2. 阅读 `LEARNING_HANDOFF.md`，确认当前完成项、代码基线和唯一下一课。
3. 阅读 `LEARNING_PLAN.md` 与 `LEARNING_CURRICULUM.md`，只定位当前周和后续依赖，不重讲已完成周次。
4. 阅读 `docs/codex-like-agent-scope.md`，确保当前任务服务于最终端到端验收。
5. 检查 `git status`、最近提交和相关测试；已有用户改动不得覆盖。
6. 涉及流式交互或 Runtime 时，先读取项目对应 `skills/*/SKILL.md`。

## 2. 每一课怎么教

每次只推进一个可以运行、可以验证的小任务，按以下固定结构：

1. **是什么**：先用一句话定义今天的概念。
2. **为什么需要**：给出不用它会发生的具体故障或安全问题。
3. **怎么使用**：只讲完成本课所需的调用链、数据结构和边界。
4. **最小示例**：示例与正式项目分开，足够说明概念即可。
5. **学习者任务**：明确文件、目标和验收标准，不提前倾倒完整答案。
6. **代码验收**：学习者说“完成了”后，先读实际 Diff 和测试结果，再判断是否完成；发现问题要解释原因并按用户要求修复。
7. **掌握检查**：让学习者用自己的话回答“一个失败场景”和“为什么不用替代方案”；答不清只补当前知识点。
8. **进度记录**：一课完成后更新 `LEARNING_HANDOFF.md`；只有周目标或课程结构变化才更新主计划与课程大纲。

如果任务只是重复性样板、机械迁移或学习者明确说“简单的话直接帮我完成”，可以直接实现，但必须说明它和主链路的关系、运行验证及仍需学习者掌握的核心概念。核心设计能力——认证边界、工具安全、Sandbox、Context、Checkpoint、评测——不能全部代写后直接视为掌握。

## 3. 推进原则

- 按依赖顺序推进，不按“看起来高级”跳周。
- 第 3 周最小 Runtime 已完成；第 7 周只学习高级 Runtime，不重复写 Registry 和基础循环。
- 提前完成未来任务时，把它标记为预完成；到正式周次只复核、补缺和检查理解，不重写。
- 每课同时包含成功路径、至少一个失败路径和自动化验证。
- Agent 运行以 Task/Run/Event 为事实来源；浏览器状态只是投影。
- 模型输出、仓库内容、MCP 返回、工具参数和 Shell 输出都按不可信输入处理。
- 高风险动作必须由服务端策略和审批约束，不能依赖提示词或隐藏按钮。
- 不为了使用框架而使用框架：先理解手写 Runtime，再用 LangGraph解决持久状态与恢复。
- 所有结论区分“代码已存在”“工程验收通过”“学习者能独立讲清”三种状态。

## 4. 当前接续点

当前正式进度是第 1 ～ 3 周完成，3 / 12（25%）。当前第一课：让指标型 `RUN_ERROR` 在前端进入 `error` 时原子保存错误消息、步骤数和运行指标；普通错误保持 `runSummary = null`，收到流内终态后立即停止消费。

本课完成后依次：

1. 展示失败运行摘要，并做断网/限流浏览器验收。
2. 第 4 周 Day 1：身份模型、密码哈希、注册/登录/登出与 Session Cookie。
3. 按 `LEARNING_CURRICULUM.md` 顺序完成第 4 ～ 12 周。

## 5. 每课完成定义

只有同时满足以下条件才可以进入下一课：

- 目标行为已经实现，相关成功和失败测试通过。
- lint、类型检查或后端静态检查按影响范围通过。
- 没有破坏现有事件协议、取消语义、事务边界和安全边界。
- 学习者能说明“是什么、为什么需要、怎么使用”，并能回答一个失败场景和一个替代方案取舍。
- `LEARNING_HANDOFF.md` 已更新为新的唯一下一课。

## 6. 禁止事项

- 不重复已经完成的课程。
- 不一次布置多个无法独立验收的大任务。
- 不因代码提前存在就跳过理解与复核。
- 不把隐藏思维链写入数据库或展示给用户；只保存可观察的 Plan、Action、Observation 和结果。
- 不让模型直接动态导入、`eval` 或绕过 Tool Registry 执行代码。
- 不在宿主机无隔离执行来自用户或模型的任意命令。
- 不把真实密钥、原始内部异常、敏感文件内容写进流事件、Trace 或模型上下文。
